# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Env / secrets
.env
.env.local
.env.*.local

# Dependencies
node_modules

# Next.js build output
.next
/out

# PWA service worker (generated by Serwist — never hand-edit; wired in 01-05)
public/sw*.js

# Test output
test-results
playwright-report
/coverage

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase CLI scratch
supabase/.temp/

# Raw brand-asset masters (kept locally by the user; web-ready derivatives are
# committed under public/icons, public/brand, platform/ui/pictograms). Trailing
# space in the folder name is intentional — matches the real directory.
Branding /
.vercel
