---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - vitest.config.ts
  - playwright.config.ts
  - .gitignore
  - .env.local.example
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - platform/.gitkeep
  - modules/welcome-pickup/.gitkeep
  - platform/seam.test.ts
  - tests/e2e/.gitkeep
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "A Next 16 App Router project builds and dev-serves from an empty repo"
    - "A platform/ file importing modules/* fails `npx eslint .` with the seam message"
    - "Vitest and Playwright are installed and runnable with config files present"
  artifacts:
    - path: "package.json"
      provides: "Next 16 + React 19 + Tailwind 4 + test runners with pinned versions"
      contains: "\"next\""
    - path: "eslint.config.mjs"
      provides: "flat-config no-restricted-imports seam rule scoped to platform/**"
      contains: "no-restricted-imports"
    - path: "vitest.config.ts"
      provides: "Vitest unit/seam test config"
    - path: "playwright.config.ts"
      provides: "Playwright smoke test config"
  key_links:
    - from: "eslint.config.mjs"
      to: "platform/**/*.{ts,tsx}"
      via: "files-scoped no-restricted-imports block targeting modules/*"
      pattern: "no-restricted-imports"
---

<objective>
Scaffold the entire greenfield Balkanity PWA (Next 16 App Router + React 19 + Tailwind v4 + ESLint flat config + TypeScript), restructure it into the one-way `platform/ ⁄ modules/welcome-pickup/` seam, enforce that seam with ESLint `no-restricted-imports` (PLAT-01), and install the Vitest + Playwright test infrastructure (Wave 0) every later plan depends on.

Purpose: This is the foundation slice of the Walking Skeleton. Without the scaffold, seam, and test runners, no later plan can land code in the right place or be verified. The seam is enforced at lint time so later phases physically cannot couple platform to a module.
Output: A buildable Next 16 project with the seam directory boundary, a failing-on-violation ESLint seam rule, and runnable Vitest + Playwright.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/SKELETON.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols/paths (exclude from drift verification — they do not exist before this phase):
- Files: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`, `vitest.config.ts`, `playwright.config.ts`, `.env.local.example`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`
- Directories (seam boundary): `platform/`, `modules/welcome-pickup/`, `tests/e2e/`
- Test files: `platform/seam.test.ts`
- npm scripts: `dev`, `build`, `lint`, `typecheck`, `test`, `test:e2e`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next 16 App Router project with locked stack</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, .gitignore, .env.local.example</files>
  <read_first>
    - CLAUDE.md (Technology Stack table — pinned versions: next 16.2.9 `^16.2`, react 19.2.7 `^19.2`, tailwindcss 4.x, @tailwindcss/postcss 4.x, typescript current; "What NOT to Use")
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Standard Stack table; "Installation" block; "Recommended Project Structure"; State of the Art — `proxy.ts` not `middleware.ts`)
    - .gitignore (existing — already ignores .env*; must add `public/sw*.js`, `node_modules`, `.next`, `test-results`, `playwright-report`)
  </read_first>
  <action>
    Scaffold with `npx create-next-app@latest` into the current repo using flags: TypeScript, Tailwind (v4), ESLint (flat config), App Router, `--no-src-dir`, no import alias change beyond default `@/*`. Because the repo already contains files (CLAUDE.md, .planning, .gitignore), scaffold into a temp dir and move generated files in, OR run create-next-app with `.` and merge — preserve existing CLAUDE.md, README.md, .planning/, .git/, and the existing .gitignore secret rules. Pin in package.json: `next@^16.2`, `react@^19.2`, `react-dom@^19.2`, `tailwindcss@^4`, `@tailwindcss/postcss@^4`. Use `next.config.ts` (TS config). Use `postcss.config.mjs` with the `@tailwindcss/postcss` plugin. `app/globals.css` starts with `@import "tailwindcss";` (brand `@theme` tokens are added in plan 01-04 — do NOT add them here). `app/layout.tsx` sets `<html lang="en">` and minimal metadata (title "Balkanity Platform"); `app/page.tsx` a trivial placeholder (role redirect wired in plan 01-03). Add npm scripts: `dev`, `build`, `lint` (`next lint` or `eslint .`), `typecheck` (`tsc --noEmit`). Append to .gitignore: `node_modules`, `.next`, `public/sw*.js`, `test-results`, `playwright-report`, `/coverage`. Create `.env.local.example` listing (with empty values + comments) `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` — note in comments which are browser-safe vs server-only. Do NOT install `@serwist/next` here (plan 01-05). Do NOT wrap next.config with withSerwist yet.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `next` `^16.2`, `react` `^19.2`, `tailwindcss` `^4` in dependencies (grep: `grep -E '"next"|"react"|"tailwindcss"' package.json` shows all three)
    - `npx next build` exits 0
    - `npx tsc --noEmit` exits 0
    - `app/globals.css` contains `@import "tailwindcss"` and does NOT yet contain `@theme` (deferred to 01-04)
    - `.gitignore` contains `public/sw*.js` and `node_modules`
    - `.env.local.example` contains `SUPABASE_SERVICE_ROLE_KEY` and does NOT prefix it with `NEXT_PUBLIC_`
  </acceptance_criteria>
  <done>Project builds and type-checks from the empty repo; brand tokens and PWA wiring are intentionally absent (added in 01-04/01-05).</done>
</task>

<task type="auto">
  <name>Task 2: Create the platform/modules seam and enforce it via ESLint no-restricted-imports</name>
  <files>platform/.gitkeep, modules/welcome-pickup/.gitkeep, eslint.config.mjs</files>
  <read_first>
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Pattern 1: one-way seam via ESLint `no-restricted-imports` flat config — exact `files`/`patterns/group/message` shape; "Recommended Project Structure")
    - .planning/phases/01-platform-foundation/01-CONTEXT.md (D — seam enforcement: modules import platform, never reverse; `wp_` table prefix)
    - eslint.config.mjs (the flat config generated by create-next-app in Task 1 — extend, do not replace, the next presets)
  </read_first>
  <action>
    Create the seam directories `platform/` and `modules/welcome-pickup/` (with `.gitkeep` files so they are tracked). In `eslint.config.mjs` (flat config, ESLint 10), keep the existing `next/core-web-vitals` + `next/typescript` config and APPEND a `files`-scoped block targeting `["platform/**/*.{ts,tsx}"]` that sets `no-restricted-imports` to `error` with a `patterns` entry whose `group` is `["@/modules/*", "modules/*", "../modules/*", "**/modules/*"]` and `message` "platform/ must not import from modules/ — the seam is one-way (modules → platform only). [PLAT-01]". This enforces PLAT-01 at the UI + server-module layer (both live under platform/ vs modules/). The DB-naming half of the seam (`wp_` prefix on module tables, unprefixed platform tables) is a convention enforced in plans 01-02 (app_users unprefixed) and Phase 2+ (wp_ tables) — note it in the seam comment but no code here.
  </action>
  <verify>
    <automated>npx eslint platform/</automated>
  </verify>
  <acceptance_criteria>
    - Directories `platform/` and `modules/welcome-pickup/` exist and are git-tracked (`.gitkeep` present)
    - `eslint.config.mjs` contains `no-restricted-imports` and the string `modules/` in a group pattern
    - `npx eslint platform/` exits 0 on the clean tree (no forbidden imports yet)
    - PROOF (executor must run, then revert): adding a temporary line `import x from "@/modules/welcome-pickup/anything";` into a `.ts` file under `platform/` makes `npx eslint platform/` exit non-zero with the seam message; removing it restores exit 0
  </acceptance_criteria>
  <done>The one-way seam is enforced at lint time; a platform→modules import is a lint error (proven and reverted). Success Criterion 2 is satisfied for the UI/server-module layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install and configure Vitest + Playwright (Wave 0 test infrastructure)</name>
  <files>vitest.config.ts, playwright.config.ts, package.json, platform/seam.test.ts, tests/e2e/.gitkeep</files>
  <read_first>
    - .planning/phases/01-platform-foundation/01-VALIDATION.md (Test Infrastructure table — quick/full run commands; Wave 0 Requirements; Per-Task Verification Map)
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Validation Architecture — Vitest unit/seam, Playwright smoke; Wave 0 Gaps)
    - package.json (scripts added in Task 1 — extend with `test` and `test:e2e`)
  </read_first>
  <behavior>
    - `npx vitest run` discovers and runs `platform/seam.test.ts` and reports at least 1 passing test
    - The seam smoke test asserts a true invariant (e.g. `platform/` and `modules/welcome-pickup/` directories exist) so Wave 0 has a real green signal, not an empty suite
    - `npx playwright test --list` succeeds (config valid) even before e2e specs are authored (those land in 01-03/01-05)
  </behavior>
  <action>
    Install dev deps: `vitest`, `@vitejs/plugin-react`, `jsdom` (or `happy-dom`), `@playwright/test`. Create `vitest.config.ts` with the React plugin, `environment: "jsdom"`, and `globals: true`; include `platform/**/*.test.ts(x)` and exclude `tests/e2e/**` + `node_modules`. Create `playwright.config.ts` pointing `testDir: "tests/e2e"`, `webServer` running `npm run dev` against the dev URL, and a single chromium project (mobile viewport acceptable for PWA smoke). Add npm scripts: `test` = `vitest run`, `test:e2e` = `playwright test`. Create `tests/e2e/.gitkeep` (specs authored in later plans). Write `platform/seam.test.ts` as the first real unit test asserting the seam directories exist (use `fs.existsSync` on `platform` and `modules/welcome-pickup`) — gives Wave 0 a non-empty green suite. Run `npx playwright install chromium` so the smoke runner has a browser.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` and `playwright.config.ts` exist
    - `npx vitest run` exits 0 and reports ≥1 passing test from `platform/seam.test.ts`
    - `npx playwright test --list` exits 0 (config parses)
    - `package.json` scripts include `test` and `test:e2e`
    - No `--watch`/watch-mode flag appears in any committed test script (VALIDATION sign-off: no watch-mode flags)
  </acceptance_criteria>
  <done>Vitest + Playwright are installed, configured, and produce a green Wave 0 baseline; later plans can add specs without bootstrapping the runners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → npm registry | Package installs (`create-next-app`, vitest, playwright) pull third-party code |
| platform/ ↔ modules/ | Architectural integrity boundary; one-way import direction must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | seam erosion (platform importing modules) | mitigate | ESLint `no-restricted-imports` lint-gate proven with a temporary forbidden import (Task 2, PLAT-01) |
| T-01-02 | Information Disclosure | secrets committed to git | mitigate | `.gitignore` already ignores `.env*`; `.env.local.example` carries empty values + comments only; no real secret values introduced this plan |
| T-01-SC | Tampering | npm/create-next-app/vitest/playwright installs | accept | All packages are first-party framework/official tooling from CLAUDE.md's locked stack, registry-confirmed 2026-06-17 (RESEARCH Package Legitimacy Audit — no `[ASSUMED]`/`[SUS]`/`[SLOP]`); no blocking checkpoint required |
</threat_model>

<verification>
- `npx next build` exits 0 (scaffold valid)
- `npx tsc --noEmit` exits 0 (types valid)
- `npx eslint platform/` exits 0 clean; exits non-zero with a temporary platform→modules import (PLAT-01 / SC-2 proof)
- `npx vitest run` exits 0 with ≥1 passing test
- `npx playwright test --list` exits 0
- `! grep -rnE "NEXT_PUBLIC_.*(SERVICE_ROLE|SECRET)" .env.local.example package.json` returns nothing
</verification>

<success_criteria>
- A Next 16 App Router project builds and dev-serves from the previously-empty repo
- The `platform/ ⁄ modules/welcome-pickup/` seam exists and a platform→modules import is a lint error (SC-2, UI/server-module layer)
- Vitest + Playwright are installed, configured, and green (Wave 0 baseline established for all later plans)
</success_criteria>

<output>
Create `.planning/phases/01-platform-foundation/01-01-SUMMARY.md` when done.
</output>
