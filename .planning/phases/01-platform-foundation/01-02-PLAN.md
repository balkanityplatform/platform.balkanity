---
phase: 01-platform-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - platform/supabase/client.ts
  - platform/supabase/server.ts
  - platform/supabase/admin.ts
  - platform/supabase/admin.client-boundary.test-fixture.tsx
  - supabase/migrations/0001_app_users_and_roles.sql
  - .env.local.example
autonomous: false
requirements: [PLAT-05, AUTH-01]
user_setup:
  - service: supabase
    why: "Apply the first (flagged/irreversible) schema migration and confirm env keys for the Balkanity project"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard (ref qyhdogajtmnvxphrslwm) → Project Settings → API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard → Project Settings → API → Publishable/anon key (confirm exact name — may be legacy anon key)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → service_role key (SERVER-ONLY, never NEXT_PUBLIC_)"
    dashboard_config:
      - task: "Sign off on the 0001 migration SQL, then apply it to ref qyhdogajtmnvxphrslwm (NEVER Kalvia utyatpadtibqqswsfvtr)"
        location: "Supabase MCP apply_migration / supabase db push / Dashboard SQL editor"
must_haves:
  truths:
    - "The browser bundle can only ever hold the anon/publishable key; the service-role key is server-only"
    - "A client component importing the service-role module fails `next build`"
    - "The app_users table exists in the Balkanity Supabase project with a role column constrained to {admin,driver,guest} and RLS enabled"
    - "Exactly one seed admin row exists (resolves to a single role)"
  artifacts:
    - path: "platform/supabase/client.ts"
      provides: "browser anon client (createBrowserClient)"
      exports: ["createClient"]
    - path: "platform/supabase/server.ts"
      provides: "server anon client (createServerClient, cookie getAll/setAll)"
      exports: ["createClient"]
    - path: "platform/supabase/admin.ts"
      provides: "service-role client, server-only guarded"
      exports: ["createAdminClient"]
      contains: "server-only"
    - path: "supabase/migrations/0001_app_users_and_roles.sql"
      provides: "app_users table + role constraint + RLS + seed admin"
      contains: "app_users"
  key_links:
    - from: "platform/supabase/admin.ts"
      to: "server-only"
      via: "top-of-file import that fails build if client-imported"
      pattern: "import \"server-only\""
    - from: "platform/supabase/server.ts"
      to: "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY"
      via: "createServerClient anon key"
      pattern: "PUBLISHABLE_KEY"
---

<objective>
Establish the three-way Supabase client split (browser-anon, server-anon cookie-bound, service-role `server-only`) per PLAT-05, prove the secret boundary fails the build when violated (SC-4), and author + apply the FLAGGED first schema migration (`app_users` + role constraint + RLS + seed admin) per AUTH-01 / D-02 — the irreversible schema gate requiring human sign-off before it touches the live Balkanity project.

Purpose: This is the data-layer of the Walking Skeleton. The client split + `server-only` guard make the key-leak boundary a build-time guarantee for every later phase; `app_users` is the table that role resolution (plan 01-03) reads. The migration is flagged because schema is irreversible and AUTH-01's "exactly one role" shape (A4) must be signed off before applying.
Output: Three Supabase client modules, a build-gate fixture proving the secret boundary, and a live `app_users` table with a seeded admin on ref `qyhdogajtmnvxphrslwm`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/SKELETON.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
NEW symbols/paths created by this plan (exclude from drift verification):
- Modules: `platform/supabase/client.ts` (exports `createClient`), `platform/supabase/server.ts` (exports `createClient`), `platform/supabase/admin.ts` (exports `createAdminClient`)
- Migration: `supabase/migrations/0001_app_users_and_roles.sql`
- DB objects: table `app_users` (columns `id`, `email`, `role`), the `app_user_role` constraint/enum, RLS policies on `app_users`, one seed admin row
- Env keys (declared, set by user in Vercel/.env.local): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`
- Build-gate fixture: `platform/supabase/admin.client-boundary.test-fixture.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Three-way Supabase client split + server-only secret boundary (PLAT-05, SC-4)</name>
  <files>platform/supabase/client.ts, platform/supabase/server.ts, platform/supabase/admin.ts, platform/supabase/admin.client-boundary.test-fixture.tsx, .env.local.example</files>
  <read_first>
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Pattern 2: three-way client split — exact `createBrowserClient`/`createServerClient` getAll/setAll shapes; Pattern 3: server-only boundary; SOTA — env var `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`)
    - CLAUDE.md ("What NOT to Use": never `@supabase/auth-helpers-nextjs`, never `getSession()` for authz, never service-role in `NEXT_PUBLIC_`; "Integration Patterns" §1)
    - .env.local.example (created in 01-01 — confirm key names match)
  </read_first>
  <action>
    Install runtime deps: `@supabase/supabase-js@^2.108`, `@supabase/ssr@^0.12`, `server-only`. Create `platform/supabase/client.ts` exporting `createClient()` that returns `createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!)`. Create `platform/supabase/server.ts` exporting an async `createClient()` that awaits `cookies()` from `next/headers` and calls `createServerClient(... PUBLISHABLE_KEY ..., { cookies: { getAll, setAll } })` — create a NEW client per call (never cache in a module global; Fluid-compute caveat), wrap `setAll` in try/catch (safe to ignore from a Server Component). Create `platform/supabase/admin.ts` with `import "server-only";` as the FIRST line, exporting `createAdminClient()` that uses non-public `process.env.SUPABASE_URL!` + `process.env.SUPABASE_SERVICE_ROLE_KEY!` with `{ auth: { autoRefreshToken: false, persistSession: false } }`. Confirm `.env.local.example` lists the four keys with correct public/server split. Create `platform/supabase/admin.client-boundary.test-fixture.tsx` — a `"use client"` component that imports `createAdminClient` from `./admin` — this fixture is the SC-4 proof: `next build` MUST fail while it exists. The executor builds with it (expect failure), then deletes the fixture and rebuilds (expect success). Do not commit the fixture in a state that breaks the build — final committed state has the fixture removed (record the failure output in the SUMMARY).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `platform/supabase/admin.ts` line 1 is `import "server-only";`
    - `grep -n "createBrowserClient" platform/supabase/client.ts` matches; `grep -n "createServerClient" platform/supabase/server.ts` matches
    - `grep -rn "getSession" platform/supabase/` returns nothing (authz uses getUser, plan 01-03)
    - `! grep -rnE "NEXT_PUBLIC_.*(SERVICE_ROLE|SECRET)" .` returns nothing (no secret under a public env name)
    - PROOF (executor runs, records output, then reverts): with `admin.client-boundary.test-fixture.tsx` present, `npx next build` FAILS with the `server-only` error; after deleting the fixture, `npx next build` exits 0
    - Final committed tree does NOT contain the client-boundary fixture (it is a transient proof)
  </acceptance_criteria>
  <done>The anon/server/service-role split exists; importing the service-role module from a client component is a build failure (SC-4 proven); no secret is exposed under a `NEXT_PUBLIC_` name.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Sign off on the first (flagged) app_users + roles migration shape</name>
  <action>PAUSE for the user to choose role storage and approve the seed-admin email + linked ref BEFORE any migration SQL is authored or applied. This is the flagged/irreversible schema gate — the executor writes no SQL and runs no apply until this decision returns. No automation precedes this checkpoint because the schema shape is the user's call (A4 / D-02).</action>
  <decision>Final shape of the FLAGGED first migration `0001_app_users_and_roles.sql` (app_users columns, role storage, RLS policies, seed-admin email) before it is written and applied to the irreversible live schema.</decision>
  <context>
    This is the flagged/irreversible schema gate (D-02, RESEARCH Open Question 2 / A4 — MEDIUM confidence). CLAUDE.md mandates sign-off on schema/auth/RLS changes BEFORE the first migration. AUTH-01 requires exactly one role per user. The migration also seeds the first admin via SQL insert (D-02 — not env allowlist, not first-user-is-admin). The executor must confirm the real admin email to seed and the linked project ref before applying.
  </context>
  <options>
    <option id="role-text-check">
      <name>Single `role` TEXT column with a CHECK constraint in {admin,driver,guest}</name>
      <pros>Simplest; one role per user (satisfies AUTH-01 "exactly one"); easy to read in role.ts; no enum migration friction for future role tweaks</pros>
      <cons>CHECK constraint edits need a migration to change allowed values</cons>
    </option>
    <option id="role-enum">
      <name>Postgres ENUM type `app_user_role` + `role` column of that type</name>
      <pros>Type-safe at DB level; reflected in generated Supabase types; self-documenting</pros>
      <cons>Adding enum values later is a heavier migration; slightly more ceremony for v1</cons>
    </option>
  </options>
  <resume-signal>Select: role-text-check or role-enum, AND provide the seed admin email address, AND confirm the linked Supabase ref is qyhdogajtmnvxphrslwm (not Kalvia).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve the 0001 migration SQL before it touches the live DB</name>
  <action>PAUSE after the executor writes `supabase/migrations/0001_app_users_and_roles.sql` (per the decision above) but BEFORE applying it. The user reviews the exact SQL and confirms the target ref. The executor applies nothing until this returns approved.</action>
  <what-built>The `0001_app_users_and_roles.sql` migration SQL (written per the decision above) is staged but NOT yet applied. It creates `app_users(id uuid PK references auth.users, email text, role <per decision>)`, enables RLS, adds policies (a user may read their own row; service-role bypasses RLS), and inserts one seed admin row. This is the irreversible schema gate — CLAUDE.md requires sign-off before it touches the live DB.</what-built>
  <how-to-verify>
    1. Read `supabase/migrations/0001_app_users_and_roles.sql` in full.
    2. Confirm the role storage matches the decision, the role values are exactly {admin,driver,guest}, RLS is ENABLED, and the self-read policy uses `auth.uid() = id`.
    3. Confirm the seed insert uses the email you approved and assigns role `admin`.
    4. Confirm the target is ref `qyhdogajtmnvxphrslwm` — run the linked-ref confirmation (Supabase MCP `list_tables` against the Balkanity project, or `supabase projects list` / dashboard URL) and verify it is NOT Kalvia `utyatpadtibqqswsfvtr`.
    5. After approval, the executor applies via Supabase MCP `apply_migration` (or `supabase db push` / dashboard SQL editor) and runs `get_advisors` for security lints.
  </how-to-verify>
  <resume-signal>Type "approved — apply to qyhdogajtmnvxphrslwm" or describe required SQL changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING — SCHEMA PUSH]: Apply 0001 migration to the live Balkanity Supabase project</name>
  <files>supabase/migrations/0001_app_users_and_roles.sql</files>
  <read_first>
    - supabase/migrations/0001_app_users_and_roles.sql (the approved SQL from the checkpoint above)
    - CLAUDE.md (Infrastructure constraint — ref qyhdogajtmnvxphrslwm ONLY, never Kalvia utyatpadtibqqswsfvtr; "If any tool reports Kalvia, STOP")
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Environment Availability — Supabase CLI unverified; fallback = Supabase MCP apply_migration / dashboard)
  </read_first>
  <action>
    This is the BLOCKING schema-push task — it modifies the live, irreversible schema and runs ONLY after the decision + human-verify checkpoints above pass. FIRST confirm the linked project ref is `qyhdogajtmnvxphrslwm` (Supabase MCP `list_tables` / `get_project_url`, or `supabase status`); if any tool reports Kalvia `utyatpadtibqqswsfvtr`, STOP immediately and do not apply. Apply `0001_app_users_and_roles.sql` using Supabase MCP `apply_migration` (preferred when the local CLI is absent) or `supabase db push` or the dashboard SQL editor. After applying, verify with `list_tables` that `app_users` exists with the expected columns and RLS enabled, query that exactly one row with role `admin` exists, and run `get_advisors` (security) — resolve any HIGH advisory before marking done. Do NOT seed any driver/guest rows (those arrive via Phase 2 invites / Phase 4 magic-link).
  </action>
  <verify>
    <automated>echo "Schema-push verification is MCP-based — executor records list_tables + advisor output in SUMMARY; see acceptance_criteria"</automated>
  </verify>
  <acceptance_criteria>
    - Linked ref confirmed as `qyhdogajtmnvxphrslwm` BEFORE apply (recorded in SUMMARY); Kalvia never targeted
    - `list_tables` shows `app_users` with columns `id`, `email`, `role` and RLS enabled
    - Exactly one row in `app_users` has `role = 'admin'` (the seed) and zero rows have other roles
    - `get_advisors` (security) returns no unresolved HIGH finding on `app_users` (RLS present)
    - The migration file is committed; the live schema matches it
  </acceptance_criteria>
  <done>`app_users` exists on the Balkanity project with the seeded admin and RLS; role resolution (plan 01-03) has a real table to read. Build/type checks alone could not have caught a missing push — the live schema is now real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server module → browser bundle | Service-role key must never cross into client-reachable code |
| client → Supabase (anon) | Browser holds only the publishable/anon key; RLS is the row boundary |
| migration tool → live DB | Schema apply must target Balkanity, never Kalvia; irreversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure / Elevation | service-role key bundled to client | mitigate | `import "server-only"` on `admin.ts` + build-gate fixture proving `next build` fails on client import (SC-4, Task 1) |
| T-02-02 | Spoofing / Elevation | secret under a `NEXT_PUBLIC_` name | mitigate | grep-gate `! grep -rnE "NEXT_PUBLIC_.*(SERVICE_ROLE|SECRET)" .` (Task 1) |
| T-02-03 | Information Disclosure | missing RLS policy on `app_users` | mitigate | RLS ENABLED + self-read policy `auth.uid() = id` in 0001; `get_advisors` security check after apply (Task 2) |
| T-02-04 | Tampering (operational) | wrong-project migration (Kalvia) | mitigate | Confirm ref `qyhdogajtmnvxphrslwm` before apply; human-verify checkpoint + STOP-on-Kalvia rule (Task 2) |
| T-02-05 | Elevation of Privilege | unauthorized admin seeded | mitigate | Seed admin email approved at the blocking human-verify checkpoint; `shouldCreateUser:false` (plan 01-03) blocks open signup |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx next build` exits 0 with the client-boundary fixture removed; FAILS while it is present (SC-4 proof recorded)
- `! grep -rnE "NEXT_PUBLIC_.*(SERVICE_ROLE|SECRET)" .` returns nothing
- `grep -rn "getSession" platform/supabase/` returns nothing
- Supabase `list_tables` shows `app_users` (RLS on) on ref `qyhdogajtmnvxphrslwm`; exactly one `admin` seed row
</verification>

<success_criteria>
- Browser bundle can only hold the anon/publishable key; a client import of the service-role module fails the build (SC-4)
- `app_users` exists on the Balkanity project with a role constrained to exactly {admin,driver,guest}, RLS enabled, and one seeded admin (AUTH-01 data layer)
- The flagged migration was signed off and applied to ref `qyhdogajtmnvxphrslwm` only
</success_criteria>

<output>
Create `.planning/phases/01-platform-foundation/01-02-SUMMARY.md` when done. Record the SC-4 build-failure output, the linked-ref confirmation, and the `list_tables`/`get_advisors` results.
</output>
