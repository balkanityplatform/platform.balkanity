---
phase: 01-platform-foundation
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - next.config.ts
  - app/sw.ts
  - app/manifest.ts
  - app/~offline/page.tsx
  - app/layout.tsx
  - app/icon.png
  - public/icons/.gitkeep
  - .gitignore
  - tests/e2e/pwa.spec.ts
autonomous: false
requirements: [PLAT-02, PLAT-03]
user_setup:
  - service: brand-assets
    why: "D-09 BLOCKER — real logo/pictogram assets are not yet in the repo; Success Criterion 5 cannot be met by placeholders"
    dashboard_config:
      - task: "Commit the real brand assets (mark-white.png, mark-teal.png, plane/route pictograms) and confirm their final committed paths"
        location: "repo (e.g. Mockups/assets/ + Balkanity Branding/, or a chosen public/ location)"
  - service: vercel
    why: "Deploy the installable PWA to the Balkanity Vercel project and set env vars"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Vercel → balkanity_platform_project → Settings → Environment Variables (Balkanity Supabase URL)"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Vercel env (publishable/anon key for ref qyhdogajtmnvxphrslwm)"
      - name: SUPABASE_URL
        source: "Vercel env (server-only)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Vercel env (server-only — NEVER NEXT_PUBLIC_)"
    dashboard_config:
      - task: "Deploy to the Balkanity Vercel project (team balkanity-platform-s-projects); confirm it is NOT a Kalvia project"
        location: "Vercel CLI / dashboard / Git integration"
must_haves:
  truths:
    - "The app installs as a PWA on mobile (manifest + service worker register) and serves a branded /~offline fallback when disconnected"
    - "Auth/sign-in/admin/confirm routes are never served stale from the SW cache (NetworkFirst, Pitfall 12 / D-06)"
    - "The installed app identity is 'Balkanity', teal #029B87 theme on white, display standalone"
    - "Real logo/pictogram assets (not placeholders) render via the design system (SC-5)"
    - "The PWA is deployed and reachable on the Balkanity Vercel project"
  artifacts:
    - path: "next.config.ts"
      provides: "withSerwist wrapper + offline precache entry"
      contains: "withSerwist"
    - path: "app/sw.ts"
      provides: "Serwist service worker with /~offline fallback + NetworkFirst for documents"
      contains: "/~offline"
    - path: "app/manifest.ts"
      provides: "PWA manifest (name Balkanity, theme teal, standalone)"
      contains: "standalone"
    - path: "app/~offline/page.tsx"
      provides: "branded offline fallback page"
  key_links:
    - from: "next.config.ts"
      to: "app/sw.ts"
      via: "withSerwist swSrc"
      pattern: "swSrc"
    - from: "app/sw.ts"
      to: "/~offline"
      via: "fallbacks entry matched on document requests"
      pattern: "~offline"
---

<objective>
Make the app an installable, offline-aware PWA (PLAT-02) and complete the brand design system with the REAL logo/pictogram assets (PLAT-03 / SC-5), then deploy to the Balkanity Vercel project. Wire `@serwist/next` (`withSerwist` + authored `app/sw.ts`), a manifest (name "Balkanity", teal `#029B87` theme on white, `display: standalone`), and a precached branded `/~offline` fallback — with auth/admin/confirm routes kept NetworkFirst so a stale signed-in shell can never be served (Pitfall 12 / D-06).

Purpose: This is the final Walking-Skeleton slice — it makes the foundation installable and deployed, the state SC-1 requires, and completes the brand system with real assets (SC-5). It is `autonomous: false` because SC-5 is BLOCKED on D-09 (assets not yet in the repo) and deployment requires the user's Vercel project + env secrets.
Output: A Serwist PWA shell, manifest, offline fallback, real brand icons/pictograms wired into the design system, and a live deployment on the Balkanity Vercel project.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/SKELETON.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-UI-SPEC.md
@CLAUDE.md
@next.config.ts
@app/layout.tsx
</context>

<artifacts_this_phase_produces>
NEW symbols/paths created by this plan (exclude from drift verification):
- `next.config.ts` wrapped with `withSerwist`; `app/sw.ts` (Serwist worker); `app/manifest.ts` (PWA manifest); `app/~offline/page.tsx` (offline fallback)
- Brand assets wired: `app/icon.png` (or chosen path), `public/icons/` (PWA icon set), pictogram imports from the committed brand-asset location
- Generated build artifact: `public/sw.js` (gitignored)
- E2E spec: `tests/e2e/pwa.spec.ts`
- Live deployment URL on the Balkanity Vercel project
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Serwist PWA shell — withSerwist, sw.ts, manifest, branded /~offline (PLAT-02, D-06/D-07)</name>
  <files>next.config.ts, app/sw.ts, app/manifest.ts, app/~offline/page.tsx, .gitignore, tests/e2e/pwa.spec.ts</files>
  <read_first>
    - .planning/phases/01-platform-foundation/01-RESEARCH.md (Pattern 4: Serwist — `withSerwistInit` config with `swSrc:"app/sw.ts"`, `swDest:"public/sw.js"`, `additionalPrecacheEntries` for `/~offline`; `app/sw.ts` with `defaultCache`, `fallbacks` matching document requests to `/~offline`; Pitfall 12 guard — auth/sign-in/admin/confirm NetworkFirst, never CacheFirst; Runtime State Inventory — gitignore `public/sw*.js`)
    - .planning/phases/01-platform-foundation/01-CONTEXT.md (D-06 offline = precached shell + branded fallback, auth/booking/claim NetworkFirst; D-07 identity — name "Balkanity"/"Balkanity Platform", theme #029B87 on white, standalone)
    - .planning/phases/01-platform-foundation/01-UI-SPEC.md (Copywriting — offlineHeading "You're offline", offlineBody; uses dictionary from 01-04)
    - next.config.ts (current — wrap with withSerwist), app/layout.tsx (add manifest link + theme-color meta if not via manifest.ts)
  </read_first>
  <action>
    Install `@serwist/next@^9.5` + `serwist@^9.5`. Wrap `next.config.ts` with `withSerwistInit({ swSrc: "app/sw.ts", swDest: "public/sw.js", additionalPrecacheEntries: [{ url: "/~offline", revision: <git-hash-or-uuid> }] })`. Author `app/sw.ts` using `Serwist` with `precacheEntries: self.__SW_MANIFEST`, `skipWaiting`, `clientsClaim`, `navigationPreload`, `runtimeCaching: defaultCache`, and a `fallbacks` entry serving `/~offline` when `request.destination === "document"`. EXPLICITLY ensure auth/sign-in/`/admin`/`/auth/confirm` routes are NetworkFirst or excluded from runtime caching (never CacheFirst/StaleWhileRevalidate) — a stale signed-in shell is the Pitfall 12 hazard; document the strategy in the SUMMARY. Create `app/~offline/page.tsx` rendering the branded offline fallback using the dictionary (`offlineHeading`/`offlineBody` from 01-04) on white surface with teal accent. Create `app/manifest.ts` (Next metadata route) returning name "Balkanity Platform", short_name "Balkanity", `theme_color "#029B87"`, `background_color "#FFFFFF"`, `display "standalone"`, start_url "/", and an `icons` array (icon paths finalized in Task 2 once real assets land — use a temporary monochrome teal placeholder ONLY for the SW/build to compile, clearly marked to be replaced; placeholders are NOT acceptable for SC-5 sign-off). Add `public/sw*.js` to `.gitignore` if 01-01 did not. Create `tests/e2e/pwa.spec.ts` asserting the service worker registers and `/~offline` renders the branded fallback copy (simulate offline via Playwright context `setOffline(true)` then navigate to a document route → expect the offline page).
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "withSerwist" next.config.ts` matches; `grep -n "~offline" app/sw.ts` matches
    - `app/manifest.ts` contains `"standalone"` and `#029B87` (grep both)
    - `npx next build` exits 0 and generates `public/sw.js`; `public/sw.js` is gitignored (`git check-ignore public/sw.js` succeeds)
    - `npx playwright test tests/e2e/pwa.spec.ts` exits 0 (SW registers; `/~offline` served when offline)
    - SUMMARY records that auth/sign-in/admin/confirm are NetworkFirst (Pitfall 12 guard) — and a grep/inspection confirms no CacheFirst on those routes
  </acceptance_criteria>
  <done>The app registers a service worker, precaches the shell + branded /~offline fallback, keeps auth routes NetworkFirst, and declares the Balkanity standalone identity (PLAT-02; real icons still pending Task 2).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Commit the real D-09 brand assets (BLOCKER for SC-5)</name>
  <action>PAUSE: the executor cannot self-supply the real Balkanity marks/pictograms — D-09 assets are confirmed absent from the repo. The user must commit them and provide final paths before Task 2 can wire real assets. This is a genuine human-only step (the source files exist only with the user). No code runs until paths return.</action>
  <what-built>The token/typography/component/PWA system is complete but references PLACEHOLDER icons. D-09's real brand assets (mark-white.png, mark-teal.png, plane/route pictograms, and the Mockups/design prototypes for chrome reference) are confirmed NOT in the repo (verified at planning time). Success Criterion 5 explicitly cannot be met by placeholders.</what-built>
  <how-to-verify>
    1. Commit the real brand assets to the repo: the logo marks (`mark-white.png`, `mark-teal.png`), the plane/route pictograms (`Balkanity Branding/`), and optionally the `Mockups/design/` HTML prototypes for brand-chrome reference.
    2. Provide the FINAL committed paths/filenames of each asset so Task 2 can import them (the planner could not assume paths — they did not exist).
    3. Confirm the marks are the real Balkanity teal/white marks (never re-drawn/invented icons — UI-SPEC rule).
  </how-to-verify>
  <resume-signal>Type "assets committed at &lt;paths&gt;" listing the committed logo + pictogram paths, or "skip SC-5 for now" to explicitly defer the real-asset criterion (NOT recommended — leaves SC-5 unmet).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wire real logo/pictogram assets into the design system + PWA icons (PLAT-03, SC-5)</name>
  <files>app/icon.png, public/icons/.gitkeep, app/manifest.ts, app/layout.tsx, app/sign-in/page.tsx</files>
  <read_first>
    - .planning/phases/01-platform-foundation/01-UI-SPEC.md (Open Dependency D-09 — real marks never re-drawn; pictograms from Balkanity Branding/ never invented; icon library = real Balkanity pictograms only)
    - .planning/phases/01-platform-foundation/01-CONTEXT.md (D-07 icon uses the real teal/white mark; D-09 confirm final committed paths)
    - the committed asset paths provided at the checkpoint above (do NOT proceed without them)
    - app/manifest.ts (placeholder icons from Task 1 — replace with the real icon set), app/layout.tsx, app/sign-in/page.tsx (place the real mark in the sign-in header)
  </read_first>
  <action>
    Using the real committed asset paths from the checkpoint: generate/derive the PWA icon set (e.g. 192px + 512px, plus a maskable variant) from `mark-teal.png`/`mark-white.png` and place them under `public/icons/` (or use a Next `app/icon.png` for the favicon). Replace the placeholder `icons` array in `app/manifest.ts` with the real icon paths + sizes + purposes (`any` + `maskable`). Render the real Balkanity mark in the sign-in header and the admin console chrome (teal mark on white per D-07). Wire the plane/route pictograms as importable assets in `platform/ui/` for later phases to consume (no new component required this phase beyond making them available). Do NOT invent or re-draw any icon — use only the committed assets. Remove every placeholder icon introduced in Task 1.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <acceptance_criteria>
    - The PWA icon set exists at real paths and `app/manifest.ts` references them (grep: manifest contains the real icon file path, not a placeholder name)
    - No placeholder icon files from Task 1 remain (the SUMMARY lists removed placeholders)
    - The real mark is rendered on the sign-in surface (grep the asset path / `next/image` import in `app/sign-in/page.tsx`)
    - `npx next build` exits 0; `npx eslint .` exits 0
    - SUMMARY confirms assets are the real committed Balkanity marks/pictograms (SC-5 met, not via placeholders)
  </acceptance_criteria>
  <done>Real logo/pictogram assets render via the design system and as PWA icons; SC-5 is satisfied with real assets, not placeholders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Deploy to Balkanity Vercel + verify mobile install / offline / magic-link</name>
  <action>The executor automates everything it can up to this point (build green, SW generated, assets wired). PAUSE here for the deploy + real-device verification that requires the user's Vercel project secrets and a physical mobile install — neither is headlessly assertable. Confirm Balkanity (not Kalvia) before deploying.</action>
  <what-built>The full Phase 1 foundation is built locally and green (scaffold + seam + client split + app_users migration applied + magic-link auth + brand tokens/components + EN/BG toggle + Serwist PWA + real assets). It is ready to deploy to the Balkanity Vercel project, which requires the user's Vercel project access and env secrets, and a real mobile install + magic-link walkthrough that cannot be asserted headlessly.</what-built>
  <how-to-verify>
    1. Set the four env vars in the Balkanity Vercel project (`balkanity_platform_project`, team `balkanity-platform-s-projects`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (the last two server-only — never NEXT_PUBLIC_). Confirm the project is Balkanity, NOT a Kalvia project.
    2. Deploy (Vercel CLI `vercel --prod` or Git integration). Capture the deployment URL.
    3. On a real mobile device, open the deployed URL → "Add to Home Screen" → launch → confirm the standalone shell shows the real Balkanity mark and teal theme.
    4. Toggle airplane mode / disconnect → navigate → confirm the branded "You're offline" page appears (and that the signed-in shell is NOT served stale).
    5. Run the magic-link walkthrough end to end on the deployed URL: submit the seeded admin email → open the emailed link → land authenticated on /admin.
    6. Optional: run Lighthouse PWA audit on the deployed URL → "installable" passes.
  </how-to-verify>
  <resume-signal>Type "deployed — &lt;url&gt;, install + offline + magic-link verified" or describe failures (e.g. install prompt missing, offline page not served, magic link not landing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker cache → user | Cached responses must never serve stale auth/admin state |
| deploy pipeline → live infra | Deploy + env secrets must target Balkanity, never Kalvia |
| env secrets → client bundle | Server-only secrets must stay out of NEXT_PUBLIC_ in Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Information Disclosure | stale-cached signed-in shell or status served by SW | mitigate | Auth/sign-in/admin/confirm routes NetworkFirst, never CacheFirst; only static shell + `/~offline` precached (Pitfall 12 / D-06, Task 1) |
| T-05-02 | Information Disclosure | server-only secret set as NEXT_PUBLIC_ in Vercel | mitigate | Deploy checkpoint explicitly separates the two public vs two server-only env vars; never NEXT_PUBLIC_ for service-role |
| T-05-03 | Tampering (operational) | deploy to the wrong project (Kalvia) | mitigate | Deploy checkpoint confirms `balkanity_platform_project` / Balkanity Supabase ref; STOP if Kalvia (CLAUDE.md) |
| T-05-04 | Spoofing (brand integrity) | invented/re-drawn icons instead of real marks | mitigate | Asset-commit checkpoint + Task 2 uses only committed real assets; SC-5 not met by placeholders (D-09) |
</threat_model>

<verification>
- `npx next build` exits 0 and generates `public/sw.js` (gitignored)
- `npx playwright test tests/e2e/pwa.spec.ts` exits 0 (SW registers; /~offline served offline)
- `npx eslint .` && `npx tsc --noEmit` exit 0
- Manual (documented in SUMMARY): mobile install → standalone shell with real mark; offline fallback served; deployed magic-link walkthrough lands on /admin; Lighthouse PWA installable passes
- Deploy targets Balkanity Vercel project (confirmed, not Kalvia)
</verification>

<success_criteria>
- The app installs as a PWA on mobile and serves a branded offline-aware shell, deployed to the Balkanity Vercel project (SC-1)
- Auth/admin routes are NetworkFirst (no stale signed-in shell)
- Real logo/pictogram assets render via the design system and as PWA icons (SC-5, real assets not placeholders)
</success_criteria>

<output>
Create `.planning/phases/01-platform-foundation/01-05-SUMMARY.md` when done. Record the deployment URL, the mobile install + offline + magic-link walkthrough results, the NetworkFirst guard confirmation, and the committed asset paths used.
</output>
