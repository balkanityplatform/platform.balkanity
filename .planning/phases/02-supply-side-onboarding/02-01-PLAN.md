---
phase: 02-supply-side-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/slug/slugify.ts
  - platform/slug/slugify.test.ts
  - platform/slug/uniqueness.test.ts
  - platform/money/commission.ts
  - platform/money/commission.test.ts
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
  - package.json
autonomous: true
requirements: [ONBD-03, ONBD-04]
must_haves:
  truths:
    - "slugify('Sunny Beach') returns 'sunny-beach' and a Cyrillic-only label falls back to a non-empty base"
    - "commissionCents/netCents/estStripeFeeCents return integer cents with the documented rounding (EEA 1.5% + €0.25)"
    - "Every UI-SPEC Copywriting Contract key exists in both en.ts and bg.ts (tsc parity gate passes)"
  artifacts:
    - path: "platform/slug/slugify.ts"
      provides: "URL-safe slugify pure function (D-08, Cyrillic-empty fallback per Pitfall 2)"
      exports: ["slugify"]
    - path: "platform/money/commission.ts"
      provides: "Integer-cents commission/net/fee math + EUR formatter (D-05/D-06/D-07)"
      exports: ["commissionCents", "netCents", "estStripeFeeCents", "fmtEur"]
    - path: "platform/slug/slugify.test.ts"
      provides: "Wave 0 unit coverage for ONBD-03 slug rules + fallback"
    - path: "platform/money/commission.test.ts"
      provides: "Wave 0 unit coverage for ONBD-04 cents math + rounding"
    - path: "platform/slug/uniqueness.test.ts"
      provides: "Wave 0 unit coverage for collision-suffix strategy (mocked taken-check)"
  key_links:
    - from: "platform/i18n/bg.ts"
      to: "platform/i18n/en.ts"
      via: "Dict = typeof en parity contract"
      pattern: "Dict"
---

<objective>
Establish the leaf-level, dependency-free building blocks every later slice in this phase
consumes: the pure `slugify()` and money/`commission` utilities (with their Wave 0 unit tests
written first), and ALL new EN/BG dictionary keys from the UI-SPEC Copywriting Contract. No DB,
no routes, no UI components — this plan exists so the CRUD slices (02–05) import finished,
tested primitives and finished copy rather than inventing them mid-flight.

Purpose: Unblock waves 2+ with tested transforms (slugify per D-08, commission/net/fee per
D-05/D-06/D-07) and a complete, type-checked bilingual dictionary so no later plan ever touches
`en.ts`/`bg.ts` (avoids a serial write conflict across parallel CRUD plans).
Output: `platform/slug/slugify.ts` (+2 tests), `platform/money/commission.ts` (+1 test),
extended `platform/i18n/{en,bg}.ts`, and an explicit `zod` dependency in `package.json`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-supply-side-onboarding/02-CONTEXT.md
@.planning/phases/02-supply-side-onboarding/02-RESEARCH.md
@.planning/phases/02-supply-side-onboarding/02-PATTERNS.md
@.planning/phases/02-supply-side-onboarding/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: slugify utility + Wave 0 tests (RED→GREEN)</name>
  <files>platform/slug/slugify.ts, platform/slug/slugify.test.ts, platform/slug/uniqueness.test.ts</files>
  <read_first>
    - `.planning/phases/02-supply-side-onboarding/02-RESEARCH.md` lines 269-291 (Pattern 3: slugify body + uniqueSlug collision-suffix) and lines 355-358 (Pitfall 2: Cyrillic-empty fallback) — copy the documented transform.
    - `.planning/phases/02-supply-side-onboarding/02-PATTERNS.md` lines 269-278 ("No Analog Found": pure fn, ships with co-located Vitest test).
    - `platform/auth/role.test.ts` lines 1-40 — the existing Vitest analog: `import { describe, expect, it } from "vitest"`, `vi.mock` shape, file naming `*.test.ts`.
    - `vitest.config.ts` — confirms `include: ["platform/**/*.test.{ts,tsx}"]` so these tests are picked up; quick run is `npm test`.
  </read_first>
  <behavior>
    - slugify("Sunny Beach Resort") === "sunny-beach-resort"
    - slugify("Côte d'Azur") strips Latin diacritics → "cote-d-azur" (no leading/trailing/double hyphens)
    - slugify("  Multiple   Spaces  ") collapses to "multiple-spaces"
    - slugify("Слънчев бряг") (Cyrillic-only) === "" from the raw transform; the uniqueness helper falls back to a non-empty base ("dest")
    - slugify truncates to <= 80 chars
    - uniqueSlug-style collision: given a mocked "taken" predicate that returns true for "x" once, the resolver returns "x-2"
  </behavior>
  <action>
    Write `platform/slug/slugify.test.ts` FIRST (RED) asserting the behaviors above against the
    not-yet-written `slugify` export. Then implement `platform/slug/slugify.ts` exporting
    `slugify(input: string): string` using the RESEARCH Pattern 3 transform: `.normalize("NFKD")`,
    strip combining marks, `.toLowerCase()`, replace `[^a-z0-9]+` with `-`, trim leading/trailing
    hyphens, collapse `-{2,}` runs, `.slice(0, 80)`. Also write `platform/slug/uniqueness.test.ts`
    covering the collision-suffix strategy: a pure helper `nextSlugCandidate(base: string, n: number)`
    that returns `base` for n===1 and `${base}-${n}` for n>1, with the empty-base fallback to `"dest"`.
    Export `nextSlugCandidate` from `slugify.ts` so the destinations server action (Plan 04) reuses it
    rather than re-implementing the suffix loop. No new dependency — hand-rolled per D-08 / CLAUDE.md
    "simplest thing that works". Run `npm test` until GREEN.
  </action>
  <verify>
    <automated>npx vitest run platform/slug/slugify.test.ts platform/slug/uniqueness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run platform/slug/slugify.test.ts platform/slug/uniqueness.test.ts` exits 0 with all assertions green.
    - `platform/slug/slugify.ts` exports both `slugify` and `nextSlugCandidate` (`grep -E "export function (slugify|nextSlugCandidate)" platform/slug/slugify.ts` returns 2 matches).
    - Source assertion: `platform/slug/slugify.ts` contains `normalize("NFKD")` and `slice(0, 80)`.
    - Behavior: slugify of a Cyrillic-only string returns "" and the test asserts the "dest" fallback via nextSlugCandidate.
  </acceptance_criteria>
  <done>slugify + nextSlugCandidate implemented and fully covered by green Wave 0 unit tests; Cyrillic-empty fallback proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commission/money utility + Wave 0 tests + explicit zod dep</name>
  <files>platform/money/commission.ts, platform/money/commission.test.ts, package.json</files>
  <read_first>
    - `.planning/phases/02-supply-side-onboarding/02-RESEARCH.md` lines 293-310 (Pattern 4: commissionCents/netCents/estStripeFeeCents/fmtEur bodies) and line 111 (add `"zod": "^4.4"` to dependencies so the import is explicit, not a transitive hoist).
    - `CLAUDE.md` Verified Provider Facts row "Stripe — EEA cards" — fee is **1.5% + €0.25** (25 cents fixed); this is the estimate-only note for D-06.
    - `.planning/phases/02-supply-side-onboarding/02-CONTEXT.md` D-05/D-06/D-07 — commission is whole-percent of price; "you keep (before fees)" = price − commission; fee is an estimate note only; money stored as integer minor units (cents).
    - `platform/auth/role.test.ts` lines 1-40 — Vitest test shape to mirror.
    - `package.json` lines 11-19 (dependencies block) — add `zod` alongside existing deps.
  </read_first>
  <behavior>
    - commissionCents(10000, 15) === 1500  (15% of €100.00)
    - commissionCents(999, 15) === 150     (rounds half-up: 149.85 → 150)
    - netCents(10000, 15) === 8500          (price − commission)
    - estStripeFeeCents(10000) === 175       (Math.round(10000*0.015)=150, +25 fixed)
    - estStripeFeeCents(0) === 25            (fixed €0.25 floor)
    - fmtEur(8500) === "85.00"
    - commissionCents(priceCents, 0) === 0 and commissionCents(priceCents, 100) === priceCents (range endpoints)
  </behavior>
  <action>
    Write `platform/money/commission.test.ts` FIRST (RED) asserting the behaviors above. Then implement
    `platform/money/commission.ts` exporting `commissionCents(priceCents: number, pct: number): number`
    (= `Math.round((priceCents * pct) / 100)`, whole-percent, round half-up per D-05 planner rounding
    call), `netCents(priceCents, pct)` (= `priceCents - commissionCents(...)`), `estStripeFeeCents(priceCents)`
    (= `Math.round(priceCents * 0.015) + 25`, EEA 1.5% + €0.25 estimate-only, D-06), and
    `fmtEur(cents: number): string` (= `(cents / 100).toFixed(2)`). Integer cents only — never floats (D-07).
    Add a header comment noting these are DISPLAY-ONLY derivations: never persist commission/net/fee.
    Separately, add `"zod": "^4.4"` to the `dependencies` object in `package.json` (zod 4.4.3 is already
    resolved in node_modules but is currently only a transitive hoist; making it explicit is required for
    server-side form validation in Plans 02–05 per RESEARCH Standard Stack). Run `npm install` to update
    the lockfile, then `npm test` until GREEN.
  </action>
  <verify>
    <automated>npx vitest run platform/money/commission.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run platform/money/commission.test.ts` exits 0 with all assertions green.
    - `grep -E "export function (commissionCents|netCents|estStripeFeeCents)" platform/money/commission.ts` returns 3 matches; `grep "export const fmtEur" platform/money/commission.ts` returns 1.
    - Source assertion: `platform/money/commission.ts` contains `+ 25` (the €0.25 fixed fee) and `* 0.015` (the 1.5% rate).
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies.zod?0:1)"` exits 0 (zod is now an explicit dependency).
    - Behavior: estStripeFeeCents(10000) === 175 is asserted green.
  </acceptance_criteria>
  <done>commission/net/fee/fmtEur implemented with integer-cents semantics, proven by green Wave 0 tests; zod added as an explicit dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Add all Phase 2 UI-SPEC copy keys to en.ts + bg.ts (parity gate)</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts</files>
  <read_first>
    - `.planning/phases/02-supply-side-onboarding/02-UI-SPEC.md` lines 117-152 (the full Copywriting Contract: EN canonical values for every CTA, empty state, error, "you keep" line, slug warning, invite delivery note, deactivate/delete confirmations, and the blocked-deactivation copy).
    - `platform/i18n/en.ts` lines 7-36 — the `en` object to extend in place; `Dict = typeof en` auto-propagates the shape (line 36 / 33-36).
    - `platform/i18n/bg.ts` (full) — must add the SAME keys or `tsc` fails (parity gate T-04-03); mirror the existing Bulgarian translation style.
    - `.planning/phases/02-supply-side-onboarding/02-PATTERNS.md` lines 231-233 — exact list of keys to add and the "never hard-code strings in JSX (PLAT-04)" rule.
  </read_first>
  <action>
    Extend the `en` object in `platform/i18n/en.ts` with every Phase 2 key from the UI-SPEC Copywriting
    Contract, using the EN canonical values verbatim. Concrete keys to add (camelCase ids):
    `createCompanyCta`, `addPropertyCta`, `saveDestinationCta`, `generateInviteLinkCta`, `saveChangesCta`,
    `cancelCta`; empty-state pairs `companiesEmptyHeading`/`companiesEmptyBody`,
    `propertiesEmptyHeading`/`propertiesEmptyBody`, `destinationsEmptyHeading`/`destinationsEmptyBody`,
    `driversEmptyHeading`/`driversEmptyBody`; errors `fieldRequired`, `slugTaken`, `slugInvalid`,
    `commissionRange`, `saveFailed`; "you keep" lines `youKeepCommissionLine`, `youKeepNetLine`,
    `youKeepFeeNote`; `slugEditWarning`, `inviteLinkDeliveryNote`, `inviteLinkCopyCta`,
    `driverAlreadyInvited`; deactivate/delete confirmations `deactivateDestinationConfirm`/`...Body`,
    `deactivatePropertyConfirm`/`...Body`, `deactivateCompanyConfirm`/`...Body`, `deleteForeverConfirm`/`...Body`,
    `deactivateConfirmCta`, `deleteForeverCta`, `keepCta`; blocked-deactivation `deactivateCompanyBlocked`,
    `deactivatePropertyBlocked`; field labels `companyNameLabel`, `propertyNameLabel`, `destinationLabelLabel`,
    `slugLabel`, `addressLabel`, `zoneLabel`, `airportLabel`, `priceLabel`, `commissionPctLabel`,
    `activeLabel`, `inactiveLabel`, `driverNameLabel`, `driverPhoneLabel`, and page titles
    `companiesTitle`, `propertiesTitle`, `destinationsTitle`, `driversTitle`, `inviteDriverTitle`.
    The "you keep" lines use `{pct}`/`{amount}` placeholder tokens exactly as written in the UI-SPEC
    (line interpolation happens in the form island, Plan 04). Then add the identical key set to `bg.ts`
    with Bulgarian translations (different values, same keys) so the `Dict = typeof en` parity gate holds.
    Do NOT change any existing Phase 1 key. No i18n library (D-05).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (proves bg.ts satisfies the `Dict = typeof en` shape — the parity gate).
    - Source assertion: `grep -c "youKeepFeeNote" platform/i18n/en.ts` === 1 AND `grep -c "youKeepFeeNote" platform/i18n/bg.ts` === 1.
    - Source assertion: `grep -c "deactivateCompanyBlocked" platform/i18n/en.ts` === 1 AND in bg.ts === 1.
    - Source assertion: `grep -c "slugEditWarning" platform/i18n/en.ts` === 1 (D-10 coral warning copy present).
    - Behavior: a key removed from bg.ts would fail `npm run typecheck` (parity gate is the enforcement).
  </acceptance_criteria>
  <done>Every UI-SPEC Copywriting Contract key exists in both dictionaries; tsc parity gate green; no later plan needs to touch en.ts/bg.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in this plan) | Pure functions + static config only; no untrusted input is processed, no DB, no auth, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | slugify of guest-shared label | accept | slugify is a pure string transform with no PII input; the label is admin-typed, not guest data. No PII path exists in this plan. |
| T-02-02 | Tampering | money math correctness (financial display) | mitigate | Integer-cents only (no float drift); behaviors pinned by Wave 0 unit tests incl. rounding endpoints; values are display-only and never persisted (Phase 3 owns real fee logic). |
| T-02-SC | Tampering | npm install (`zod` made explicit) | accept | No NEW package introduced — `zod@4.4.3` is already resolved/vetted (Phase 1; RESEARCH Package Legitimacy Audit). Making the existing transitive dep explicit adds no new supply-chain surface; slopcheck N/A per RESEARCH. |
</threat_model>

<verification>
- `npm test` (full vitest run) green including the three new test files.
- `npm run typecheck` green (dictionary parity gate).
- No new route, no DB change, no service-role usage in this plan.
</verification>

<success_criteria>
- slugify + nextSlugCandidate + commission/net/fee/fmtEur exported and unit-tested green (ONBD-03, ONBD-04 leaf logic).
- All UI-SPEC copy keys present in en.ts and bg.ts; tsc parity gate passes.
- zod is an explicit `dependencies` entry.
</success_criteria>

<output>
Create `.planning/phases/02-supply-side-onboarding/02-01-SUMMARY.md` when done.
</output>
