---
phase: 03-payments-trust-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - platform/payments/single-writer.test.ts
  - app/api/stripe/webhook/route.contract.test.ts
  - platform/rls/payments-schema.test.ts
  - platform/payments/checkout.test.ts
  - platform/payments/fee.test.ts
  - tests/e2e/webhook-forged.spec.ts
  - tests/e2e/success-spoof.spec.ts
  - .planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md
autonomous: true
requirements: [BOOK-05, HLTH-01]
user_setup:
  - service: stripe
    why: "Code-created Checkout Sessions + signature-verified webhook (TEST mode this phase, D-02)"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard (toggle TEST mode) -> Developers -> API keys -> Secret key (sk_test_...)"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe CLI `stripe listen --forward-to localhost:3000/api/stripe/webhook` prints whsec_... (local); or Dashboard -> Developers -> Webhooks -> signing secret"
    dashboard_config:
      - task: "Install Stripe CLI for the replay gate (SC3)"
        location: "brew install stripe/stripe-cli/stripe (macOS) then `stripe login`"

must_haves:
  truths:
    - "Every Wave 0 test file from 03-VALIDATION.md exists and runs (RED is acceptable for not-yet-implemented targets)"
    - "stripe@^22.2 is installed and the lockfile is committed"
    - "The replay gate has a written CLI runbook an operator can follow in TEST mode"
  artifacts:
    - path: "platform/payments/single-writer.test.ts"
      provides: "Source-level grep gate: exactly one status='paid' writer (SC1)"
      contains: "paid"
    - path: "app/api/stripe/webhook/route.contract.test.ts"
      provides: "Asserts runtime='nodejs' + req.text() + no req.json() (SC1)"
      contains: "nodejs"
    - path: "platform/rls/payments-schema.test.ts"
      provides: "Source-level contract for migration 0003 (SC3/SC4)"
      contains: "webhook_events"
    - path: "platform/payments/checkout.test.ts"
      provides: "Asserts sessions.create args: EUR, integer unit_amount, metadata.transfer_id (SC5)"
      contains: "metadata"
    - path: "platform/payments/fee.test.ts"
      provides: "Asserts fee_cents derived from balance_transaction.fee (SC5/D-05)"
      contains: "balance_transaction"
    - path: "tests/e2e/webhook-forged.spec.ts"
      provides: "HTTP adversarial gate: forged POST -> 400, zero state change (SC2)"
      contains: "400"
    - path: "tests/e2e/success-spoof.spec.ts"
      provides: "HTTP adversarial gate: success_url never writes paid (SC2)"
      contains: "paid"
    - path: ".planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md"
      provides: "Stripe CLI replay runbook for SC3"
      contains: "stripe events resend"
  key_links:
    - from: "vitest.config.ts include globs"
      to: "platform/payments/*.test.ts + app/api/stripe/webhook/route.contract.test.ts"
      via: "platform/** and app/** include patterns"
      pattern: "platform/\\*\\*"
    - from: "package.json dependencies"
      to: "stripe@^22.2"
      via: "npm install"
      pattern: "\"stripe\""
---

<objective>
Establish the Nyquist test baseline for the payments trust spine BEFORE any implementation: create every Wave 0 test scaffold listed in 03-VALIDATION.md, install the one new dependency (`stripe@^22.2`), and write the operator runbook for the Stripe-CLI replay gate (SC3). These tests describe the exact contract the rest of the phase must satisfy; most start RED (their implementation targets do not exist yet) and turn GREEN as Plans 02-04 land.

Purpose: A trust-spine phase must prove its invariants. Writing the source-level and unit tests first locks the contract (single `paid` writer, `nodejs` runtime + raw body, UNIQUE event_id, EUR/integer-cents checkout, real-fee recording, forged/spoof gates) so implementation cannot silently drift.
Output: 7 test files (5 vitest, 2 Playwright), the `stripe` dependency + committed lockfile, and the replay runbook.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-trust-spine/03-CONTEXT.md
@.planning/phases/03-payments-trust-spine/03-RESEARCH.md
@.planning/phases/03-payments-trust-spine/03-PATTERNS.md
@.planning/phases/03-payments-trust-spine/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install stripe@^22.2 and verify the SDK is clean</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current deps; stripe is NOT present yet)
    - .planning/phases/03-payments-trust-spine/03-RESEARCH.md §"Standard Stack" + §"Package Legitimacy Audit" (stripe@22.2.1 verified, no postinstall, official SDK — Approved)
  </read_first>
  <action>
    Run `npm view stripe version` (expect 22.2.x) and `npm view stripe scripts.postinstall` (expect empty) to re-confirm the audit at install time per D-02 / RESEARCH §"Version verification". Then `npm install stripe@^22.2`. `stripe` is the only new dependency this phase; `@supabase/supabase-js`, `zod`, `server-only` are already present. Do NOT add `@stripe/stripe-js` — CLAUDE.md "What NOT to Use" locks the server 303-redirect path (no client Stripe dep). After install, run `npx tsc --noEmit` to confirm the package's typings resolve and that the `apiVersion` literal `'2026-05-27.dahlia'` will type-check against the installed v22 typings (Research Q2 planner-time check — if the literal is rejected, record the exact accepted version string in the SUMMARY rather than casting `as any`). Commit `package.json` + `package-lock.json`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.stripe) {process.exit(1)}" && grep -q '"stripe"' package-lock.json && echo "stripe installed + locked"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies.stripe` is present and on the `^22.2` line; `package-lock.json` is updated and committed.
    - `@stripe/stripe-js` is NOT added to dependencies.
    - `npm view stripe scripts.postinstall` returned empty (no install-time side effects), recorded in the SUMMARY.
    - `npx tsc --noEmit` exits 0 with stripe typings resolvable.
  </acceptance_criteria>
  <done>stripe@^22.2 installed, lockfile committed, no @stripe/stripe-js, postinstall confirmed empty.</done>
</task>

<task type="auto">
  <name>Task 2: Create the 5 vitest source-level + unit test scaffolds</name>
  <files>platform/payments/single-writer.test.ts, app/api/stripe/webhook/route.contract.test.ts, platform/rls/payments-schema.test.ts, platform/payments/checkout.test.ts, platform/payments/fee.test.ts</files>
  <read_first>
    - platform/rls/supply-rls.test.ts (EXACT analog: read-source + strip-comments harness, regex assertions, Balkanity-ref guardrail asserts at lines 9-29, 66-70, 85-88)
    - .planning/phases/03-payments-trust-spine/03-PATTERNS.md §"platform/rls/payments-schema.test.ts" + §"single-writer.test.ts + route.contract.test.ts" + §"checkout.test.ts + fee.test.ts"
    - .planning/phases/03-payments-trust-spine/03-VALIDATION.md §"Per-Task Verification Map" (the runnable command per criterion) + §"Wave 0 Requirements"
    - vitest.config.ts (include globs: platform/**, app/** — these paths are picked up; tests/e2e/** is excluded)
  </read_first>
  <action>
    Author five vitest files mirroring `supply-rls.test.ts` (read source text, strip `--`/comment lines, assert via regex):
    (1) `platform/rls/payments-schema.test.ts` — source-level contract for `supabase/migrations/0003_payments_spine.sql`: asserts RLS enabled on BOTH `wp_transfers` and `webhook_events`; exactly one `for select to authenticated using (public.is_admin())` admin-read policy each; NO `for insert|for update|for delete` policy; UNIQUE index on `webhook_events (event_id)`; required `webhook_events` columns `event_id`, `type`, `signature_result`, `outcome`; `wp_transfers` columns `status`, `amount_cents integer not null check (amount_cents >= 0)`, `currency`, `fee_cents`, `paid_at`, `stripe_checkout_session_id`, `stripe_payment_intent_id`, FK to `destinations`; the Balkanity-ref guardrail strings `qyhdogajtmnvxphrslwm` AND `Kalvia (utyatpadtibqqswsfvtr)` present in the (un-stripped) migration header. (2) `platform/payments/single-writer.test.ts` — read source across `app/`, `platform/`, `modules/` (recursively, skipping `node_modules`/`.next`/`*.test.*`), strip comments, assert exactly ONE file contains a `status:` set to `paid` write (the webhook route); use a comment-filtered match so header prose does not self-invalidate the gate. (3) `app/api/stripe/webhook/route.contract.test.ts` — read `app/api/stripe/webhook/route.ts`; assert the literal `export const runtime = "nodejs"` present, `req.text()` present, `req.json(` ABSENT (Pitfall 1/2). (4) `platform/payments/checkout.test.ts` — mock the Stripe SDK; call the checkout helper; assert `stripe.checkout.sessions.create` invoked with `mode:"payment"`, `currency:"eur"`, integer `unit_amount`, `metadata.transfer_id` (SC5). (5) `platform/payments/fee.test.ts` — mock an expanded PaymentIntent whose `latest_charge.balance_transaction.fee` is an integer; assert the fee helper returns that exact integer-cents value and guards a null balance_transaction (D-05, Pitfall 5). These reference implementation files that DO NOT exist yet (Plans 02-04) — they are EXPECTED to be RED now; that is the Nyquist baseline. Do NOT stub the implementations here.
  </action>
  <verify>
    <automated>npx vitest run platform/rls/payments-schema.test.ts platform/payments/single-writer.test.ts platform/payments/checkout.test.ts platform/payments/fee.test.ts app/api/stripe/webhook/route.contract.test.ts 2>&1 | grep -Eq "Test Files|FAIL|PASS|No test files" && test -f platform/rls/payments-schema.test.ts && test -f platform/payments/single-writer.test.ts && test -f app/api/stripe/webhook/route.contract.test.ts && test -f platform/payments/checkout.test.ts && test -f platform/payments/fee.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist at the exact paths above and are collected by `vitest run` (covered by `vitest.config.ts` include globs).
    - `payments-schema.test.ts` asserts: 2x `enable row level security`, the no-write-policy rule (`not.toMatch(/for insert|for update|for delete/)`), UNIQUE `webhook_events (event_id)`, the required column set, and both Balkanity-ref guardrail strings.
    - `single-writer.test.ts` strips comments before counting `paid` writers (no bare `== 0` on unfiltered text; header prose cannot satisfy or break the gate).
    - `route.contract.test.ts` asserts `runtime = "nodejs"` AND `req.text()` present AND `req.json(` absent.
    - `checkout.test.ts` asserts `currency:"eur"`, integer `unit_amount`, `metadata.transfer_id`, `mode:"payment"`. `fee.test.ts` asserts `fee_cents === balance_transaction.fee` and null-guard.
    - Failures at this stage are RED-because-unimplemented, not RED-because-malformed (the test files themselves compile and run).
  </acceptance_criteria>
  <done>Five vitest scaffolds exist, collected by vitest, encoding the SC1/SC3/SC4/SC5/D-05 contracts; RED pending Plans 02-04.</done>
</task>

<task type="auto">
  <name>Task 3: Create the 2 Playwright adversarial-gate specs + the replay runbook</name>
  <files>tests/e2e/webhook-forged.spec.ts, tests/e2e/success-spoof.spec.ts, .planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/03-payments-trust-spine/03-RESEARCH.md §"Code Examples" (forged-POST curl sketch) + §"Common Pitfalls" (Pitfall 3 success-URL)
    - .planning/phases/03-payments-trust-spine/03-VALIDATION.md §"Manual-Only Verifications" (replay requires Stripe CLI) + §"Wave 0 Requirements"
    - vitest.config.ts (note: `tests/e2e/**` is EXCLUDED from vitest; these run via `npm run test:e2e` / Playwright only)
    - package.json (the `test:e2e` script -> `playwright test`)
  </read_first>
  <action>
    Author two Playwright specs under `tests/e2e/` (NOT picked up by vitest; run via `npm run test:e2e`):
    (1) `webhook-forged.spec.ts` — POST to `/api/stripe/webhook` with header `stripe-signature: t=1,v1=deadbeef` and a JSON-ish body for a fake `evt_forged` `checkout.session.completed`; assert HTTP 400 AND zero state change (no `webhook_events` row for `evt_forged`, and any seeded transfer remains `requested` — the assertion may read state via the success display page or a service-role check helper, documented inline). Also include a missing-`stripe-signature` variant expecting 400.
    (2) `success-spoof.spec.ts` — request the `/pay/success?t=<seeded-transfer-id>` page directly (simulating a guest who spoofs the redirect) WITHOUT any webhook firing; assert the transfer is NOT `paid` (page renders a non-paid status) — proving the success page is display-only (Pitfall 3, BOOK-05). These specs reference routes/pages that do not exist until Plan 04 — they are RED now (expected). Mark any seeding that requires the live TEST DB as gated behind Plan 05 and document the prerequisite at the top of each spec.
    Then write `03-REPLAY-RUNBOOK.md` — the operator runbook for SC3 (Stripe CLI is NOT installed on this machine; the runbook is followed in Plan 05): `brew install stripe/stripe-cli/stripe` then `stripe login`; `stripe listen --forward-to localhost:3000/api/stripe/webhook` (copy the printed `whsec_…` into `STRIPE_WEBHOOK_SECRET`); trigger a real signed `checkout.session.completed` against a seeded `wp_transfers` row; capture the `evt_…` id; `stripe events resend evt_…`; assert exactly one `webhook_events` row for that `event_id` (UNIQUE) and `wp_transfers.paid_at` unchanged on the second delivery (replay → exactly one effect). Note all of this runs in Stripe TEST mode (D-02); no real money moves.
  </action>
  <verify>
    <automated>test -f tests/e2e/webhook-forged.spec.ts && test -f tests/e2e/success-spoof.spec.ts && test -f .planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md && grep -q "stripe events resend" .planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md && grep -q "deadbeef" tests/e2e/webhook-forged.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both specs exist under `tests/e2e/`; the forged spec asserts HTTP 400 + zero state change for `evt_forged` and a missing-signature 400 variant; the success-spoof spec asserts the transfer is NOT `paid` after a direct success-page hit.
    - Neither spec is collected by `vitest run` (they live under the vitest `exclude` glob `tests/e2e/**`).
    - `03-REPLAY-RUNBOOK.md` contains the full `stripe listen` + `stripe events resend` sequence, the TEST-mode note (D-02), and the exactly-one-effect assertion (one `webhook_events` row + unchanged `paid_at`).
  </acceptance_criteria>
  <done>Two Playwright adversarial specs + the SC3 replay runbook exist; RED/manual pending Plans 04-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local install | Supply-chain trust for the new `stripe` dependency |
| (no runtime boundary this plan) | This plan ships tests + a dependency only; no request-handling code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `npm install stripe@^22.2` (supply chain) | mitigate | Re-confirm `npm view stripe version` (22.2.x) + `npm view stripe scripts.postinstall` empty at install time; `stripe` is the official project-locked SDK (RESEARCH §"Package Legitimacy Audit" — Approved, no postinstall). Commit the lockfile to pin the resolved tree. |
| T-03-01 | Tampering | Wave 0 test gates themselves | mitigate | `single-writer.test.ts` strips comments before counting `paid` writers so header prose cannot self-invalidate the gate; `payments-schema.test.ts` asserts on comment-stripped SQL so a commented-out policy cannot satisfy the contract (mirrors `supply-rls.test.ts`). |
| T-03-ID | Information Disclosure | Stripe TEST secrets in `.env.local` | accept (this plan) | This plan does not write env values; secret handling + server-only boundary is enforced in Plan 03 (T-03-ID2). STATE.md SECURITY TODO (`.env.local.example` holds real secrets) is tracked separately; do not add Stripe keys to any tracked file. |
| T-03-SC2 | Tampering | adversarial specs are accurate | mitigate | Forged spec sends a deliberately-bad `stripe-signature` and a missing-signature variant; success-spoof spec hits the redirect URL directly with no webhook — both encode the real attacker action, not a friendly path. |
</threat_model>

<verification>
- `npm run test` collects (and may show RED for) the five vitest payment tests — RED here is expected (targets unimplemented).
- `npx tsc --noEmit` passes (stripe typings resolve; test files compile).
- `npm run test:e2e` discovers the two adversarial specs (they will fail until Plan 04 ships the routes/pages — expected).
- The replay runbook exists and is self-contained for an operator in Plan 05.
</verification>

<success_criteria>
- All 7 test files + the replay runbook exist at the exact paths in `files_modified`.
- `stripe@^22.2` is in `dependencies`; lockfile committed; no `@stripe/stripe-js`.
- The five vitest files are collected by `vitest run`; the two e2e specs are NOT (excluded glob).
- Tests encode SC1 (single writer + nodejs/raw-body), SC3 (UNIQUE event_id), SC4 (webhook_events columns + RLS no-write), SC5/D-05 (EUR/integer checkout + real fee), and SC2 (forged-400 + success-spoof) contracts.
</success_criteria>

<output>
Create `.planning/phases/03-payments-trust-spine/03-01-SUMMARY.md` when done.
</output>
