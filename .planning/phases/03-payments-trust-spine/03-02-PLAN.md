---
phase: 03-payments-trust-spine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - supabase/migrations/0003_payments_spine.sql
autonomous: true
requirements: [BOOK-05, HLTH-01]

must_haves:
  truths:
    - "Migration 0003 authors a minimal wp_transfers table the webhook can flip to paid (D-03)"
    - "Migration 0003 authors a webhook_events audit/idempotency table with UNIQUE event_id (HLTH-01)"
    - "Both tables are RLS-enabled, admin-read only, with NO write policy (writes via service-role only)"
    - "The migration carries the Balkanity-ref guardrail header and is FLAGGED for sign-off before apply"
  artifacts:
    - path: "supabase/migrations/0003_payments_spine.sql"
      provides: "wp_transfers (minimal money-spine columns) + webhook_events + RLS + UNIQUE event_id"
      contains: "create table public.webhook_events"
      min_lines: 60
  key_links:
    - from: "supabase/migrations/0003_payments_spine.sql"
      to: "public.is_admin()"
      via: "admin-read RLS policy reuses the 0002 helper (not redefined)"
      pattern: "using \\(public\\.is_admin\\(\\)\\)"
    - from: "wp_transfers.destination_id"
      to: "public.destinations(id)"
      via: "FK so metadata.transfer_id resolves to a real row"
      pattern: "references public\\.destinations"
    - from: "webhook_events.event_id"
      to: "UNIQUE index/constraint"
      via: "insert-first idempotency authority"
      pattern: "unique"
---

<objective>
Author migration `0003_payments_spine.sql` — the FLAGGED schema the money path writes against. It creates (a) a minimal `wp_transfers` table with only the money-spine columns the webhook needs (D-03), so `metadata.transfer_id` resolves to a real row; and (b) the platform-generic `webhook_events` audit/idempotency table with a UNIQUE `event_id` (HLTH-01). Both tables are RLS-enabled, admin-read only, with NO write policy — every write goes through the service-role client, exactly as Phase 2's `0002` established.

This plan authors the migration FILE and turns `platform/rls/payments-schema.test.ts` (from Plan 01) GREEN. It does NOT apply the migration to the live database — the live push is the BLOCKING, human-signed-off task in Plan 05 (D-04, ROADMAP sign-off note).

Purpose: The webhook's single-writer + idempotency guarantees are proven against a REAL table, not a throwaway fixture (D-03). Phase 4 will ALTER this table to add PII + full lifecycle columns (D-04) — it must already exist.
Output: `supabase/migrations/0003_payments_spine.sql`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-payments-trust-spine/03-CONTEXT.md
@.planning/phases/03-payments-trust-spine/03-RESEARCH.md
@.planning/phases/03-payments-trust-spine/03-PATTERNS.md
@supabase/migrations/0002_supply_tables.sql
@platform/rls/payments-schema.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0003 — wp_transfers (minimal) + webhook_events + RLS</name>
  <files>supabase/migrations/0003_payments_spine.sql</files>
  <read_first>
    - supabase/migrations/0002_supply_tables.sql (EXACT analog — header guardrail lines 1-3; integer-cents + CHECK convention lines 73-74; `is_admin()` helper lines 22-33 — REUSE, do NOT redefine; `enable row level security` block lines 99-104; admin-read `for select to authenticated using (public.is_admin())` policies lines 112-122; the `destinations_slug_key` unique-index pattern line 83)
    - platform/rls/payments-schema.test.ts (the source-level contract this migration must satisfy — written in Plan 01; read it so the SQL matches the asserted regexes exactly)
    - .planning/phases/03-payments-trust-spine/03-PATTERNS.md §"supabase/migrations/0003_payments_spine.sql" (column list + RLS posture + UNIQUE key guidance)
    - .planning/phases/03-payments-trust-spine/03-RESEARCH.md §"Architecture Patterns" (the schema box in the System Architecture Diagram) + Assumption A1 (wp_ prefix vs unprefixed)
  </read_first>
  <action>
    Author `supabase/migrations/0003_payments_spine.sql` mirroring `0002`:
    HEADER (verbatim shape, asserted by the schema test): `-- 0003_payments_spine.sql`; `-- FLAGGED / IRREVERSIBLE schema migration — requires human sign-off before apply.`; `-- Target: Balkanity Supabase project ref qyhdogajtmnvxphrslwm ONLY. NEVER Kalvia (utyatpadtibqqswsfvtr).` Include trust-boundary notes referencing the threat IDs (single-writer `paid`, insert-first idempotency, admin-read/no-write) and a seam note (`wp_transfers` = module table → `wp_` prefix; `webhook_events` = platform-generic → UNPREFIXED).
    DO NOT redefine `public.is_admin()` — it already exists from `0002`; the new policies reuse it.
    TABLE 1 `public.wp_transfers` (D-03, minimal money-spine columns ONLY): `id uuid primary key default gen_random_uuid()`; `destination_id uuid not null references public.destinations(id) on delete restrict` (so `metadata.transfer_id` resolves to a real destination-backed row); `status text not null default 'requested'` (the `paid` writer's target); `amount_cents integer not null check (amount_cents >= 0)`; `currency text not null default 'eur'` (D-01 — EUR, no BGN layer); `fee_cents integer` (nullable — the actual fee from the balance transaction, D-05; null when not yet available, Phase 8 backfills, Pitfall 5); `stripe_checkout_session_id text`; `stripe_payment_intent_id text`; `paid_at timestamptz`; `created_at timestamptz not null default now()`. Add a `table` comment noting Phase 4 ALTERs this to add PII + full lifecycle columns (D-04) and that `status` is flipped to `paid` ONLY by the verified webhook (BOOK-05).
    TABLE 2 `public.webhook_events` (HLTH-01 audit + idempotency): `id uuid primary key default gen_random_uuid()`; `event_id text not null` (Stripe `evt_…` — the idempotency key); `type text not null` (e.g. `checkout.session.completed`); `signature_result text not null` (e.g. `valid`); `outcome text not null default 'received'` (`received` → `processed` / `duplicate_skipped` / `no_matching_transfer`); `payload jsonb` (the verified event for audit, discretion column); `created_at timestamptz not null default now()`. Add UNIQUE on `event_id`: `create unique index webhook_events_event_id_key on public.webhook_events (event_id)` (mirrors `destinations_slug_key`) — the race-safe replay authority (SC3, insert-first dedup).
    RLS: `enable row level security` on BOTH tables; one `create policy "<table>_admin_read" on public.<table> for select to authenticated using (public.is_admin())` each; NO insert/update/delete policy on either (writes via service-role only — Phase 2 lock; the webhook's service-role client bypasses RLS).
    Integer cents end-to-end (no floats); EUR currency default (D-01). This is FILE AUTHORING ONLY — do NOT run `supabase db push` / apply to the live DB here (Plan 05 BLOCKING task owns the signed-off apply, D-04).
  </action>
  <verify>
    <automated>npx vitest run platform/rls/payments-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run platform/rls/payments-schema.test.ts` is GREEN (was RED after Plan 01).
    - Migration text (comment-stripped) contains exactly 2x `enable row level security`, exactly two `for select to authenticated using (public.is_admin())` admin-read policies, and ZERO `for insert|for update|for delete` policies.
    - `create unique index webhook_events_event_id_key on public.webhook_events (event_id)` present.
    - `wp_transfers` has `amount_cents integer not null check (amount_cents >= 0)`, `currency` (default `'eur'`), `fee_cents` (nullable integer), `status`, `paid_at`, `stripe_checkout_session_id`, `stripe_payment_intent_id`, and `destination_id ... references public.destinations(id)`.
    - `webhook_events` has `event_id`, `type`, `signature_result`, `outcome` columns.
    - Header contains both `qyhdogajtmnvxphrslwm` and `Kalvia (utyatpadtibqqswsfvtr)`; `public.is_admin()` is referenced but NOT redefined.
    - The migration was NOT applied to the live DB by this plan (no `supabase db push` executed).
  </acceptance_criteria>
  <done>0003 migration file exists, encodes the D-03 wp_transfers + HLTH-01 webhook_events + admin-read/no-write RLS contract, and the source-level schema test is GREEN; live apply deferred to Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role write -> Postgres | The only writer of `wp_transfers`/`webhook_events`; tables deny anon/authenticated writes by RLS |
| migration author -> Supabase project | Wrong-project (Kalvia) apply would corrupt the wrong env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-AC | Elevation of Privilege | `wp_transfers` / `webhook_events` write access | mitigate | RLS enabled + admin-read SELECT only + NO insert/update/delete policy → anon/authenticated cannot write; only the service-role webhook can (asserted by `payments-schema.test.ts` `not.toMatch(/for insert|for update|for delete/)`). |
| T-03-RP | Tampering / Repudiation | replayed Stripe event | mitigate | `webhook_events_event_id_key` UNIQUE on `event_id` is the DB-level race-safe idempotency authority that the webhook's insert-first dedup relies on (SC3). |
| T-03-ENV | Tampering (wrong env) | applying 0003 to Kalvia | mitigate | Balkanity-ref guardrail in the header (`qyhdogajtmnvxphrslwm` only; never Kalvia `utyatpadtibqqswsfvtr`), asserted by the source-level test; live apply via Supabase CLI/Management token (NOT MCP — MCP hits Kalvia) under the Plan 05 BLOCKING sign-off. |
| T-03-FK | Tampering | unresolvable `metadata.transfer_id` | mitigate | `destination_id` FK `on delete restrict` + a real `wp_transfers` row (D-03) so the `paid` UPDATE targets a real id; webhook logs `no_matching_transfer` rather than 500 when absent (Pitfall 6, handled in Plan 04). |
</threat_model>

<verification>
- `npx vitest run platform/rls/payments-schema.test.ts` GREEN.
- Full quick suite `npm run test` shows the schema contract green (single-writer/route-contract/checkout/fee remain RED until Plans 03-04).
- Manual eyeball: no `supabase db push` was run; the migration file is staged for the Plan 05 sign-off.
</verification>

<success_criteria>
- `supabase/migrations/0003_payments_spine.sql` exists with the D-03 `wp_transfers` + HLTH-01 `webhook_events` schema, admin-read/no-write RLS, UNIQUE `event_id`, and the Balkanity-ref FLAGGED header.
- The source-level schema contract test is GREEN.
- The migration is NOT yet applied to the live database (deferred to Plan 05, signed off).
</success_criteria>

<output>
Create `.planning/phases/03-payments-trust-spine/03-02-SUMMARY.md` when done.
</output>
