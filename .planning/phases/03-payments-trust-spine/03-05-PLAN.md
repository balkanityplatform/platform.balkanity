---
phase: 03-payments-trust-spine
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - .planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md
autonomous: false
requirements: [BOOK-05, HLTH-01]

must_haves:
  truths:
    - "Migration 0003 is applied to the Balkanity Supabase project (qyhdogajtmnvxphrslwm), AFTER human schema sign-off (D-04, FLAGGED)"
    - "ADVERSARIAL GATE: a forged/unsigned webhook POST returns 400 with zero state change; a spoofed success-URL never writes paid"
    - "ADVERSARIAL GATE: replaying the same Stripe event.id twice produces exactly one effect"
    - "A grep confirms paid is written in exactly one code path; the full suite is green"
  artifacts:
    - path: ".planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md"
      provides: "Recorded evidence (commands + outputs) for the schema push + the three adversarial gates"
      contains: "forged"
  key_links:
    - from: "supabase/migrations/0003_payments_spine.sql"
      to: "Balkanity Postgres (qyhdogajtmnvxphrslwm)"
      via: "Supabase CLI / Management token (NOT MCP)"
      pattern: "qyhdogajtmnvxphrslwm"
    - from: "Stripe CLI (TEST mode)"
      to: "/api/stripe/webhook"
      via: "stripe listen + stripe events resend"
      pattern: "stripe events resend"
---

<objective>
Close the phase by proving the trust spine against the LIVE Balkanity database in Stripe TEST mode. First apply migration `0003` (FLAGGED / irreversible — human schema sign-off required, D-04) to the Balkanity project via the Supabase CLI / Management token (NOT MCP — MCP hits Kalvia). Then demonstrate the three adversarial gates that build-and-type-check CANNOT prove from source alone: (1) forged/unsigned POST → 400 + zero state change, and spoofed success-URL never writes `paid`; (2) replayed `event.id` → exactly one effect; plus the grep single-writer gate. Record all evidence.

Purpose: Build/type checks pass without the push (source-level tests read migration TEXT, not the live DB) — a false-positive verification state. The live push + the three TEST-mode adversarial gates are mandatory before the phase can pass (ROADMAP success criteria 2 & 3 are hard acceptance bars).
Output: `0003` live on Balkanity; `03-GATES-EVIDENCE.md` with the recorded gate runs.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-payments-trust-spine/03-CONTEXT.md
@.planning/phases/03-payments-trust-spine/03-VALIDATION.md
@.planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md
@supabase/migrations/0003_payments_spine.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Schema sign-off + apply migration 0003 to Balkanity</name>
  <files>supabase/migrations/0003_payments_spine.sql, .planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md</files>
  <read_first>
    - supabase/migrations/0003_payments_spine.sql (the FLAGGED migration to sign off + apply)
    - .planning/phases/03-payments-trust-spine/03-CONTEXT.md (D-04 — FLAGGED, sign-off before apply) + .planning/STATE.md handoff (CLI/Management token authed; never MCP/Kalvia)
    - platform/rls/payments-schema.test.ts (confirm GREEN before apply)
  </read_first>
  <action>
    BLOCKING, autonomous:false — human schema sign-off is required BEFORE apply (D-04, FLAGGED, same gate as Phase 2's 0002). After sign-off, the executor applies `0003` to the Balkanity project ref `qyhdogajtmnvxphrslwm` via the Supabase CLI / Management token (`SUPABASE_ACCESS_TOKEN`) — NOT MCP (MCP reaches only Kalvia per memory). Confirm the target ref echoes `qyhdogajtmnvxphrslwm` before applying; if any tool reports `utyatpadtibqqswsfvtr` (Kalvia), STOP immediately. Then verify the live schema via the IPv4 pooler — both `wp_transfers` and `webhook_events` exist, RLS enabled on both, UNIQUE `event_id` present — and record the apply commands + verification outputs in `03-GATES-EVIDENCE.md`. The detailed review checklist is in `<how-to-verify>`.
  </action>
  <what-built>
    Migration `supabase/migrations/0003_payments_spine.sql` (authored in Plan 02): the minimal `wp_transfers` money-spine table (D-03) + the `webhook_events` audit/idempotency table (HLTH-01), both RLS-enabled, admin-read, no write policy, with UNIQUE `event_id`. This migration is FLAGGED / irreversible — it requires human schema sign-off BEFORE apply (D-04, ROADMAP sign-off note, same gate as Phase 2's 0002).
  </what-built>
  <how-to-verify>
    1. Review `supabase/migrations/0003_payments_spine.sql` end to end — confirm: the Balkanity-ref header guardrail (`qyhdogajtmnvxphrslwm` ONLY; never Kalvia `utyatpadtibqqswsfvtr`); `wp_transfers` columns are MINIMAL (money-spine only — no PII/lifecycle, which Phase 4 ALTERs in, D-04); both tables are RLS-enabled with admin-read SELECT and NO write policy; UNIQUE `webhook_events (event_id)`; integer-cents CHECK on `amount_cents`. Confirm `platform/rls/payments-schema.test.ts` is GREEN.
    2. On sign-off, the executor applies `0003` to the Balkanity project `qyhdogajtmnvxphrslwm` via the Supabase CLI / Management token (set `SUPABASE_ACCESS_TOKEN`) — NOT MCP (MCP reaches only Kalvia per memory). Before applying, confirm the target ref echoes `qyhdogajtmnvxphrslwm`; if any tool reports `utyatpadtibqqswsfvtr` (Kalvia), STOP.
    3. Verify the live schema via the IPv4 pooler: `wp_transfers` and `webhook_events` exist; RLS is enabled on both; the UNIQUE `event_id` constraint/index is present. Record the commands + outputs in `03-GATES-EVIDENCE.md`.
  </how-to-verify>
  <verify>
    <human-check>Schema reviewed and signed off; 0003 applied to qyhdogajtmnvxphrslwm (NOT Kalvia); live tables + RLS + UNIQUE event_id verified via IPv4 pooler; evidence recorded.</human-check>
  </verify>
  <resume-signal>Type "approved" after sign-off + successful apply + live verification, or describe the schema concern to address.</resume-signal>
  <acceptance_criteria>
    - Human sign-off recorded BEFORE apply (D-04 FLAGGED gate).
    - `0003` applied to Balkanity ref `qyhdogajtmnvxphrslwm` via CLI/Management token (NOT MCP); never Kalvia.
    - Live verification confirms both tables exist, RLS enabled on both, UNIQUE `event_id` present — recorded in `03-GATES-EVIDENCE.md`.
  </acceptance_criteria>
  <done>Migration 0003 is signed off and live on Balkanity; the live schema matches the source-level contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Run the three TEST-mode adversarial gates + grep single-writer gate</name>
  <files>.planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-payments-trust-spine/03-REPLAY-RUNBOOK.md (the SC3 Stripe-CLI replay runbook)
    - tests/e2e/webhook-forged.spec.ts + tests/e2e/success-spoof.spec.ts (the SC2 adversarial specs)
    - app/api/stripe/webhook/route.ts (the single paid writer under test)
    - .planning/phases/03-payments-trust-spine/03-VALIDATION.md §"Sampling Rate" (phase-gate criteria) + §"Manual-Only Verifications"
  </read_first>
  <action>
    Run, in Stripe TEST mode (D-02 — no real money), the three adversarial gates that source/type checks cannot prove, plus the grep single-writer gate, and record every command + output in `03-GATES-EVIDENCE.md`. GATE A (SC2): `npm run test:e2e` for `webhook-forged.spec.ts` (forged + missing-signature POST → 400, zero state change) and `success-spoof.spec.ts` (direct `/pay/success` hit → transfer NOT `paid`). GATE B (SC3): follow `03-REPLAY-RUNBOOK.md` — `stripe listen` → trigger a real signed `checkout.session.completed` for a seeded `wp_transfers` row → capture `evt_…` → `stripe events resend evt_…` → assert exactly one `webhook_events` row + `paid_at` unchanged on replay (and the transfer flipped to `paid` exactly once with `fee_cents` recorded). GATE C (SC1): grep for `status='paid'` writers across `app/`/`platform/`/`modules/` (excluding tests) → exactly one file (the webhook route); then `npm run test && npm run typecheck && npm run lint` fully green. The detailed prereqs + per-gate steps are in `<how-to-verify>`.
  </action>
  <what-built>
    The webhook route (Plan 04) on the live schema (Task 1). This task runs, in Stripe TEST mode (D-02 — no real money), the three adversarial gates that are hard acceptance bars (ROADMAP SC2 & SC3) plus the grep single-writer gate (SC1).
  </what-built>
  <how-to-verify>
    Prereqs: Stripe CLI installed (`brew install stripe/stripe-cli/stripe`; `stripe login` — TEST mode) per `03-REPLAY-RUNBOOK.md`; `STRIPE_SECRET_KEY` (sk_test_…) + `STRIPE_WEBHOOK_SECRET` (whsec_… from `stripe listen`) set server-only in `.env.local` (NEVER a tracked file / NEXT_PUBLIC_); dev server running; a seeded `wp_transfers` row (status `requested`) whose id is used as `metadata.transfer_id`.
    GATE A — Forged + success-spoof (SC2): run `npm run test:e2e` for `tests/e2e/webhook-forged.spec.ts` (forged signature + missing-signature POST → 400; assert no `webhook_events` row for `evt_forged`, seeded transfer still `requested`) and `tests/e2e/success-spoof.spec.ts` (hit `/pay/success?t=<id>` directly with no webhook → transfer NOT `paid`). Both PASS.
    GATE B — Replay → exactly one effect (SC3): follow `03-REPLAY-RUNBOOK.md` — `stripe listen --forward-to localhost:3000/api/stripe/webhook`; trigger a real signed `checkout.session.completed` for the seeded transfer; capture the `evt_…` id; `stripe events resend evt_…`. Assert exactly ONE `webhook_events` row for that `event_id` and `wp_transfers.paid_at` unchanged on the second delivery (and the transfer flipped to `paid` exactly once, with `fee_cents` recorded from the balance transaction).
    GATE C — Single-writer grep (SC1): `grep -RnE "status['\"]?\s*[:=]\s*['\"]paid" app/ platform/ modules/ --include=*.ts --include=*.tsx | grep -v '\.test\.'` shows the `paid` write in exactly ONE file (the webhook route). Also confirm `npm run test && npm run typecheck && npm run lint` is fully green.
    Record every command + its output in `03-GATES-EVIDENCE.md`.
  </how-to-verify>
  <verify>
    <human-check>All three adversarial gates demonstrated in Stripe TEST mode (forged → 400 + zero state change; success-spoof → no paid; replay → exactly one effect with fee recorded); grep shows a single paid writer; full suite green; evidence recorded in 03-GATES-EVIDENCE.md.</human-check>
  </verify>
  <resume-signal>Type "approved" once all three gates pass and evidence is recorded, or describe the failing gate.</resume-signal>
  <acceptance_criteria>
    - Forged/unsigned POST → HTTP 400 with zero state change (no `webhook_events` row, transfer unchanged); success-URL hit → transfer NOT `paid` (SC2).
    - Replayed `event.id` → exactly one `webhook_events` row (UNIQUE) + `paid_at` unchanged on replay; the transfer was flipped to `paid` exactly once with `fee_cents` recorded from `balance_transaction.fee` (SC3 + SC5/D-05).
    - `grep` for `status='paid'` writers returns exactly one non-test file (the webhook route) (SC1).
    - `npm run test && npm run typecheck && npm run lint` fully green.
    - `03-GATES-EVIDENCE.md` records all commands + outputs.
  </acceptance_criteria>
  <done>All three adversarial gates pass in TEST mode, the single-writer gate is proven, the full suite is green, and the evidence is recorded — the phase acceptance bars are met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author -> Supabase project | Wrong-project (Kalvia) apply corrupts the wrong env |
| Stripe (TEST) -> /api/stripe/webhook | Forged/replayed events are the adversary being tested here |
| guest -> /pay/success | Spoofed redirect must not confirm payment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-ENV2 | Tampering (wrong env) | applying 0003 to Kalvia | mitigate | Confirm the target ref echoes `qyhdogajtmnvxphrslwm` before apply; STOP if any tool reports `utyatpadtibqqswsfvtr`; apply via Supabase CLI / Management token, NOT MCP (MCP hits Kalvia, per memory). Human sign-off gate before apply (D-04 FLAGGED). |
| T-03-GATE1 | Spoofing | forged/unsigned webhook | mitigate (PROVEN) | GATE A demonstrates forged + missing-signature → 400 + zero state change live (SC2). |
| T-03-GATE2 | Tampering / Repudiation | event replay | mitigate (PROVEN) | GATE B demonstrates `stripe events resend` → exactly one effect (UNIQUE `event_id` + insert-first; `paid_at` unchanged on replay) (SC3). |
| T-03-GATE3 | Spoofing / Tampering | success-URL spoof | mitigate (PROVEN) | GATE A success-spoof spec proves the display-only success page never writes `paid` (SC2). |
| T-03-SC | Tampering | Stripe TEST secrets handling | mitigate | `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` kept server-only in `.env.local`, never a tracked file / `NEXT_PUBLIC_`; honor STATE.md SECURITY TODO (do not add to `.env.local.example`). Legitimacy checkpoint is blocking-human, never auto-approved. |
</threat_model>

<verification>
- `npx vitest run platform/rls/payments-schema.test.ts` GREEN before apply; live schema verified post-apply via IPv4 pooler.
- `npm run test:e2e` (forged + success-spoof) PASS; the CLI replay gate shows exactly one effect.
- `grep` shows a single `paid` writer; `npm run test && npm run typecheck && npm run lint` fully green.
- `03-GATES-EVIDENCE.md` records the schema-apply commands + all three gate runs.
</verification>

<success_criteria>
- Migration `0003` is signed off and live on the Balkanity project (never Kalvia).
- All three adversarial gates pass in Stripe TEST mode; the grep single-writer gate confirms one `paid` writer; the full suite is green.
- Recorded evidence exists for the phase verification step.
</success_criteria>

<output>
Create `.planning/phases/03-payments-trust-spine/03-05-SUMMARY.md` when done.
</output>
