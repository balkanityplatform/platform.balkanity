---
phase: 04-transfer-entity-booking-form
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/transfers/lifecycle.ts
  - platform/transfers/lifecycle.test.ts
  - app/pickup/[slug]/booking.test.ts
  - platform/transfers/confirmation.test.ts
  - tests/e2e/guest-status.spec.ts
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
autonomous: true
requirements: [XFER-01, BOOK-02, BOOK-03, BOOK-04, BOOK-06, BOOK-07, AUTH-02]
must_haves:
  truths:
    - "The lifecycle transition map permits requested→paid and rejects illegal jumps (e.g. requested→completed)"
    - "Wave 0 test specs exist and reference the not-yet-built booking/status/confirmation surfaces (RED until later plans land)"
    - "Every new Phase 4 copy key exists in BOTH en.ts and bg.ts (tsc Dict parity gate stays green)"
  artifacts:
    - path: "platform/transfers/lifecycle.ts"
      provides: "TS allowed-transition map (mirror of the 0004 DB trigger) + canTransition helper + friendly-error key"
      contains: "ALLOWED_TRANSITIONS"
    - path: "platform/transfers/lifecycle.test.ts"
      provides: "Every legal/illegal transition pair asserted (XFER-01)"
    - path: "app/pickup/[slug]/booking.test.ts"
      provides: "Booking action zod-boundary + insert/checkout-call unit specs (BOOK-02/03)"
    - path: "platform/transfers/confirmation.test.ts"
      provides: "Confirmation stub builds a magic link, writes no status='paid' (BOOK-06)"
    - path: "tests/e2e/guest-status.spec.ts"
      provides: "Status page timeline + receipt render e2e (BOOK-07/AUTH-02)"
    - path: "platform/i18n/en.ts"
      provides: "All Phase 4 booking/status/track/email copy keys (EN canonical)"
    - path: "platform/i18n/bg.ts"
      provides: "BG translations matching the en.ts Dict shape"
  key_links:
    - from: "platform/transfers/lifecycle.ts"
      to: "platform/ui/StatusDot.tsx"
      via: "imports the TransferState union (never a second state enum)"
      pattern: "import.*TransferState.*StatusDot"
    - from: "platform/transfers/lifecycle.test.ts"
      to: "platform/transfers/lifecycle.ts"
      via: "asserts ALLOWED_TRANSITIONS pairs"
      pattern: "ALLOWED_TRANSITIONS|canTransition"
---

<objective>
Lay the Wave 0 test baseline + the pure TS lifecycle transition map + the full EN/BG copy surface for Phase 4 — the foundation every later plan builds on. No production behavior ships here; the new test specs land RED (they reference surfaces built in Plans 03/04), the lifecycle map is the TS mirror of the migration-0004 DB trigger (D-08/D-09/D-10), and every UI-SPEC copy key lands in both dictionaries behind the tsc Dict parity gate.

Purpose: Per the Nyquist baseline pattern proven in 03-01, failing specs must exist before the surfaces they verify so each later task turns a known RED spec GREEN. The lifecycle map gives the app layer friendly errors while the DB trigger (Plan 02) is the hard backstop. Copy parity must be green before any island consumes a key.
Output: `platform/transfers/lifecycle.ts` (+ test), three new RED specs, all new copy keys in en.ts/bg.ts.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transfer-entity-booking-form/04-CONTEXT.md
@.planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md
@.planning/phases/04-transfer-entity-booking-form/04-PATTERNS.md
@.planning/phases/04-transfer-entity-booking-form/04-UI-SPEC.md

# Analogs the executor copies from:
@platform/ui/StatusDot.tsx
@platform/payments/single-writer.test.ts
@platform/i18n/en.ts
@platform/i18n/dictionary.ts
</context>

<artifacts_this_phase_produces>
This plan creates (downstream drift-verification should treat these as net-new, not pre-existing):
- `platform/transfers/lifecycle.ts` — exports `ALLOWED_TRANSITIONS` (`Record<TransferState, TransferState[]>`), `canTransition(from, to): boolean`, and `LIFECYCLE_ORDER` (the 7 happy-path states in order; `cancelled` rendered separately).
- `platform/transfers/lifecycle.test.ts`, `app/pickup/[slug]/booking.test.ts`, `platform/transfers/confirmation.test.ts`, `tests/e2e/guest-status.spec.ts` — new test specs (RED until Plans 03/04).
- New `en`/`bg` dictionary keys (see Task 3 for the full key list).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lifecycle transition map (TS mirror of the DB trigger) + RED unit spec</name>
  <files>platform/transfers/lifecycle.ts, platform/transfers/lifecycle.test.ts</files>
  <read_first>
    - platform/ui/StatusDot.tsx (the TransferState 8-state union — import it; NEVER redeclare a state enum)
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Pattern 2 — the allowed-transition map the trigger encodes; D-08/D-09/D-10)
    - .planning/phases/04-transfer-entity-booking-form/04-PATTERNS.md ("No Analog Found" — lifecycle.ts is a plain Record consuming the TransferState union)
  </read_first>
  <behavior>
    - canTransition('requested','paid') === true (the webhook's first real transition; Pitfall 4 — MUST be allowed)
    - canTransition('requested','cancelled') === true; ('paid','cancelled'), ('claimed','cancelled'), ('en_route','cancelled'), ('arrived','cancelled') all true (D-10 admin pre-pickup cancel)
    - canTransition('picked_up','cancelled') === false and canTransition('completed','cancelled') === false (terminal; D-10)
    - canTransition('completed', <any>) === false and canTransition('cancelled', <any>) === false (terminal — no outbound)
    - canTransition('requested','completed') === false and ('paid','claimed') === true, ('claimed','en_route') === true, ('en_route','arrived') === true, ('arrived','picked_up') === true, ('picked_up','completed') === true
    - canTransition(s, s) for an unchanged status is out of scope of the map (the trigger early-returns on no-op); test that a same-state pair is NOT treated as a legal forward transition
    - LIFECYCLE_ORDER lists exactly the 7 happy-path states requested→completed in order, excluding cancelled
  </behavior>
  <action>
    Create `platform/transfers/lifecycle.ts` importing `type TransferState` from `@/platform/ui/StatusDot` (the single source of truth — Don't-Hand-Roll lock). Export `ALLOWED_TRANSITIONS: Record<TransferState, TransferState[]>` encoding EXACTLY the map from RESEARCH Pattern 2: requested→[paid,cancelled]; paid→[claimed,cancelled]; claimed→[en_route,cancelled]; en_route→[arrived,cancelled]; arrived→[picked_up,cancelled]; picked_up→[completed]; completed→[]; cancelled→[]. Export `canTransition(from: TransferState, to: TransferState): boolean` returning `ALLOWED_TRANSITIONS[from].includes(to)`. Export `LIFECYCLE_ORDER: readonly TransferState[]` = the 7 happy-path states in lifecycle order (requested, paid, claimed, en_route, arrived, picked_up, completed) for the timeline — `cancelled` is NOT in this array (UI-SPEC: rendered as a distinct terminal row only when reached). Add a header comment stating this file is the friendly app-layer mirror of the migration-0004 `wp_enforce_transfer_transition` DB trigger which is the hard backstop (D-08). Then write `platform/transfers/lifecycle.test.ts` (vitest) asserting every behavior above — iterate all 8×8 state pairs and assert each against the expected map so any future divergence from the DB trigger fails. This is a GREEN test for this task (the map exists in this same task).
  </action>
  <verify>
    <automated>npm run test -- platform/transfers/lifecycle.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- platform/transfers/lifecycle.test.ts` passes (GREEN — map + test land together)
    - `platform/transfers/lifecycle.ts` imports `TransferState` from `@/platform/ui/StatusDot` and declares NO local state union (grep: `type TransferState` appears only as an import, not a redeclaration)
    - `ALLOWED_TRANSITIONS.requested` contains both `'paid'` and `'cancelled'`
    - `ALLOWED_TRANSITIONS.picked_up` does NOT contain `'cancelled'`; `ALLOWED_TRANSITIONS.completed` and `.cancelled` are empty arrays
    - `LIFECYCLE_ORDER` has length 7 and does not include `'cancelled'`
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>The TS transition map exactly mirrors the planned DB trigger map, imports the canonical TransferState union, and its exhaustive pair test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 RED specs — booking action, confirmation stub, guest-status e2e</name>
  <files>app/pickup/[slug]/booking.test.ts, platform/transfers/confirmation.test.ts, tests/e2e/guest-status.spec.ts</files>
  <read_first>
    - platform/payments/single-writer.test.ts (the established Nyquist-baseline RED-spec style + the dynamic-import-to-stay-tsc-clean-while-RED pattern from 03-01)
    - platform/payments/checkout.test.ts (how checkout calls are mocked in a unit spec)
    - tests/e2e/success-spoof.spec.ts (the existing guest-facing e2e shape + Playwright config conventions)
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; the exact files + behaviors each spec covers)
  </read_first>
  <action>
    Author three RED specs that reference surfaces built in Plans 03/04 (they fail now; later plans turn them GREEN). Mirror 03-01's runtime-string dynamic-import + typed-cast trick so the specs are tsc-clean before the implementation files exist but stay RED at runtime.
    (a) `app/pickup/[slug]/booking.test.ts` (vitest) — covers BOOK-02/BOOK-03. Mock `@/platform/payments/checkout` (`createCheckoutSession`) and `@/platform/supabase/admin` (`createAdminClient`). Assert: a submit missing a required field (email/phone/name/pax/flight_no/arrival_date/arrival_time) returns `{status:'error'}` WITHOUT calling `createCheckoutSession`; a valid submit re-reads `destinations.price_cents` (NEVER an amount from FormData), inserts a `wp_transfers` row with `status:'requested'` + the destination's `price_cents` as `amount_cents`, then calls `createCheckoutSession({transferId, amountCents})` exactly once with the server-sourced amount.
    (b) `platform/transfers/confirmation.test.ts` (vitest) — covers BOOK-06. Mock `createAdminClient` so `auth.admin.generateLink` returns a fixture `properties.action_link`. Assert `sendBookingConfirmation(transferId, guestEmail)` returns an object whose `magicLink` contains `/auth/confirm?type=magiclink&next=/status/${transferId}` and that the module SOURCE contains no `status: 'paid'` write (re-use the single-writer.test.ts grep approach scoped to `platform/transfers/confirmation-email.ts`).
    (c) `tests/e2e/guest-status.spec.ts` (Playwright) — covers BOOK-07/AUTH-02. Skeleton e2e that visits `/status/<seeded-id>` and asserts the lifecycle timeline (a `[data-testid="status-dot"]` per state) and a receipt line matching `/Paid €/` render for a paid transfer; mark steps that need a live magic-link session with `test.fixme`/`test.skip` annotations referencing Plan 04 so the spec is collected but does not block on un-built auth. Add a header comment in each file: `NYQUIST BASELINE — RED until Plan 0X lands {surface}`.
    Do NOT implement any production file here. The specs must import/reference the future module paths.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- app/pickup platform/transfers/confirmation.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (specs are tsc-clean via dynamic-import/typed-cast even though impl files do not yet exist)
    - `npm run test -- app/pickup platform/transfers/confirmation.test.ts` reports the two new specs as FAILING (RED) — the failure message references the missing booking action / confirmation module, NOT a syntax error
    - Each new spec file contains the header token `NYQUIST BASELINE`
    - `booking.test.ts` asserts `createCheckoutSession` is NOT called on an invalid submit and is called once on a valid submit with a server-sourced amount
    - `confirmation.test.ts` asserts the built magic link contains `next=/status/` and the target module contains no `status: 'paid'` literal
    - `tests/e2e/guest-status.spec.ts` is collected by `npx playwright test --list` without error
  </acceptance_criteria>
  <done>Three new specs exist, are tsc-clean, and are RED at runtime referencing the surfaces Plans 03/04 will build.</done>
</task>

<task type="auto">
  <name>Task 3: Add all Phase 4 copy keys to en.ts and bg.ts (Dict parity)</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts</files>
  <read_first>
    - platform/i18n/en.ts (the Dict source of truth; append-keys pattern from Phase 1/2)
    - platform/i18n/bg.ts (must satisfy the exact en.ts shape — a missing/extra key fails tsc)
    - .planning/phases/04-transfer-entity-booking-form/04-UI-SPEC.md (Copywriting Contract — EN canonical values for every key)
  </read_first>
  <action>
    Append a `// --- Phase 4: booking + status (UI-SPEC Copywriting Contract) ---` section to `en.ts` with EVERY key from the UI-SPEC Copywriting Contract, using the UI-SPEC EN canonical values. Cover all groups: booking page (`bookingFareCaption` = "Airport transfer · {airport} → {zone}", `bookingTotalToPay` = "Total to pay", `bookingYourDetails`, `bookingFullNameLabel`, `bookingEmailLabel`, `bookingPhoneLabel`, `bookingFlightLabel`, `bookingArrivalDateLabel`, `bookingArrivalTimeLabel`, `bookingPassengersLabel`, `bookingPassengersHelp` = "1 to 8", `bookingLuggageLabel`, `bookingNotesLabel`, `bookingNotesPlaceholder`, `bookingContinueCta` = "Continue to payment", `bookingBackCta`); disclosure (`disclosureHeading` = "Prepaid & non-refundable", `disclosureBody`, `disclosureCheckboxLabel`, `disclosureBlockedError`); validation errors (`bookingFieldRequired` = "This field is required.", `bookingInvalidEmail`, `bookingInvalidPhone`, `bookingArrivalPast`, `bookingPassengersRange`, `bookingFailed`); inactive slug (`slugUnavailableHeading`, `slugUnavailableBody`); confirmation email (`confirmEmailSubject`, `confirmEmailHeading`, `confirmEmailBody`, `confirmEmailCta` = "Track my booking", `confirmEmailFooter`); status page (`statusTitle` = "Booking status", `statusYourTrip`, `statusRouteLine`, `statusArrivalLine`, `statusFlightLine`, `statusPaxLine`, `statusTimelineHeading` = "Status", `statusReceiptHeading` = "Payment", `statusReceiptPaidLine` = "Paid €{amount} on {paidDate}", `statusReceiptSubNote`, `statusDriverHeading` = "Your driver", `statusDriverLine` = "{driverFirstName} · {driverPhone}", `statusDriverPreClaimNote`, `statusExpired`, `statusExpiredCta` = "Get a new link"); track page (`trackTitle` = "Track your booking", `trackBody`, `trackEmailLabel`, `trackSendCta` = "Send my booking link", `trackSuccessNeutral`, `trackError`). Use exactly the UI-SPEC EN strings (with the `{placeholder}` tokens preserved). Add matching BG translations for EVERY key to `bg.ts` (translate the EN copy; keep `{placeholders}` verbatim). Do NOT reuse a Phase 2 key name that already exists for a different meaning — these are new keys.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (Dict parity gate green — bg.ts satisfies the en.ts shape with zero missing/extra keys)
    - `grep -c 'statusReceiptPaidLine' platform/i18n/en.ts platform/i18n/bg.ts` returns 1 in each file
    - `grep 'Paid €{amount} on {paidDate}' platform/i18n/en.ts` matches (SC4 receipt copy present verbatim)
    - `grep 'Continue to payment' platform/i18n/en.ts` matches and `grep 'Prepaid & non-refundable' platform/i18n/en.ts` matches
    - Every UI-SPEC Copywriting Contract key (booking, disclosure, validation, inactive-slug, confirmation-email, status, track groups) exists in both files
  </acceptance_criteria>
  <done>All Phase 4 copy keys exist in both dictionaries; tsc Dict parity gate is green; later islands can consume the keys without re-deriving copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure test/utility/copy authoring; no runtime trust boundary crossed. The lifecycle map is consumed at the app layer for friendly errors only; the DB trigger (Plan 02) is the real enforcement boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `platform/transfers/lifecycle.ts` drifting from the DB trigger map | mitigate | The exhaustive 8×8 pair test (Task 1) pins the TS map to the same allowed-transition table the migration-0004 trigger encodes; any divergence fails the unit test. The DB trigger remains the hard backstop (D-08) — the TS map is advisory only. |
| T-04-02 | Information Disclosure | A second hand-rolled state→colour or state enum diverging from the canonical lifecycle | mitigate | `lifecycle.ts` imports `TransferState` from `StatusDot` (the platform-wide contract); acceptance grep forbids a local redeclaration. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or this phase (RESEARCH §Package Legitimacy Audit: zero new packages). No slopcheck required; no `[ASSUMED]`/`[SUS]` packages exist. |
</threat_model>

<verification>
- `npm run test -- platform/transfers/lifecycle.test.ts` GREEN (map + test together).
- The three Wave-0 specs are tsc-clean and RED at runtime (referencing surfaces from Plans 03/04).
- `npm run typecheck` GREEN (Dict parity holds with all new keys in en.ts + bg.ts).
</verification>

<success_criteria>
- Lifecycle TS map exists, imports the canonical TransferState union, and exhaustively matches the planned DB-trigger map.
- Wave 0 RED specs exist for BOOK-02/03 (booking action), BOOK-06 (confirmation stub), BOOK-07/AUTH-02 (guest status e2e).
- All UI-SPEC copy keys present in both dictionaries; tsc Dict parity gate green.
</success_criteria>

<output>
Create `.planning/phases/04-transfer-entity-booking-form/04-01-SUMMARY.md` when done.
</output>
