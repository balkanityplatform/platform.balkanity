---
phase: 04-transfer-entity-booking-form
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - supabase/migrations/0004_transfer_entity.sql
autonomous: true
requirements: [XFER-01, AUTH-02, BOOK-01]
user_setup: []
must_haves:
  truths:
    - "Migration 0004 ALTERs the existing wp_transfers to add NULL-able guest PII + lifecycle columns (existing Phase-3 seed rows survive)"
    - "A BEFORE-UPDATE trigger encodes the full 8-state map and permits requested→paid (Pitfall 4) while rejecting illegal jumps, firing even on the service-role path"
    - "A guest-self-read RLS SELECT policy uses (select auth.jwt() ->> 'email') = guest_email (NOT deprecated auth.email()) and coexists with the existing admin-read"
    - "A narrow anon-read policy exposes only active destinations for the /pickup public read"
  artifacts:
    - path: "supabase/migrations/0004_transfer_entity.sql"
      provides: "wp_transfers PII+lifecycle ALTER + transition-guard trigger + guest-self-read RLS + active-destination anon read"
      contains: "wp_enforce_transfer_transition"
  key_links:
    - from: "supabase/migrations/0004_transfer_entity.sql"
      to: "public.wp_transfers"
      via: "BEFORE UPDATE trigger wp_transfers_transition_guard"
      pattern: "before update on public.wp_transfers"
    - from: "supabase/migrations/0004_transfer_entity.sql"
      to: "auth.jwt() email claim"
      via: "guest-self-read RLS using clause"
      pattern: "auth.jwt\\(\\) ->> 'email'"
---

<objective>
Author migration `0004_transfer_entity.sql` — the FLAGGED / irreversible schema change that turns the minimal Phase-3 `wp_transfers` money-spine into the full guest-PII + lifecycle transfer entity. Three parts in one file: (1) ALTER `wp_transfers` to add NULL-able PII + lifecycle + driver-scaffold columns; (2) a BEFORE-UPDATE transition-guard trigger encoding the complete 8-state machine (D-08/D-09/D-10); (3) two RLS policies — a guest-self-read SELECT using the JWT email claim (D-05) and a narrow anon-read for active destinations (BOOK-01, the public `/pickup` read path Phase 2 deferred).

Purpose: This is the data-layer authority for the whole phase. The trigger enforces lifecycle legality regardless of which client writes (service-role bypasses RLS but NOT triggers — D-08). The RLS policies are the real PII boundary (UI masking leaks via the auto-REST API). This task AUTHORS THE FILE ONLY; the live apply to Balkanity is the BLOCKING signed-off task in Plan 05.
Output: `supabase/migrations/0004_transfer_entity.sql` (not applied).
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-transfer-entity-booking-form/04-CONTEXT.md
@.planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md
@.planning/phases/04-transfer-entity-booking-form/04-PATTERNS.md

# Analogs the executor copies from (header guardrail, RLS, no-write-policy lock):
@supabase/migrations/0003_payments_spine.sql
@supabase/migrations/0002_supply_tables.sql

# The TS mirror of the trigger map (must match exactly):
@platform/transfers/lifecycle.ts
</context>

<artifacts_this_phase_produces>
This plan creates (net-new):
- `supabase/migrations/0004_transfer_entity.sql` containing:
  - new `wp_transfers` columns: `guest_name`, `guest_email`, `guest_phone`, `pax`, `luggage_count`, `flight_no`, `arrival_at`, `notes`, `driver_id` (all NULL-able)
  - function `public.wp_enforce_transfer_transition()` + trigger `wp_transfers_transition_guard`
  - RLS policy `wp_transfers_guest_self_read`
  - RLS policy `destinations_public_active_read`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ALTER wp_transfers — add NULL-able PII + lifecycle + driver-scaffold columns</name>
  <files>supabase/migrations/0004_transfer_entity.sql</files>
  <read_first>
    - supabase/migrations/0003_payments_spine.sql (the existing wp_transfers columns — the ALTER is additive ONLY; header guardrail lines 1-32: Balkanity ref qyhdogajtmnvxphrslwm ONLY, never Kalvia; the no-write-policy lock)
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Runtime State Inventory + A3 — additive NULL-able columns so Phase-3 seed rows survive; the webhook's column list is a strict subset and must not break)
    - .planning/phases/04-transfer-entity-booking-form/04-PATTERNS.md (0004 migration analog → 0003 header conventions)
  </read_first>
  <action>
    Create `supabase/migrations/0004_transfer_entity.sql`. Open with a header comment block COPYING the 0003 guardrail style: FLAGGED / IRREVERSIBLE — human sign-off before apply; target Balkanity ref `qyhdogajtmnvxphrslwm` ONLY, NEVER Kalvia `utyatpadtibqqswsfvtr`; live apply is the Plan 05 BLOCKING signed-off task via Supabase CLI/Management token (NOT MCP — MCP hits Kalvia). State that this ALTERs (does NOT create) `wp_transfers` (created in 0003, D-04). Then `alter table public.wp_transfers add column if not exists ...` for EACH (all NULL-able, no NOT NULL, no defaults that would rewrite rows): `guest_name text`, `guest_email text`, `guest_phone text`, `pax integer check (pax is null or (pax >= 1 and pax <= 8))` (D-03 range), `luggage_count integer check (luggage_count is null or luggage_count >= 0)`, `flight_no text`, `arrival_at timestamptz`, `notes text`, `driver_id uuid references auth.users(id) on delete set null` (the Phase-5/6 claim scaffold — D-06 reverse-reveal joins driver_profiles on this). Add an index `create index if not exists wp_transfers_arrival_at_idx on public.wp_transfers (arrival_at)` (the Phase 5/6 claim pool sorts on arrival_at — CLAIM-06). Add a `create index if not exists wp_transfers_guest_email_idx on public.wp_transfers (guest_email)` (the guest-self-read RLS filters on it). Keep the existing no-write-policy lock intact (do NOT add any INSERT/UPDATE/DELETE policy on wp_transfers). Use `if not exists` everywhere so a partial re-run is safe.
  </action>
  <verify>
    <automated>grep -q 'add column if not exists guest_email' supabase/migrations/0004_transfer_entity.sql && grep -q 'qyhdogajtmnvxphrslwm' supabase/migrations/0004_transfer_entity.sql && grep -q 'references auth.users(id) on delete set null' supabase/migrations/0004_transfer_entity.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0004_transfer_entity.sql` exists with a header naming Balkanity ref `qyhdogajtmnvxphrslwm` and the FLAGGED/sign-off note
    - All nine columns added via `add column if not exists` and ALL are NULL-able (no `not null` on the new columns)
    - `pax` carries a `check` constraint bounding it to 1–8; `luggage_count` carries a `>= 0` check
    - `driver_id` is `uuid references auth.users(id) on delete set null`
    - No INSERT/UPDATE/DELETE policy is added on `wp_transfers` (grep: no `for insert`/`for update`/`for delete` on wp_transfers in this file)
  </acceptance_criteria>
  <done>The ALTER adds all PII + lifecycle + driver-scaffold columns NULL-ably so existing seed rows survive, with the claim-sort and guest-email indexes, preserving the no-write-policy lock.</done>
</task>

<task type="auto">
  <name>Task 2: BEFORE-UPDATE lifecycle transition-guard trigger (full 8-state map, D-08/D-09/D-10)</name>
  <files>supabase/migrations/0004_transfer_entity.sql</files>
  <read_first>
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Pattern 2 — the exact trigger SQL incl. the `is not distinct from` no-op early-return + the full allowed-transition map + Pitfall 4 + the D-10 actor-vs-state split note)
    - platform/transfers/lifecycle.ts (the TS map this trigger MUST match exactly — same allowed pairs)
    - app/api/stripe/webhook/route.ts (the webhook's `requested → paid` UPDATE with `.neq("status","paid")` — the trigger MUST allow requested→paid or Stripe retries forever; Pitfall 4)
  </read_first>
  <action>
    Append to the SAME file `supabase/migrations/0004_transfer_entity.sql`: `create or replace function public.wp_enforce_transfer_transition() returns trigger language plpgsql as $$ ... $$;` following RESEARCH Pattern 2 verbatim in shape. Body: early-return `new` when `new.status is not distinct from old.status` (no-op / non-status updates pass through — this composes with the webhook's `.neq("status","paid")` backstop). Then `if not ( ... ) then raise exception 'illegal transfer transition: % -> %', old.status, new.status using errcode = 'check_violation'; end if;` where the allowed map is EXACTLY: requested→(paid,cancelled); paid→(claimed,cancelled); claimed→(en_route,cancelled); en_route→(arrived,cancelled); arrived→(picked_up,cancelled); picked_up→(completed). completed and cancelled are terminal (no outbound). This MUST include requested→paid (Pitfall 4). Then `create trigger wp_transfers_transition_guard before update on public.wp_transfers for each row execute function public.wp_enforce_transfer_transition();` (drop-if-exists first for re-runnability: `drop trigger if exists wp_transfers_transition_guard on public.wp_transfers;`). Add a comment documenting the D-10 split: the trigger guarantees STATE legality (cancelled allowed only from the five pre-pickup states); the ACTOR legality (admin-only cancel) is an app-layer getCurrentRole() gate that lands in Phase 6 — the trigger has no auth context on the service-role path. Note that triggers fire even on the service-role client (only RLS is bypassed).
  </action>
  <verify>
    <automated>grep -q 'before update on public.wp_transfers' supabase/migrations/0004_transfer_entity.sql && grep -q "old.status = 'requested' and new.status in ('paid'" supabase/migrations/0004_transfer_entity.sql && grep -q "errcode = 'check_violation'" supabase/migrations/0004_transfer_entity.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Function `public.wp_enforce_transfer_transition()` and trigger `wp_transfers_transition_guard` (BEFORE UPDATE, FOR EACH ROW) both present
    - The allowed map permits `requested → paid` (Pitfall 4) — grep confirms `requested` → `paid` in the map
    - `picked_up` maps ONLY to `completed` (no `cancelled`); `completed`/`cancelled` have no outbound transitions (terminal)
    - An illegal transition `raise exception ... using errcode = 'check_violation'`
    - The function early-returns on `new.status is not distinct from old.status` (no-op / non-status update pass-through)
    - The allowed pairs match `platform/transfers/lifecycle.ts` ALLOWED_TRANSITIONS exactly (same 6 source states with the same target lists)
    - A comment documents the D-10 state-vs-actor split (trigger = state legality; app = actor legality)
  </acceptance_criteria>
  <done>The trigger encodes the complete 8-state map matching the TS mirror, allows the webhook's requested→paid, rejects illegal jumps with check_violation, and fires on every writer including service-role.</done>
</task>

<task type="auto">
  <name>Task 3: Guest-self-read RLS (auth.jwt email) + narrow active-destination anon read</name>
  <files>supabase/migrations/0004_transfer_entity.sql</files>
  <read_first>
    - supabase/migrations/0003_payments_spine.sql (the existing `wp_transfers_admin_read` policy this coexists with — Postgres ORs permissive SELECT policies)
    - supabase/migrations/0002_supply_tables.sql (tail — the `destinations_admin_read` policy + the explicit "Phase 4 will add a NARROW anon/guest SELECT policy for ACTIVE destination slugs only" note)
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Pattern 3 corrected helper `(select auth.jwt() ->> 'email')` — NOT deprecated `auth.email()`; Pattern 4 active-destination anon read; State of the Art table)
  </read_first>
  <action>
    Append to the SAME file: (a) `create policy "wp_transfers_guest_self_read" on public.wp_transfers for select to authenticated using ( (select auth.jwt() ->> 'email') = guest_email );` — use the `auth.jwt() ->> 'email'` form wrapped in a `(select ...)` subquery (RESEARCH State of the Art: `auth.email()` is deprecated; the subquery wrap is the initPlan-caching best practice). Add a comment: this coexists with `wp_transfers_admin_read` (Postgres ORs permissive SELECT policies → admins read all, a guest reads only their own row, a non-owning driver matches neither → zero rows; this is the data-layer PII boundary Phase 5 hardens). (b) `create policy "destinations_public_active_read" on public.destinations for select to anon, authenticated using ( active = true );` — the public `/pickup/<slug>` read path Phase 2 deferred (Pitfall 1; 0002 tail note). Add a comment: this coexists with `destinations_admin_read`; inactive destinations return zero rows → `/pickup/<inactive-slug>` shows the neutral "not available" state. Guard both with `drop policy if exists` first for re-runnability. Do NOT add any wp_transfers WRITE policy (the no-write-policy lock holds — writes are service-role only).
  </action>
  <verify>
    <automated>grep -q "auth.jwt() ->> 'email'" supabase/migrations/0004_transfer_entity.sql && ! grep -q 'auth.email()' supabase/migrations/0004_transfer_entity.sql && grep -q 'destinations_public_active_read' supabase/migrations/0004_transfer_entity.sql && grep -q 'using ( active = true )' supabase/migrations/0004_transfer_entity.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `wp_transfers_guest_self_read` SELECT policy uses `(select auth.jwt() ->> 'email') = guest_email` and is `to authenticated`
    - The file contains ZERO occurrences of the deprecated `auth.email()` helper (grep `auth.email()` returns nothing)
    - `destinations_public_active_read` SELECT policy is `to anon, authenticated using ( active = true )`
    - No write policy (`for insert`/`for update`/`for delete`) is added on `wp_transfers` anywhere in the file
    - Both policies use `drop policy if exists` before create for re-runnability
    - Comments document the OR-coexistence with the existing admin-read policies and the non-owning-driver → zero-rows guarantee
  </acceptance_criteria>
  <done>The guest-self-read RLS uses the current auth.jwt email form (never deprecated auth.email), the active-destination anon read unblocks /pickup, and the no-write-policy lock is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated client → `wp_transfers` (REST/supabase-js) | Untrusted readers; RLS is the only thing standing between a guest/driver and another guest's PII. |
| anon → `destinations` (public `/pickup` read) | Public, unauthenticated read of bookable-destination metadata. |
| any writer (webhook, future claim RPC, admin/driver, service-role) → `wp_transfers.status` | The lifecycle integrity boundary; the BEFORE-UPDATE trigger fires on ALL of them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ID1 | Information Disclosure | Guest reads another guest's PII via the auto-generated REST API | mitigate | `wp_transfers_guest_self_read` RLS `(select auth.jwt() ->> 'email') = guest_email` — data-layer boundary, not UI. A JWT email ≠ guest_email matches no policy → zero rows. |
| T-04-ID2 | Information Disclosure | Non-owning authenticated driver reads guest PII pre-claim | mitigate | No matching permissive SELECT policy for a non-owning authenticated user → zero rows (Phase 5 adds the masked pool on top). The no-write-policy lock prevents tampering. |
| T-04-TMP1 | Tampering | Illegal lifecycle jump (e.g. requested→completed, picked_up→cancelled) by ANY writer incl. service-role | mitigate | `wp_enforce_transfer_transition` BEFORE-UPDATE trigger raises `check_violation` on any non-mapped transition; service-role does NOT bypass triggers (only RLS). |
| T-04-TMP2 | Tampering | Webhook `requested → paid` wrongly blocked → Stripe retries forever / customer charged-unpaid | mitigate | The allowed map MUST include requested→paid (Pitfall 4); Task 2 acceptance greps for it; Plan 05 runbook seeds and asserts the live transition before sign-off. |
| T-04-ID3 | Information Disclosure | `destinations_public_active_read` exposes inactive/secret destinations | mitigate | `using ( active = true )` — inactive destinations return zero rows; the policy exposes only the bookable booking-page metadata (non-PII). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (RESEARCH §Package Legitimacy Audit: zero new packages). No slopcheck required. |
</threat_model>

<verification>
- The migration file is syntactically self-consistent (header + ALTER + function + trigger + two policies) — apply is deferred to Plan 05.
- No deprecated `auth.email()` anywhere; the corrected `auth.jwt() ->> 'email'` form used.
- The trigger's allowed map matches `platform/transfers/lifecycle.ts` exactly (cross-checked in Plan 05's adversarial runbook against the live DB).
- No new write policy on `wp_transfers` (single-writer + service-role-only locks preserved).
</verification>

<success_criteria>
- `0004_transfer_entity.sql` exists with the FLAGGED header (Balkanity-only), the additive NULL-able PII+lifecycle ALTER, the full 8-state BEFORE-UPDATE trigger (incl. requested→paid), the guest-self-read RLS (auth.jwt email form), and the active-destination anon read.
- File is NOT applied (live apply is the Plan 05 BLOCKING signed-off task).
</success_criteria>

<output>
Create `.planning/phases/04-transfer-entity-booking-form/04-02-SUMMARY.md` when done.
</output>
