---
phase: 04-transfer-entity-booking-form
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - tests/runbooks/0004-lifecycle-trigger.md
autonomous: false
requirements: [XFER-01, BOOK-01, BOOK-03, AUTH-02, BOOK-07]
user_setup: []
must_haves:
  truths:
    - "Migration 0004 is applied to the Balkanity Supabase project (qyhdogajtmnvxphrslwm) — and verifiably NOT Kalvia"
    - "The live BEFORE-UPDATE trigger permits requested→paid and raises check_violation on illegal jumps (requested→completed, picked_up→cancelled, completed→anything)"
    - "A non-owning authenticated reader gets zero rows from wp_transfers (guest-self-read RLS); the active-destination anon read returns the booking-page columns for an active slug"
    - "The full booking→pay→confirm→track slice works end-to-end against the live schema (booking inserts requested, Checkout completes, webhook flips paid, status page renders the timeline + receipt)"
  artifacts:
    - path: "tests/runbooks/0004-lifecycle-trigger.md"
      provides: "The psql/service-role adversarial runbook proving the trigger + RLS on the live DB (mirrors the 03-05 gate style)"
  key_links:
    - from: "supabase/migrations/0004_transfer_entity.sql"
      to: "Balkanity Supabase (qyhdogajtmnvxphrslwm)"
      via: "supabase db push / Management-token psql apply"
      pattern: "qyhdogajtmnvxphrslwm"
---

<objective>
Apply the FLAGGED migration `0004` to the Balkanity Supabase project, then adversarially prove the trigger + RLS on the live database, then verify the whole Phase-4 slice end-to-end. This is the [BLOCKING] schema-push gate: build/type/test pass WITHOUT the push (types come from config, not the live DB), so the phase cannot truly pass verification until `0004` is live. The push is review-gated (the `wp_transfers` ALTER is flagged/irreversible) — a human signs off before the migration runs.

Purpose: Closes XFER-01 at the data layer (the DB trigger is the authority — D-08), confirms the guest-self-read + active-destination RLS (AUTH-02/BOOK-01), and runs the live booking→pay→confirm→track flow against the real schema. Mirrors the 03-05 live-gate style (forged/replay/spoof) for the new lifecycle trigger.
Output: the live-applied schema + the adversarial runbook record.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-transfer-entity-booking-form/04-CONTEXT.md
@.planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md

# The migration to apply + the trigger map to assert against:
@supabase/migrations/0004_transfer_entity.sql
@platform/transfers/lifecycle.ts

# Prior live-apply + adversarial-gate precedent:
@supabase/migrations/0003_payments_spine.sql
</context>

<artifacts_this_phase_produces>
This plan creates (net-new):
- `tests/runbooks/0004-lifecycle-trigger.md` — the recorded psql/service-role adversarial runbook (trigger legal/illegal transitions + RLS isolation) executed against the live Balkanity DB.
This plan APPLIES (no new source file): `supabase/migrations/0004_transfer_entity.sql` to the live Balkanity project.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING/SIGN-OFF] Review + apply migration 0004 to Balkanity</name>
  <files>supabase/migrations/0004_transfer_entity.sql</files>
  <what-built>
    Migration `supabase/migrations/0004_transfer_entity.sql` (authored in Plan 02): the additive NULL-able PII + lifecycle ALTER on `wp_transfers`, the `wp_enforce_transfer_transition` BEFORE-UPDATE trigger (full 8-state map), the `wp_transfers_guest_self_read` RLS policy (auth.jwt email form), and the `destinations_public_active_read` anon policy. This is a FLAGGED / irreversible schema change requiring human sign-off before it runs against the live database.
  </what-built>
  <read_first>
    - supabase/migrations/0004_transfer_entity.sql (the exact DDL being applied — review every statement)
    - CLAUDE.md (Infrastructure lock: Balkanity ref qyhdogajtmnvxphrslwm ONLY; NEVER Kalvia utyatpadtibqqswsfvtr — if any tool reports Kalvia, STOP)
    - .planning/STATE.md (Handoff — access via local creds/Management token, NOT MCP; MCP hits Kalvia)
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Environment Availability — apply via Supabase CLI / Management token / psql, never MCP)
  </read_first>
  <action>
    This is a blocking human-sign-off checkpoint — Claude AUTOMATES the apply but the operator must confirm the target project FIRST and approve before it runs (the wp_transfers ALTER is flagged/irreversible). Steps: (1) CONFIRM TARGET — resolve the project ref and prove it is Balkanity `qyhdogajtmnvxphrslwm` (NEVER Kalvia `utyatpadtibqqswsfvtr`); if any tool reports Kalvia, STOP. (2) Apply `0004_transfer_entity.sql` via the NON-MCP path with `SUPABASE_ACCESS_TOKEN` set: `supabase db push` against the Balkanity-linked project, or Management-token `psql` against the Balkanity DB URL. (3) Verify live: the nine new NULL-able `wp_transfers` columns (`\d public.wp_transfers`), the trigger `wp_transfers_transition_guard` (`select tgname from pg_trigger`), and the policies `wp_transfers_guest_self_read` + `destinations_public_active_read` (`select policyname from pg_policies`). (4) Confirm the Phase-3 seed rows survived (new columns NULL, no row lost). Do NOT proceed to Tasks 2/3 until the operator approves.
  </action>
  <how-to-verify>
    1. CONFIRM TARGET FIRST: verify the project ref resolves to Balkanity `qyhdogajtmnvxphrslwm` (e.g. `supabase projects list` or the linked-project ref / the DB URL host `qyhdogajtmnvxphrslwm.supabase.co`). If ANY tool reports Kalvia `utyatpadtibqqswsfvtr`, STOP and switch — do NOT apply.
    2. Review the DDL in `0004_transfer_entity.sql` (ALTER columns all NULL-able; trigger map includes requested→paid; RLS uses `auth.jwt() ->> 'email'`, not `auth.email()`; no new wp_transfers write policy).
    3. Apply via the NON-MCP path with the Management token in the environment (set `SUPABASE_ACCESS_TOKEN`): `supabase db push` against the Balkanity-linked project (or apply `0004_transfer_entity.sql` via Management-token `psql` against the Balkanity DB URL). Target ONLY `qyhdogajtmnvxphrslwm`.
    4. Confirm the objects exist live: the new `wp_transfers` columns (`\d public.wp_transfers` shows guest_email/guest_name/pax/arrival_at/driver_id…), the trigger (`select tgname from pg_trigger where tgname='wp_transfers_transition_guard'`), and the two policies (`select policyname from pg_policies where policyname in ('wp_transfers_guest_self_read','destinations_public_active_read')`).
    5. Confirm existing Phase-3 seed rows survived the ALTER (the new columns are NULL on them; no row lost).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms 0004 applied to Balkanity qyhdogajtmnvxphrslwm (never Kalvia): the nine new columns, the wp_transfers_transition_guard trigger, and the two policies are live, and the Phase-3 seed rows survived intact.</human-check>
  </verify>
  <acceptance_criteria>
    - The applied project ref is Balkanity `qyhdogajtmnvxphrslwm` (NEVER Kalvia `utyatpadtibqqswsfvtr`) — explicitly confirmed before apply
    - `\d public.wp_transfers` shows the nine new NULL-able columns
    - `pg_trigger` contains `wp_transfers_transition_guard`; `pg_policies` contains `wp_transfers_guest_self_read` and `destinations_public_active_read`
    - Pre-existing seed rows are intact (no data loss; new columns NULL)
  </acceptance_criteria>
  <done>Migration 0004 is applied to Balkanity only, with the new columns, transition trigger, and both RLS policies live and the Phase-3 seed rows intact.</done>
  <resume-signal>Type "approved" once 0004 is confirmed applied to Balkanity (qyhdogajtmnvxphrslwm) with the columns, trigger, and policies live; or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Adversarial DB-trigger + RLS runbook against the live schema (XFER-01 / SC5)</name>
  <files>tests/runbooks/0004-lifecycle-trigger.md</files>
  <read_first>
    - platform/transfers/lifecycle.ts (the allowed-transition map the live trigger must match exactly)
    - supabase/migrations/0004_transfer_entity.sql (the trigger + RLS definitions under test)
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Pattern 2 adversarial test note — seed a transfer, attempt skip/terminal transitions via service-role UPDATE; triggers fire even on service-role; Validation Architecture → the DB-trigger adversarial runbook gap, mirror the 03-05 gate style)
  </read_first>
  <action>
    Create `tests/runbooks/0004-lifecycle-trigger.md` recording an executed adversarial runbook against the LIVE Balkanity DB (Management-token psql / service-role; NOT MCP). Document each command + observed result:
    (a) TRIGGER — legal: seed a `requested` transfer (service-role insert into a real destination); `update ... set status='paid'` SUCCEEDS (requested→paid, Pitfall 4). Then drive paid→claimed→en_route→arrived→picked_up→completed and confirm each SUCCEEDS.
    (b) TRIGGER — illegal (each MUST raise `check_violation`): on a fresh `requested` row `update set status='completed'` (skip) → raises; on a `picked_up` row `update set status='cancelled'` (terminal-ish) → raises; on a `completed` row `update set status='requested'` (terminal outbound) → raises. Confirm the service-role path does NOT bypass the trigger.
    (c) RLS — isolation: with a non-owning authenticated context (a JWT whose email ≠ the row's guest_email), `select * from wp_transfers where id=<seeded>` returns ZERO rows (guest-self-read boundary). With the active-destination anon read, an anon select of an `active=true` destination returns the booking-page columns, and an `active=false` destination returns zero rows.
    Record PASS/FAIL per case + the exact error code/message for the illegal transitions. Clean up the seed rows afterward (or note they are harmless TEST rows like the Phase-3 seeds). This runbook is the XFER-01 data-layer DoD gate (mirrors 03-05).
  </action>
  <verify>
    <human-check>The runbook records: requested→paid SUCCEEDS; the three illegal transitions each RAISE check_violation on the service-role path; a non-owning reader gets zero rows; the active-destination anon read works and inactive returns zero rows. All cases PASS.</human-check>
  </verify>
  <acceptance_criteria>
    - `tests/runbooks/0004-lifecycle-trigger.md` exists and records every case with its command + observed result
    - The legal chain requested→paid→…→completed all SUCCEED live
    - Each of the three illegal transitions RAISES `check_violation` (recorded error code/message) — proving the trigger fires on the service-role path
    - A non-owning authenticated reader returns ZERO rows from `wp_transfers`; the active-destination anon read returns the booking columns for an active slug and zero rows for an inactive slug
    - The live trigger's allowed pairs match `platform/transfers/lifecycle.ts` exactly
  </acceptance_criteria>
  <done>The live DB trigger + RLS are adversarially proven: legal transitions pass, illegal jumps raise check_violation even via service-role, and PII isolation + active-slug exposure behave per D-05/D-08/BOOK-01.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite verification + booking→pay→confirm→track end-to-end smoke</name>
  <files>tests/runbooks/0004-lifecycle-trigger.md</files>
  <read_first>
    - .planning/phases/04-transfer-entity-booking-form/04-RESEARCH.md (Validation Architecture → Full suite command + the Phase-gate definition; the booking/status test map)
    - app/pickup/[slug]/actions.ts (the booking action under test)
    - app/api/stripe/webhook/route.ts (the paid writer + the confirmation stub call)
    - app/status/[id]/page.tsx (the status read under test)
    - .planning/STATE.md (Stripe CLI available; Phase-3 live replay precedent)
  </read_first>
  <action>
    Run the full validation suite and a live end-to-end smoke, appending the results to `tests/runbooks/0004-lifecycle-trigger.md` (or a sibling section): (a) `npm run test && npm run typecheck && npm run lint && npm run test:e2e` — ALL GREEN, including the previously-RED Plan-01 specs (`platform/transfers/lifecycle.test.ts`, `app/pickup/[slug]/booking.test.ts`, `platform/transfers/confirmation.test.ts`) now GREEN and `platform/payments/single-writer.test.ts` still exactly one paid writer. (b) Live slice smoke (TEST mode, mirroring the Phase-3 Stripe-CLI flow): open `/pickup/<active-slug>` → confirm fare + form render; submit the form → confirm a `requested` `wp_transfers` row is created and the redirect goes to a Stripe Checkout URL; complete the TEST Checkout (Stripe CLI `stripe listen --forward-to localhost:3000/api/stripe/webhook`) → confirm the webhook flips the row to `paid` (the live trigger allows requested→paid) and the `[BOOK-06 stub]` confirmation magic link is logged; click the revealed magic link → confirm `/auth/confirm` lands on `/status/<id>` (same browser, PKCE) and the status page renders the lifecycle timeline + the "Paid €X on {date}" receipt for the guest's own row only. Record PASS/FAIL per step. If `next` open-redirect, getSession usage, or a stale SW serve appears, FAIL and stop.
  </action>
  <verify>
    <automated>npm run test && npm run typecheck && npm run lint</automated>
    <human-check>The live end-to-end smoke is recorded PASS: /pickup renders → submit creates requested + redirects to Checkout → TEST pay → webhook flips paid + logs the confirmation magic link → magic link lands on /status/<id> → timeline + receipt render for the guest's own row only.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run test && npm run typecheck && npm run lint` all GREEN; `npm run test:e2e` GREEN
    - The Plan-01 specs (lifecycle, booking, confirmation) are GREEN; `single-writer.test.ts` still finds exactly one paid writer
    - The live smoke records: requested row created on submit; Checkout redirect; webhook flips to `paid`; confirmation magic link logged; magic link lands on `/status/<id>`; the status page renders the timeline + the paid receipt for the owning guest only
    - No open-redirect, no `getSession` on the status path, no stale-cached guest document observed
  </acceptance_criteria>
  <done>The full suite is green and the live booking→pay→confirm→track slice works end-to-end against the applied 0004 schema — Phase 4 is verifiably complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Balkanity DB (schema apply) | The flagged/irreversible migration crosses into production state; wrong-project (Kalvia) application is the catastrophic failure mode. |
| service-role UPDATE → `wp_transfers.status` (trigger) | The runbook proves the trigger fires even here (service-role bypasses RLS but NOT triggers). |
| non-owning authenticated JWT → `wp_transfers` (RLS) | The runbook proves zero-row isolation at the data layer. |
| Stripe webhook (verified) → live paid transition | The end-to-end smoke proves the live trigger allows requested→paid (no Stripe retry storm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ENV | Tampering | Applying 0004 to the wrong project (Kalvia) | mitigate | Task 1 is a blocking-human checkpoint: confirm the ref is `qyhdogajtmnvxphrslwm` BEFORE apply; STOP if any tool reports Kalvia `utyatpadtibqqswsfvtr`; apply via Management-token/CLI (NOT MCP, which hits Kalvia). |
| T-04-TMP5 | Tampering | The live trigger silently diverges from the TS map / blocks requested→paid | mitigate | Task 2 runbook asserts the live allowed/illegal transitions against `lifecycle.ts`; Task 3 proves requested→paid live via the real webhook (no retry storm). |
| T-04-ID8 | Information Disclosure | Live RLS does not actually isolate guest PII | mitigate | Task 2 runbook proves a non-owning authenticated reader gets zero rows on the live DB. |
| T-04-SPOOF3 | Spoofing | A second paid writer slipped in during the phase | mitigate | Task 3 asserts `single-writer.test.ts` still finds exactly one writer; the success-spoof e2e stays green. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (RESEARCH §Package Legitimacy Audit: zero new packages). No slopcheck required. |
</threat_model>

<verification>
- Migration 0004 confirmed applied to Balkanity (qyhdogajtmnvxphrslwm), columns/trigger/policies live, seed rows intact.
- Adversarial runbook: legal chain passes, three illegal transitions raise check_violation (service-role path), RLS isolation + active-slug exposure correct.
- Full suite GREEN (Plan-01 RED specs now GREEN; single-writer still one writer; success-spoof e2e green).
- Live booking→pay→confirm→track smoke PASS.
</verification>

<success_criteria>
- The flagged 0004 schema is live on Balkanity only, with the trigger + RLS adversarially proven (XFER-01 data-layer DoD gate).
- The whole Phase-4 guest slice works end-to-end against the live schema.
- All locked invariants hold: single paid writer, getUser-not-getSession, no open redirect, NetworkFirst guest pages.
</success_criteria>

<output>
Create `.planning/phases/04-transfer-entity-booking-form/04-05-SUMMARY.md` when done.
</output>
