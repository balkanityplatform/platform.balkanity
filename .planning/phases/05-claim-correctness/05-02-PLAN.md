---
phase: 05-claim-correctness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - supabase/migrations/0005_claim_correctness.sql
  - platform/transfers/claim.ts
autonomous: true
requirements: [CLAIM-02, CLAIM-03]
must_haves:
  truths:
    - "Migration 0005 authors a masked wp_pool read exposing ONLY the 8 D-01 pre-claim columns (date/arrival_at, airport, zone, flight_no, amount_cents, pax, luggage_count) and physically omitting guest_name/email/phone, exact address, notes — for status='paid' AND driver_id IS NULL"
    - "Migration 0005 authors claim_transfer(p_transfer_id uuid) as a SECURITY DEFINER RPC that derives driver_id from auth.uid() internally, performs ONE atomic conditional UPDATE ... WHERE id=$1 AND status='paid' AND driver_id IS NULL ... RETURNING *, and returns a typed {ok, reason, transfer} result (winner gets the full row, losers get ok=false/reason='already_claimed'/transfer=null)"
    - "claim_transfer has set search_path='' with every relation schema-qualified; EXECUTE is granted to authenticated and revoked from public/anon"
    - "A claiming-driver RLS SELECT policy (driver_id = auth.uid()) is added on wp_transfers; NO write policy is added (the no-write-policy lock holds; the claim is the only sanctioned write, via the DEFINER RPC); paid → claimed passes the existing 0004 transition trigger unchanged"
    - "The 0005 file is AUTHORED ONLY (not applied) and carries the FLAGGED header + Balkanity-only guardrail strings"
    - "A thin platform/transfers/claim.ts invokes claim_transfer via the caller-auth server client (never service-role) and branches on data.ok with no follow-up PII read"
  artifacts:
    - path: "supabase/migrations/0005_claim_correctness.sql"
      provides: "wp_pool masked read + claim_transfer() SECURITY DEFINER RPC + wp_transfers_claimed_driver_read RLS policy"
      contains: "claim_transfer"
      min_lines: 70
    - path: "platform/transfers/claim.ts"
      provides: "claimTransfer(transferId) caller-auth RPC wrapper returning the typed {ok, reason, transfer} result"
      exports: ["claimTransfer"]
      min_lines: 20
  key_links:
    - from: "supabase/migrations/0005_claim_correctness.sql"
      to: "public.wp_transfers (atomic conditional UPDATE)"
      via: "UPDATE ... WHERE status='paid' AND driver_id IS NULL ... RETURNING *"
      pattern: "status\\s*=\\s*'paid'\\s+and\\s+driver_id is null"
    - from: "platform/transfers/claim.ts"
      to: "claim_transfer RPC"
      via: "caller-auth supabase.rpc('claim_transfer')"
      pattern: "rpc\\(\"claim_transfer\""
    - from: "supabase/migrations/0005_claim_correctness.sql"
      to: "public.is_admin() (migration 0002)"
      via: "reused (NOT redefined) for admin coverage"
      pattern: "is_admin"
---

<objective>
Author the single FLAGGED migration `0005_claim_correctness.sql` — the entire data-layer contract for Phase 5 — plus the thin caller-auth claim wrapper. Resolve RESEARCH Open Q1 by implementing the masked pool as a SECURITY DEFINER read (function or definer-owned read) so the base table stays 0-rows for non-claiming drivers (preserving SC4) while the masked read still physically omits PII columns (preserving SC1). FILE AUTHORING ONLY — the live apply is the Plan 03 BLOCKING signed-off task. This turns `platform/rls/claim-schema.test.ts` (Plan 01) GREEN.

Purpose: Build the two definition-of-done invariants as small, hardened Postgres objects (atomic conditional UPDATE for concurrency, structural column omission + RLS for PII) with the SECURITY DEFINER hardening and grant scoping that the threat model requires.
Output: `supabase/migrations/0005_claim_correctness.sql` (FLAGGED, authored not applied) + `platform/transfers/claim.ts`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-claim-correctness/05-CONTEXT.md
@.planning/phases/05-claim-correctness/05-RESEARCH.md
@.planning/phases/05-claim-correctness/05-PATTERNS.md

# Schema analogs — build ON these, do NOT recreate
@supabase/migrations/0002_supply_tables.sql
@supabase/migrations/0003_payments_spine.sql
@supabase/migrations/0004_transfer_entity.sql
# Code analogs
@platform/transfers/lifecycle.ts
@platform/auth/role.ts
@platform/supabase/server.ts
@platform/rls/claim-schema.test.ts
</context>

## Phase Goal

**As a** driver (or admin acting as a driver), **I want to** read a masked pool of paid/unclaimed transfers and atomically win a claim under concurrency, **so that** I receive the full transfer row exactly when I am the single winner and never see another guest's PII before then.

## Artifacts this phase produces

> Full list of new symbols across all Phase 5 plans. This plan (02) creates the data-layer + wrapper symbols marked ★.

- ★ `wp_pool` — masked pre-claim read (SECURITY DEFINER read per Open Q1; exposes only the 8 D-01 columns).
- ★ `claim_transfer(p_transfer_id uuid)` — SECURITY DEFINER atomic-claim RPC.
- ★ `wp_claim_result` — composite return type `{ ok boolean, reason text, transfer public.wp_transfers }`.
- ★ `wp_transfers_claimed_driver_read` — RLS SELECT policy `using ((select auth.uid()) = driver_id)`.
- ★ `supabase/migrations/0005_claim_correctness.sql` — the FLAGGED migration (authored, not applied).
- ★ `platform/transfers/claim.ts` — `claimTransfer(transferId)` thin caller-auth RPC wrapper returning `{ ok, reason, transfer }`.
- `platform/rls/claim-schema.test.ts`, `tests/claim/fixtures.ts`, `tests/claim/concurrency.gate.test.ts`, `tests/claim/pii-payload.gate.test.ts` *(Plan 01)*.
- `.planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md` *(Plan 03)*.

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0005 — masked wp_pool read + claim_transfer RPC + claiming-driver RLS (FLAGGED, file only)</name>
  <files>supabase/migrations/0005_claim_correctness.sql</files>
  <read_first>
    - supabase/migrations/0004_transfer_entity.sql (FLAGGED header lines 1-8 to copy verbatim; the RLS SELECT policy shape lines 146-149; the transition trigger lines 87-127 already permits paid→claimed; the no-write-policy lock note lines 46-47; re-runnability note lines 39-40)
    - supabase/migrations/0002_supply_tables.sql (is_admin() SECURITY DEFINER helper lines 22-33 — REUSE, do NOT redefine; destinations.zone/airport columns the pool joins)
    - supabase/migrations/0003_payments_spine.sql (wp_transfers columns the RPC returns; the no-write-policy + admin-read lock)
    - .planning/phases/05-claim-correctness/05-RESEARCH.md (Pattern 1 lines 179-205 masked read; Pattern 2 lines 207-256 RPC; Pattern 3 lines 258-269 RLS; Open Q1 lines 449-454 — the SECURITY-DEFINER-read recommendation; Pitfall 4 search_path; Pitfall 5 grant/revoke)
    - .planning/phases/05-claim-correctness/05-PATTERNS.md (the 0005 section lines 24-66 — exact reuse points + line refs)
    - platform/rls/claim-schema.test.ts (the contract this migration must satisfy — every regex)
  </read_first>
  <action>
    Create `supabase/migrations/0005_claim_correctness.sql`. Copy the `0004` FLAGGED header verbatim (lines 1-8 shape) and ensure the strings `qyhdogajtmnvxphrslwm` and `Kalvia (utyatpadtibqqswsfvtr)` appear (the contract test asserts both against the raw text). Document THIS FILE IS AUTHORED, NOT APPLIED (live apply via Management API is the Plan 03 BLOCKING signed-off task — NOT MCP/`db push`/direct host). Every statement must be re-runnable (`create or replace`, `drop ... if exists`, `create ... if not exists`).

    Resolve Open Q1 with a SECURITY DEFINER masked read (NOT a security_invoker view that would force a permissive base-table read policy re-opening the SC3/SC4 PII path). Implement `wp_pool` as a SECURITY DEFINER read that returns ONLY the 8 D-01 columns — date/`arrival_at`, `d.airport`, `d.zone` (AREA only, NEVER `d.address`), `t.flight_no` (operational, D-02), `t.amount_cents` (fare), `t.pax`, `t.luggage_count`, plus `t.id`/`t.status` for keying — joining `public.destinations d` on `t.destination_id`, filtered to `t.status='paid' AND t.driver_id IS NULL`. Physically OMIT `guest_name`, `guest_email`, `guest_phone`, `d.address`, `t.notes` (never selected — the payload structurally cannot carry them). Restrict the read to drivers+admins (role IN ('driver','admin'), D-06) and `set search_path = ''` with fully-qualified relations. (If the contract regex `security_invoker = on` is the chosen branch instead, you MUST pair it with a base-table read policy — do NOT; the definer read keeps the base table 0-rows for non-claiming drivers per SC4. Add NO base-table pre-claim SELECT policy for drivers.) Grant the masked read to `authenticated`.

    Define the composite return type `create type public.wp_claim_result as (ok boolean, reason text, transfer public.wp_transfers)` (drop-if-exists first for re-runnability). Author `create or replace function public.claim_transfer(p_transfer_id uuid) returns public.wp_claim_result language plpgsql security definer set search_path = ''`. Inside: read `v_uid uuid := (select auth.uid())`; if null return `(false, 'not_authenticated', null)::public.wp_claim_result`. Perform the SINGLE atomic conditional `update public.wp_transfers set status='claimed', driver_id=v_uid where id=p_transfer_id and status='paid' and driver_id is null returning * into v_row` (this UPDATE is the concurrency control — D-04 — never a SELECT-then-UPDATE; never trust a client-supplied driver id). If `not found` return `(false, 'already_claimed', null)::public.wp_claim_result` (graceful loser, zero PII — D-03). Otherwise return `(true, null, v_row)::public.wp_claim_result` (winner gets the full row atomically via RETURNING * — D-03, no second read). The `paid → claimed` write passes the existing 0004 transition trigger unchanged. Then `revoke execute on function public.claim_transfer(uuid) from public, anon;` and `grant execute on function public.claim_transfer(uuid) to authenticated;` (D-06, Pitfall 5).

    Add the claiming-driver RLS SELECT policy on `public.wp_transfers` mirroring `0004` lines 146-149: `drop policy if exists "wp_transfers_claimed_driver_read" on public.wp_transfers; create policy "wp_transfers_claimed_driver_read" on public.wp_transfers for select to authenticated using ((select auth.uid()) = driver_id);` — admins are already covered by the existing `wp_transfers_admin_read` (0003), so a driver-self clause suffices (RESEARCH Q3, D-08); a non-claiming driver matches no policy → 0 rows (SC4). Add NO `for insert/update/delete` policy anywhere — the no-write-policy lock holds (the DEFINER RPC is the only sanctioned write). REUSE `public.is_admin()` (0002) — do NOT redefine it.
  </action>
  <verify>
    <automated>npx vitest run platform/rls/claim-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `0005 migration text contains` the FLAGGED header strings `qyhdogajtmnvxphrslwm` and `Kalvia (utyatpadtibqqswsfvtr)`.
    - `0005 migration text contains` `set search_path = ''`, `grant execute on function public.claim_transfer ... to authenticated`, and `revoke execute on function public.claim_transfer ... from public, anon`.
    - `0005 migration text contains` the atomic predicate `status = 'paid' and driver_id is null` and `returning *`; the masked read selects none of `guest_name`/`guest_email`/`guest_phone`/`address`/`notes`.
    - Source assertion: no `for update`/`for insert`/`for delete` policy on `wp_transfers`; `is_admin()` is referenced but not redefined (no second `create ... function public.is_admin` in this file).
    - Behavior: `npx vitest run platform/rls/claim-schema.test.ts` now exits 0 (GREEN) — the contract is satisfied by the authored text.
    - The file is NOT applied to any live DB by this task (authoring only).
  </acceptance_criteria>
  <done>0005 is authored, FLAGGED, re-runnable, satisfies every claim-schema contract assertion, and implements the masked read as a SECURITY DEFINER read (Open Q1 resolved) with the hardened atomic-claim RPC and the claiming-driver RLS policy. Not yet applied.</done>
</task>

<task type="auto">
  <name>Task 2: Thin caller-auth claim wrapper</name>
  <files>platform/transfers/claim.ts</files>
  <read_first>
    - platform/auth/role.ts (the caller-auth server pattern: `createClient()` from @/platform/supabase/server + auth.getUser; the maybeSingle error-vs-empty discipline)
    - platform/supabase/server.ts (the caller-auth client — anon key + cookie-bound JWT so auth.uid() is the real driver)
    - platform/supabase/admin.ts (the service-role client this file MUST NOT import — D-04)
    - platform/transfers/lifecycle.ts (the paid→claimed edge this wraps; the "DB is the authority" framing)
    - .planning/phases/05-claim-correctness/05-PATTERNS.md (claim.ts section lines 69-86 — the exact wrapper shape; branch on data.ok, no follow-up PII read)
    - .planning/phases/05-claim-correctness/05-RESEARCH.md (D-03 typed result; Pitfall 7 no follow-up read)
  </read_first>
  <action>
    Create `platform/transfers/claim.ts` exporting `async function claimTransfer(transferId: string)`. Import `createClient` from `@/platform/supabase/server` (caller-auth — NEVER `@/platform/supabase/admin`; service-role would bypass RLS and break the `auth.uid()`-keyed claim, D-04). Call `const supabase = await createClient(); const { data, error } = await supabase.rpc("claim_transfer", { p_transfer_id: transferId });`. Branch on the typed result `data.ok` (D-03) — return the `{ ok, reason, transfer }` shape to the caller; do NOT use try/catch as control flow and do NOT issue any follow-up `.select()` for PII (the winner's full row is already in `data.transfer` via RETURNING * — Pitfall 7). Surface a genuine transport `error` distinctly from a graceful `ok=false` loser (mirror role.ts's error-vs-empty discipline). Type the return against the composite result if generated types exist, otherwise a local `{ ok: boolean; reason: string | null; transfer: ... | null }` type. The full driver/admin UI consuming this is Phase 6 — keep this thin.
  </action>
  <verify>
    <automated>npm run typecheck && grep -q 'rpc("claim_transfer"' platform/transfers/claim.ts && grep -q "@/platform/supabase/server" platform/transfers/claim.ts && ! grep -q "@/platform/supabase/admin" platform/transfers/claim.ts && echo "caller-auth-only OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `platform/transfers/claim.ts` exists and exports `claimTransfer`.
    - Source assertion: imports `createClient` from `@/platform/supabase/server`; does NOT import `@/platform/supabase/admin` (no service-role on the claim path).
    - Source assertion: calls `supabase.rpc("claim_transfer", { p_transfer_id: ... })`, branches on `data.ok`, and contains no `.select(` follow-up PII read after the rpc.
    - Behavior: `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>A thin, caller-auth claim wrapper exists that branches on the typed result and never reads PII via a second round trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (driver/admin JWT) → claim_transfer RPC | The RPC must derive identity from auth.uid(), never a client argument |
| non-claiming driver → wp_transfers base table | RLS must return 0 rows; the masked read is the only pre-claim path |
| SECURITY DEFINER function → schema resolution | search_path must be empty + relations fully qualified |
| authored migration file → live DB | Never auto-applied; live apply is a separate signed-off task (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-PII | Information Disclosure | wp_pool / wp_transfers base-table payload to a non-claiming driver | mitigate | SECURITY DEFINER masked read selects only the 8 D-01 columns (physical omission); NO base-table driver read policy → base table returns 0 rows (SC3/SC4) |
| T-05-EOP1 | Elevation of Privilege | claim_transfer SECURITY DEFINER search_path injection | mitigate | `set search_path = ''` + every relation schema-qualified (`public.wp_transfers`, `auth.uid()`) (Pitfall 4) |
| T-05-EOP2 | Elevation of Privilege | RPC callable by anon/public | mitigate | `revoke execute from public, anon` + `grant execute to authenticated` + null-auth.uid() guard (Pitfall 5, D-06) |
| T-05-SPOOF | Spoofing | client claims on behalf of another driver | mitigate | RPC reads `auth.uid()` internally; signature takes only `p_transfer_id` — no driver-id argument (D-04) |
| T-05-RACE | Tampering | double-claim under concurrency | mitigate | ONE atomic conditional UPDATE `WHERE status='paid' AND driver_id IS NULL` under READ COMMITTED — never RLS, never SELECT-then-UPDATE (D-04, Pitfall 2) |
| T-05-MONEY | Tampering (money invariant) | a second `status='paid'` writer via the claim path | mitigate | claim only moves paid→claimed; never sets status='paid'; single-writer.test.ts stays green |
| T-05-KALVIA | Tampering / misconfig | applying 0005 to the wrong project | mitigate | File authored only; FLAGGED header carries the Balkanity-only guardrail; live apply gated to Management API in Plan 03 (Pitfall 6) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH §Standard Stack); nothing to gate |
</threat_model>

<verification>
- `npx vitest run platform/rls/claim-schema.test.ts` — GREEN (the authored 0005 satisfies the contract).
- `npm run typecheck` — clean.
- `npx vitest run platform/payments/single-writer.test.ts` — STILL GREEN (claim adds no `status='paid'` writer).
- `npm run test` — full unit suite green; `tests/claim/**` live gates stay RED until the Plan 03 apply (expected — they need the live DB objects).
</verification>

<success_criteria>
- 0005 authored, FLAGGED, Balkanity-guarded, re-runnable; masked pool physically omits PII (8 columns only); claim_transfer is a hardened SECURITY DEFINER atomic-claim RPC with the typed result + grant/revoke; claiming-driver RLS policy added; no write policy added.
- Open Q1 resolved as a SECURITY DEFINER read so the base table stays 0-rows for non-claiming drivers (SC4) — flagged for sign-off as part of the FLAGGED migration in Plan 03.
- claim-schema.test.ts is GREEN; single-writer.test.ts stays GREEN.
- Thin caller-auth claim wrapper exists (no service-role, no follow-up PII read).
</success_criteria>

<output>
Create `.planning/phases/05-claim-correctness/05-02-SUMMARY.md` when done.
</output>
