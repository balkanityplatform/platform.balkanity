---
phase: 05-claim-correctness
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md
autonomous: false
requirements: [CLAIM-02, CLAIM-03]
user_setup: []
must_haves:
  truths:
    - "A human reviews and signs off migration 0005 BEFORE it touches the live DB (FLAGGED schema/RLS/RPC change), with the Open-Q1 SECURITY-DEFINER-read decision explicitly approved"
    - "Migration 0005 is applied to the Balkanity project qyhdogajtmnvxphrslwm ONLY (never Kalvia) via the Supabase Management API /database/query endpoint, with the ref guardrail run BEFORE apply"
    - "Post-apply, wp_pool + claim_transfer + wp_transfers_claimed_driver_read exist on the live DB and PostgREST exposes the view + RPC"
    - "ADVERSARIAL GATE (SC2): N simultaneous claim_transfer() calls on one seeded paid row yield exactly one ok=true winner and N-1 ok=false reason='already_claimed' losers — proven live"
    - "ADVERSARIAL GATE (SC3): reading wp_pool with a non-claiming driver JWT returns zero PII keys (name/contact/exact address/notes); reading the base table returns 0 rows — proven live (flight_no present is expected, not a failure)"
    - "Both gate runs + the apply are recorded in 05-GATES-EVIDENCE.md (mirror 03-GATES-EVIDENCE.md); seeded test rows are torn down"
  artifacts:
    - path: ".planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md"
      provides: "Recorded sign-off, ref guardrail, Management-API apply, post-apply object verification, and both live adversarial-gate outputs"
      min_lines: 60
  key_links:
    - from: "Management API /v1/projects/qyhdogajtmnvxphrslwm/database/query"
      to: "live Balkanity DB"
      via: "BEGIN; <0005 SQL>; insert schema_migrations; COMMIT"
      pattern: "qyhdogajtmnvxphrslwm/database/query"
    - from: "tests/claim/concurrency.gate.test.ts"
      to: "live claim_transfer RPC"
      via: "N parallel caller-auth rpc calls"
      pattern: "claim_transfer"
    - from: "tests/claim/pii-payload.gate.test.ts"
      to: "live wp_pool + wp_transfers"
      via: "non-claiming-driver JWT reads"
      pattern: "wp_pool"
---

<objective>
Close Phase 5: obtain human sign-off on the FLAGGED migration `0005` (including the Open-Q1 SECURITY-DEFINER-read decision), apply it to the LIVE Balkanity DB via the Supabase Management API (never MCP/Kalvia, never `db push`), verify the live objects, then run BOTH adversarial gates live against the seeded TEST DB and record everything in `05-GATES-EVIDENCE.md` — mirroring the Phase 3 evidence pattern. Both gates MUST pass before the phase closes (crown-jewel correctness phase).

Purpose: Prove the two definition-of-done invariants at the data layer with live, adversarial evidence (one winner under concurrency; zero PII to a non-claiming driver) — not source inspection.
Output: `0005` live on Balkanity; both adversarial gates GREEN live; `05-GATES-EVIDENCE.md` recorded.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-claim-correctness/05-CONTEXT.md
@.planning/phases/05-claim-correctness/05-RESEARCH.md

# The exact apply + evidence pattern to mirror
@.planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md
# The objects being applied + proven
@supabase/migrations/0005_claim_correctness.sql
@tests/claim/concurrency.gate.test.ts
@tests/claim/pii-payload.gate.test.ts
@tests/claim/fixtures.ts
</context>

## Phase Goal

**As a** Balkanity operator, **I want to** ship the claim data layer to the live Balkanity DB only after sign-off and prove both correctness invariants with live adversarial evidence, **so that** the pilot can never double-claim a transfer or leak guest PII to a non-claiming driver.

## Artifacts this phase produces

> Full list of new symbols across all Phase 5 plans. This plan (03) creates the evidence runbook marked ★ and applies the Plan-02 objects to the live DB.

- `wp_pool`, `claim_transfer(p_transfer_id uuid)`, `wp_claim_result`, `wp_transfers_claimed_driver_read` — applied LIVE this plan (authored in Plan 02).
- `supabase/migrations/0005_claim_correctness.sql` — applied LIVE this plan (authored in Plan 02).
- ★ `.planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md` — sign-off + apply + both live adversarial-gate outputs.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Sign off the FLAGGED migration 0005 before live apply</name>
  <action>Pause for human sign-off on the FLAGGED migration 0005 (schema + RLS + SECURITY DEFINER RPC) BEFORE any live DDL runs. Surface the decision below and do NOT proceed to apply until the operator selects approve-apply. This is the review gate per CLAUDE.md (flag schema/auth/RLS/payment changes) + the ROADMAP Phase 5 sign-off note. Precondition: `platform/rls/claim-schema.test.ts` is GREEN and the Balkanity ref guardrail has been run.</action>
  <decision>Approve applying the FLAGGED migration 0005 (wp_pool masked read + claim_transfer SECURITY DEFINER RPC + claiming-driver RLS) to the LIVE Balkanity DB, including the Open-Q1 mechanism choice.</decision>
  <context>
    0005 is a FLAGGED, irreversible schema/RLS/RPC change (review gate per CLAUDE.md + ROADMAP Phase 5 note). It adds a masked pre-claim read, a SECURITY DEFINER atomic-claim RPC, and a claiming-driver RLS SELECT policy on wp_transfers. RESEARCH Open Q1 was resolved by implementing the masked pool as a SECURITY DEFINER read (NOT a security_invoker view), so the base table stays 0-rows for non-claiming drivers (SC4) while the masked read physically omits PII (SC1). Before this, run the Balkanity ref guardrail (curl the project, confirm qyhdogajtmnvxphrslwm, confirm Kalvia utyatpadtibqqswsfvtr absent) and confirm `platform/rls/claim-schema.test.ts` is GREEN. Apply path: Supabase Management API POST /v1/projects/qyhdogajtmnvxphrslwm/database/query — NEVER MCP (reaches Kalvia), NEVER `supabase db push`, NEVER the direct db.<ref> host (IPv6-only). Record 0005 in supabase_migrations.schema_migrations in the SAME transaction.
  </context>
  <options>
    <option id="approve-apply">
      <name>Approve & apply now (SECURITY DEFINER masked read)</name>
      <pros>Unblocks the live adversarial gates; base table stays 0-rows for non-claiming drivers (SC4 tight); D-01 column omission preserved.</pros>
      <cons>Irreversible schema change on the live pilot DB.</cons>
    </option>
    <option id="revise-invoker">
      <name>Revise to security_invoker view first</name>
      <pros>Matches D-01's literal "security_invoker view" naming.</pros>
      <cons>Requires a permissive base-table read policy that re-opens the SC3/SC4 PII path — RESEARCH recommends against; would weaken the boundary.</cons>
    </option>
  </options>
  <verify>Operator selects an option; no automated check (this is a sign-off gate).</verify>
  <done>Sign-off recorded (approve-apply or revise-invoker). No live DDL has run before this point.</done>
  <resume-signal>Select: approve-apply or revise-invoker (with required changes)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Apply 0005 to Balkanity (Management API) + verify live objects</name>
  <read_first>
    - .planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md (the exact Management-API apply: ref guardrail, BEGIN…COMMIT with schema_migrations row, post-apply verification queries)
    - supabase/migrations/0005_claim_correctness.sql (the SQL being applied)
    - .planning/phases/05-claim-correctness/05-RESEARCH.md (Runtime State Inventory lines 347-359 — PostgREST schema reload; Pitfall 6 lines 393-397 — Kalvia guardrail)
    - CLAUDE.md (Infrastructure constraint — Balkanity ref ONLY)
  </read_first>
  <action>
    Apply migration 0005 to the LIVE Balkanity DB and verify the resulting objects, in this exact order. (1) Ref guardrail: `curl -s https://api.supabase.com/v1/projects/qyhdogajtmnvxphrslwm -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN"` and assert the response name/ref is Balkanity and that Kalvia (utyatpadtibqqswsfvtr) appears nowhere in resolved env. (2) Confirm `npx vitest run platform/rls/claim-schema.test.ts` is GREEN. (3) Apply via `POST /v1/projects/qyhdogajtmnvxphrslwm/database/query` with body `BEGIN; <0005 SQL>; insert into supabase_migrations.schema_migrations (version, name, statements) values ('0005','claim_correctness', ARRAY[...]); COMMIT;` — NEVER MCP, NEVER `supabase db push`, NEVER the direct db.<ref> host. (4) Post-apply verification queries: `wp_pool` relation exists; `claim_transfer` function exists with `prosecdef=true` and a non-null `proconfig` containing `search_path=`; `wp_transfers_claimed_driver_read` policy exists; NO `for update/insert/delete` policy on wp_transfers; migration history shows 0005. (5) Confirm PostgREST exposes `wp_pool` + `claim_transfer` (issue `NOTIFY pgrst, 'reload schema'` if needed). Record the ref guardrail, the apply response, and the post-apply object list into `05-GATES-EVIDENCE.md`. Per Pitfall 6 + D-04/D-06.
  </action>
  <what-built>
    The FLAGGED migration 0005 applied to Balkanity via the Management API with the pre-apply ref guardrail, the schema_migrations history row written in the same transaction, and post-apply verification of wp_pool + claim_transfer (security definer, search_path set) + the claiming-driver RLS policy + the no-write-policy lock + PostgREST exposure.
  </what-built>
  <how-to-verify>
    1. Read the recorded ref-guardrail output and confirm it shows `ref: qyhdogajtmnvxphrslwm` and "Kalvia ref present anywhere? → NO".
    2. Confirm the apply response was `[]` (empty = DDL success) and the schema_migrations row for 0005 was written in the same transaction.
    3. Confirm the post-apply object list shows wp_pool, claim_transfer (security definer, search_path set), and the claiming-driver policy — and NO write policy on wp_transfers.
  </how-to-verify>
  <verify>
    <automated>grep -q "qyhdogajtmnvxphrslwm/database/query" .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md && grep -qi "claim_transfer" .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md && echo "apply evidence recorded"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: the ref-guardrail curl resolves to `qyhdogajtmnvxphrslwm` (Balkanity) and Kalvia `utyatpadtibqqswsfvtr` is absent from resolved env.
    - Behavior: post-apply, `claim_transfer` exists with `prosecdef = true` and `proconfig` containing `search_path=`; `wp_pool` exists; `wp_transfers_claimed_driver_read` exists; no `for update/insert/delete` policy on `wp_transfers`.
    - Source assertion: `05-GATES-EVIDENCE.md` records the Management-API endpoint `qyhdogajtmnvxphrslwm/database/query`, the empty `[]` apply response, and the migration-history row for 0005.
  </acceptance_criteria>
  <done>0005 is live on Balkanity (never Kalvia), applied via the Management API with the ref guardrail; wp_pool + claim_transfer + the claiming-driver policy exist and PostgREST exposes them; the no-write-policy lock holds; the apply is recorded in 05-GATES-EVIDENCE.md.</done>
  <resume-signal>Type "applied" once the live objects are verified, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Run both live adversarial gates + record 05-GATES-EVIDENCE.md</name>
  <read_first>
    - tests/claim/concurrency.gate.test.ts + tests/claim/pii-payload.gate.test.ts (the gates now runnable against the live objects)
    - tests/claim/fixtures.ts (seed/teardown — TEST-DB only)
    - .planning/phases/03-payments-trust-spine/03-GATES-EVIDENCE.md (the evidence FORMAT: gate sections with recorded commands + outputs, acceptance summary table, cleanup note, frontmatter `gates:` block)
    - .planning/phases/05-claim-correctness/05-RESEARCH.md (Sampling Rate lines 502-505 — phase gate requires both gates demonstrated live; Pattern 4/5 expected outputs)
  </read_first>
  <action>
    Run both adversarial gates live against the Balkanity DB in a TEST context (seed → assert → teardown) and record the evidence. (1) `npx vitest run tests/claim/concurrency.gate.test.ts` — seed one paid/unclaimed transfer + N (≥20) driver JWTs, fire N parallel claim_transfer() calls, assert exactly one ok=true winner + N-1 ok=false reason='already_claimed', loop K times. (2) `npx vitest run tests/claim/pii-payload.gate.test.ts` — a non-claiming driver reads wp_pool (zero PII keys; flight_no present is expected) and the base table (0 rows). (3) Confirm `npx vitest run platform/payments/single-writer.test.ts` is STILL GREEN (no second `paid` writer). (4) Write `05-GATES-EVIDENCE.md` mirroring 03-GATES-EVIDENCE.md — a frontmatter `gates:` block (schema_apply, gate_concurrency_one_winner/SC2, gate_pii_zero_keys/SC3, single_writer/SC1), the ref guardrail, the apply record, both recorded gate outputs, and an acceptance-summary table. Tear down every seeded row + driver user after the run (leave the live DB as found). Per CLAIM-02/CLAIM-03, SC2/SC3/SC4.
  </action>
  <what-built>
    Both Phase 5 adversarial gates run live: the N-parallel concurrency gate (exactly one winner, looped K times) and the non-claiming-driver PII gate (zero PII keys + 0 base rows), the single-writer gate confirmed still green, and 05-GATES-EVIDENCE.md recorded with both gate outputs and the seeded-rows teardown.
  </what-built>
  <how-to-verify>
    1. Read 05-GATES-EVIDENCE.md GATE (concurrency): confirm it shows exactly 1 winner and N-1 `already_claimed` across the looped runs.
    2. Read 05-GATES-EVIDENCE.md GATE (PII): confirm the non-claiming-driver pool payload shows zero of {guest_name, guest_email, guest_phone, address, notes}, flight_no present, and the base-table read returned 0 rows.
    3. Confirm the acceptance summary table marks both adversarial gates and the single-writer gate PASS, and that seeded test rows were cleaned up.
  </how-to-verify>
  <verify>
    <automated>npx vitest run platform/payments/single-writer.test.ts && grep -qiE "exactly one|1 winner|already_claimed" .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md && grep -qiE "zero PII|guest_name|0 rows" .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md && echo "both gate evidences recorded + single-writer green"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx vitest run tests/claim/concurrency.gate.test.ts` exits 0 — exactly one winner, N-1 `already_claimed` losers, across K looped runs.
    - Behavior: `npx vitest run tests/claim/pii-payload.gate.test.ts` exits 0 — zero PII keys in the non-claiming-driver pool payload (flight_no present is OK), 0 base-table rows.
    - Behavior: `npx vitest run platform/payments/single-writer.test.ts` exits 0 (no second `paid` writer introduced by the claim path).
    - Source assertion: `05-GATES-EVIDENCE.md` records both gate command outputs, a passing acceptance-summary table, and the seeded-rows cleanup note; frontmatter `gates:` block marks both adversarial gates passed.
  </acceptance_criteria>
  <done>Both adversarial gates pass live (one winner under concurrency; zero PII to a non-claiming driver), single-writer stays green, and 05-GATES-EVIDENCE.md records the apply + both gate outputs with seeded data torn down.</done>
  <resume-signal>Type "gates-pass" once both adversarial gates are green and evidence is recorded, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Balkanity DB | FLAGGED DDL applied only after human sign-off, only to qyhdogajtmnvxphrslwm |
| Management API token → project selection | Must resolve to Balkanity; Kalvia must be absent (ref guardrail) |
| N concurrent driver JWTs → claim_transfer | The live concurrency proof; one winner is the invariant |
| non-claiming driver JWT → wp_pool / wp_transfers | The live PII proof; zero PII keys + 0 base rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-KALVIA | Tampering / misconfig | applying 0005 to Kalvia instead of Balkanity | mitigate | Ref guardrail curl BEFORE apply (assert qyhdogajtmnvxphrslwm, assert Kalvia absent); Management API only — never MCP/db push/direct host (Pitfall 6) |
| T-05-FLAG | Tampering | unreviewed FLAGGED schema change reaching live | mitigate | Blocking human sign-off decision task BEFORE apply (autonomous:false); claim-schema.test.ts GREEN precondition |
| T-05-FALSEGREEN | Tampering | concurrency gate that serializes / false-greens | mitigate | Live N-parallel run looped K times; recorded winner/loser counts in evidence |
| T-05-PIILEAK | Information Disclosure | live PII leak to a non-claiming driver via raw API | mitigate | Live non-claiming-JWT read asserts zero PII keys + 0 base rows (SC3/SC4) |
| T-05-RESIDUE | Information Disclosure | seeded test PII left on the live DB | mitigate | Teardown deletes every seeded transfer/destination/property/company/driver after the run (03-GATES-EVIDENCE cleanup pattern) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (RESEARCH §Standard Stack); nothing to gate |
</threat_model>

<verification>
- Ref guardrail confirms Balkanity (qyhdogajtmnvxphrslwm), Kalvia absent — BEFORE apply.
- 0005 live; wp_pool + claim_transfer (security definer, search_path set) + wp_transfers_claimed_driver_read present; no write policy on wp_transfers; PostgREST exposes the view + RPC.
- `npx vitest run tests/claim/concurrency.gate.test.ts` — GREEN live (exactly one winner).
- `npx vitest run tests/claim/pii-payload.gate.test.ts` — GREEN live (zero PII keys + 0 base rows).
- `npx vitest run platform/payments/single-writer.test.ts` — GREEN (single `paid` writer intact).
- `05-GATES-EVIDENCE.md` records sign-off, apply, and both gate outputs; seeded rows torn down.
</verification>

<success_criteria>
- Human sign-off recorded before any live DDL; 0005 applied to Balkanity ONLY via the Management API.
- Both ADVERSARIAL GATES pass live: N-parallel claim → exactly one winner (SC2); non-claiming-driver payload → zero PII keys + 0 base rows (SC3/SC4).
- single-writer.test.ts stays green (no second `paid` writer).
- Evidence recorded in 05-GATES-EVIDENCE.md; seeded test data cleaned up.
</success_criteria>

<output>
Create `.planning/phases/05-claim-correctness/05-03-SUMMARY.md` when done.
</output>
