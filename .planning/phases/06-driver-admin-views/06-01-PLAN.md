---
phase: 06-driver-admin-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
  - platform/transfers/lifecycle.ts
  - platform/transfers/lifecycle.test.ts
  - supabase/migrations/0006_release_and_audit.sql
  - platform/transfers/pool.masking.test.ts
  - app/driver/advance.ownership.test.ts
  - app/driver/advance.lifecycle.test.ts
  - app/driver/run/RunView.test.tsx
  - app/admin/transfers/actions.test.ts
  - app/admin/transfers/TransfersView.test.tsx
  - platform/payments/refund.test.ts
  - platform/payments/single-writer.test.ts
autonomous: false
requirements: [CLAIM-01, CLAIM-04, CLAIM-05, CLAIM-06, OPS-01, OPS-02, OPS-03, OPS-04]
user_setup: []

must_haves:
  truths:
    - "Every new Phase-6 EN copy key has a matching BG key (tsc Dict-parity gate passes)"
    - "ALLOWED_TRANSITIONS.claimed includes 'paid' (the release backward edge) and the 8x8 pair test still passes"
    - "Migration 0006 file exists, authored-not-applied, Balkanity-ref-only, idempotent, adds the claimed->paid trigger edge + last_action_* columns"
    - "Wave 0 RED specs exist for pool masking, driver advance ownership+lifecycle, driver run ordering/completed-drop (CLAIM-06), admin transfers filter/search/coral-pinning (OPS-01), admin actions, and refund hook"
    - "single-writer test acknowledges exactly the webhook + the narrow release action as the only status='paid' writers"
  artifacts:
    - path: "supabase/migrations/0006_release_and_audit.sql"
      provides: "claimed->paid trigger edge (release) + last_action_reason/by/at columns"
      contains: "claimed"
    - path: "platform/transfers/lifecycle.ts"
      provides: "ALLOWED_TRANSITIONS.claimed includes paid"
      contains: "paid"
    - path: "platform/transfers/pool.masking.test.ts"
      provides: "CLAIM-01 pool no-PII-keys RED spec"
    - path: "app/driver/advance.ownership.test.ts"
      provides: "CLAIM-04 driver-advance ownership RED spec"
    - path: "app/driver/advance.lifecycle.test.ts"
      provides: "CLAIM-05 legal-edge-only RED spec"
    - path: "app/driver/run/RunView.test.tsx"
      provides: "CLAIM-06 run ordered-by-arrival + completed-drops-to-Completed-today RED spec"
    - path: "app/admin/transfers/actions.test.ts"
      provides: "OPS-03 admin re-gate + assign/reassign/release/cancel RED spec"
    - path: "app/admin/transfers/TransfersView.test.tsx"
      provides: "OPS-01 status filter + free-text search + stuck/unclaimed coral pinning RED spec"
    - path: "platform/payments/refund.test.ts"
      provides: "OPS-04 refund-hook shape + idempotency RED spec"
  key_links:
    - from: "platform/transfers/lifecycle.ts"
      to: "supabase/migrations/0006_release_and_audit.sql"
      via: "claimed->paid edge mirrored in both the TS map and the DB trigger"
      pattern: "claimed.*paid"
    - from: "platform/transfers/lifecycle.test.ts"
      to: "platform/transfers/lifecycle.ts"
      via: "8x8 pair test pins the map to the trigger"
      pattern: "canTransition"
    - from: "app/driver/run/RunView.test.tsx"
      to: "app/driver/run/RunView.tsx"
      via: "Plan 03 Task 2 consumes this RED spec as its CLAIM-06 gate"
      pattern: "arrival_at"
    - from: "app/admin/transfers/TransfersView.test.tsx"
      to: "app/admin/transfers/TransfersView.tsx"
      via: "Plan 04 Task 1 consumes this RED spec as its OPS-01 gate"
      pattern: "needsAttention"
---

<objective>
Lay the Phase-6 foundation as a single Wave-1 plan: (1) author every EN/BG dictionary key the four downstream UI plans consume, (2) extend the lifecycle map + its 8x8 pin-test in lockstep with a new `claimed->paid` backward edge for `release` (D-14), (3) author the FLAGGED sign-off-gated migration `0006` (the trigger edge + the `last_action_*` audit columns, D-14/D-15) authored-not-applied, and (4) land the Wave-0 Nyquist RED specs that pin the contracts the driver/admin slices must satisfy.

Purpose: Downstream plans 02-05 must not invent copy, re-derive the lifecycle, or discover the contract by exploring the codebase — they receive copy keys, a pinned transition map, an authored migration file, and failing tests that define "done." This plan touches the LOCKED Phase-5 data layer (the lifecycle trigger), so it is `autonomous: false`: the migration delta requires sign-off before the live apply in Plan 05.

Output: dictionary keys (en.ts/bg.ts), `ALLOWED_TRANSITIONS.claimed += "paid"` + an updated `lifecycle.test.ts`, `supabase/migrations/0006_release_and_audit.sql` (authored only), and seven Wave-0 RED/contract test files (`pool.masking`, `advance.ownership`, `advance.lifecycle`, `run/RunView`, `admin/transfers/actions`, `admin/transfers/TransfersView`, `payments/refund`) plus the widened single-writer contract.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-driver-admin-views/06-CONTEXT.md
@.planning/phases/06-driver-admin-views/06-RESEARCH.md
@.planning/phases/06-driver-admin-views/06-PATTERNS.md
@.planning/phases/06-driver-admin-views/06-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies:
- **`supabase/migrations/0006_release_and_audit.sql` (NEW)** — adds the trigger-legal `claimed -> paid` backward edge to `wp_enforce_transfer_transition()` (release, D-14) and ALTERs `wp_transfers` to add `last_action_reason text`, `last_action_by uuid`, `last_action_at timestamptz` (audit, D-15). Authored, NOT applied (live apply is Plan 05).
- **`platform/transfers/lifecycle.ts` (MODIFIED)** — `ALLOWED_TRANSITIONS.claimed` gains `"paid"` (the release edge); the map stays the cosmetic mirror of the trigger.
- **`platform/transfers/lifecycle.test.ts` (MODIFIED)** — the EXPECTED map + 8x8 pair test updated so `claimed->paid` is asserted legal; release edge spot-asserted.
- **`platform/payments/single-writer.test.ts` (MODIFIED)** — contract widened to allow exactly two `status='paid'` writers: the webhook route AND the narrow gated release action (`app/admin/transfers/actions.ts`), forbidding any other.
- **`platform/i18n/en.ts` + `bg.ts` (MODIFIED)** — every Phase-6 copy key from the UI-SPEC Copywriting Contract.
- **Wave-0 RED specs (NEW):** `platform/transfers/pool.masking.test.ts`, `app/driver/advance.ownership.test.ts`, `app/driver/advance.lifecycle.test.ts`, `app/driver/run/RunView.test.tsx` (CLAIM-06), `app/admin/transfers/actions.test.ts`, `app/admin/transfers/TransfersView.test.tsx` (OPS-01), `platform/payments/refund.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Phase-6 dictionary keys (EN + BG parity)</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts</files>
  <read_first>
    - platform/i18n/en.ts (existing key shape; `Dict = typeof en` is the source of truth)
    - platform/i18n/bg.ts (must satisfy the exact same shape — a missing/extra key fails tsc)
    - .planning/phases/06-driver-admin-views/06-UI-SPEC.md "Copywriting Contract" (the verbatim EN strings + key names)
  </read_first>
  <action>
    Append all Phase-6 keys to BOTH `en.ts` and `bg.ts` (EN verbatim from the UI-SPEC Copywriting Contract; BG translated). Driver keys: `claimTransferCta`, `poolEmptyHeading`, `poolEmptyBody`, `claimLostToast`, `claimFailedToast`, `myRunTitle`, `runEmptyHeading`, `runEmptyBody`, `advanceToEnRouteCta`, `advanceToArrivedCta`, `advanceToPickedUpCta`, `advanceToCompletedCta`, `completedTodayTitle`, `advanceFailedToast`. Admin keys: `transfersTitle`, `filterByStatusLabel`, `needsAttentionFilterCta`, `transferSearchPlaceholder`, `transfersEmptyHeading`, `transfersEmptyBody`, `transfersNoMatchBody`, `needsAttentionBadge`, `assignDriverCta`, `reassignDriverCta`, `releaseTransferCta`, `cancelTransferCta`, `refundTransferCta`, `actionReasonLabel`, `refundAmountLabel`, `refundFeeDisclosure`, `cancelOfferRefundCta`, `cancelTransferConfirm`, `refundConfirm`, `reassignConfirm`, `releaseConfirm`. Do NOT add `saveFailed`/`fieldRequired` (reuse the existing keys). The `refundFeeDisclosure`, `refundConfirm` strings contain `{fee}`/`{amount}` interpolation tokens — keep the literal `{fee}`/`{amount}` placeholders in the string (the consuming component substitutes them). Per D-12/D-10/D-11 (fee disclosure, reason gate, cancel-never-auto-refund copy).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (proves EN/BG key parity under `Dict = typeof en`).
    - `grep -c "refundFeeDisclosure" platform/i18n/en.ts platform/i18n/bg.ts` returns 1 in each file.
    - All 14 driver keys + all admin keys above are present in both files (a `grep -v '^//' platform/i18n/bg.ts | grep -c claimTransferCta` returns 1).
    - The `{fee}` and `{amount}` placeholder tokens are present literally in `refundFeeDisclosure`/`refundConfirm`.
  </acceptance_criteria>
  <done>Both dictionaries carry every Phase-6 key with EN+BG parity; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Lifecycle release edge (claimed->paid) + pinned 8x8 test</name>
  <files>platform/transfers/lifecycle.ts, platform/transfers/lifecycle.test.ts</files>
  <read_first>
    - platform/transfers/lifecycle.ts (the ALLOWED_TRANSITIONS map + canTransition; the "cosmetic mirror of the DB trigger" contract)
    - platform/transfers/lifecycle.test.ts (the independently-authored EXPECTED map + the 8x8 cross-product loop that pins the two together)
    - supabase/migrations/0004_transfer_entity.sql (lines 100-114 — the trigger's allowed-map shape the TS map mirrors)
    - .planning/phases/06-driver-admin-views/06-CONTEXT.md D-14 (release restricted to status='claimed')
  </read_first>
  <action>
    In `lifecycle.ts`, change `claimed: ["en_route", "cancelled"]` to `claimed: ["en_route", "cancelled", "paid"]` — the new release backward edge (D-14). Update the header comment to note `claimed->paid` is the release edge (operational rewind, mirrored by the 0006 trigger). Leave all other edges untouched; do NOT add `en_route->paid` or any other backward edge (D-14 restricts release to `claimed` only). In `lifecycle.test.ts`, update the independently-authored `EXPECTED.claimed` set to `new Set(["en_route", "cancelled", "paid"])` so the 8x8 cross-product still passes, and add one spot assertion `expect(canTransition("claimed","paid")).toBe(true)` plus `expect(canTransition("en_route","paid")).toBe(false)` (release is claimed-only). Keep the same-state and terminal-state assertions unchanged. This map + the 0006 trigger (Task 3) must stay byte-for-byte consistent in their allowed-claimed set.
  </action>
  <verify>
    <automated>npx vitest run platform/transfers/lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run platform/transfers/lifecycle.test.ts` is GREEN (all 64 pairs + spot assertions).
    - `grep -A1 "claimed:" platform/transfers/lifecycle.ts | grep -c paid` returns >=1.
    - The new spot assertions `canTransition('claimed','paid')===true` and `canTransition('en_route','paid')===false` are present and pass.
    - No other state's allowed array gained a backward edge (only `claimed`).
  </acceptance_criteria>
  <done>The TS lifecycle map carries the single new `claimed->paid` release edge and the 8x8 pin-test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Author migration 0006 (release trigger edge + audit columns) — FLAGGED, authored-not-applied</name>
  <files>supabase/migrations/0006_release_and_audit.sql</files>
  <read_first>
    - supabase/migrations/0005_claim_correctness.sql (lines 1-9 — the FLAGGED/Balkanity-only/authored-not-applied header to copy verbatim; the Management-API apply note)
    - supabase/migrations/0004_transfer_entity.sql (lines 49-60 — the `add column if not exists` ALTER pattern; lines 87-127 — the `wp_enforce_transfer_transition()` function being AMENDED + its trigger)
    - .planning/phases/06-driver-admin-views/06-CONTEXT.md D-14 + D-15 (the exact deltas)
    - .planning/phases/06-driver-admin-views/06-PATTERNS.md "supabase/migrations/0006_*.sql" section (the two minimal deltas + locked conventions)
  </read_first>
  <action>
    Create `supabase/migrations/0006_release_and_audit.sql`. Copy the 0005 header banner verbatim, adjusting the object summary: FLAGGED/IRREVERSIBLE; Balkanity ref `qyhdogajtmnvxphrslwm` ONLY, NEVER Kalvia `utyatpadtibqqswsfvtr`; AUTHORED-NOT-APPLIED with the live apply deferred to Plan 05 via the Supabase Management API `/database/query` with `SUPABASE_ACCESS_TOKEN` (NOT MCP, NOT `supabase db push`, NOT direct-host psql DDL). Two deltas:
    (1) `create or replace function public.wp_enforce_transfer_transition()` — copy the entire 0004 function body and add ONE allowed pair to the map: `(old.status = 'claimed' and new.status in ('en_route', 'cancelled', 'paid'))` (the new `paid` is the release backward edge, D-14; restricted to `claimed` only — do NOT add `en_route->paid`). Keep the no-op early-return and the `check_violation` raise unchanged. Update the function comment to note the release edge. Re-create the `wp_transfers_transition_guard` trigger with `drop trigger if exists` first (re-runnable).
    (2) `alter table public.wp_transfers add column if not exists last_action_reason text;` + `last_action_by uuid` + `last_action_at timestamptz` — all NULL-able, no default (0004 ALTER pattern). Add a `comment on column` for `last_action_reason` noting it stores the D-10 admin audit reason for cancel/refund/reassign/release (distinct from the Stripe `reason` enum). Every statement idempotent (`create or replace` / `add column if not exists` / `drop ... if exists`). Do NOT add any INSERT/UPDATE/DELETE RLS policy — the no-write-policy lock holds; writes stay service-role-only.
  </action>
  <verify>
    <automated>grep -c "claimed' and new.status in ('en_route', 'cancelled', 'paid')" supabase/migrations/0006_release_and_audit.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0006_release_and_audit.sql` exists.
    - `grep -c "qyhdogajtmnvxphrslwm" supabase/migrations/0006_release_and_audit.sql` >= 1 AND `grep -c "utyatpadtibqqswsfvtr" ...` >= 1 (both refs named in the guardrail banner).
    - `grep -c "add column if not exists last_action_reason text" supabase/migrations/0006_release_and_audit.sql` returns 1; same for `last_action_by uuid` and `last_action_at timestamptz`.
    - The amended allowed-map line includes `'paid'` ONLY on the `claimed` row (grep confirms no `en_route' and new.status in (...'paid'` line).
    - Header contains "AUTHORED" + "NOT APPLIED" (or equivalent) and the Management-API note — NO `db push` / MCP apply runs in this task.
    - `grep -v '^--' supabase/migrations/0006_release_and_audit.sql | grep -c "create policy"` returns 0 (no new write policy).
  </acceptance_criteria>
  <done>Migration 0006 is authored (file only), idempotent, Balkanity-guarded, encoding exactly the claimed->paid trigger edge + the three audit columns; no live apply performed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave-0 Nyquist RED specs (7 specs) + single-writer contract update</name>
  <files>platform/transfers/pool.masking.test.ts, app/driver/advance.ownership.test.ts, app/driver/advance.lifecycle.test.ts, app/driver/run/RunView.test.tsx, app/admin/transfers/actions.test.ts, app/admin/transfers/TransfersView.test.tsx, platform/payments/refund.test.ts, platform/payments/single-writer.test.ts</files>
  <read_first>
    - .planning/phases/06-driver-admin-views/06-RESEARCH.md "Validation Architecture" (the Req->Test map + Wave 0 gaps — the exact files + behaviors to assert; CLAIM-06 -> RunView.test.tsx, OPS-01 -> TransfersView.test.tsx)
    - platform/payments/single-writer.test.ts (the existing grep-gate to widen for the release writer)
    - platform/rls/claim-schema.test.ts (the readFileSync + comment-strip source-contract pattern to mirror for new source-level gates)
    - tests/claim/fixtures.ts (the driver/admin JWT caller-auth fixtures to reuse for ownership/lifecycle gates)
    - platform/transfers/lifecycle.ts (ALLOWED_TRANSITIONS — the advance.lifecycle gate asserts the resolved next-edge)
    - .planning/phases/06-driver-admin-views/06-CONTEXT.md D-06 (run ordered by arrival; completed drops to "Completed today"), D-07/D-08/D-09 (admin sort/controls/stuck definition), D-12 (refund full/partial + fee disclosure) and 06-RESEARCH.md Pattern 3 (admin list query) + Pattern 5 (refund hook shape)
  </read_first>
  <behavior>
    - pool.masking.test.ts: a `wp_pool()` result row's keys contain ZERO of {guest_name, guest_email, guest_phone, address, notes}; `flight_no` is PRESENT (operational, non-PII). RED until Plan 02 wires the consuming pool read (assert against the masked-column contract; reuse the Phase-5 PII_KEYS set). May be source-level (assert PoolView selects only `wp_pool` columns) OR live-env-gated like the Phase-5 gates.
    - advance.ownership.test.ts: `advanceStatus(transferId)` called for a transfer whose `driver_id !== caller uid` returns an error and writes nothing (CLAIM-04 ownership gate). RED until Plan 03 implements `advanceStatus`.
    - advance.lifecycle.test.ts: `advanceStatus` only ever resolves a `canTransition`-legal forward next-edge via `ALLOWED_TRANSITIONS`; an illegal target is never written (CLAIM-05). RED until Plan 03.
    - run/RunView.test.tsx: (CLAIM-06) given a fixture of active claimed rows, `<RunView>` renders the active-run cards ordered by `arrival_at` ASCENDING (soonest first — assert the rendered card order matches the arrival-sorted ids); a row with `status='completed'` does NOT appear in the active run and instead appears under the "Completed today" (`completedTodayTitle`) collapsed section. Assert the active list excludes completed and the Completed-today section includes it. RED until Plan 03 Task 2 builds `RunView.tsx`. Component spec (jsdom render) — mirror the Phase-1 component-test harness; if the island cannot render in isolation yet, source-level-assert the ordering+partition contract (RunView orders by arrival_at and partitions completed) so it is RED-by-absence, never false-pass.
    - actions.test.ts: each admin action (`assign`/`reassign`/`release`/`cancel`) re-gates `getCurrentRole()==='admin'`; a non-admin caller is rejected; `release` targets `claimed->paid`+clears driver_id; `cancel` is trigger-legal. RED until Plan 05.
    - admin/transfers/TransfersView.test.tsx: (OPS-01) given a fixture of mixed-status transfer rows, `<TransfersView>` (a) narrows the rendered rows when a status filter is applied (only the selected statuses remain); (b) free-text search matches across guest name / flight no. / destination (a query substring present in any of the three keeps the row, a non-matching query empties it to the `transfersNoMatchBody` state); (c) stuck/unclaimed rows are pinned to the TOP of the list and carry the coral `needsAttentionBadge` TEXT marker (assert the badge text is rendered AND the needs-attention rows precede the rest — per D-07 pin, D-08 controls, D-09 stuck definition). RED until Plan 04 Task 1 builds `TransfersView.tsx`. Component spec (jsdom render); if the island cannot render in isolation yet, source-level-assert the filter/search/coral-pinning contract so it is RED-by-absence, never false-pass.
    - refund.test.ts: `refundPayment` calls `stripe.refunds.create` with `payment_intent` + an `idempotencyKey`; omitting `amountCents` yields a full refund; it NEVER sets `status='paid'` (OPS-04, D-12, Pitfall 3). RED until Plan 05.
    - single-writer.test.ts: widen the grep gate so EXACTLY two production files may write `status:'paid'` — `app/api/stripe/webhook/route.ts` AND `app/admin/transfers/actions.ts` (the narrow gated release action) — and assert no third writer. This is the ONE deliberate widening (D-15); document why in the test header.
  </behavior>
  <action>
    Create the seven RED spec files mirroring the Phase-5 Nyquist baseline discipline (`platform/rls/claim-schema.test.ts` for source-level gates; `tests/claim/*.gate.test.ts` for live-env-gated gates — `describe.skip` when the live TEST-DB env is absent, never false-pass; the Phase-1 jsdom component-test harness for the two `*View` component specs). Each spec asserts the behavior above and is RED by design (the implementation lands in Plans 02/03/04/05). Use the shared driver/admin JWT fixtures from `tests/claim/fixtures.ts` where a caller identity is needed.
    For `app/driver/run/RunView.test.tsx` (CLAIM-06): seed a small fixture of claimed rows with deliberately out-of-order `arrival_at` values plus one `completed` row, render `<RunView>` (or, if it cannot yet render standalone, source-assert via the readFileSync+strip pattern that the component orders by `arrival_at` ascending and partitions `completed` into the `completedTodayTitle` section). Assert (i) active-card order equals the arrival-sorted id order, (ii) the `completed` row is absent from the active list and present under "Completed today".
    For `app/admin/transfers/TransfersView.test.tsx` (OPS-01): seed a fixture of mixed-status rows including an unclaimed `paid` row (needs-attention) and a claimed row; render `<TransfersView>` (or source-assert the filter/search/pin contract if standalone render is not yet possible). Assert (i) applying a status filter narrows the rendered rows, (ii) a free-text query matching guest name OR flight no. OR destination keeps the matching row and a non-matching query yields the `transfersNoMatchBody` empty state, (iii) the needs-attention/unclaimed rows are pinned first and render the coral `needsAttentionBadge` text marker (never colour alone).
    For `single-writer.test.ts`, update the assertion from "exactly one" to "exactly these two known writers" — add `app/admin/transfers/actions.ts` to an allowlist set, assert the writer set is a subset of `{webhook route, admin transfers actions}` (not "must equal", so it stays GREEN before Plan 05 wires the release action — see the acceptance note), and keep the header explanation of the single deliberate exception (D-15: the release edge, written by a gated service-role action behind `getCurrentRole()` — never a client). Do NOT implement any production code in this task — specs + the contract widening only.
  </action>
  <verify>
    <automated>npx vitest run platform/transfers/pool.masking.test.ts app/driver/advance.ownership.test.ts app/driver/advance.lifecycle.test.ts app/driver/run/RunView.test.tsx app/admin/transfers/actions.test.ts app/admin/transfers/TransfersView.test.tsx platform/payments/refund.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven new spec files exist and either FAIL (RED — implementation absent) or skip cleanly via the live-env gate (never false-pass).
    - `app/driver/run/RunView.test.tsx` exists and asserts arrival_at-ascending active-card ordering AND the completed-drops-to-"Completed today" partition (`grep -c "arrival_at" app/driver/run/RunView.test.tsx` >= 1; `grep -c "completedTodayTitle\|Completed today" app/driver/run/RunView.test.tsx` >= 1).
    - `app/admin/transfers/TransfersView.test.tsx` exists and asserts status-filter narrowing + name/flight/destination search + coral needs-attention pinning (`grep -c "needsAttention" app/admin/transfers/TransfersView.test.tsx` >= 1; `grep -c "needsAttentionBadge" app/admin/transfers/TransfersView.test.tsx` >= 1).
    - `single-writer.test.ts` references both `app/api/stripe/webhook/route.ts` and `app/admin/transfers/actions.ts` as the allowed `paid` writers and forbids a third (`grep -c "admin/transfers/actions" platform/payments/single-writer.test.ts` >= 1).
    - At this plan's commit (before Plan 05 wires the release action) `npx vitest run platform/payments/single-writer.test.ts` MUST still reflect reality: it allows the two known writers; with only the webhook present today it passes for the present writer and does not falsely require the not-yet-built release action (encode the allowlist as "writers ⊆ {webhook, admin actions}", not "must equal").
    - No production source file (non-test) is created in this task.
  </acceptance_criteria>
  <done>Seven Wave-0 RED specs encode the CLAIM-01/04/05/06 + OPS-01/03/04 contracts; the single-writer gate is widened to the two known `paid` writers with the release exception documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→migration file | The 0006 DDL is authored here but crosses into the LOCKED Phase-5 data layer (the lifecycle trigger). Untrusted until human sign-off + the gated live apply (Plan 05). |
| test fixtures→live TEST DB | Wave-0 live-env-gated specs may seed a TEST DB; service-role is confined to seed/teardown (reuse Phase-5 two-identity split). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | 0006 trigger edge `claimed->paid` | mitigate | Restricted to `claimed` ONLY (no `en_route->paid`); authored-not-applied; sign-off gate before live apply (Plan 05). The map + trigger pinned by the 8x8 test. |
| T-06-02 | Tampering / money integrity | single-writer contract | mitigate | Gate widened to EXACTLY {webhook, gated release action}; any third `status='paid'` writer fails the contract. Release is a service-role action behind `getCurrentRole()` — never a client. |
| T-06-03 | Spoofing | migration target ref | mitigate | Balkanity-only guardrail banner names both refs; live apply (Plan 05) re-asserts the ref before DDL. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new deps). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `npm run typecheck` clean (EN/BG parity).
- `npx vitest run platform/transfers/lifecycle.test.ts` GREEN (release edge pinned).
- `npx vitest run platform/payments/single-writer.test.ts` reflects the two-writer allowlist.
- The seven Wave-0 specs are RED or skip-clean (never false-pass) — including `run/RunView.test.tsx` (CLAIM-06) and `admin/transfers/TransfersView.test.tsx` (OPS-01).
- Migration 0006 authored, Balkanity-guarded, idempotent, NOT applied.
</verification>

<success_criteria>
- Dictionary keys for all five Phase-6 surfaces exist with EN+BG parity.
- `ALLOWED_TRANSITIONS.claimed` includes `paid`; the 8x8 pin-test is GREEN.
- `0006_release_and_audit.sql` is authored (file only) with the trigger edge + three audit columns.
- Wave-0 RED specs pin the CLAIM-01/04/05/06 + OPS-01/03/04 contracts (seven files).
- single-writer gate widened to the two known `paid` writers (webhook + release action).
</success_criteria>

<output>
Create `.planning/phases/06-driver-admin-views/06-01-SUMMARY.md` when done.
</output>
