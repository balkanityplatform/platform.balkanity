---
phase: 06-driver-admin-views
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/admin/transfers/page.tsx
  - app/admin/transfers/TransfersView.tsx
  - app/admin/transfers/[id]/page.tsx
  - app/admin/transfers/[id]/TransferDetailView.tsx
  - app/admin/page.tsx
autonomous: true
requirements: [OPS-01, OPS-02]
user_setup: []

must_haves:
  truths:
    - "Admin opens /admin/transfers and sees all transfers, default-sorted soonest-arrival-first, with needs-attention rows pinned to the top in coral (with a text marker)"
    - "Admin can filter by status, one-tap a needs-attention quick filter, and free-text search across guest name / flight no. / destination"
    - "Stuck = unclaimed always coral, plus near-arrival-unclaimed and arrived-stalled coral via simple pilot constants (UI highlight only, no alerts)"
    - "Admin opens a transfer detail page showing the lifecycle timeline + trip/payment details"
    - "The admin console nav links to Transfers"
  artifacts:
    - path: "app/admin/transfers/page.tsx"
      provides: "Transfers list RSC — admin guard + filter/search/sort + needsAttention compute"
      contains: "arrival_at"
    - path: "app/admin/transfers/TransfersView.tsx"
      provides: "Slate-console list island — filter chips, search box, coral pinning + badge"
      min_lines: 50
    - path: "app/admin/transfers/[id]/page.tsx"
      provides: "Transfer detail RSC — single unmasked row + joined destination"
    - path: "app/admin/transfers/[id]/TransferDetailView.tsx"
      provides: "Detail island — LifecycleTimeline + trip/payment facts (actions wired in Plan 05)"
  key_links:
    - from: "app/admin/transfers/page.tsx"
      to: "wp_transfers_admin_read"
      via: "admin reads all rows on the anon cookie-bound client (RLS exercised)"
      pattern: "from..wp_transfers"
    - from: "app/admin/page.tsx"
      to: "/admin/transfers"
      via: "console nav link"
      pattern: "/admin/transfers"
    - from: "app/admin/transfers/[id]/TransferDetailView.tsx"
      to: "platform/ui/LifecycleTimeline"
      via: "detail renders the lifecycle timeline"
      pattern: "LifecycleTimeline"
---

<objective>
Build the admin transfers list + detail vertical slice: `/admin/transfers` (admin-guarded, slate console) reads all transfers via the admin RLS, applies status filter + a needs-attention quick filter + free-text search (name/flight/destination), computes the coral "needs attention" highlight per row, and renders the list default-sorted soonest-arrival-first with coral rows pinned to top (each carrying a text badge — never colour alone). `/admin/transfers/[id]` opens the detail with the lifecycle timeline + trip/payment facts. The console nav gains a Transfers link. After this slice an admin can find, triage, and open any transfer.

Purpose: OPS-01 (list with filter + search) and OPS-02 (detail page) land here. This is read-only triage — the four ops actions + refund are Plan 05, hung on this detail page. The slice mirrors the four existing admin console slices exactly (server-guard → island), reading UNMASKED rows via `wp_transfers_admin_read` (distinct from the driver's masked pool).

Output: `app/admin/transfers/page.tsx`, `TransfersView.tsx`, `[id]/page.tsx`, `[id]/TransferDetailView.tsx`, and a Transfers link added to `app/admin/page.tsx`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-driver-admin-views/06-CONTEXT.md
@.planning/phases/06-driver-admin-views/06-UI-SPEC.md
@.planning/phases/06-driver-admin-views/06-PATTERNS.md
@app/admin/drivers/page.tsx
@app/admin/drivers/DriversView.tsx
@app/admin/page.tsx
@platform/ui/StatusDot.tsx
@platform/ui/LifecycleTimeline.tsx
@platform/ui/DataList.tsx
</context>

<artifacts_this_phase_produces>
- **`app/admin/transfers/page.tsx` (NEW)** — list RSC; `getCurrentRole()==='admin'` guard; reads all rows via `wp_transfers_admin_read` on the anon cookie-bound client with status filter + name/flight search + joined destination; computes `needsAttention` per row; sorts coral-pinned-then-arrival.
- **`app/admin/transfers/TransfersView.tsx` (NEW)** — slate-console island: status filter chips, needs-attention quick filter, search box, per-row `StatusDot` + coral `needsAttentionBadge` text marker, rows linking to detail.
- **`app/admin/transfers/[id]/page.tsx` (NEW)** — detail RSC; reads the single unmasked row + joined destination.
- **`app/admin/transfers/[id]/TransferDetailView.tsx` (NEW)** — detail island: `LifecycleTimeline` + trip/payment facts. Action controls are added in Plan 05.
- **`app/admin/page.tsx` (MODIFIED)** — adds `{ href: "/admin/transfers", label: t.transfersTitle }` to the console nav.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Transfers list RSC + filter/search/sort + coral pinning island + console nav</name>
  <files>app/admin/transfers/page.tsx, app/admin/transfers/TransfersView.tsx, app/admin/page.tsx</files>
  <read_first>
    - app/admin/drivers/page.tsx (the EXACT server-guard → dict prop bag → island pattern; anon cookie-bound read so admin RLS is exercised — NOT service-role)
    - app/admin/drivers/DriversView.tsx (the slate console chrome + `<ul>` divide row pattern to mirror)
    - .planning/phases/06-driver-admin-views/06-PATTERNS.md "Pattern 3" / "app/admin/transfers/page.tsx" + "TransfersView.tsx" (the read shape with `.in(status)` filter, `.or(ilike)` search, in-RSC needsAttention compute + coral stable-sort)
    - platform/ui/StatusDot.tsx (dot+label per row) + platform/ui/DataList.tsx (the row primitive shape)
    - .planning/phases/06-driver-admin-views/06-UI-SPEC.md (admin copy keys; coral needs-attention rows MUST carry a text marker `needsAttentionBadge`; D-07 sort, D-08 controls, D-09 stuck definition)
    - app/admin/page.tsx (the console nav array to append the Transfers link to)
    - .planning/phases/06-driver-admin-views/06-CONTEXT.md D-09 (stuck = unclaimed always + near-arrival-unclaimed + arrived-stalled; thresholds = simple pilot constants, planner discretion)
  </read_first>
  <action>
    Create `app/admin/transfers/page.tsx`: admin-guarded RSC (`getCurrentRole() !== "admin"` → redirect). Read on the anon cookie-bound `createClient()` (admin RLS via `wp_transfers_admin_read` is the data gate — NOT service-role): `.from("wp_transfers").select("id,status,arrival_at,guest_name,flight_no,driver_id,amount_cents, destinations(zone,airport,address)").order("arrival_at", { ascending: true })`. Accept `searchParams` for the status filter, the needs-attention quick filter, and the free-text search; apply the status filter as `.in("status", [...])` and the name/flight search as `.or("guest_name.ilike.%...%,flight_no.ilike.%...%")` at the query layer; apply destination-name search in-RSC after the read (pilot volume — RESEARCH A3). Compute `needsAttention` per row in the RSC with simple constants: unclaimed (`status='paid' && driver_id is null`) is ALWAYS true; additionally true when (a) unclaimed AND `arrival_at` within N hours (pick a constant, e.g. 6h), or (b) `status='arrived'` and not advanced past a stall window (pick a constant, e.g. 2h since arrival_at). Stable-sort so `needsAttention` rows are pinned to the top, then soonest arrival (D-07). Pass rows (with the per-row `needsAttention` flag), `lang`, and the dict prop bag to `<TransfersView>`.
    Create `app/admin/transfers/TransfersView.tsx` (`"use client"`): slate console chrome (mirror DriversView). Render a status filter (chips or a `Select`), a one-tap `needsAttentionFilterCta` quick filter, and a search box (`transferSearchPlaceholder`) — each updates the URL searchParams (router) to re-query server-side. Render rows as a `<ul>` divide list (each: guest_name or a neutral label, destination zone/airport, arrival_at, fare, a `StatusDot state={row.status}`); a `needsAttention` row gets the coral `needsAttentionBadge` TEXT marker (never coral background alone — WCAG 1.4.1) and is visually pinned (the RSC already ordered them first). Each row links to `/admin/transfers/<id>`. Empty states: `transfersEmptyHeading`/`transfersEmptyBody` for no rows, `transfersNoMatchBody` for a filtered-empty result. Hit targets ≥44px.
    In `app/admin/page.tsx`, add `{ href: "/admin/transfers", label: t.transfersTitle }` to the console nav array (alongside companies/properties/destinations/drivers).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run app/admin/transfers/TransfersView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The list reads on the anon cookie-bound client, NOT service-role (`grep -c "createAdminClient" app/admin/transfers/page.tsx` == 0; `grep -c "createClient" app/admin/transfers/page.tsx` >= 1) and orders by `arrival_at` (`grep -c 'order("arrival_at"' app/admin/transfers/page.tsx` >= 1).
    - `needsAttention` is computed in the RSC and coral rows are pinned first (`grep -ci "needsAttention" app/admin/transfers/page.tsx` >= 1).
    - The status filter uses `.in("status"` and the search uses `.or(` ilike (`grep -c ".or(" app/admin/transfers/page.tsx` >= 1).
    - `TransfersView.tsx` renders a `StatusDot` per row and the coral `needsAttentionBadge` text marker (`grep -c "needsAttentionBadge" app/admin/transfers/TransfersView.tsx` >= 1; `grep -c "StatusDot" app/admin/transfers/TransfersView.tsx` >= 1).
    - `app/admin/page.tsx` links to `/admin/transfers` (`grep -c "/admin/transfers" app/admin/page.tsx` >= 1).
    - The Plan-01 `TransfersView.test.tsx` gate reflects the filter/search/coral-pinning behavior (GREEN or wired).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>The admin transfers list reads unmasked rows via admin RLS, filters/searches/sorts with coral needs-attention pinned-and-badged, and the console links to it.</done>
</task>

<task type="auto">
  <name>Task 2: Transfer detail page (lifecycle + trip/payment facts)</name>
  <files>app/admin/transfers/[id]/page.tsx, app/admin/transfers/[id]/TransferDetailView.tsx</files>
  <read_first>
    - app/admin/drivers/page.tsx (the server-guard read-single pattern)
    - platform/ui/LifecycleTimeline.tsx (the timeline rendered from `current={row.status}`)
    - platform/ui/StatusDot.tsx (status display)
    - .planning/phases/06-driver-admin-views/06-UI-SPEC.md (admin detail layout is planner discretion; slate chrome; trip/payment facts; the action CTAs `assignDriverCta`/`reassignDriverCta`/`releaseTransferCta`/`cancelTransferCta`/`refundTransferCta` — render their LABELS only here, the wiring is Plan 05)
    - .planning/phases/06-driver-admin-views/06-CONTEXT.md D-02 (detail shows lifecycle + trip/payment)
  </read_first>
  <action>
    Create `app/admin/transfers/[id]/page.tsx`: admin-guarded RSC. Read the single unmasked row by id on the anon cookie-bound client (`wp_transfers_admin_read` gives the admin the full row) + the joined destination: `.from("wp_transfers").select("*, destinations(zone,airport,address)").eq("id", id).single()`. Resolve dict + lang. Pass the row + `lang` + the dict prop bag to `<TransferDetailView>`. If no row, render a neutral not-found state.
    Create `app/admin/transfers/[id]/TransferDetailView.tsx` (`"use client"`): slate console chrome. Render `LifecycleTimeline current={row.status}`, the trip facts (guest name/phone, arrival_at, airport, zone, exact address from the join, flight_no, pax, luggage, notes), and the payment facts (fare from amount_cents, paid_at, the recorded fee, stripe payment intent reference). Lay out the FIVE action control placeholders (assign/reassign/release/cancel/refund) using their dict labels — render the buttons/section but leave the onClick/action wiring to Plan 05 (this task is read-only detail; do not import the actions file). Destructive labels use the coral token. ≥44px hit targets, 52px primary CTAs. NetworkFirst data (status detail must never be stale-served — Pitfall 4).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/transfers/[id]/page.tsx` reads the single row on the anon cookie-bound client (`grep -c "createAdminClient" app/admin/transfers/[id]/page.tsx` == 0) joined to destination, and is admin-guarded (`grep -c "getCurrentRole" app/admin/transfers/[id]/page.tsx` >= 1).
    - `TransferDetailView.tsx` renders `LifecycleTimeline` (`grep -c "LifecycleTimeline" app/admin/transfers/[id]/TransferDetailView.tsx` >= 1) and shows trip + payment facts.
    - The five action labels are present as placeholders (`grep -c "refundTransferCta\|cancelTransferCta\|releaseTransferCta\|reassignDriverCta\|assignDriverCta" app/admin/transfers/[id]/TransferDetailView.tsx` >= 1) but no action import exists yet (`grep -c "from \"./actions\"\|from \"../actions\"" app/admin/transfers/[id]/TransferDetailView.tsx` == 0).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>The transfer detail page shows the lifecycle timeline + trip/payment facts with action placeholders, ready for Plan 05 to wire the ops actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser→/admin/transfers RSC | Untrusted; role re-gated server-side via getCurrentRole(); reads via admin RLS, never service-role into the UI. |
| searchParams→server query | Filter/search params are untrusted input; applied via parameterized PostgREST filters, not string-built SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-AC1 | Elevation of Privilege | list/detail pages | mitigate | `getCurrentRole()==='admin'` server-side guard before any read; non-admin → redirect. |
| T-06-INJ | Tampering | search/filter input | mitigate | Status filter + ilike search applied via PostgREST `.in`/`.or` (parameterized); no string-concatenated SQL; admin RLS scopes the read. |
| T-06-COLOR | (a11y) Information Disclosure | coral rows | mitigate | needs-attention rows always carry the `needsAttentionBadge` text marker (WCAG 1.4.1) — never colour alone. |
| T-06-STALE | Integrity | detail data | mitigate | Status/detail data NetworkFirst, never stale-served from SW cache (Pitfall 4). |
| T-06-SC | Tampering | npm installs | accept | No package installs (zero new deps). |
</threat_model>

<verification>
- `npm run typecheck` clean.
- List + detail read on the anon cookie-bound client (admin RLS); no service-role read into the UI.
- Filter/search/sort + coral pinning + text badge present.
- Console nav links to /admin/transfers.
- `TransfersView.test.tsx` reflects the behavior.
</verification>

<success_criteria>
- Admin sees all transfers, default-sorted soonest-arrival, coral needs-attention pinned + badged.
- Status filter, needs-attention quick filter, and name/flight/destination search work.
- Detail page shows lifecycle + trip/payment facts.
- Console nav links to Transfers.
</success_criteria>

<output>
Create `.planning/phases/06-driver-admin-views/06-04-SUMMARY.md` when done.
</output>
