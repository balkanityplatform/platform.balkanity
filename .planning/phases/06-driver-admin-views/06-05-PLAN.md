---
phase: 06-driver-admin-views
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-04"]
files_modified:
  - platform/payments/refund.ts
  - app/admin/transfers/actions.ts
  - app/admin/transfers/[id]/TransferDetailView.tsx
  - app/admin/transfers/[id]/RefundForm.tsx
  - .planning/phases/06-driver-admin-views/06-MIGRATION-EVIDENCE.md
autonomous: false
requirements: [OPS-03, OPS-04, CLAIM-04]
user_setup: []

must_haves:
  truths:
    - "Migration 0006 is applied LIVE to Balkanity (qyhdogajtmnvxphrslwm) after sign-off: the claimed->paid release edge and last_action_reason/by/at columns exist on the live DB"
    - "Admin can assign (one-tap), reassign, release, and cancel a transfer; reassign/release/cancel require a confirm dialog + reason note; assign is one-tap with no reason"
    - "Release returns a claimed transfer to the pool (driver_id=null, status=paid) and is restricted to status='claimed'; the released row reappears in wp_pool()"
    - "Admin can issue a manual full-or-partial Stripe refund from the detail page; the fee-not-recovered disclosure is ALWAYS shown; the refund hook is server-only, idempotency-keyed, and never sets paid"
    - "Cancel never auto-refunds — it only offers a refund shortcut; no Phase-6 path other than release writes status='paid'"
  artifacts:
    - path: "platform/payments/refund.ts"
      provides: "Server-only refundPayment hook (stripe.refunds.create + idempotencyKey)"
      contains: "server-only"
    - path: "app/admin/transfers/actions.ts"
      provides: "assign/reassign/release/cancel/refund gated service-role actions"
      contains: "getCurrentRole"
    - path: "app/admin/transfers/[id]/RefundForm.tsx"
      provides: "Refund amount + reason form with fee disclosure"
    - path: ".planning/phases/06-driver-admin-views/06-MIGRATION-EVIDENCE.md"
      provides: "Sign-off + live-apply record for migration 0006"
  key_links:
    - from: "app/admin/transfers/actions.ts"
      to: "platform/payments/refund.ts"
      via: "refund action calls refundPayment"
      pattern: "refundPayment"
    - from: "app/admin/transfers/[id]/TransferDetailView.tsx"
      to: "app/admin/transfers/actions.ts"
      via: "action buttons + confirm dialogs invoke the actions"
      pattern: "from \"../actions\""
    - from: "app/admin/transfers/actions.ts"
      to: "last_action_reason"
      via: "cancel/refund/reassign/release persist the audit reason"
      pattern: "last_action_reason"
---

<objective>
Close Phase 6 with the admin ops + refund vertical slice AND the [BLOCKING] live migration apply: apply the FLAGGED migration `0006` to Balkanity after sign-off (the `claimed->paid` release edge + `last_action_*` audit columns), build the five gated admin Server Actions (assign one-tap; reassign/release/cancel confirm+reason; refund full/partial), build the server-only refund hook, and wire the actions + confirm dialogs + refund form (with the always-on fee-not-recovered disclosure) into the detail page from Plan 04. After this slice an admin can assign/reassign/release/cancel and issue a manual Stripe refund.

Purpose: OPS-03 (assign/reassign/release/cancel) and OPS-04 (manual refund) land here, plus the CLAIM-04 admin-only release/reassign side of "drivers cannot un-claim." This is the heaviest, most security-sensitive plan: it touches the LOCKED data layer (the live migration apply is sign-off-gated → `autonomous: false`), introduces the narrow gated `paid` writer (release, D-15), and moves real money (refund). The single-`paid`-writer contract (widened in Plan 01) and the migration-0004 trigger are the backstops.

Output: `platform/payments/refund.ts`, `app/admin/transfers/actions.ts`, `app/admin/transfers/[id]/RefundForm.tsx`, the wired `TransferDetailView.tsx`, the live-applied migration 0006, and `06-MIGRATION-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-driver-admin-views/06-CONTEXT.md
@.planning/phases/06-driver-admin-views/06-RESEARCH.md
@.planning/phases/06-driver-admin-views/06-UI-SPEC.md
@.planning/phases/06-driver-admin-views/06-PATTERNS.md
@supabase/migrations/0006_release_and_audit.sql
@app/admin/companies/actions.ts
@platform/payments/stripe.ts
@platform/payments/fee.ts
@.planning/phases/05-claim-correctness/05-03-SUMMARY.md
@.planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
- **`platform/payments/refund.ts` (NEW)** — server-only `refundPayment({paymentIntentId, amountCents?, reason?, idempotencyKey})` → `stripe.refunds.create` (full when amount omitted; idempotency-keyed; never sets `paid`).
- **`app/admin/transfers/actions.ts` (NEW)** — `assign` (one-tap), `reassign`/`release`/`cancel` (confirm + required reason), `refund` (full/partial) — all gated service-role actions behind `getCurrentRole()==='admin'`; cancel/refund/reassign/release persist `last_action_reason`/`last_action_by`/`last_action_at`; `release` writes `driver_id=null, status='paid'` (the narrow gated `paid` writer, D-15) restricted to `status='claimed'`.
- **`app/admin/transfers/[id]/RefundForm.tsx` (NEW)** — amount (pre-filled full, editable down) + reason + the always-shown `refundFeeDisclosure` (fee from `recordedFeeCents`).
- **`app/admin/transfers/[id]/TransferDetailView.tsx` (MODIFIED)** — wires the five actions, the confirm dialogs (D-10), the cancel→offer-refund shortcut (D-11), and the RefundForm.
- **Live migration 0006 on Balkanity** + **`06-MIGRATION-EVIDENCE.md`** (sign-off + apply record).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-only refund hook (stripe.refunds.create + idempotency)</name>
  <files>platform/payments/refund.ts</files>
  <read_first>
    - platform/payments/stripe.ts (the `import "server-only"` first-line posture + `getStripe()` factory to reuse)
    - platform/payments/fee.ts (server-only, recorded-truth discipline — the disclosure figure source)
    - .planning/phases/06-driver-admin-views/06-RESEARCH.md "Pattern 5" + Code Example (the VERIFIED stripe@22.2.1 refunds.create signature: `{payment_intent, amount?, reason?}, {idempotencyKey}`)
    - .planning/phases/06-driver-admin-views/06-PATTERNS.md "platform/payments/refund.ts" (the exact hook shape)
    - platform/payments/refund.test.ts (the Plan-01 RED gate this turns GREEN)
    - .planning/phases/06-driver-admin-views/06-CONTEXT.md D-12 (full/partial; never client-side; never sets paid)
  </read_first>
  <behavior>
    - refundPayment with no `amountCents` → calls `stripe.refunds.create` with `payment_intent` and NO `amount` (full refund).
    - refundPayment with `amountCents` → passes `amount` (partial).
    - Always passes the supplied `idempotencyKey` as a request option (double-submit/retry never double-refunds — Pitfall 3).
    - `reason` defaults to `"requested_by_customer"` (the Stripe enum — distinct from the D-10 free-text audit reason).
    - The module NEVER writes `status='paid'` and is server-only (a client import fails the build).
    - Stripe TEST-MODE refund smoke (resolves RESEARCH Open Q3): using a Stripe **test** secret key, `refundPayment` against a test PaymentIntent returns a refund with `status: 'succeeded'` for both a full refund (no `amountCents`) and a partial refund (`amountCents` < paid); a second call with the SAME `idempotencyKey` does NOT issue a second refund. This MUST pass BEFORE the live migration apply (Task 2) and any real-money refund (Task 3) — it is the pre-real-money gate.
  </behavior>
  <action>
    Create `platform/payments/refund.ts` with `import "server-only"` as the FIRST line. Export `refundPayment(opts: { paymentIntentId: string; amountCents?: number; reason?: "requested_by_customer"; idempotencyKey: string })` returning `getStripe().refunds.create({ payment_intent: opts.paymentIntentId, amount: opts.amountCents, reason: opts.reason ?? "requested_by_customer" }, { idempotencyKey: opts.idempotencyKey })`. Add NO DB write here (the action records audit/never sets paid). Keep the Stripe `reason` enum strictly separate from the free-text audit reason (RESEARCH A4). Mirror the `getStripe`-mock surface so the Plan-01 unit test can stub it.
    Add a Stripe TEST-MODE refund smoke (RESEARCH Open Q3 — the pre-real-money gate): a live-key-gated test that, when a Stripe **test** secret key is present in the env, creates a test PaymentIntent (or reuses a seeded test charge) and asserts `refundPayment` returns `status: 'succeeded'` for a full and a partial refund, and that a re-call with the SAME `idempotencyKey` returns the same refund (no double-refund). `describe.skip` cleanly when the Stripe TEST env is absent (never false-pass), mirroring the Phase-5 live-env gate discipline. This smoke MUST be GREEN/skip-clean before Task 2 (the live migration apply) and Task 3 (real-money-capable refund wiring) proceed.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run platform/payments/refund.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - First line is `import "server-only";` (`head -1 platform/payments/refund.ts` is the server-only import).
    - Calls `refunds.create` with `payment_intent` + an `idempotencyKey` request option (`grep -c "idempotencyKey" platform/payments/refund.ts` >= 1; `grep -c "payment_intent" platform/payments/refund.ts` >= 1).
    - Omitting `amountCents` yields no `amount` coercion to 0 (passes `undefined` → full refund).
    - No `status` / `paid` write anywhere in the file (`grep -c "paid" platform/payments/refund.ts` == 0).
    - `platform/payments/refund.test.ts` (Plan-01 RED) is GREEN.
    - Stripe TEST-MODE refund smoke (RESEARCH Open Q3): with a Stripe **test** secret key present, a test-PaymentIntent refund returns `status: 'succeeded'` (full + partial) and the same `idempotencyKey` does not double-refund; it `describe.skip`s cleanly when the TEST env is absent (never false-pass). This passes/skip-cleans BEFORE Task 2's live migration apply and Task 3's real-money-capable wiring.
    - `single-writer.test.ts` stays consistent (refund hook adds no `paid` writer).
  </acceptance_criteria>
  <done>The server-only refund hook calls stripe.refunds.create with idempotency, supports full/partial, never sets paid, and a Stripe TEST-MODE refund smoke (succeeded full+partial, idempotency-safe) passes/skip-cleans as the pre-real-money gate (RESEARCH Open Q3) before the live apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Sign-off + apply migration 0006 LIVE to Balkanity</name>
  <files>supabase/migrations/0006_release_and_audit.sql, .planning/phases/06-driver-admin-views/06-MIGRATION-EVIDENCE.md</files>
  <action>
    Human sign-off gate (autonomous: false) before any live DDL. After approval, apply the authored `0006_release_and_audit.sql` LIVE to Balkanity (`qyhdogajtmnvxphrslwm`) via the Supabase Management API `POST /v1/projects/qyhdogajtmnvxphrslwm/database/query` with `SUPABASE_ACCESS_TOKEN`, in one `BEGIN … COMMIT` that ALSO inserts the `supabase_migrations.schema_migrations` history row for `0006` (mirror the 05-03 apply). Run a `curl` ref-guardrail FIRST asserting Balkanity is resolved and Kalvia (`utyatpadtibqqswsfvtr`) is absent — STOP if any tool reports Kalvia. Do NOT use the Supabase MCP tools (permission-denied for this project), NOT `supabase db push` against the direct `db.<ref>.supabase.co` host (IPv6-only/unreachable), NOT direct-host psql DDL. After apply, probe the live objects (the amended `wp_enforce_transfer_transition` permits `claimed->paid`; the three `last_action_*` columns exist on `wp_transfers`; NO new write policy was added — no-write-policy lock intact) and record the sign-off + ref guardrail output + apply response + post-apply object checks in `06-MIGRATION-EVIDENCE.md`, mirroring `05-GATES-EVIDENCE.md`. See `<what-built>` / `<how-to-verify>` for the full checkpoint narrative.
  </action>
  <verify>
    <human-check>06-MIGRATION-EVIDENCE.md records: Balkanity ref resolved + Kalvia absent; HTTP 2xx Management-API apply response; the live trigger amended to permit claimed->paid; the three last_action_* columns present on the live wp_transfers; no new write policy on wp_transfers.</human-check>
  </verify>
  <what-built>
    Migration `0006_release_and_audit.sql` was authored in Plan 01 (file only): it adds the trigger-legal `claimed->paid` release edge to `wp_enforce_transfer_transition()` (D-14, restricted to `status='claimed'`) and the `last_action_reason`/`last_action_by`/`last_action_at` audit columns to `wp_transfers` (D-15). This is a FLAGGED schema/RLS/trigger change to Phase 5's LOCKED data layer and requires human sign-off BEFORE the live apply. Build/type checks pass WITHOUT the apply (types come from config, not the live DB), so the apply is mandatory to avoid a false-positive verification of the release/audit paths.

    Apply path (PROJECT-SPECIFIC — do NOT deviate, per project memory + the 0005/05-03 apply record):
    - Target Balkanity ONLY, ref `qyhdogajtmnvxphrslwm`. NEVER Kalvia (`utyatpadtibqqswsfvtr`). Run a `curl` ref-guardrail against the Management API BEFORE the DDL to assert Balkanity is resolved and Kalvia is absent; if any tool reports Kalvia, STOP.
    - Apply via the Supabase Management API `POST /v1/projects/qyhdogajtmnvxphrslwm/database/query` with `SUPABASE_ACCESS_TOKEN`, in one `BEGIN … COMMIT` that ALSO inserts the `supabase_migrations.schema_migrations` history row for `0006` (mirror the 05-03 apply). NOT the Supabase MCP tools (permission-denied for this project), NOT `supabase db push` against the direct `db.<ref>.supabase.co` host (IPv6-only/unreachable), NOT direct-host psql DDL.
    - After apply, verify the live objects: the amended `wp_enforce_transfer_transition` allows `claimed->paid`; the three `last_action_*` columns exist on `wp_transfers`; NO new write policy was added (no-write-policy lock intact). Record everything in `.planning/phases/06-driver-admin-views/06-MIGRATION-EVIDENCE.md` (sign-off note + ref guardrail output + apply response + post-apply object checks), mirroring `05-GATES-EVIDENCE.md`.
  </what-built>
  <how-to-verify>
    0. PRE-GATE (RESEARCH Open Q3): confirm Task 1's Stripe TEST-MODE refund smoke passed/skip-cleaned (test-mode full+partial refund `succeeded` + idempotency-safe) BEFORE approving this live apply — the test-mode refund path must be proven before any real-money-capable migration lands.
    1. Review the authored `supabase/migrations/0006_release_and_audit.sql` — confirm it touches ONLY the `claimed->paid` edge (no other backward edge) and the three NULL-able audit columns; no INSERT/UPDATE/DELETE policy.
    2. Confirm this is acceptable as a change to the locked Phase-5 trigger.
    3. Approve the live apply to Balkanity (`qyhdogajtmnvxphrslwm`). The executor will run the ref guardrail, apply via the Management API, verify the live objects, and write `06-MIGRATION-EVIDENCE.md`.
    4. After apply, confirm `06-MIGRATION-EVIDENCE.md` shows: Balkanity ref resolved + Kalvia absent, `HTTP 2xx` apply response, the live trigger amended, the three columns present, no new write policy.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/06-driver-admin-views/06-MIGRATION-EVIDENCE.md` exists and records the sign-off, the ref guardrail (Balkanity resolved, Kalvia absent), the Management-API apply response, and the post-apply object checks.
    - The live `wp_enforce_transfer_transition` permits `claimed->paid` (evidenced by a probe in the evidence file) and the three `last_action_*` columns exist on the live `wp_transfers`.
    - No new write policy on `wp_transfers` (no-write-policy lock intact).
    - Applied to `qyhdogajtmnvxphrslwm` ONLY; Kalvia never targeted.
  </acceptance_criteria>
  <done>Migration 0006 is applied LIVE to Balkanity after sign-off; the claimed->paid release edge + last_action_* columns exist on the live DB; 06-MIGRATION-EVIDENCE.md records it; Kalvia never targeted.</done>
  <resume-signal>Type "approved" to apply migration 0006 to Balkanity, or describe concerns.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Admin ops actions (assign/reassign/release/cancel/refund) + wire detail view + refund form</name>
  <files>app/admin/transfers/actions.ts, app/admin/transfers/[id]/RefundForm.tsx, app/admin/transfers/[id]/TransferDetailView.tsx</files>
  <read_first>
    - app/admin/companies/actions.ts (the per-action getCurrentRole re-gate + zod + createAdminClient + revalidatePath skeleton each of the five exports follows)
    - .planning/phases/06-driver-admin-views/06-PATTERNS.md "app/admin/transfers/actions.ts" (assign/reassign clean; cancel trigger-legal; release the one non-clean op = claimed->paid + clear driver_id; refund wraps refund.ts + persists last_action_reason)
    - platform/payments/refund.ts (the hook from Task 1) + platform/payments/fee.ts (recordedFeeCents for the disclosure figure)
    - supabase/migrations/0006_release_and_audit.sql (the live claimed->paid edge release relies on; the last_action_* columns the audit writes target)
    - app/admin/transfers/[id]/TransferDetailView.tsx (the Plan-04 read-only detail with the five action placeholders to wire)
    - app/admin/drivers/InviteDriverForm.tsx (the useActionState form + role=status/role=alert feedback shape for RefundForm + confirm dialogs)
    - .planning/phases/06-driver-admin-views/06-UI-SPEC.md (the confirm copy strings; D-10 reason gate on cancel/refund/reassign/release; D-11 cancel offers refund, never auto; D-12 refund full/partial + always-shown disclosure)
    - app/admin/transfers/actions.test.ts (the Plan-01 RED gate this turns GREEN)
  </read_first>
  <behavior>
    - Every action re-gates `getCurrentRole()!=='admin'` → reject (service-role bypasses RLS, so this is THE authz gate).
    - assign(id, driverId): one-tap, sets driver_id (+ status paid->claimed if applicable), NO reason required.
    - reassign(id, driverId, reason): sets driver_id, persists last_action_reason/by/at; reason required.
    - release(id, reason): restricted to status='claimed'; clears driver_id and sets status='paid' (the row reappears in wp_pool); persists audit; reason required.
    - cancel(id, reason): trigger-legal ->cancelled; persists audit; reason required; NEVER auto-refunds.
    - refund(id, amount?, reason): resolves stripe_payment_intent_id from the row, calls refundPayment with a stable idempotencyKey, persists audit; NEVER writes status='paid'.
    - zod validates amount (positive int ≤ paid amount), reason (non-empty for the gated three), uuids; generic dictionary-keyed errors only.
  </behavior>
  <action>
    Create `app/admin/transfers/actions.ts` (`"use server"`) with five exports following the companies/actions.ts skeleton (per-action `getCurrentRole()==='admin'` re-gate, zod at the boundary, `createAdminClient()` write, `revalidatePath("/admin/transfers/"+id)`):
    - `assign`: one-tap; set `driver_id` (and, if the row is `paid`, the assign moves `paid->claimed` per the trigger); no reason.
    - `reassign`: set `driver_id`; persist `last_action_reason`/`last_action_by` (the acting admin's uid)/`last_action_at`; reason required.
    - `release`: GUARD `status==='claimed'` (D-14) — reject otherwise; `update({ driver_id: null, status: 'paid', last_action_reason, last_action_by, last_action_at }).eq("id", id).eq("status", "claimed")`. This is the ONE narrow gated `paid` writer (D-15) — it is in the single-writer allowlist; the live 0006 trigger permits the `claimed->paid` edge. Reason required.
    - `cancel`: `update({ status: 'cancelled', last_action_* })` (trigger-legal from pre-pickup states); reason required; NEVER calls refundPayment (D-11).
    - `refund`: resolve `stripe_payment_intent_id` + `amount_cents` from the row; call `refundPayment({ paymentIntentId, amountCents: parsed.amount, idempotencyKey: 'refund:'+id+':'+(parsed.amount ?? 'full') })`; persist `last_action_*`; NEVER write `status='paid'` (D-12, Pitfall 2/3).
    Create `app/admin/transfers/[id]/RefundForm.tsx` (`"use client"`): a `useActionState` form — amount field pre-filled to the full paid amount (editable down), a reason field (required), and the ALWAYS-shown `refundFeeDisclosure` rendered with `recordedFeeCents` substituted into the `{fee}` token (D-12). Disable the submit button while pending (Pitfall 3 double-submit guard).
    Modify `app/admin/transfers/[id]/TransferDetailView.tsx`: wire the five action placeholders — assign as a one-tap button; reassign/release/cancel behind a confirm dialog requiring the reason note (D-10) using the `*Confirm` copy; cancel additionally offers the `cancelOfferRefundCta` shortcut that opens the RefundForm but does NOT auto-refund (D-11); refund opens the RefundForm. Import from `../actions` and `./RefundForm`. Destructive buttons use the coral token.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run app/admin/transfers/actions.test.ts platform/payments/single-writer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five actions re-gate `getCurrentRole()==='admin'` (`grep -c "getCurrentRole" app/admin/transfers/actions.ts` >= 5).
    - `release` guards `status==='claimed'` and writes `status: 'paid'` + clears driver_id (`grep -c '"claimed"' app/admin/transfers/actions.ts` >= 1; the release path sets driver_id null + status paid).
    - cancel/refund/reassign/release persist `last_action_reason` (`grep -c "last_action_reason" app/admin/transfers/actions.ts` >= 1); assign does NOT require a reason.
    - `refund` calls `refundPayment` with an `idempotencyKey` and NEVER writes `status='paid'` (`grep -c "refundPayment" app/admin/transfers/actions.ts` >= 1; the refund branch has no `status: 'paid'`).
    - cancel NEVER calls `refundPayment` (the cancel branch has no refund call — D-11).
    - `RefundForm.tsx` always renders `refundFeeDisclosure` (`grep -c "refundFeeDisclosure" app/admin/transfers/[id]/RefundForm.tsx` >= 1) and disables submit while pending.
    - `TransferDetailView.tsx` now imports the actions (`grep -c "../actions" app/admin/transfers/[id]/TransferDetailView.tsx` >= 1) and gates reassign/release/cancel behind a confirm+reason dialog (assign is one-tap).
    - `actions.test.ts` (Plan-01 RED) is GREEN/skip-clean; `single-writer.test.ts` GREEN with EXACTLY the webhook + `app/admin/transfers/actions.ts` as the two `status='paid'` writers (no third).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>The five admin ops actions are gated and audited; release is the narrow claimed-only paid writer; refund is full/partial, idempotent, fee-disclosed, and never sets paid; cancel never auto-refunds; the detail view wires confirm dialogs + the refund form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→live Balkanity DB | The 0006 apply mutates the LOCKED data layer; sign-off-gated, Balkanity-ref-guarded, Management-API-only. |
| admin browser→ops actions | Untrusted; each action re-gates getCurrentRole()==='admin' (service-role bypasses RLS — the action check is the only gate). |
| refund action→live Stripe | Real money moves; idempotency-keyed; server-only hook; secret key never client-reachable. |
| client→FormData (reason/amount) | Untrusted input; zod-validated (positive int ≤ paid; non-empty reason; uuids). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-AC2 | Elevation of Privilege | ops actions | mitigate | In-action `getCurrentRole()==='admin'` on all five (service-role bypasses RLS — the action check is THE gate); a non-admin direct call is rejected. |
| T-06-PAID | Tampering / money integrity | release `paid` write | mitigate | release is the ONLY narrow gated `paid` writer (D-15), restricted to `status='claimed'` via `.eq("status","claimed")`; single-writer contract pins {webhook, this action}; the live 0006 trigger is the legality backstop. |
| T-06-REFUND | Tampering / financial | refund | mitigate | server-only hook; stable `idempotencyKey` (no double-refund on retry/double-click — Pitfall 3); submit disabled while pending; cancel never auto-refunds (D-11). |
| T-06-DISCLOSE | (policy) | refund disclosure | mitigate | `refundFeeDisclosure` ALWAYS rendered with the recorded fee (D-12) — an admin never refunds assuming the fee returns. |
| T-06-KALVIA | Spoofing | migration target | mitigate | ref guardrail BEFORE apply asserts Balkanity (`qyhdogajtmnvxphrslwm`) + Kalvia absent; Management-API-only; sign-off gate. |
| T-06-INPUT | Tampering | refund amount/reason | mitigate | zod: amount positive int ≤ paid amount; reason non-empty for the gated three; uuids; generic dictionary-keyed errors. |
| T-06-SC | Tampering | npm installs | accept | No package installs (zero new deps — RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- Migration 0006 applied LIVE to Balkanity after sign-off; `06-MIGRATION-EVIDENCE.md` records it; Kalvia never targeted.
- `npm run typecheck` clean.
- Five gated admin actions; release is claimed-only paid writer; refund idempotent + never sets paid; cancel never auto-refunds.
- `actions.test.ts` GREEN/skip-clean; `single-writer.test.ts` GREEN with exactly {webhook, admin transfers actions}.
- RefundForm always shows the fee disclosure; detail view wires confirm+reason dialogs.
</verification>

<success_criteria>
- Migration 0006 live on Balkanity (release edge + audit columns) after sign-off.
- Admin assigns (one-tap), reassigns/releases/cancels (confirm+reason), and refunds (full/partial, fee-disclosed) from the detail page.
- Release returns a claimed transfer to the pool (driver_id null, status paid), claimed-only.
- Refund is server-only, idempotent, never sets paid; cancel never auto-refunds.
- No Phase-6 path other than release writes status='paid'.
</success_criteria>

<output>
Create `.planning/phases/06-driver-admin-views/06-05-SUMMARY.md` when done.
</output>
