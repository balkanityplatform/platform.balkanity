---
phase: 07-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - supabase/migrations/0007_notifications.sql
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
  - platform/i18n/dictionary.ts
  - app/pickup/[slug]/actions.ts
  - platform/notifications/send-email.test.ts
  - platform/notifications/single-sender.test.ts
  - platform/notifications/notifications-rls.test.ts
  - platform/notifications/assigned-email.test.ts
  - platform/notifications/digest.test.ts
  - platform/notifications/locale.test.ts
  - app/driver/advance.notify.test.ts
  - app/admin/drivers/invite.notify.test.ts
autonomous: false
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-05, NOTF-06]
user_setup:
  - service: resend
    why: "Transactional email send for all guest/admin/driver emails"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys (server-only; never NEXT_PUBLIC_)"

must_haves:
  truths:
    - "resend package is installed and importable"
    - "Migration 0007 file exists with notifications + email_log tables, wp_transfers.locale column, and digest-preference storage — authored, NOT applied"
    - "getDictFor(lang) resolves EN/BG copy by argument without a request cookie"
    - "createBooking persists the booking language into wp_transfers.locale"
    - "Wave-0 RED test specs exist for the engine, bell RLS, assigned-email, digest, locale, arrived-fan-out, and invite-fan-out behaviors"
    - "All new email-copy and bell-copy dictionary keys exist in both en.ts and bg.ts (tsc Dict parity holds)"
  artifacts:
    - path: "supabase/migrations/0007_notifications.sql"
      provides: "notifications + email_log tables, wp_transfers.locale, digest preference storage, RLS SELECT-only + no-write-policy lock"
      contains: "create table public.notifications"
    - path: "platform/i18n/dictionary.ts"
      provides: "getDictFor(lang) cookie-free locale accessor"
      contains: "getDictFor"
    - path: "platform/notifications/send-email.test.ts"
      provides: "RED Nyquist gate: cap soft-block + idempotency (Resend mocked)"
    - path: "platform/notifications/single-sender.test.ts"
      provides: "RED grep gate: resend.emails.send appears only in send-email.ts"
  key_links:
    - from: "app/pickup/[slug]/actions.ts"
      to: "wp_transfers.locale"
      via: "getLang() captured into the insert"
      pattern: "locale\\s*:\\s*lang"
    - from: "platform/i18n/dictionary.ts"
      to: "en/bg dictionaries"
      via: "getDictFor by argument"
      pattern: "getDictFor"
---

<objective>
Lay the Phase 7 foundation in a single vertical-enabling slice: install the only new dependency (`resend`), author the flagged `0007` migration FILE (notifications + email_log + `wp_transfers.locale` + digest-preference storage — authored, not applied), add the cookie-free `getDictFor(lang)` accessor plus all new EN/BG dictionary keys, capture the booking locale in `createBooking`, and land the Wave-0 Nyquist RED test specs (Resend MOCKED in every test per D-15).

Purpose: Every downstream plan (engine, bell, fan-out, digest, live-apply) depends on these primitives existing. The RED specs turn GREEN as Plans 02–05 land; the migration file is applied LIVE in Plan 06.
Output: `resend` in package.json; `0007_notifications.sql`; `getDictFor` + new dict keys; `createBooking` locale write; 8 RED test specs.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-notifications/07-RESEARCH.md
@.planning/phases/07-notifications/07-PATTERNS.md
@.planning/phases/07-notifications/07-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan creates these new symbols (downstream drift-verification must treat them as newly-created, not pre-existing):
- New tables: `public.notifications`, `public.email_log`
- New column: `wp_transfers.locale`
- New digest-preference storage (table `public.driver_notification_prefs` OR columns on `driver_profiles` — see Task 2; the migration declares the chosen shape)
- New i18n accessor: `getDictFor(lang: Lang): Dict`
- New dictionary keys (en.ts + bg.ts): `alertsTrigger`, `alertsTriggerAria`, `alertsPanelTitle`, `markAllReadCta`, `alertsEmptyHeading`, `alertsEmptyBody`, `alertsLoadFailed`, `notifNewPoolTitle`, `notifRunAssignedTitle`, `notifRunReassignedTitle`, `notifRunReleasedTitle`, `notifRunCancelledTitle`, `notifNewBookingTitle`, `notifEmailCapTitle`, `digestPrefTitle`, `digestPrefBody`, `digestEnableLabel`, `digestTimeLabel`, `digestSaveCta`, `digestSavedToast`, `digestSaveFailed`, `emailAssignedSubject`, `emailArrivedSubject`, `emailAdminBookingSubject`, `emailInviteSubject`, `emailDigestSubject`, plus email heading/body/cta/footer copy keys for assigned/arrived/admin-alert/invite/digest templates
- New test files: `platform/notifications/{send-email,single-sender,notifications-rls,assigned-email,digest,locale}.test.ts`, `app/driver/advance.notify.test.ts`, `app/admin/drivers/invite.notify.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [GATE] Verify and install resend, confirm RESEND_API_KEY</name>
  <read_first>
    - .planning/phases/07-notifications/07-RESEARCH.md (## Package Legitimacy Audit — resend disposition; ## Standard Stack — version `^6` → 6.14.0, empty postinstall)
    - package.json (confirm `resend` is NOT already a dependency)
    - CLAUDE.md (Recommended Stack — `resend` `^6.12` locked; Resend key server-only, never NEXT_PUBLIC_)
  </read_first>
  <what-built>Nothing yet — this is the pre-install legitimacy gate required by the planner protocol (the slopcheck tool was unavailable during research, so `resend` is treated per the degradation rule even though it is the officially-locked SDK).</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/resend — confirm it is the official Resend SDK (repo github.com/resend/resend-node), latest ~6.14.0, millions of weekly downloads, no postinstall script.
    2. Confirm `RESEND_API_KEY` exists (or will be added) as a server-only env var in Vercel production (NOT a `NEXT_PUBLIC_` name).
    3. After approval the executor runs `npm install resend` (resolves `^6` → 6.14.0) and commits the package.json + lockfile change.
  </how-to-verify>
  <action>On approval, run `npm install resend` from the repo root (adds `resend@^6` to dependencies; lockfile resolves 6.14.0). Do NOT add `@react-email/*` (D: plain HTML templates only). Do NOT touch any other dependency. Record the installed version in the SUMMARY.</action>
  <verify>
    <automated>node -e "require('resend'); console.log('resend importable')" && grep -q '"resend"' package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `resend` at `^6` (resolves 6.14.0).
    - `require('resend')` succeeds (package present in node_modules).
    - No `@react-email` or other new dependency added.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install resend, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author migration 0007 (notifications + email_log + wp_transfers.locale + digest prefs) — FILE ONLY</name>
  <files>supabase/migrations/0007_notifications.sql</files>
  <read_first>
    - supabase/migrations/0006_release_and_audit.sql (copy the header/guardrail block verbatim: FLAGGED/IRREVERSIBLE note, Balkanity ref `qyhdogajtmnvxphrslwm` ONLY / NEVER Kalvia, "AUTHORED NOT APPLIED — Management API only, not MCP/db push", `add column if not exists` additive pattern, RLS no-write-policy lock at lines 28-34)
    - supabase/migrations/0003_payments_spine.sql (webhook_events: UNPREFIXED platform-generic table + UNIQUE index on event_id — the dedup pattern to mirror for email_log.idempotency_key)
    - supabase/migrations/0005_claim_correctness.sql (is_admin() / RLS SELECT policy conventions)
    - .planning/phases/07-notifications/07-RESEARCH.md (Pattern 1 notifications DDL L196-222; email_log DDL L313-328; Pattern 6 locale column L383)
    - .planning/phases/07-notifications/07-PATTERNS.md (### supabase/migrations/0007_notifications.sql — exact analog + conventions)
  </read_first>
  <action>
    Author `supabase/migrations/0007_notifications.sql` AUTHORED-NOT-APPLIED (live apply is Plan 06). Copy the 0006 header guardrail block (Balkanity ref `qyhdogajtmnvxphrslwm` only, never Kalvia `utyatpadtibqqswsfvtr`; Management API `/database/query` apply, never MCP / never `supabase db push`). Use `create table ... if not exists` / `create index if not exists` / `add column if not exists` for re-runnable safety. The migration declares FOUR things:

    1. `public.notifications` (UNPREFIXED platform-generic, like webhook_events — PLAT-01 seam): columns `id uuid pk default gen_random_uuid()`, `recipient_id uuid not null references auth.users(id) on delete cascade`, `type text not null`, `entity_type text` (polymorphic — NO `transfer_id` column, SC#1), `entity_id uuid`, `title text not null`, `body text`, `read_at timestamptz` (NULL = unread; per-user read state D-05), `created_at timestamptz not null default now()`. Add partial index `notifications_recipient_unread_idx on (recipient_id, created_at desc) where read_at is null`. Enable RLS. Policy `notifications_own_read` FOR SELECT TO authenticated USING ((select auth.uid()) = recipient_id). NO insert/update/delete policy (service-role-only writes — the established no-write-policy lock; mark-read is a gated service-role action, NOT a client write policy — Open Q1 resolved to the gated action).

    2. `public.email_log` (UNPREFIXED platform-generic): columns `id uuid pk default gen_random_uuid()`, `idempotency_key text not null`, `recipient text not null`, `tier text not null` ('critical' | 'best_effort'), `outcome text not null` ('sent' | 'failed' | 'skipped_cap'), `created_at timestamptz not null default now()`. Add `unique index email_log_idempotency_key_key on (idempotency_key)` (race-safe dedup, mirrors webhook_events.event_id) and `index email_log_created_at_idx on (created_at)` (daily-count query). Enable RLS. Policy `email_log_admin_read` FOR SELECT TO authenticated USING (public.is_admin()). NO write policy (service-role only; Phase 8 gauge reads this).

    3. `alter table public.wp_transfers add column if not exists locale text` (D-17; NULL-able, no default → no row rewrite; existing rows NULL → EN fallback).

    4. Digest preference storage. CHOOSE columns on `driver_profiles` (consistent with how driver display profile already lives there): `add column if not exists digest_enabled boolean not null default false` (D-07 off-by-default) and `add column if not exists digest_send_hour smallint` (0–23 self-chosen send hour, NULL until set; D-08). Document the choice in a comment. RLS: driver_profiles already has its policies from earlier migrations; do NOT add a client write policy — the digest-preference write is a gated service-role action (Plan 05).

    Seam note in the file: notifications/email_log are platform-generic → UNPREFIXED; wp_transfers / driver_profiles are module objects (already wp_/module-scoped). NO `transfer_id` column anywhere on the shared tables (polymorphic entity_type/entity_id only).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0007_notifications.sql && grep -q "create table" supabase/migrations/0007_notifications.sql && grep -qi "qyhdogajtmnvxphrslwm" supabase/migrations/0007_notifications.sql && grep -q "notifications_own_read" supabase/migrations/0007_notifications.sql && grep -q "email_log_idempotency_key_key" supabase/migrations/0007_notifications.sql && grep -q "add column if not exists locale" supabase/migrations/0007_notifications.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists; header names the Balkanity ref `qyhdogajtmnvxphrslwm` and the "NEVER Kalvia / Management API only / AUTHORED NOT APPLIED" guardrail.
    - `notifications` + `email_log` are UNPREFIXED with NO `transfer_id` column (grep `transfer_id` returns no match in either CREATE TABLE block; polymorphic `entity_type`/`entity_id` present).
    - `notifications` has exactly one SELECT policy (`notifications_own_read`, own-rows-only) and NO insert/update/delete policy.
    - `email_log` has a UNIQUE index on `idempotency_key` and an admin-only SELECT policy; NO write policy.
    - `wp_transfers.locale` and the two `driver_profiles` digest-preference columns are added with `if not exists`, NULL-able / defaulted-false (no row rewrite).
    - The migration is NOT applied to any remote DB by this task (file authoring only).
  </acceptance_criteria>
  <done>Migration 0007 file authored with the four declarations, RLS SELECT-only + no-write-policy lock, Balkanity-ref guardrail, and the polymorphic (no transfer_id) shape; not applied.</done>
</task>

<task type="auto">
  <name>Task 3: getDictFor accessor + all new EN/BG dictionary keys + createBooking locale capture</name>
  <files>platform/i18n/dictionary.ts, platform/i18n/en.ts, platform/i18n/bg.ts, app/pickup/[slug]/actions.ts</files>
  <read_first>
    - platform/i18n/dictionary.ts (getLang/getDict at L15-22 — add the cookie-free getDictFor alongside)
    - platform/i18n/en.ts (L1-40 — the `export const en = { ... }` shape; `Dict = typeof en` is the parity source of truth)
    - platform/i18n/bg.ts (must satisfy the exact en shape — a missing/extra key fails tsc)
    - app/pickup/[slug]/actions.ts (createBooking — getDict at L51 already imports the i18n module; the .insert at L101-115)
    - .planning/phases/07-notifications/07-UI-SPEC.md (## Copywriting Contract — every prescribed EN key + string for bell/feed/digest/email subjects)
    - .planning/phases/07-notifications/07-PATTERNS.md (### platform/i18n/dictionary.ts — getDictFor signature; createBooking `locale: lang` write at L236)
  </read_first>
  <action>
    In `platform/i18n/dictionary.ts` add a cookie-free accessor: `export function getDictFor(lang: Lang): Dict { return lang === "bg" ? bg : en; }`. Keep getLang/getDict unchanged (they remain the cookie-bound path for request-context callers). getDictFor is for webhook/cron/email paths that have no request cookie (Pitfall 2).

    In `platform/i18n/en.ts` add every new key from the UI-SPEC Copywriting Contract with the prescribed EN strings: bell/feed keys (`alertsTrigger`="Alerts", `alertsTriggerAria`="Alerts, {count} unread", `alertsPanelTitle`="Alerts", `markAllReadCta`="Mark all read", `alertsEmptyHeading`="You're all caught up", `alertsEmptyBody`, `alertsLoadFailed`); notification item titles (`notifNewPoolTitle`="New transfer available to claim", `notifRunAssignedTitle`, `notifRunReassignedTitle`, `notifRunReleasedTitle`, `notifRunCancelledTitle`, `notifNewBookingTitle`="New paid booking", `notifEmailCapTitle`); digest-preference UI (`digestPrefTitle`="Daily digest", `digestPrefBody`, `digestEnableLabel`, `digestTimeLabel`="Send at", `digestSaveCta`, `digestSavedToast`, `digestSaveFailed`); email subjects (`emailAssignedSubject`="Your driver is assigned", `emailArrivedSubject`="Your driver has arrived", `emailAdminBookingSubject`="New paid booking", `emailInviteSubject`="You're invited to drive with Balkanity", `emailDigestSubject`); and the heading/body/cta/footer copy keys each new email template (assigned/arrived/admin-alert/invite/digest) needs for the templates.ts in Plan 02 (e.g. `emailAssignedHeading`, `emailAssignedBody` with `{driverName}`/`{driverPhone}` tokens, `emailArrivedHeading`/`emailArrivedBody`, `emailAdminBookingHeading`/`emailAdminBookingBody`, `emailInviteHeading`/`emailInviteBody`/`emailInviteCta`, `emailDigestHeading`/`emailDigestIntro`/`emailDigestEmptyBody`). Add matching BG translations for every new key in `platform/i18n/bg.ts` (real BG copy, not EN echoes — the project widens Dict values to string so BG may differ while tsc still catches a missing key).

    In `app/pickup/[slug]/actions.ts createBooking`: capture `const lang = await getLang();` (import getLang from the i18n module — already importing getDict) and add `locale: lang` to the `.insert({...})` object (alongside guest_email etc.). This persists the booking language so the webhook-fired confirmation email (Plan 03) can resolve guest copy via getDictFor(transfer.locale ?? 'en'). Existing rows have NULL locale → EN fallback. This adds NO new write surface and does NOT touch the amount/status logic (still `status: 'requested'`, not a paid writer).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "getDictFor" platform/i18n/dictionary.ts && grep -q "alertsTrigger" platform/i18n/en.ts && grep -q "alertsTrigger" platform/i18n/bg.ts && grep -q "locale: lang" app/pickup/[slug]/actions.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `getDictFor(lang)` exported from dictionary.ts; returns `bg` for "bg" else `en`; no cookie read.
    - Every UI-SPEC Copywriting Contract key exists in BOTH en.ts and bg.ts (tsc Dict parity gate passes — `npx tsc --noEmit` clean).
    - `createBooking` writes `locale: lang` into the wp_transfers insert; no other behavioral change (status still 'requested').
    - `app/pickup/[slug]/actions.ts` still contains NO `status: 'paid'` literal (single-writer gate unaffected).
  </acceptance_criteria>
  <done>getDictFor accessor added; all new EN+BG keys present and tsc-parity-clean; createBooking persists booking locale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave-0 Nyquist RED specs (Resend MOCKED) for engine, bell RLS, emails, digest, locale, fan-out</name>
  <files>platform/notifications/send-email.test.ts, platform/notifications/single-sender.test.ts, platform/notifications/notifications-rls.test.ts, platform/notifications/assigned-email.test.ts, platform/notifications/digest.test.ts, platform/notifications/locale.test.ts, app/driver/advance.notify.test.ts, app/admin/drivers/invite.notify.test.ts</files>
  <read_first>
    - platform/transfers/confirmation.test.ts (the RED-by-runtime-string-import + stripComments grep-gate pattern to mirror)
    - platform/payments/single-writer.test.ts (the source-tree grep gate to mirror for single-sender.test.ts)
    - .planning/phases/07-notifications/07-RESEARCH.md (## Validation Architecture — Phase Requirements → Test Map + Wave 0 Gaps: the exact behavior each spec pins)
    - .planning/phases/07-notifications/07-RESEARCH.md (Pattern 3 sendEmail body — the contract send-email.test.ts asserts; D-09/10/12 tiers)
  </read_first>
  <behavior>
    - send-email.test.ts (NOTF-06): with Resend MOCKED — a `best_effort` send at email_log daily count >= SOFT_CAP returns outcome `skipped_cap` and does NOT call resend.emails.send; a `critical` send above the cap STILL calls resend.emails.send; a second call with the same `idempotency_key` after a prior `sent` returns `duplicate` and does NOT re-send.
    - single-sender.test.ts (NOTF-06): grep the source tree (strip comments) — `resend.emails.send` / `new Resend(` appears in EXACTLY one production file: `platform/notifications/send-email.ts`. RED now (file absent / pattern not yet sole).
    - notifications-rls.test.ts (NOTF-01): a caller-auth read of `notifications` returns ONLY rows where `recipient_id = auth.uid()` (own-rows-only). (Source-level contract / mock RLS as the confirmation test does — runtime-string import of the refetch action; RED until Plan 03.)
    - assigned-email.test.ts (NOTF-02/D-16): the "driver assigned" email body contains the driver first name AND phone tokens, and `to` is the transfer's guest_email (never a driver/admin address).
    - digest.test.ts (NOTF-05): the digest builder output contains the masked wp_pool fields (date/arrival/airport/zone/flight_no/fare/pax/luggage) + the driver's own claimed runs, and ZERO guest-PII keys (no guest_name/guest_email/guest_phone/notes/exact address).
    - locale.test.ts (D-17): a guest email built for a transfer with locale='bg' resolves BG copy; locale=null resolves EN; admin/invite always resolve EN.
    - advance.notify.test.ts (NOTF-02): advancing to `arrived` fires the guest "driver arrived" email exactly once; advancing to `en_route` fires NO email.
    - invite.notify.test.ts (NOTF-04): the invite path calls sendEmail (critical tier) and the returned state no longer carries `actionLink`.
  </behavior>
  <action>
    Create the eight test files as Nyquist RED specs, Resend ALWAYS MOCKED (D-15 caveat — CI/local never depend on live sends). Mirror `platform/transfers/confirmation.test.ts`: use a runtime-string dynamic import (`@/platform/notifications/send-email` etc.) so each file type-checks BEFORE its implementation module exists and THROWS at runtime → RED now. For the two grep gates (single-sender.test.ts, and the source assertions inside others) copy the `stripComments` + source-tree walk from `single-writer.test.ts`. Mock `@/platform/supabase/admin` (createAdminClient → fake from/insert/select chain), `resend` (Resend → mocked emails.send), and `@/platform/i18n/dictionary` where needed (mirror confirmation.test.ts mock shape). Each test encodes the behavior in <behavior> above. These stay RED until Plans 02–05 land the implementations; do NOT create the implementation modules here.
  </action>
  <verify>
    <automated>npx vitest run platform/notifications/single-sender.test.ts 2>&1 | grep -qE "fail|FAIL|❯|✖|no tests|cannot find|Error" ; npx tsc --noEmit && echo "RED-baseline-and-tsc-clean"</automated>
  </verify>
  <acceptance_criteria>
    - All eight test files exist and `npx tsc --noEmit` is clean (runtime-string imports keep them type-safe before impls exist).
    - Running each spec FAILS today (RED by design — implementation modules not built in this plan).
    - Every test that exercises a send path mocks the Resend client (no live send in any test).
    - single-sender.test.ts asserts the single Resend call-site invariant via a comment-stripped source-tree grep (mirrors single-writer.test.ts).
  </acceptance_criteria>
  <done>Eight Wave-0 RED specs authored, tsc-clean, Resend mocked, encoding the engine/bell/email/digest/locale/fan-out contracts that Plans 02–05 turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest browser → createBooking server action | Untrusted FormData; locale captured server-side from the lang cookie (not a client amount/price) |
| migration authoring → live DB | The 0007 file is authored here but NEVER applied to a remote in this plan (apply is the Plan 06 signed-off gate) |
| npm registry → repo | New dependency (`resend`) crosses a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | notifications/email_log tables | mitigate | RLS SELECT-only + NO write policy in 0007 — service-role-only writes (Task 2); polymorphic entity_type/entity_id, no transfer_id column (PLAT-01 seam) |
| T-07-02 | Information Disclosure | wp_transfers.locale capture | accept | locale is a 2-char EN/BG tag, not PII; captured server-side, no new read surface |
| T-07-03 | Elevation of Privilege | premature live migration apply | mitigate | 0007 is AUTHORED-NOT-APPLIED; header guardrail names Balkanity ref only / never Kalvia / Management-API-only; live apply is the Plan 06 blocking sign-off |
| T-07-SC | Tampering | `npm install resend` | mitigate | Task 1 blocking-human checkpoint verifies the package on npmjs.com before install (resend is the officially-locked SDK; slopcheck unavailable → degradation rule applies) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (Dict parity for all new EN/BG keys).
- `require('resend')` succeeds; package.json lists `resend`.
- 0007 migration file present with the four declarations, RLS lock, and Balkanity-ref guardrail; NOT applied.
- Eight Wave-0 specs are RED (implementations absent) and Resend-mocked.
- `createBooking` writes `locale: lang`; still no `status: 'paid'` literal in the booking action.
</verification>

<success_criteria>
- resend installed (checkpoint-approved); getDictFor accessor + all new EN/BG keys present (tsc clean); createBooking persists booking locale; 0007 authored-not-applied; 8 RED Nyquist specs landed with Resend mocked.
</success_criteria>

<output>
Create `.planning/phases/07-notifications/07-01-SUMMARY.md` when done.
</output>
