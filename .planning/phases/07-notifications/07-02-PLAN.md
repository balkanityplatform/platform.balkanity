---
phase: 07-notifications
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - platform/notifications/send-email.ts
  - platform/notifications/notify.ts
  - platform/notifications/templates.ts
autonomous: true
requirements: [NOTF-06, NOTF-02, NOTF-03, NOTF-01]

must_haves:
  truths:
    - "sendEmail() is the ONLY place resend.emails.send is ever called"
    - "sendEmail() checks email_log by idempotency_key before sending and records every outcome (sent/failed/skipped_cap)"
    - "best_effort sends are soft-blocked at >= EMAIL_SOFT_CAP daily sends; critical sends always go through"
    - "insertNotification() writes a polymorphic notification row via the service-role client (no transfer_id column)"
    - "templates.ts builds plain-HTML email bodies via getDictFor(lang) token interpolation (no react-email)"
  artifacts:
    - path: "platform/notifications/send-email.ts"
      provides: "the single Resend call-site with cap/idempotency/rate guard"
      exports: ["sendEmail"]
      contains: "resend.emails.send"
    - path: "platform/notifications/notify.ts"
      provides: "insertNotification + markRead/markAllRead gated service-role actions"
      exports: ["insertNotification"]
    - path: "platform/notifications/templates.ts"
      provides: "plain-HTML email template builders keyed by getDictFor"
  key_links:
    - from: "platform/notifications/send-email.ts"
      to: "email_log"
      via: "idempotency check + outcome insert (service-role)"
      pattern: "from\\([\"'`]email_log"
    - from: "platform/notifications/notify.ts"
      to: "notifications"
      via: "service-role insert"
      pattern: "from\\([\"'`]notifications"
---

<objective>
Build the Phase 7 notification engine — the genuinely new primitive plus its two helpers, with no UI and no lifecycle wiring yet. Three server-only modules: `send-email.ts` (THE single `resend.emails.send` call-site, fronted by the email_log idempotency check, the ≤EMAIL_SOFT_CAP best-effort soft cap, the ≤5 req/s defense, and per-outcome logging), `notify.ts` (`insertNotification` service-role insert + gated `markRead`/`markAllRead` actions), and `templates.ts` (plain-HTML template builders via `getDictFor`).

Purpose: This is the seam every downstream plan calls. Plan 03 (un-stub + fan-out) and Plan 05 (digest) consume `sendEmail`/`insertNotification`/`templates`. Building it alone keeps the cap/idempotency logic at peak quality and turns the `send-email.test.ts` + `single-sender.test.ts` + `assigned-email.test.ts` + `locale.test.ts` Wave-0 specs GREEN.
Output: `platform/notifications/{send-email,notify,templates}.ts`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-notifications/07-RESEARCH.md
@.planning/phases/07-notifications/07-PATTERNS.md
@.planning/phases/07-notifications/07-UI-SPEC.md
@platform/supabase/admin.ts
@platform/transfers/confirmation-email.ts
</context>

<artifacts_this_phase_produces>
New symbols created by this plan (downstream drift-verification must treat as newly-created):
- `sendEmail(opts)` — exported from `platform/notifications/send-email.ts`; opts `{ to, subject, html, tier: 'critical'|'best_effort', idempotencyKey }`; returns `{ outcome: 'sent'|'skipped_cap'|'duplicate'|'failed' }`
- `insertNotification(...)`, `markRead(...)`, `markAllRead(...)` — exported from `platform/notifications/notify.ts`
- Template builders in `platform/notifications/templates.ts` (e.g. `assignedEmail`, `arrivedEmail`, `adminBookingEmail`, `inviteEmail`, `digestEmail`) — each returns `{ subject, html }`
- Env constant `EMAIL_SOFT_CAP` (default 90), sender constant `noreply@send.balkanity.com`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: send-email.ts — the single Resend call-site with cap/idempotency/rate guard (NOTF-06)</name>
  <files>platform/notifications/send-email.ts</files>
  <read_first>
    - platform/notifications/send-email.test.ts (the Wave-0 RED contract this turns GREEN — cap soft-block, idempotency, critical-always-sends)
    - platform/notifications/single-sender.test.ts (the grep gate — resend.emails.send must live ONLY here)
    - .planning/phases/07-notifications/07-RESEARCH.md (Pattern 3 sendEmail body L253-310; D-09 tiers; D-10 soft cap; D-12 skipped_cap-not-retried; Pitfall 3 ≤5 req/s; Pitfall 4 three-layer dedup; Code Examples — daily-count head query)
    - platform/supabase/admin.ts (createAdminClient — service-role client; `import "server-only"` line 1 pattern)
    - .planning/phases/07-notifications/07-PATTERNS.md (### send-email.ts — server-only boundary copy-verbatim; invariants list)
  </read_first>
  <action>
    Create `platform/notifications/send-email.ts` with `import "server-only";` as line 1 (build-fail guard — RESEND_API_KEY/service-role key never reach the browser, PLAT-05). Read `EMAIL_SOFT_CAP` from `process.env.EMAIL_SOFT_CAP ?? 90` and the sender constant `noreply@send.balkanity.com` (D-13). Export `async function sendEmail(opts: { to: string; subject: string; html: string; tier: 'critical' | 'best_effort'; idempotencyKey: string }): Promise<{ outcome: 'sent' | 'skipped_cap' | 'duplicate' | 'failed' }>` following Pattern 3 exactly:
    1. Acquire the service-role client via `createAdminClient()`.
    2. IDEMPOTENCY: query `email_log` by `idempotency_key`; if an existing row has `outcome === 'sent'`, return `{ outcome: 'duplicate' }` and DO NOT send (Pitfall 4 — webhook-retry safety).
    3. DAILY COUNT: head `count` query on `email_log` where `created_at >= todayUTC` (`setUTCHours(0,0,0,0)`) and `outcome = 'sent'`.
    4. SOFT CAP (D-10): if `tier === 'best_effort'` AND count >= EMAIL_SOFT_CAP, insert an `email_log` row with `outcome: 'skipped_cap'` and return `{ outcome: 'skipped_cap' }` (NOT retried, D-12 — the in-app feed carries the info). `critical` always proceeds.
    5. SEND: `new Resend(process.env.RESEND_API_KEY!)`; `resend.emails.send({ from: SENDER, to, subject, html }, { idempotencyKey })` — the idempotencyKey is the SECOND arg (defense-in-depth, 24h Resend dedup window). Wrap in try/catch.
    6. Insert the outcome (`sent` on no error, `failed` on error or thrown) into `email_log` with idempotency_key/recipient/tier/outcome.
    7. CAP-NEAR ALARM (D-11): when the daily count crosses a near-threshold (e.g. >= EMAIL_SOFT_CAP - 10) and an `email_cap_near` alarm has not already been logged today, insert an admin `email_cap_near` notification via insertNotification (free against the cap — NO email). Resolve the admin recipient(s) by querying `app_users` where `role = 'admin'` (forward-compatible; Open Q2). Wrap the alarm in its own try/catch so it never affects the send outcome.

    This module performs ZERO `wp_transfers` writes (money lock — single-writer.test.ts stays green). It is the ONLY file that constructs Resend or calls `.emails.send` (single-sender.test.ts gate).
  </action>
  <verify>
    <automated>npx vitest run platform/notifications/send-email.test.ts platform/notifications/single-sender.test.ts && npx vitest run platform/payments/single-writer.test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `send-email.test.ts` and `single-sender.test.ts` pass (GREEN).
    - `single-writer.test.ts` still passes (sendEmail writes no `status: 'paid'`).
    - Line 1 is `import "server-only";`.
    - `resend.emails.send` / `new Resend(` appears in NO other production file (the grep gate enforces this).
    - best_effort >= EMAIL_SOFT_CAP → `skipped_cap` (logged, not sent, not retried); critical above cap → sent; duplicate idempotency_key with prior 'sent' → `duplicate` (no re-send).
  </acceptance_criteria>
  <done>sendEmail is the single, idempotent, cap-guarded, rate-aware Resend call-site; cap/idempotency/single-sender specs GREEN; money lock intact.</done>
</task>

<task type="auto">
  <name>Task 2: notify.ts (insertNotification + gated markRead/markAllRead) + templates.ts</name>
  <files>platform/notifications/notify.ts, platform/notifications/templates.ts</files>
  <read_first>
    - platform/notifications/assigned-email.test.ts (the template contract — name+phone in body, to = guest_email)
    - platform/notifications/locale.test.ts (templates resolve copy via getDictFor by argument)
    - .planning/phases/07-notifications/07-RESEARCH.md (Code Examples — service-role notification insert L476-486; Pattern 1 markRead gated service-role action L223; Pattern 6 getDictFor-by-argument)
    - .planning/phases/07-notifications/07-PATTERNS.md (### notify.ts — insertNotification shape + markRead two-part gate; ### templates.ts — fill() + plain-HTML pattern from confirmation-email.ts L31-34,76-81)
    - platform/transfers/confirmation-email.ts (the fill() helper + plain-HTML body pattern to copy)
    - app/driver/actions.ts (the two-part caller-identity → service-role gate pattern to mirror for markRead)
    - platform/i18n/dictionary.ts (getDictFor accessor added in Plan 01)
    - platform/supabase/server.ts (createClient — caller-auth client for the identity check in markRead)
  </read_first>
  <action>
    `platform/notifications/notify.ts` — `import "server-only";` line 1. Export:
    - `insertNotification(opts: { recipientId: string; type: string; title: string; entityType?: string; entityId?: string; body?: string }): Promise<void>` — service-role `createAdminClient().from('notifications').insert({ recipient_id, type, entity_type, entity_id, title, body })`. Polymorphic — pass `entity_type: 'transfer'` + `entity_id: transferId` for transfer-scoped notifications; NEVER a `transfer_id` column (SC#1). Wrap so a failed insert is non-fatal to the caller's lifecycle write (callers also log-and-continue, but keep this defensive).
    - `markRead(notificationId: string)` and `markAllRead()` — GATED SERVICE-ROLE actions (NOT a client write RLS policy — preserves the no-write-policy lock, Open Q1). Each: read caller identity on the caller-auth client (`createClient().auth.getUser()`); then service-role UPDATE scoped `recipient_id = user.id` (markRead also `.eq('id', notificationId)`), setting `read_at = now()`. A forged call for another user's row matches 0 rows (the recipient_id scope is the gate). These power the bell's "mark read on open" / "mark all read" (Plan 03 calls them). Mark these `"use server"` if Plan 03 calls them directly as server actions, OR keep them as plain server-only functions wrapped by a `"use server"` action file in Plan 03 — pick the simpler wiring and document it.

    `platform/notifications/templates.ts` — `import "server-only";` line 1. Copy the `fill(template, vars)` token helper from confirmation-email.ts (L31-34). Export one builder per email, each taking the resolved values + a `Lang` and returning `{ subject, html }` built from `getDictFor(lang)` copy keys (added in Plan 01) and plain HTML strings (single-column, ~560px, inline teal `#029B87` accent + slate `#2F4858` body on white, one ≥44px CTA max — Email Layout Contract). Builders:
    - `assignedEmail({ driverName, driverPhone, ...trip }, lang)` — body interpolates driver first name + phone (D-16); subject `emailAssignedSubject`.
    - `arrivedEmail({ ...trip }, lang)` — simple heads-up, NO driver info (D-16); subject `emailArrivedSubject`.
    - `adminBookingEmail({ ...booking }, lang='en')` — EN (D-17); subject `emailAdminBookingSubject`.
    - `inviteEmail({ actionLink }, lang='en')` — EN (D-17); ONE CTA "Set your password" → actionLink; subject `emailInviteSubject`.
    - `digestEmail({ poolItems, ownRuns }, lang='en')` — EN; lists masked pool fields + own runs (consumed by Plan 05's digest.ts); subject `emailDigestSubject`. NEVER includes guest PII.
    Templates do NOT call getDict() (no request cookie on webhook/cron paths — Pitfall 2); they take `lang` by argument.
  </action>
  <verify>
    <automated>npx vitest run platform/notifications/assigned-email.test.ts platform/notifications/locale.test.ts && grep -q "getDictFor" platform/notifications/templates.ts && grep -q 'from("notifications")' platform/notifications/notify.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `assigned-email.test.ts` passes: the assigned template body contains driver first name + phone; the `to` resolution is the transfer's guest_email (asserted via the builder + caller contract).
    - `locale.test.ts` passes: guest templates resolve BG copy for locale='bg', EN for null; admin/invite templates EN.
    - `notify.ts` inserts into `notifications` via the service-role client with polymorphic `entity_type`/`entity_id` (no `transfer_id` column anywhere).
    - markRead/markAllRead scope every UPDATE to `recipient_id = auth.uid()` (no client write RLS policy introduced).
    - `templates.ts` uses `getDictFor` + plain HTML (no `react-email` import); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>insertNotification + gated markRead/markAllRead + all six plain-HTML template builders exist; assigned-email + locale specs GREEN; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → Resend HTTP | RESEND_API_KEY (server-only) crosses to the external send provider |
| server → notifications/email_log tables | service-role writes (RLS-bypassing) — the in-action gate is the only authz |
| caller → markRead/markAllRead | caller identity (auth.uid()) is the recipient-scope gate on the read-state write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SE1 | Information Disclosure | RESEND_API_KEY / service-role key | mitigate | `import "server-only"` line 1 of all three modules (build-fail if a client imports); key read from non-NEXT_PUBLIC_ name (PLAT-05) |
| T-07-SE2 | Denial of Service (cap exhaustion) | rogue resend.emails.send call-site | mitigate | All sends route through sendEmail(); single-sender.test.ts grep gate asserts the sole call-site; best_effort soft-cap at EMAIL_SOFT_CAP (D-10) |
| T-07-SE3 | Tampering/duplication | webhook-retry double-send | mitigate | email_log idempotency_key check-before-send + UNIQUE index (Plan 01) + Resend native idempotencyKey 2nd arg (three layers, Pitfall 4) |
| T-07-SE4 | Spoofing/Tampering | fake notification insert / read-state tamper | mitigate | No client write RLS policy; insertNotification is service-role-only; markRead/markAllRead scoped to recipient_id = auth.uid() |
| T-07-SE5 | Tampering (money) | second `paid` writer via the email path | mitigate | send-email.ts/notify.ts/templates.ts perform ZERO wp_transfers writes; single-writer.test.ts stays green |
| T-07-SE6 | Information Disclosure | driver phone in non-guest channel | mitigate | Only assignedEmail interpolates name+phone; arrived/admin/digest templates carry none; the `to` is the row's guest_email (Pitfall 5) |
</threat_model>

<verification>
- `npx vitest run platform/notifications/` — send-email, single-sender, assigned-email, locale specs GREEN.
- `single-writer.test.ts` still GREEN (no new paid writer).
- `resend.emails.send` appears only in send-email.ts.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- The engine exists: sendEmail (single, idempotent, cap-guarded), insertNotification + gated markRead/markAllRead, and six plain-HTML templates resolving copy by locale — with the cap/idempotency/single-sender/assigned/locale Wave-0 specs GREEN and the money lock intact.
</success_criteria>

<output>
Create `.planning/phases/07-notifications/07-02-SUMMARY.md` when done.
</output>
