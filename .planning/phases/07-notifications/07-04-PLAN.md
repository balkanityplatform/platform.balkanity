---
phase: 07-notifications
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - platform/transfers/confirmation-email.ts
  - app/api/stripe/webhook/route.ts
  - app/driver/actions.ts
  - app/admin/transfers/actions.ts
  - app/admin/drivers/actions.ts
  - app/admin/drivers/DriversView.tsx
autonomous: true
requirements: [NOTF-02, NOTF-03, NOTF-04, NOTF-01]

must_haves:
  truths:
    - "On the verified paid transition the guest receives a booking confirmation email (un-stubbed, critical tier) and the admin receives a booking-alert email + in-app notification; all drivers get a new_paid_pool in-app notification"
    - "On claimed (self-claim or admin assign) the guest receives a driver-assigned email carrying the driver first name + phone; the assigned driver gets a run_assigned in-app notification"
    - "On arrived the guest receives a driver-arrived heads-up email; en_route fires NO email"
    - "Admin reassign/release/cancel fire the matching driver in-app notification (run_reassigned/run_released/run_cancelled); release also re-enters the pool (new_paid_pool)"
    - "The driver invite is sent email-only via sendEmail (critical) and the inline copy-paste action_link reveal is removed"
    - "Every fan-out is log-and-continue — a send/insert failure never changes the money/lifecycle write; no second paid writer is introduced"
  artifacts:
    - path: "platform/transfers/confirmation-email.ts"
      provides: "un-stubbed confirmation send via sendEmail (signature unchanged)"
      contains: "sendEmail"
    - path: "app/api/stripe/webhook/route.ts"
      provides: "paid fan-out: confirmation + admin alert + admin/driver notifications (log-and-continue)"
    - path: "app/admin/drivers/actions.ts"
      provides: "email-only invite via sendEmail; no actionLink in state"
  key_links:
    - from: "platform/transfers/confirmation-email.ts"
      to: "platform/notifications/send-email.ts"
      via: "sendEmail({ tier: 'critical', idempotencyKey: `confirm:${transferId}` })"
      pattern: "sendEmail"
    - from: "app/driver/actions.ts claimAction"
      to: "guest driver-assigned email"
      via: "post-claim narrow service-role {name,phone} read → sendEmail to guest_email"
      pattern: "sendEmail"
---

<objective>
Wire every notification + email off the EXISTING lifecycle transitions, and un-stub the two seams Phases 2 and 4 deliberately left. On `paid`: un-stub the guest confirmation (critical) + add the admin booking-alert email (best_effort) + admin `new_paid_booking` and all-drivers `new_paid_pool` in-app notifications. On `claimed` (self-claim and admin assign): guest "driver assigned" email with the driver's first name + phone (D-16) + the assigned driver's `run_assigned` notification. On `arrived`: guest "driver arrived" heads-up (en_route stays silent). On admin reassign/release/cancel: the matching driver in-app notification (release also re-enters the pool). Driver invite goes email-only (D-14) — remove the inline action_link reveal.

Purpose: This is what makes the bell and the inbox actually fill. Every fan-out is a log-and-continue block hanging off an existing transition — NEVER a second `paid` writer, NEVER a rollback of the money/lifecycle write on a send failure.
Output: the six seam files wired; advance.notify + invite.notify specs GREEN; confirmation.test.ts + single-writer.test.ts stay GREEN.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-notifications/07-RESEARCH.md
@.planning/phases/07-notifications/07-PATTERNS.md
@.planning/phases/07-notifications/07-UI-SPEC.md
@platform/notifications/send-email.ts
@platform/notifications/notify.ts
@platform/notifications/templates.ts
</context>

<artifacts_this_phase_produces>
This plan WIRES existing transitions; the consumed symbols (sendEmail, insertNotification, templates) are produced by Plans 01-02. It introduces no new public symbols, only:
- The un-stubbed body of `sendBookingConfirmation` (signature unchanged)
- New log-and-continue fan-out blocks in webhook/route.ts, driver/actions.ts, admin/transfers/actions.ts
- Removal of `actionLink` from `InviteDriverState` (app/admin/drivers/actions.ts) and the reveal from DriversView
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Un-stub guest confirmation + driver invite (signatures unchanged, email-only invite)</name>
  <files>platform/transfers/confirmation-email.ts, app/admin/drivers/actions.ts, app/admin/drivers/DriversView.tsx</files>
  <read_first>
    - platform/transfers/confirmation-email.ts (L83-91 the Phase-4 console.info stub to replace; keep the signature + generateLink build verbatim)
    - platform/transfers/confirmation.test.ts (L102-111 grep gate — module must contain NO `status: 'paid'`; L85-100 magiclink contract — keep GREEN)
    - app/admin/drivers/actions.ts (L36-42 InviteDriverState with actionLink; L93-102 generateLink type:'invite'; L138 the `return { status: "ok", actionLink: ... }` reveal to replace)
    - app/admin/drivers/invite.notify.test.ts (the Wave-0 RED contract — invite calls sendEmail critical, state no longer carries actionLink)
    - app/admin/drivers/DriversView.tsx (the copy-paste link reveal UI to remove)
    - platform/notifications/send-email.ts + templates.ts (sendEmail + inviteEmail builder — Plan 02)
    - platform/i18n/dictionary.ts (getDictFor — for locale-by-argument; invite copy is EN)
    - .planning/phases/07-notifications/07-PATTERNS.md (### confirmation-email.ts un-stub; ### admin/drivers/actions.ts un-stub — exact in-place edits)
  </read_first>
  <action>
    `platform/transfers/confirmation-email.ts`: keep `sendBookingConfirmation(transferId, guestEmail)` signature IDENTICAL and the generateLink magic-link build verbatim. For D-17, read the persisted `wp_transfers.locale` (new column, Plan 01) via the admin client for THIS transferId and resolve copy with `getDictFor(locale ?? 'en')` instead of `getDict()` (no request cookie on the webhook path — Pitfall 2). Replace the `console.info("[BOOK-06 stub] ...")` block (L86-89) with `await sendEmail({ to: guestEmail, subject, html, tier: "critical", idempotencyKey: \`confirm:${transferId}\` })`. The module MUST still contain NO `status: 'paid'` literal (confirmation.test.ts + single-writer.test.ts grep gates stay GREEN) — sendEmail only touches email_log, never wp_transfers. Keep returning `{ to, magicLink, html }` so the existing magiclink test assertion (L97-99) still passes.

    `app/admin/drivers/actions.ts` (NOTF-04, D-14): keep the getCurrentRole admin re-gate (L66), the zod validation (L70-78), generateLink type:'invite' (L93-102), and the app_users/driver_profiles inserts unchanged. Replace the final reveal (L138 `return { status: "ok", actionLink: data.properties.action_link }`) with: capture `const actionLink = data.properties.action_link;`, build the invite HTML via the `inviteEmail({ actionLink }, 'en')` template (EN, D-17), `await sendEmail({ to: parsed.data.email, subject, html, tier: "critical", idempotencyKey: \`invite:${userId}\` })` wrapped log-and-continue, then `return { status: "ok" }`. Remove `actionLink?` from `InviteDriverState` (L36-42).

    `app/admin/drivers/DriversView.tsx`: remove the copy-paste action_link reveal UI (it no longer exists in the state). The success path now just confirms the invite was emailed (reuse an existing success copy key or the neutral success state). Keep the rest of DriversView (including the NotificationBell mounted in Plan 03) intact.
  </action>
  <verify>
    <automated>npx vitest run app/admin/drivers/invite.notify.test.ts platform/transfers/confirmation.test.ts platform/payments/single-writer.test.ts && grep -q "sendEmail" platform/transfers/confirmation-email.ts && ! grep -q "actionLink" app/admin/drivers/DriversView.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `invite.notify.test.ts` GREEN: the invite path calls sendEmail (critical) and the returned state has no `actionLink`.
    - `confirmation.test.ts` GREEN: the magiclink contract holds AND the module still has NO `status: 'paid'` literal.
    - `single-writer.test.ts` GREEN (no new paid writer).
    - `sendBookingConfirmation` signature unchanged; confirmation resolves copy via getDictFor(locale ?? 'en').
    - DriversView no longer reveals/copies the action_link; `actionLink` removed from InviteDriverState.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Confirmation + invite emails send through sendEmail (signatures unchanged, invite email-only, no inline reveal); money/magiclink grep gates stay GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: paid-transition fan-out in the webhook (confirmation already un-stubbed) + admin alert + in-app notifications</name>
  <files>app/api/stripe/webhook/route.ts</files>
  <read_first>
    - app/api/stripe/webhook/route.ts (L183-205 — the post-`processed` log-and-continue block around sendBookingConfirmation; the single paid writer above it; `paidRows[0].guest_email` access)
    - platform/payments/single-writer.test.ts (the grep gate — the webhook is the sanctioned paid writer; do NOT add any other status:'paid' write)
    - platform/notifications/notify.ts (insertNotification — admin new_paid_booking + driver new_paid_pool)
    - platform/notifications/send-email.ts + templates.ts (sendEmail best_effort + adminBookingEmail)
    - .planning/phases/07-notifications/07-RESEARCH.md (Pattern 4 hook-point table — paid row; D-09 admin alert best_effort; Open Q2 admin recipient resolution)
    - .planning/phases/07-notifications/07-PATTERNS.md (### webhook/route.ts — add the same log-and-continue shape for alert + notifications)
  </read_first>
  <action>
    In the `checkout.session.completed` paid branch, AFTER the existing `outcome: 'processed'` update and the (now un-stubbed) `sendBookingConfirmation` call, ADD three independently `try/catch`-wrapped fan-outs (each `console.error`s the error — NOT recipient PII — and continues; NONE may change the HTTP status of the money-bearing write above):
    1. Admin booking-alert email (best_effort, D-09): resolve admin recipient(s) by querying `app_users` where `role = 'admin'` (forward-compatible; a single ADMIN_ALERT_EMAIL env is also acceptable for the pilot — Open Q2; pick the app_users query). Build via `adminBookingEmail(...)` and `sendEmail({ to: adminEmail, subject, html, tier: "best_effort", idempotencyKey: \`admin-alert:${transferId}\` })`.
    2. Admin `new_paid_booking` in-app notification (D-03): `insertNotification({ recipientId: adminUserId, type: 'new_paid_booking', entityType: 'transfer', entityId: transferId, title: <pre-rendered EN/BG notifNewBookingTitle> })` for each admin.
    3. Driver `new_paid_pool` in-app notifications to ALL drivers (D-02): query `app_users` where `role = 'driver'`, insertNotification `type: 'new_paid_pool'` for each (a new paid transfer entered the claimable pool). These are DB inserts (not Resend calls) — they don't count against the cap (Pitfall 3).

    The notification `title` is the pre-rendered dictionary copy (the row stores the title string, not a key — research Pattern 1). Do NOT add any `wp_transfers` write here beyond the existing single paid write (single-writer gate).
  </action>
  <verify>
    <automated>npx vitest run platform/payments/single-writer.test.ts && grep -q "insertNotification\|new_paid_pool\|new_paid_booking" app/api/stripe/webhook/route.ts && grep -q "best_effort" app/api/stripe/webhook/route.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The webhook fires: un-stubbed guest confirmation (critical, Task 1), admin booking-alert email (best_effort), admin new_paid_booking + all-drivers new_paid_pool notifications.
    - Every fan-out is independently try/catch-wrapped; a failure logs the error (not PII) and continues; HTTP status of the paid write is unaffected.
    - `single-writer.test.ts` GREEN — the webhook remains the only paid writer; no new status:'paid' write added.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The paid transition fans out confirmation + admin alert + admin/driver in-app notifications, all log-and-continue, with the single paid writer untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: claimed/arrived (driver) + assign/reassign/release/cancel (admin) fan-out</name>
  <files>app/driver/actions.ts, app/admin/transfers/actions.ts</files>
  <read_first>
    - app/driver/actions.ts (claimAction L34-36 — the thin claim wrapper; advanceStatus L73-120 — resolves `arrived` via ALLOWED_TRANSITIONS; the caller-auth vs service-role discipline)
    - app/admin/transfers/actions.ts (assign L106-145 paid→claimed; reassign L157-199; release L208-253 claimed→paid; cancel L260-295 — the gated service-role action skeleton; actingAdminId)
    - app/driver/advance.notify.test.ts (Wave-0 RED — arrived fires the guest arrived email once; en_route fires none)
    - platform/notifications/send-email.ts + templates.ts (assignedEmail with name+phone; arrivedEmail heads-up)
    - platform/notifications/notify.ts (insertNotification — run_assigned/run_reassigned/run_released/run_cancelled + new_paid_pool on release)
    - platform/supabase/admin.ts (createAdminClient — for the narrow {name,phone} driver_profiles read)
    - .planning/phases/07-notifications/07-RESEARCH.md (Pattern 4 hook-point table — claimed/arrived/admin-ops rows; the claimed-by-self note L362 — narrow service-role {name,phone} read in the ACTION not claim.ts; Pitfall 5 — to: is always guest_email)
    - .planning/phases/07-notifications/07-PATTERNS.md (### driver/actions.ts + admin/transfers/actions.ts hook points; to: is the row's guest_email)
  </read_first>
  <action>
    `app/driver/actions.ts`:
    - `claimAction`: after a SUCCESSFUL claim (result.ok), in a log-and-continue block, do a NARROW service-role read of `driver_profiles` `{ name, phone }` for the claiming driver (auth.uid()), read the transfer's `guest_email` + `locale` from the returned row / a narrow service-role read, build via `assignedEmail({ driverName: firstName(name), driverPhone: phone, ...trip }, locale ?? 'en')`, and `sendEmail({ to: guest_email, subject, html, tier: "best_effort", idempotencyKey: \`assigned:${transferId}\` })`. The `to:` is ALWAYS the row's guest_email (Pitfall 5 — never a driver/admin channel). Keep this in the ACTION, NOT in the thin `claim.ts` wrapper (claim.ts stays a pure pass-through). Do NOT change the claim race logic.
    - `advanceStatus`: after a successful advance, when the new status is `arrived`, fire the guest "driver arrived" email (best_effort, `idempotencyKey: \`arrived:${transferId}\``, to = guest_email, via arrivedEmail — a simple heads-up, NO driver info per D-16). When the new status is `en_route` (or any other), fire NO email. Log-and-continue; never roll back the advance write.

    `app/admin/transfers/actions.ts` — add a log-and-continue fan-out after each successful gated write (do NOT alter the gates, zod, guards, or last_action_* audit):
    - `assign` (paid→claimed): guest "driver assigned" email (assignedEmail, name+phone of the assigned driver via narrow service-role read; to = guest_email; idempotencyKey `assigned:${id}`) + assigned driver `run_assigned` insertNotification.
    - `reassign`: the affected driver(s) `run_reassigned` insertNotification (the newly-assigned driver; if the previously-claiming driver differs, notify them too).
    - `release` (claimed→paid): the previously-claiming driver `run_released` insertNotification + a `new_paid_pool` notification to all drivers (the row re-enters the pool).
    - `cancel`: if the row had a driver, that driver `run_cancelled` insertNotification.
    Each notification `title` is the pre-rendered dictionary copy. The guest assigned email is the same builder as claimAction. All wrapped log-and-continue — a notify/send failure NEVER rolls back the lifecycle write.
  </action>
  <verify>
    <automated>npx vitest run app/driver/advance.notify.test.ts app/driver/advance.lifecycle.test.ts app/driver/advance.ownership.test.ts && grep -q "assigned:" app/driver/actions.ts && grep -q "run_released\|run_reassigned\|run_cancelled\|run_assigned" app/admin/transfers/actions.ts && npx vitest run platform/payments/single-writer.test.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `advance.notify.test.ts` GREEN: arrived fires the guest arrived email exactly once; en_route fires no email.
    - `advance.lifecycle.test.ts` + `advance.ownership.test.ts` still GREEN (advance gates unchanged).
    - claimAction + admin assign fire the guest assigned email with driver first name + phone; `to:` is always the row's guest_email (never a driver/admin channel).
    - reassign/release/cancel fire the matching driver notification; release also re-enters the pool (new_paid_pool).
    - The narrow {name,phone} read lives in the action, not claim.ts (claim.ts unchanged).
    - `single-writer.test.ts` GREEN (release remains the only sanctioned non-webhook paid writer; no new paid writer); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>claimed/arrived/admin-ops transitions fan out the correct guest emails + driver notifications, log-and-continue, with the lifecycle/ownership/money gates intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verified webhook → fan-out | the single paid writer fires emails/notifications AFTER the money write; never a second paid writer |
| claim/admin action → guest email | driver name+phone read service-role; revealed ONLY to the row's guest_email |
| send/notify failure → lifecycle write | log-and-continue isolates fan-out failures from the money/lifecycle write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-FO1 | Tampering (money) | second paid writer via the fan-out | mitigate | All fan-out is log-and-continue off existing transitions; confirmation-email.ts + the new blocks write ZERO wp_transfers; single-writer.test.ts grep gate stays green |
| T-07-FO2 | Information Disclosure | driver phone to the wrong recipient | mitigate | assignedEmail `to:` is ALWAYS the row's guest_email (server-read); narrow service-role {name,phone} read; arrived/admin/digest carry no driver PII (Pitfall 5) |
| T-07-FO3 | Denial of Service | fan-out failure rolling back the paid/lifecycle write | mitigate | every send/insert independently try/catch-wrapped; failures console.error the error (not PII) and continue |
| T-07-FO4 | Tampering/duplication | webhook-retry double-send of confirmation/admin-alert | mitigate | stable idempotencyKey per (event, transfer) — `confirm:`/`admin-alert:`/`assigned:`/`arrived:` — checked in email_log before send (Plan 02) |
| T-07-FO5 | Information Disclosure | recipient PII leaking into logs | mitigate | log-and-continue console.error logs the error object, never the recipient address/PII body (V7) |
</threat_model>

<verification>
- `npx vitest run app/driver/ platform/transfers/confirmation.test.ts platform/payments/single-writer.test.ts` — advance.notify/lifecycle/ownership + confirmation + single-writer GREEN.
- `npx vitest run app/admin/drivers/invite.notify.test.ts` GREEN.
- Fan-out blocks present in webhook/driver/admin actions; all log-and-continue.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Every lifecycle transition (paid/claimed/assign/arrived/reassign/release/cancel) and the driver invite drive the correct email + in-app notification — guest assigned carries driver name+phone to the guest only, en_route is silent, the invite is email-only — all log-and-continue, with the single paid writer and the lifecycle/ownership gates untouched.
</success_criteria>

<output>
Create `.planning/phases/07-notifications/07-04-SUMMARY.md` when done.
</output>
