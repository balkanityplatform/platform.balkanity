---
phase: 07-notifications
plan: 05
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - platform/notifications/digest.ts
  - app/driver/settings/page.tsx
  - app/driver/settings/DigestPreferenceCard.tsx
  - app/driver/settings/actions.ts
autonomous: true
requirements: [NOTF-05]

must_haves:
  truths:
    - "buildDigest(driverId) returns the morning snapshot of the claimable pool (masked wp_pool fields) PLUS the driver's own upcoming claimed runs — with ZERO guest PII"
    - "sendDueDigests() is an invokable function that sends digests through sendEmail (best_effort) for opted-in drivers; the time trigger that fires it is Phase 8"
    - "A driver can opt in to the daily digest (off by default) and pick a self-chosen send hour, saved as a driver preference"
    - "The digest preference write is a gated service-role action (no client write RLS policy)"
  artifacts:
    - path: "platform/notifications/digest.ts"
      provides: "buildDigest(driverId) + sendDueDigests() invokable"
      exports: ["buildDigest", "sendDueDigests"]
    - path: "app/driver/settings/DigestPreferenceCard.tsx"
      provides: "opt-in Toggle (off by default) + send-hour Select + save"
      min_lines: 30
    - path: "app/driver/settings/actions.ts"
      provides: "gated service-role saveDigestPreference action"
      exports: ["saveDigestPreference"]
  key_links:
    - from: "platform/notifications/digest.ts"
      to: "wp_pool masked fields + own claimed runs"
      via: "buildDigest reads the masked pool, never guest PII"
      pattern: "wp_pool"
    - from: "app/driver/settings/actions.ts"
      to: "driver_profiles digest columns"
      via: "gated service-role update (recipient = auth.uid())"
      pattern: "digest_enabled"
---

<objective>
Build the driver daily-digest slice: the content builder, the opt-in preference UI, and an INVOKABLE "send due digests" function. `buildDigest(driverId)` assembles a morning snapshot of the currently-claimable pool (the same masked `wp_pool` fields as Phase 5 — date, arrival time, airport, zone, flight no., fare, pax, luggage; NEVER guest PII) PLUS the driver's own upcoming claimed runs for that day. `sendDueDigests()` iterates opted-in drivers and sends through `sendEmail` (best_effort). The preference UI is an off-by-default Toggle + a self-chosen send-hour Select on a driver settings page, saved via a gated service-role action.

Purpose: NOTF-05 in full EXCEPT the time trigger. The cron that fires `sendDueDigests()` at each driver's chosen hour is Phase 8 (Supabase pg_cron + pg_net) — this plan ships the builder, the preference + UI, and the invokable send, with the seam flagged. Turns the digest Wave-0 spec GREEN.
Output: digest.ts, the driver settings page + DigestPreferenceCard + saveDigestPreference action.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-notifications/07-RESEARCH.md
@.planning/phases/07-notifications/07-PATTERNS.md
@.planning/phases/07-notifications/07-UI-SPEC.md
@platform/notifications/send-email.ts
@platform/notifications/templates.ts
</context>

<artifacts_this_phase_produces>
New symbols created by this plan:
- `buildDigest(driverId)` + `sendDueDigests()` — exported from `platform/notifications/digest.ts`
- `app/driver/settings/page.tsx` — driver settings RSC (role-gated) hosting the digest card
- `DigestPreferenceCard` — client island (Toggle + Select + save) in `app/driver/settings/DigestPreferenceCard.tsx`
- `saveDigestPreference(...)` — gated service-role action in `app/driver/settings/actions.ts`
- New route segment `app/driver/settings/`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: digest.ts — buildDigest (masked pool + own runs, zero PII) + sendDueDigests invokable</name>
  <files>platform/notifications/digest.ts</files>
  <read_first>
    - platform/notifications/digest.test.ts (Wave-0 RED — digest includes masked pool fields + own claimed runs, ZERO guest PII keys — this turns GREEN)
    - app/driver/actions.ts (refetchPool — the masked wp_pool() RPC read shape; the 9 pre-claim columns)
    - app/driver/page.tsx (the masked wp_pool() read + the exact 9 non-PII columns — copy the projection)
    - platform/notifications/send-email.ts + templates.ts (sendEmail best_effort + digestEmail builder)
    - platform/supabase/admin.ts (createAdminClient — for the cron-invokable read of opted-in drivers + own runs)
    - .planning/phases/07-notifications/07-RESEARCH.md (D-06 digest content; Pitfall 3 ≤5 req/s sequence-with-delay for multi-driver fan-out; D-08 seam — time trigger is Phase 8)
    - .planning/phases/07-notifications/07-PATTERNS.md (### digest.ts — masked wp_pool read + sendEmail loop)
  </read_first>
  <action>
    Create `platform/notifications/digest.ts` with `import "server-only";` line 1. Export:
    - `buildDigest(driverId: string): Promise<{ poolItems: PoolItem[]; ownRuns: RunItem[] }>` — assemble the snapshot. POOL: read the masked claimable pool — the SAME masked fields the wp_pool RPC exposes (date/arrival/airport/zone/flight_no/fare/pax/luggage). Because sendDueDigests runs without a caller JWT (cron context), read the masked projection via the service-role client BUT select ONLY the 9 non-PII columns explicitly (never guest_name/guest_email/guest_phone/notes/exact address) — the projection IS the PII gate here (mirror the wp_pool column set exactly). OWN RUNS: the driver's own claimed/active transfers for the day (driver_id = driverId, status in the active set) — these legitimately include the trip context the owning driver already has post-claim, but the digest body uses only operational fields, never re-exposes another guest's PII. Return the structured snapshot; do NOT render HTML here (templates.ts owns rendering).
    - `sendDueDigests(): Promise<void>` — the INVOKABLE function (Phase 8 cron calls it). Query opted-in drivers (`driver_profiles.digest_enabled = true`, joined to app_users for the email) whose `digest_send_hour` matches the current hour (the caller/cron passes or the function computes the current hour). For each: `buildDigest(driverId)`, render via `digestEmail({ poolItems, ownRuns }, 'en')`, `sendEmail({ to: driverEmail, subject, html, tier: "best_effort", idempotencyKey: \`digest:${driverId}:${todayUTCdate}\` })`. SEQUENCE the sends with a small delay (~250ms) so the multi-driver fan-out stays ≤5 req/s (Pitfall 3) — NOT Promise.all. Each send is independent (one failure does not abort the loop). FLAG in a header comment: the time trigger that fires this at each driver's chosen hour is Phase 8 (Supabase pg_cron + pg_net) — Phase 7 ships only this invokable function.

    The digest never contains guest PII (digest.test.ts asserts zero PII keys).
  </action>
  <verify>
    <automated>npx vitest run platform/notifications/digest.test.ts && grep -q "sendDueDigests\|buildDigest" platform/notifications/digest.ts && grep -qi "phase 8\|phase8\|cron" platform/notifications/digest.ts && ! grep -q "guest_name\|guest_phone\|guest_email\|notes" platform/notifications/digest.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `digest.test.ts` GREEN: buildDigest output includes the masked pool fields + own runs and ZERO guest-PII keys.
    - `buildDigest` selects ONLY the 9 non-PII pool columns (never guest_name/email/phone/notes/exact address).
    - `sendDueDigests` sends best_effort via sendEmail with a stable per-(driver, day) idempotencyKey, sequenced (≤5 req/s), one-failure-does-not-abort.
    - A header comment FLAGS the Phase 8 cron time-trigger seam.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>buildDigest (masked, zero-PII) + the invokable sendDueDigests exist; digest spec GREEN; the cron time-trigger seam is flagged for Phase 8.</done>
</task>

<task type="auto">
  <name>Task 2: driver settings page + DigestPreferenceCard (opt-in Toggle + send-hour Select) + gated save action</name>
  <files>app/driver/settings/page.tsx, app/driver/settings/DigestPreferenceCard.tsx, app/driver/settings/actions.ts</files>
  <read_first>
    - platform/ui/Toggle.tsx (the opt-in toggle primitive — min-h-[44px], accent-teal checked)
    - platform/ui/Select.tsx (the send-hour Select primitive)
    - platform/ui/Card.tsx (p-[24px] card surface), platform/ui/Button.tsx (save CTA), platform/ui/Toast.tsx (neutral save toast)
    - app/admin/transfers/actions.ts (the gated service-role action skeleton — re-gate then service-role write; zod at the boundary)
    - app/driver/page.tsx (the role-gated RSC + warm-light chrome + dictionary prop-bag pattern to mirror for the settings page)
    - platform/supabase/server.ts + admin.ts (caller-auth identity check then service-role write)
    - .planning/phases/07-notifications/07-UI-SPEC.md (### Driver daily-digest preference UI copy keys; Interaction Contracts — Toggle OFF by default, Select disabled until on; Component Inventory — Card+Toggle+Select+Button+Toast)
  </read_first>
  <action>
    `app/driver/settings/page.tsx` — role-gated RSC (`getCurrentRole()` !== 'driver' → redirect /sign-in; never getSession). Read the driver's current digest preference (digest_enabled, digest_send_hour) via the caller-auth client (own row) or a narrow service-role read keyed to auth.uid(). Resolve the digest copy keys server-side (digestPrefTitle, digestPrefBody, digestEnableLabel, digestTimeLabel, digestSaveCta, digestSavedToast, digestSaveFailed). Render the warm-light driver chrome (white header + logo chip + LanguageToggle — mirror RunView header) and `<DigestPreferenceCard initial={{enabled, hour}} lang={lang} copy={...} />`.

    `app/driver/settings/DigestPreferenceCard.tsx` — `"use client"` island. A `Card` (p-[24px]) with heading digestPrefTitle + body digestPrefBody. A `Toggle` labelled digestEnableLabel, OFF by default (initial false), accent-teal checked. A `Select` of whole-hour options (00:00–23:00) labelled digestTimeLabel — DISABLED/ignored until the toggle is on. A `Button` save CTA (digestSaveCta) calling `saveDigestPreference`. A neutral `Toast` (digestSavedToast) on success; a coral Toast (digestSaveFailed) only on genuine failure. All hit targets ≥44px (Toggle/Button/Select already enforce min-h-[44px]).

    `app/driver/settings/actions.ts` — `"use server"`. Export `saveDigestPreference(prev, formData)` as a GATED SERVICE-ROLE action: zod-validate `{ enabled: boolean, hour: 0..23 (required only when enabled) }` at the boundary; caller identity via the caller-auth client (`auth.getUser()` + getCurrentRole()==='driver'); then service-role UPDATE `driver_profiles` SET `digest_enabled`, `digest_send_hour` WHERE `user_id = user.id` (the auth.uid() scope is the gate — never a client-supplied id). NO client write RLS policy. Return a typed state mirroring the other action-state shapes; generic dictionary-keyed errors only.
  </action>
  <verify>
    <automated>grep -q "getCurrentRole" app/driver/settings/page.tsx && grep -q "digest_enabled" app/driver/settings/actions.ts && grep -q '"use server"' app/driver/settings/actions.ts && grep -q "Toggle\|Select" app/driver/settings/DigestPreferenceCard.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The settings page is driver-role-gated server-side (getCurrentRole, never getSession).
    - The Toggle is OFF by default; the send-hour Select is disabled/ignored until the toggle is on.
    - saveDigestPreference is a gated service-role action scoping the UPDATE to user_id = auth.uid(); no client write RLS policy.
    - Save success shows the neutral digestSavedToast; genuine failure shows coral digestSaveFailed.
    - `npx tsc --noEmit` clean; all hit targets ≥44px; copy from the dictionary (no hard-coded strings).
  </acceptance_criteria>
  <done>A driver can opt in (off by default) and pick a send hour on a role-gated settings page, saved via a gated service-role action with no client write policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/invokable sendDueDigests → drivers | runs without a caller JWT; the explicit non-PII column projection is the gate |
| driver browser → saveDigestPreference | caller identity (auth.uid()) is the row-scope gate on the preference write |
| digest content → driver inbox | only masked pool fields + the driver's own runs; never another guest's PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-DG1 | Information Disclosure | guest PII leaking into the digest | mitigate | buildDigest selects ONLY the 9 masked wp_pool columns (never guest_name/email/phone/notes/exact address); digest.test.ts asserts zero PII keys |
| T-07-DG2 | Tampering | client setting another driver's preference | mitigate | saveDigestPreference scopes the UPDATE to user_id = auth.uid() (gated service-role); no client write RLS policy |
| T-07-DG3 | Denial of Service (cap/rate) | multi-driver digest fan-out exceeding 5 req/s or the cap | mitigate | sequenced sends (~250ms) not Promise.all (Pitfall 3); best_effort tier → soft-capped by sendEmail (D-10); stable per-(driver,day) idempotencyKey prevents double-send |
| T-07-DG4 | (correctness) | digest fired without a Phase 8 cron | accept | Phase 7 ships only the invokable function; the time trigger is Phase 8 — flagged in a header comment (D-08 seam) |
</threat_model>

<verification>
- `npx vitest run platform/notifications/digest.test.ts` GREEN.
- digest.ts contains no guest-PII column references; the Phase 8 cron seam is flagged.
- saveDigestPreference is gated service-role (user_id = auth.uid()); Toggle off by default.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- A driver can opt in to a daily digest (off by default) at a self-chosen hour; buildDigest assembles a zero-PII masked snapshot + the driver's own runs; sendDueDigests is an invokable best_effort, rate-safe send — with the time trigger flagged for Phase 8.
</success_criteria>

<output>
Create `.planning/phases/07-notifications/07-05-SUMMARY.md` when done.
</output>
