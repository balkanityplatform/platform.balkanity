---
phase: 07-notifications
plan: 06
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - .planning/phases/07-notifications/07-GATES-EVIDENCE.md
autonomous: false
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-05, NOTF-06]

must_haves:
  truths:
    - "Migration 0007 is applied LIVE to the Balkanity Supabase project (ref qyhdogajtmnvxphrslwm) — notifications + email_log tables + wp_transfers.locale + digest columns exist on the live DB"
    - "The send.balkanity.com subdomain is verified in Resend and RESEND_API_KEY is set server-only in Vercel production"
    - "A real verified email (confirmation or invite) sends from noreply@send.balkanity.com to a real inbox (D-15 completion gate)"
    - "The full notification fan-out works end-to-end against the live schema (booking → confirmation + admin alert + bell; claim → assigned email; arrived email)"
  artifacts:
    - path: ".planning/phases/07-notifications/07-GATES-EVIDENCE.md"
      provides: "evidence of the live apply, verified subdomain, and live-delivery UAT"
      contains: "qyhdogajtmnvxphrslwm"
  key_links:
    - from: "supabase/migrations/0007_notifications.sql"
      to: "Balkanity live DB (qyhdogajtmnvxphrslwm)"
      via: "Supabase Management API /database/query (never MCP, never db push)"
      pattern: "qyhdogajtmnvxphrslwm"
---

<objective>
Close Phase 7: apply migration `0007` LIVE to the Balkanity Supabase project, complete the runtime-state items code alone cannot cover (the verified `send.balkanity.com` Resend subdomain + the `RESEND_API_KEY` Vercel env), and prove real email delivery (the D-15 completion gate). Then run the end-to-end notification fan-out against the live schema and record evidence.

Purpose: The phase CANNOT pass verification without the live schema (build/type checks pass without the push because types come from config, not the live DB — a false-positive verification state). And per D-14/D-15, the invite path no longer has a manual fallback, so the phase is not "done" until verified emails actually deliver from the verified subdomain.
Output: live `0007` applied, verified subdomain + env, a real delivered email, and `07-GATES-EVIDENCE.md`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-notifications/07-RESEARCH.md
@supabase/migrations/0007_notifications.sql
@.planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md
</context>

<artifacts_this_phase_produces>
- `.planning/phases/07-notifications/07-GATES-EVIDENCE.md` — the live-apply + verified-subdomain + live-delivery evidence record (no new code symbols; this is the operational gate plan)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [USER] Verify send.balkanity.com subdomain in Resend + confirm RESEND_API_KEY in Vercel</name>
  <read_first>
    - .planning/phases/07-notifications/07-RESEARCH.md (## Environment Availability — DNS records: MX host `send` → feedback-smtp.<region>.amazonses.com pri 10, SPF TXT host `send` → v=spf1 include:amazonses.com ~all, DKIM TXT host resend._domainkey; A1 — exact values are region/domain-specific, shown in the Resend dashboard — copy verbatim; A2 — free tier 1 verified domain, confirm in dashboard)
    - CLAUDE.md (Resend subdomain pattern; RESEND_API_KEY server-only never NEXT_PUBLIC_)
    - .planning/STATE.md (Handoff — current sender is the test sender onboarding@resend.dev, delivers only to balkanityplatform@gmail.com)
  </read_first>
  <files>(manual provider/DNS config — no repo file; the outcome is recorded in .planning/phases/07-notifications/07-GATES-EVIDENCE.md)</files>
  <what-built>Code is complete (Plans 01-05). This step covers the two runtime states a code-only change cannot: the Resend sending-domain verification (live provider config, not in git) and the Vercel env var. These are manual because adding DNS records and verifying a domain in the Resend dashboard has no CLI/API the executor can drive end-to-end.</what-built>
  <how-to-verify>
    1. In the Resend dashboard, add the domain `send.balkanity.com` and copy the exact DNS records shown (an MX on host `send`, an SPF TXT on host `send`, a DKIM TXT on host `resend._domainkey`) — values are region-specific; copy them verbatim. Add those records to the `balkanity.com` DNS zone. Wait for Resend to show the domain as Verified. Confirm the free tier allows this 1 domain (A2).
    2. Confirm `RESEND_API_KEY` is set in Vercel production env as a server-only var (NOT a `NEXT_PUBLIC_` name).
    3. Reply with the verified-domain status (a screenshot/text confirmation) and confirmation the env var is set.
  </how-to-verify>
  <action>This is a human-only step (no CLI/API drives Resend domain verification end-to-end). On "verified", the executor records the verified sender (`noreply@send.balkanity.com`), the verified-domain status, and the confirmed `RESEND_API_KEY` env presence in 07-GATES-EVIDENCE.md. Do NOT proceed to the live apply (Task 2) until the domain shows Verified.</action>
  <verify>
    <automated>grep -qi "send.balkanity.com" .planning/phases/07-notifications/07-GATES-EVIDENCE.md 2>/dev/null && echo OK || echo "pending user verification — evidence not yet recorded"</automated>
  </verify>
  <acceptance_criteria>
    - send.balkanity.com shows Verified in the Resend dashboard (SPF/DKIM/MX records added to balkanity.com DNS).
    - RESEND_API_KEY is set server-only in Vercel production (not a NEXT_PUBLIC_ name).
    - The verified sender + env status are recorded in 07-GATES-EVIDENCE.md.
  </acceptance_criteria>
  <done>send.balkanity.com shows Verified in Resend; RESEND_API_KEY is set server-only in Vercel production; both recorded in 07-GATES-EVIDENCE.md.</done>
  <resume-signal>Type "verified" once send.balkanity.com shows Verified in Resend and RESEND_API_KEY is set in Vercel prod, or describe the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Apply migration 0007 LIVE to Balkanity (Management API) — sign-off gated</name>
  <read_first>
    - supabase/migrations/0007_notifications.sql (the exact DDL to apply — re-read in full before applying)
    - .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md (the prior live-apply runbook: Management API /database/query with SUPABASE_ACCESS_TOKEN, ref guardrail re-assertion, residue-free apply)
    - .planning/STATE.md memory refs (Management/REST APIs reach Balkanity; live DB psql pooler path; use CLI/Management token, NOT MCP)
    - CLAUDE.md (schema FLAGGED/irreversible — sign-off before the migration; Balkanity ref qyhdogajtmnvxphrslwm ONLY, NEVER Kalvia utyatpadtibqqswsfvtr)
  </read_first>
  <files>.planning/phases/07-notifications/07-GATES-EVIDENCE.md (applies supabase/migrations/0007_notifications.sql to the live DB; writes the apply evidence here — no repo source file is modified)</files>
  <what-built>Migration 0007 (notifications + email_log + wp_transfers.locale + driver_profiles digest columns) is authored in Plan 01 but NOT applied. This task applies it LIVE to Balkanity after sign-off. Schema is flagged/irreversible per the brief.</what-built>
  <how-to-verify>
    BEFORE applying: re-assert the target ref is `qyhdogajtmnvxphrslwm` (Balkanity) and confirm Kalvia (`utyatpadtibqqswsfvtr`) is NOT the target — if any tool reports Kalvia, STOP. Apply 0007 via the Supabase Management API `/database/query` with `SUPABASE_ACCESS_TOKEN` (NEVER MCP — it reaches only Kalvia; NEVER `supabase db push`; NEVER a direct host psql DDL run unless via the documented session-pooler path). After applying, verify on the live DB:
    1. `notifications` + `email_log` tables exist; `notifications` has exactly the `notifications_own_read` SELECT policy and NO write policy; `email_log` has the admin-read SELECT policy + the UNIQUE index on `idempotency_key` and NO write policy.
    2. `wp_transfers.locale` column exists (NULL-able); `driver_profiles.digest_enabled` (default false) + `digest_send_hour` exist.
    3. The no-write-policy lock on wp_transfers is intact (only SELECT policies); the single-writer money lock is unaffected.
    4. No residue/temp objects left behind.
    Record the applied evidence (table list, policy list, column list) in 07-GATES-EVIDENCE.md.
  </how-to-verify>
  <action>On "approved", apply `supabase/migrations/0007_notifications.sql` to Balkanity (ref qyhdogajtmnvxphrslwm) via the Supabase Management API /database/query with SUPABASE_ACCESS_TOKEN, re-asserting the ref guardrail first. Then run the live verification queries (table/policy/column existence) and write the results to 07-GATES-EVIDENCE.md. Never target Kalvia.</action>
  <verify>
    <automated>grep -q "qyhdogajtmnvxphrslwm" .planning/phases/07-notifications/07-GATES-EVIDENCE.md && grep -qi "notifications\|email_log\|locale\|digest_enabled" .planning/phases/07-notifications/07-GATES-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 0007 applied to Balkanity (ref qyhdogajtmnvxphrslwm) via Management API — NOT MCP, NOT db push.
    - Live DB shows notifications + email_log tables, the own_read / admin_read SELECT policies (no write policy), email_log UNIQUE idempotency_key index, wp_transfers.locale, and the two driver_profiles digest columns.
    - The wp_transfers no-write-policy lock + single-writer money lock are intact.
    - Evidence recorded in 07-GATES-EVIDENCE.md; no residue left.
  </acceptance_criteria>
  <done>Migration 0007 is applied LIVE to Balkanity (qyhdogajtmnvxphrslwm); the live tables/policies/columns are verified and the locks intact; apply evidence recorded in 07-GATES-EVIDENCE.md.</done>
  <resume-signal>Type "approved" to apply 0007 LIVE to Balkanity (qyhdogajtmnvxphrslwm), or describe a concern.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [GATE] Live email delivery + end-to-end fan-out UAT (D-15 completion gate)</name>
  <read_first>
    - .planning/phases/07-notifications/07-RESEARCH.md (### Validation Architecture — Phase gate: full unit suite green + D-15 live-delivery UAT; D-15 — verified emails actually send to real recipients)
    - app/admin/drivers/actions.ts (the email-only invite path — no inline fallback now, D-14)
    - platform/transfers/confirmation-email.ts (the un-stubbed confirmation)
    - .planning/STATE.md (the open 02-05 invite walkthrough UAT — this gate also closes it end-to-end with a real email)
  </read_first>
  <files>.planning/phases/07-notifications/07-GATES-EVIDENCE.md (records the live-delivery + end-to-end fan-out UAT results — no repo source file is modified)</files>
  <what-built>The full notification system is code-complete and the live schema is applied. This gate proves real delivery from the verified subdomain (D-15 — the invite path has no manual fallback, so delivery MUST be verified) and the end-to-end fan-out against the live DB.</what-built>
  <how-to-verify>
    On the live deployment (balkanityplatformproject.vercel.app) with the verified send.balkanity.com sender:
    1. INVITE (NOTF-04/D-14): admin invites a driver → confirm a real invite email arrives from noreply@send.balkanity.com with a working set-password link (closes the open 02-05 walkthrough). Confirm NO inline action_link is shown in the UI.
    2. BOOKING → PAID (NOTF-02/03): complete a test booking → after the verified webhook flips it to paid, confirm the guest receives the confirmation email AND the admin receives the booking-alert email AND the admin bell shows a new_paid_booking AND drivers' bells show new_paid_pool.
    3. CLAIM (NOTF-02/D-16): a driver claims → confirm the guest receives the "driver assigned" email carrying the driver's first name + phone, and the assigned driver's bell shows run_assigned.
    4. ARRIVED: driver advances to arrived → confirm the guest receives the "driver arrived" heads-up email; advancing to en_route sends NO email.
    5. BELL: confirm the bell unread count, mark-read-on-open, and Mark-all-read work for both a driver and the admin; the feed polls live (focus refresh).
    6. CAP/DIGEST (NOTF-05/06): confirm a driver can opt into the digest at a chosen hour and that buildDigest/sendDueDigests is invokable (manual invoke is fine — the cron trigger is Phase 8); confirm best-effort emails log to email_log.
    Record pass/fail per item in 07-GATES-EVIDENCE.md.
  </how-to-verify>
  <action>On approval/UAT completion, record the live-delivery + end-to-end fan-out results (pass/fail per item, the verified sender address, the delivered-email evidence) in 07-GATES-EVIDENCE.md. If any item fails, surface it as a gap for a follow-up gap-closure plan rather than marking the phase done.</action>
  <verify>
    <automated>npx vitest run && grep -qi "delivered\|verified\|noreply@send.balkanity.com\|D-15" .planning/phases/07-notifications/07-GATES-EVIDENCE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A real email delivered from noreply@send.balkanity.com to a real inbox (D-15 gate met).
    - The invite path delivered email-only with a working set-password link and NO inline reveal (closes 02-05 UAT).
    - The booking/claim/arrived fan-out + the bell behaviors all verified against the live schema; en_route silent.
    - Full unit suite (`npx vitest run`) GREEN.
    - Results recorded in 07-GATES-EVIDENCE.md.
  </acceptance_criteria>
  <done>A real email delivered from the verified subdomain (D-15 met), the invite is email-only with a working link, the booking/claim/arrived/bell fan-out is verified end-to-end against the live schema, the unit suite is GREEN, and results are recorded in 07-GATES-EVIDENCE.md.</done>
  <resume-signal>Type "approved" once a real email delivered from the verified subdomain and the end-to-end fan-out UAT passed, or list the failing items.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Management token → Balkanity live DB | the only sanctioned live-DDL path (Management API, never MCP/Kalvia) |
| Resend dashboard / DNS → live send identity | provider config not in git; the verified subdomain is the delivery trust anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-APPLY1 | Elevation of Privilege | applying DDL to the wrong project (Kalvia) | mitigate | Re-assert ref qyhdogajtmnvxphrslwm before apply; STOP if any tool reports Kalvia (utyatpadtibqqswsfvtr); Management API only, never MCP |
| T-07-APPLY2 | Tampering | unreviewed flagged schema reaching prod | mitigate | autonomous:false — blocking human sign-off before the live apply (schema is flagged/irreversible per the brief) |
| T-07-APPLY3 | Spoofing (email) | unverified sender landing in spam / spoofable | mitigate | verified send.balkanity.com subdomain (SPF/DKIM via Resend) is the D-15 completion gate before go-live |
| T-07-APPLY4 | Information Disclosure | live UAT leaking real guest PII | accept | UAT uses test bookings; the assigned email's driver phone goes only to the (test) guest_email per the established boundary |
</threat_model>

<verification>
- 0007 applied LIVE to Balkanity (qyhdogajtmnvxphrslwm); tables/policies/columns verified on the live DB; locks intact.
- send.balkanity.com verified in Resend; RESEND_API_KEY set server-only in Vercel.
- A real email delivered from the verified subdomain (D-15).
- `npx vitest run` GREEN; end-to-end fan-out verified; evidence in 07-GATES-EVIDENCE.md.
</verification>

<success_criteria>
- Migration 0007 is live on Balkanity, the send.balkanity.com subdomain is verified, a real email delivers from noreply@send.balkanity.com, and the booking/claim/arrived/invite fan-out + bell all work end-to-end against the live schema — with evidence recorded.
</success_criteria>

<output>
Create `.planning/phases/07-notifications/07-06-SUMMARY.md` when done.
</output>
