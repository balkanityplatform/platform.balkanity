---
phase: 08-platform-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0008_platform_health.sql
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
  - platform/health/reconcile.test.ts
  - platform/health/stuck.test.ts
  - app/api/cron/health/route.test.ts
  - app/api/cron/digest/route.test.ts
  - app/admin/health/EmailCapGauge.test.tsx
autonomous: true
requirements: [HLTH-02, HLTH-03, HLTH-04, HLTH-05]
must_haves:
  truths:
    - "supabase/migrations/0008_platform_health.sql exists, is FLAGGED/authored-not-applied, and creates pg_cron + pg_net extensions, the health_events table, and two idempotent cron schedules"
    - "Wave-0 RED specs exist for reconciliation detection, stuck predicate, both cron-route auth gates, and the email-cap gauge — all referenced by later plans' verify commands"
    - "Every Phase-8 EN copy key from 08-UI-SPEC.md exists in en.ts with a BG parity key in bg.ts (tsc Dict-parity gate stays green)"
  artifacts:
    - path: "supabase/migrations/0008_platform_health.sql"
      provides: "health_events table + pg_cron/pg_net extensions + 2 cron schedules (FILE ONLY)"
      contains: "create table if not exists public.health_events"
    - path: "platform/health/reconcile.test.ts"
      provides: "HLTH-02 RED contract: detection, lookback no-false-positive, dedup"
    - path: "platform/health/stuck.test.ts"
      provides: "HLTH-04 RED contract: predicate + in-app-only (no sendEmail)"
    - path: "app/api/cron/health/route.test.ts"
      provides: "x-cron-secret auth gate RED contract (401, zero work)"
    - path: "app/api/cron/digest/route.test.ts"
      provides: "digest route auth gate RED contract"
    - path: "app/admin/health/EmailCapGauge.test.tsx"
      provides: "HLTH-03 RED contract: count + threshold states"
  key_links:
    - from: "0008_platform_health.sql"
      to: "is_admin() (from 0002)"
      via: "health_events_admin_read SELECT policy reuses is_admin(), never redefines it"
      pattern: "using \\( public\\.is_admin\\(\\) \\)"
    - from: "platform/i18n/bg.ts"
      to: "platform/i18n/en.ts Dict type"
      via: "tsc Dict-parity gate"
      pattern: "healthTitle|emailCapLabel|stuckHeading|reconHeading"
---

<objective>
Lay the Phase-8 foundation as one shared, non-overlapping slice: author the FLAGGED migration `0008_platform_health.sql` (file only — extensions, `health_events` table, two idempotent cron schedules), land all Wave-0 Nyquist RED specs that the three implementation slices turn GREEN, and add every Phase-8 EN/BG dictionary key so the parity gate stays green when widgets import copy.

Purpose: Phase 8 is wiring existing seams to a scheduler. This plan creates the single migration file every live-apply depends on, the test baseline that makes each later slice verifiable, and the copy keys the admin UI consumes — with zero file overlap so Wave 2 slices run in parallel.
Output: 1 migration file (authored-not-applied), 5 RED spec files, EN+BG dictionary additions.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-platform-health/08-CONTEXT.md
@.planning/phases/08-platform-health/08-RESEARCH.md
@.planning/phases/08-platform-health/08-PATTERNS.md
@.planning/phases/08-platform-health/08-UI-SPEC.md

@supabase/migrations/0007_notifications.sql
@platform/notifications/send-email.ts
@platform/notifications/digest.ts
@platform/i18n/en.ts
@platform/i18n/dictionary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0008_platform_health.sql (FILE ONLY — extensions + health_events + cron schedules)</name>
  <files>supabase/migrations/0008_platform_health.sql</files>
  <read_first>
    - supabase/migrations/0007_notifications.sql — copy the FLAGGED header verbatim style (lines 1-9): authored-not-applied, Balkanity ref qyhdogajtmnvxphrslwm ONLY, NEVER Kalvia (utyatpadtibqqswsfvtr), live apply via Management API /database/query (NOT MCP, NOT `supabase db push`); copy the table + RLS + re-runnability conventions (lines 56-66, 84-103, 140-156)
    - .planning/phases/08-platform-health/08-PATTERNS.md — the `0008_platform_health.sql` section (health_events shape, RLS pattern, NEW extensions + cron schedule block)
    - .planning/phases/08-platform-health/08-RESEARCH.md — §Code Examples (health_events DDL, idempotent reschedule block) and §Pattern 1 (cron.schedule + net.http_post + vault.decrypted_secrets shape)
  </read_first>
  <action>
    Create `supabase/migrations/0008_platform_health.sql`, FILE-ONLY (authored, NOT applied — live apply is Plan 05). Mirror the 0007 FLAGGED header (re-assert: Balkanity ref `qyhdogajtmnvxphrslwm` ONLY, NEVER Kalvia `utyatpadtibqqswsfvtr`; apply via Supabase Management API `/database/query` with `SUPABASE_ACCESS_TOKEN`; NOT MCP, NOT `supabase db push`).

    Statements in order:
    1. `create extension if not exists pg_cron;` and `create extension if not exists pg_net;` (both verified available-but-not-installed on Balkanity: pg_cron 1.6.4, pg_net 0.20.3). These MUST be the first DDL — `cron.schedule` errors with "schema cron does not exist" otherwise (Pitfall 6).
    2. `create table if not exists public.health_events` — UNPREFIXED (platform-generic, PLAT-01). Columns: `id uuid primary key default gen_random_uuid()`, `kind text not null` (values `'reconciliation_discrepancy'` | `'stuck_unclaimed'`), `entity_type text` (polymorphic — NO `transfer_id` column, SC#1), `entity_id text` (text, not uuid — Stripe event ids are not uuid), `detail jsonb`, `resolved_at timestamptz` (NULL = open), `created_at timestamptz not null default now()`. Add a `comment on table` mirroring 0007's documentation discipline.
    3. `create index if not exists health_events_open_idx on public.health_events (kind, entity_id) where resolved_at is null;` — the partial index that backs the per-entity dedup check (Pitfall 2).
    4. `alter table public.health_events enable row level security;` then `drop policy if exists "health_events_admin_read"` + `create policy "health_events_admin_read" ... for select to authenticated using ( public.is_admin() );`. Reuse 0002's `is_admin()` — do NOT redefine it. NO INSERT/UPDATE/DELETE policy → service-role-only writes (the no-write-policy lock holds).
    5. Cron secret + idempotent re-schedule block: a commented one-time `select vault.create_secret('REPLACE_WITH_LONG_RANDOM','cron_secret');` note (the real value is set at live-apply, Plan 05), then `select cron.unschedule('health-sweep') where exists (select 1 from cron.job where jobname='health-sweep');` and the same for `'digest-hourly'`, then the two `cron.schedule(...)` calls. The 15-min job `'health-sweep'` `'*/15 * * * *'` (D-02) and the hourly `'digest-hourly'` `'0 * * * *'` (D-10), each running `select net.http_post(url := 'https://balkanityplatformproject.vercel.app/api/cron/health' | '/api/cron/digest', headers := jsonb_build_object('Content-Type','application/json','x-cron-secret',(select decrypted_secret from vault.decrypted_secrets where name='cron_secret')), body := '{}'::jsonb);`.

    The production receiver URL is `https://balkanityplatformproject.vercel.app` (STATE handoff, A4). Add a comment that net.http_post is async fire-and-forget (Pitfall 4): observability is `health_events` rows + `cron.job_run_details`, not the cron return.

    Schema is FLAGGED — sign-off happens at the Plan 05 live-apply gate, not here. This task only writes the file.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0008_platform_health.sql && grep -q "create table if not exists public.health_events" supabase/migrations/0008_platform_health.sql && grep -q "create extension if not exists pg_cron" supabase/migrations/0008_platform_health.sql && grep -q "create extension if not exists pg_net" supabase/migrations/0008_platform_health.sql && grep -q "using ( public.is_admin() )" supabase/migrations/0008_platform_health.sql && grep -q "qyhdogajtmnvxphrslwm" supabase/migrations/0008_platform_health.sql && grep -q "health-sweep" supabase/migrations/0008_platform_health.sql && grep -q "digest-hourly" supabase/migrations/0008_platform_health.sql && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the 0007-style FLAGGED header naming Balkanity ref `qyhdogajtmnvxphrslwm` and explicitly forbidding Kalvia + `db push` + MCP.
    - `create extension if not exists pg_cron;` and `create extension if not exists pg_net;` are present and precede any `cron.schedule`.
    - `health_events` has `kind`, `entity_type`, `entity_id text`, `detail jsonb`, `resolved_at`, `created_at`, NO `transfer_id` column.
    - `health_events_open_idx` partial index `where resolved_at is null` exists.
    - RLS enabled; exactly one SELECT policy `using ( public.is_admin() )`; ZERO INSERT/UPDATE/DELETE policies (grep finds no `for insert|for update|for delete` on health_events).
    - Two `cron.schedule` calls (`health-sweep` `*/15`, `digest-hourly` `0 *`) each preceded by an `unschedule … where exists` guard; both reference `vault.decrypted_secrets` for `x-cron-secret`; both target `balkanityplatformproject.vercel.app`.
    - File is NOT applied to any live DB (no Management-API call in this task).
  </acceptance_criteria>
  <done>Migration 0008 authored as a file; grep gate passes; live apply deferred to Plan 05.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 RED specs (reconcile, stuck, both cron routes, email-cap gauge)</name>
  <files>platform/health/reconcile.test.ts, platform/health/stuck.test.ts, app/api/cron/health/route.test.ts, app/api/cron/digest/route.test.ts, app/admin/health/EmailCapGauge.test.tsx</files>
  <read_first>
    - platform/notifications/send-email.test.ts — the admin-client mock fixture pattern (vi.mock of `@/platform/supabase/admin` createAdminClient) Phase-7 used; reuse it for the service-role read mocks
    - platform/payments/single-writer.test.ts — confirm ROOTS already includes `app` and `platform` so the gate auto-covers `app/api/cron/*` and `platform/health/*` (no edit needed; Task documents this)
    - .planning/phases/08-platform-health/08-RESEARCH.md — §Validation Architecture "Phase Requirements → Test Map" + "Wave 0 Gaps" (the exact -t test names later plans grep)
    - platform/payments/stripe.ts — `getStripe()` is the server-only client the reconcile detection lists Checkout Sessions from (D-03 Stripe-API source of truth); the test mocks it
  </read_first>
  <action>
    Author 5 Wave-0 RED specs. Each uses runtime-string dynamic import + typed cast so the file tsc-cleans before the impl exists while staying RED at runtime (the established Nyquist baseline pattern from prior phases). Mock `@/platform/supabase/admin` (createAdminClient) and `@/platform/payments/stripe` (getStripe) so no live network/DB.

    `platform/health/reconcile.test.ts` (HLTH-02):
    - test "detection" — a Stripe paid Checkout Session with NO matching `wp_transfers` paid row → reconcile() yields exactly one discrepancy for that session/transfer.
    - test "lookback" — a paid session inside the ~10-min in-flight window is NOT flagged (Pitfall 1 no-false-positive).
    - test "dedup" — when an OPEN `health_events` row already exists for `(kind='reconciliation_discrepancy', entity_id)`, reconcile() fires NO new alert (Pitfall 2; single alert per discrepancy).
    - test "never-writes-paid" — assert (source-grep over `platform/health/reconcile.ts`, comment-stripped) that the file contains no `status:'paid'` write (belt; the single-writer gate is the authority).

    `platform/health/stuck.test.ts` (HLTH-04):
    - test predicate — a `wp_transfers` row `status='paid' AND driver_id IS NULL AND arrival_at <= now()+12h` is flagged; a claimed row and a paid row with arrival >12h away are NOT (D-04).
    - test "in-app-only" — source-grep over `platform/health/stuck.ts` (comment-stripped) asserts ZERO `sendEmail(` occurrences (D-05).

    `app/api/cron/health/route.test.ts` and `app/api/cron/digest/route.test.ts`:
    - test "unauthorized" — POST without a valid `x-cron-secret` returns 401 and performs ZERO work (the mocked reconcile/sendDueDigests is never called). Set `process.env.CRON_SECRET` in the test; send wrong/missing header.
    - test "authorized" — correct `x-cron-secret` reaches the worker (mock asserts it was invoked) and returns 200.

    `app/admin/health/EmailCapGauge.test.tsx` (HLTH-03):
    - test threshold states — given a daily 'sent' count, the gauge resolves state `ok` (< cap-10, i.e. <80), `warning` (>= cap-10, 80-89), `at-cap` (>= cap, >=90) with `cap` default 90 (the SAME `EMAIL_SOFT_CAP` constant, D-07). Assert the worded label + the "{sent} / {cap}" figure render (08-UI-SPEC copy keys).

    Run the suite and CONFIRM these are RED (impls do not exist yet). Add `app/api/cron/digest/route.test.ts` `authorized` test to also assert `sendDueDigests` (mocked) is awaited.
  </action>
  <verify>
    <automated>npm run test -- platform/health/reconcile.test.ts platform/health/stuck.test.ts app/api/cron/health/route.test.ts app/api/cron/digest/route.test.ts app/admin/health/EmailCapGauge.test.tsx 2>&1 | grep -Eq "fail|FAIL|RED|No test files|Cannot find" && echo "RED-AS-EXPECTED" ; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 5 spec files exist and are discovered by `vitest run` (not "No test files").
    - The specs are RED (the implementations they import do not exist yet) — this is the intended Wave-0 baseline.
    - `npm run typecheck` passes (runtime-string imports + typed casts keep tsc clean).
    - reconcile.test.ts has named tests matching `-t lookback` and `-t dedup` (later plans grep these exact names).
    - stuck.test.ts asserts no `sendEmail` in stuck.ts (source-grep).
    - both cron route tests have a `-t unauthorized` test asserting 401 + zero work.
    - EmailCapGauge.test.tsx asserts ok/warning/at-cap states at the 80/90 boundaries with default cap 90.
  </acceptance_criteria>
  <done>Five RED Wave-0 specs land; typecheck green; the implementations in Plans 02-04 turn them GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Add all Phase-8 EN + BG dictionary keys (Dict-parity gate)</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts</files>
  <read_first>
    - platform/i18n/en.ts — the `as const` object + `export type Dict` (tail); append keys before the closing `} as const;`
    - platform/i18n/bg.ts — the BG object the Dict type checks for parity; every EN key needs a BG counterpart or tsc fails
    - platform/i18n/dictionary.test.ts — the parity gate that fails on a missing/mistyped key
    - .planning/phases/08-platform-health/08-UI-SPEC.md — §Copywriting Contract (the exact EN source strings + key names)
  </read_first>
  <action>
    Add the Phase-8 health copy keys to `en.ts` (EN source, verbatim from 08-UI-SPEC §Copywriting Contract) and the BG parity keys to `bg.ts` (translated). Keys + EN values:
    `healthTitle` "Platform health"; `emailCapLabel` "Emails sent today"; `emailCapWarning` "Approaching daily cap"; `emailCapAtCap` "Daily cap reached — non-critical emails paused"; `emailCapZero` "No emails sent yet today."; `stuckHeading` "Stuck transfers"; `stuckBadge` "UNCLAIMED"; `stuckEmptyHeading` "No stuck transfers"; `stuckEmptyBody` "Every paid transfer has a driver or has time to spare. Nothing needs chasing right now."; `reconHeading` "Payment reconciliation"; `reconBadge` "NEEDS REVIEW"; `reconEmptyHeading` "All payments reconciled"; `reconEmptyBody` "Every paid Stripe session has a matching paid transfer. No discrepancies to investigate."; `healthResolveCta` "Mark resolved"; `healthLoadFailed` "Couldn't load health data. Refresh to try again."

    Provide natural BG translations for each in `bg.ts` (values widen to `string`, so BG strings differ from EN while a missing key still fails tsc). Do NOT add inline strings anywhere in the widget files — all copy flows through these keys.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- platform/i18n/dictionary.test.ts && grep -q "healthTitle" platform/i18n/en.ts && grep -q "healthTitle" platform/i18n/bg.ts && grep -q "reconEmptyBody" platform/i18n/bg.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All 15 health keys above exist in en.ts with the exact EN source strings from 08-UI-SPEC.
    - Every one has a BG parity key in bg.ts (BG text, not the EN string).
    - `npm run typecheck` passes (Dict parity holds).
    - dictionary.test.ts passes.
  </acceptance_criteria>
  <done>EN + BG health keys land; parity gate + typecheck green; widgets in Plan 03 consume them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live DB | The authored 0008 DDL crosses to the live Balkanity DB ONLY at the Plan-05 signed-off live-apply — never in this plan |
| dictionary keys → UI | Copy keys are static EN/BG strings; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | 0008 migration accidentally applied to Kalvia | mitigate | FLAGGED header names Balkanity ref `qyhdogajtmnvxphrslwm` ONLY + forbids Kalvia/db push/MCP; this plan only authors the file (no apply) |
| T-08-02 | Elevation of Privilege | health_events writable by anon/authenticated | mitigate | RLS enabled, single admin-read SELECT policy via `is_admin()`, ZERO write policy → service-role-only writes (no-write-policy lock) |
| T-08-03 | Information Disclosure | transfer_id / PII column on a shared health table | mitigate | health_events is polymorphic `entity_type`/`entity_id` only — NO transfer_id column (SC#1); `detail jsonb` carries non-PII facts only |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | None — Phase 8 installs ZERO npm packages (extensions are managed Supabase ext, audited APPROVED in RESEARCH §Package Legitimacy); no slopcheck checkpoint needed |
</threat_model>

<verification>
- `npm run test` discovers all 5 new specs (RED as designed) + dictionary parity green.
- `npm run typecheck` green (runtime-string imports keep tsc clean; Dict parity holds).
- 0008 migration file grep gate (Task 1 verify) passes; file is NOT applied live.
- single-writer.test.ts confirmed to cover `app/api/cron/*` + `platform/health/*` roots (no edit needed).
</verification>

<success_criteria>
- supabase/migrations/0008_platform_health.sql authored (extensions + health_events + 2 cron schedules), FLAGGED, Balkanity-only, NOT applied.
- 5 Wave-0 RED specs land and are RED; typecheck green.
- All Phase-8 EN/BG health keys exist; Dict-parity gate green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

**SQL objects (authored in 0008_platform_health.sql, applied in Plan 05):**
- `public.health_events` (table)
- `health_events_open_idx` (partial index)
- `health_events_admin_read` (RLS SELECT policy)
- `pg_cron`, `pg_net` (extensions)
- `health-sweep`, `digest-hourly` (cron jobs)
- `cron_secret` (Vault secret — value set at apply time)

**New files:**
- `supabase/migrations/0008_platform_health.sql`
- `platform/health/reconcile.test.ts`, `platform/health/stuck.test.ts`
- `app/api/cron/health/route.test.ts`, `app/api/cron/digest/route.test.ts`
- `app/admin/health/EmailCapGauge.test.tsx`

**New dictionary keys (en.ts + bg.ts):** `healthTitle`, `emailCapLabel`, `emailCapWarning`, `emailCapAtCap`, `emailCapZero`, `stuckHeading`, `stuckBadge`, `stuckEmptyHeading`, `stuckEmptyBody`, `reconHeading`, `reconBadge`, `reconEmptyHeading`, `reconEmptyBody`, `healthResolveCta`, `healthLoadFailed`
</artifacts_produced>

<output>
Create `.planning/phases/08-platform-health/08-01-SUMMARY.md` when done
</output>
