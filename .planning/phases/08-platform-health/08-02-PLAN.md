---
phase: 08-platform-health
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - platform/health/reconcile.ts
  - platform/health/stuck.ts
  - platform/health/keepalive.ts
  - app/api/cron/health/route.ts
autonomous: true
requirements: [HLTH-02, HLTH-04, HLTH-05]
must_haves:
  truths:
    - "The 15-min health route rejects any caller without a valid x-cron-secret (401, zero work) via a timing-safe compare"
    - "Reconciliation lists Stripe paid Checkout Sessions (server-only key, D-03 Stripe-API source of truth) and anti-joins wp_transfers paid, flagging discrepancies older than the lookback window — catching a never-delivered webhook"
    - "A reconciliation discrepancy fires an admin in-app notification AND a critical-tier admin email (D-09); a stuck transfer fires admin in-app ONLY (D-05); both deduped per-entity (no re-alert storm)"
    - "The route NEVER writes status:'paid' — it detects and alerts only (D-01, money single-writer lock)"
    - "A keep-alive DB write runs each cycle so the project does not pause (HLTH-05)"
  artifacts:
    - path: "platform/health/reconcile.ts"
      provides: "Stripe-API-vs-ledger detection + dedup + in-app + critical-email fan-out"
      exports: ["reconcile"]
    - path: "platform/health/stuck.ts"
      provides: "paid-but-unclaimed-within-12h predicate + in-app-only dedup alert"
      exports: ["detectStuck"]
    - path: "platform/health/keepalive.ts"
      provides: "service-role heartbeat write (defeats 7-day pause)"
      exports: ["touchHeartbeat"]
    - path: "app/api/cron/health/route.ts"
      provides: "x-cron-secret-gated nodejs route: reconcile + stuck + keep-alive"
      exports: ["POST", "runtime"]
  key_links:
    - from: "platform/health/reconcile.ts"
      to: "getStripe() checkout.sessions.list"
      via: "server-only Stripe key lists paid sessions (D-03 source of truth)"
      pattern: "getStripe\\(\\)\\.checkout\\.sessions\\.list"
    - from: "platform/health/reconcile.ts"
      to: "insertNotification + sendEmail(tier:'critical')"
      via: "discrepancy alert fan-out (D-09)"
      pattern: "tier:\\s*['\"]critical['\"]"
    - from: "app/api/cron/health/route.ts"
      to: "crypto.timingSafeEqual"
      via: "x-cron-secret constant-time compare (V6)"
      pattern: "timingSafeEqual"
---

<objective>
Build the reconciliation/stuck/keep-alive detection slice end-to-end: a server-only detection layer (`platform/health/*`) and the authenticated 15-min `app/api/cron/health` route that runs it and fans out alerts through the existing notification + email seams — DETECT-AND-ALERT only, never a `paid` writer.

Purpose: This is the DoD safety net (HLTH-02) — it catches a deliberately-dropped/never-delivered webhook within one sweep window and alerts the admin (in-app + critical email), plus stuck-transfer detection (HLTH-04) and the keep-alive heartbeat (HLTH-05), all behind a timing-safe cron-secret gate.
Output: reconcile.ts, stuck.ts, keepalive.ts, app/api/cron/health/route.ts — turning the Plan-01 RED specs GREEN.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-platform-health/08-CONTEXT.md
@.planning/phases/08-platform-health/08-RESEARCH.md
@.planning/phases/08-platform-health/08-PATTERNS.md

@app/api/stripe/webhook/route.ts
@platform/notifications/digest.ts
@platform/notifications/send-email.ts
@platform/notifications/notify.ts
@platform/payments/stripe.ts
@platform/supabase/admin.ts
@supabase/migrations/0008_platform_health.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconciliation detection + alert fan-out (platform/health/reconcile.ts)</name>
  <files>platform/health/reconcile.ts</files>
  <behavior>
    - Lists Stripe paid Checkout Sessions (server-only key) and anti-joins wp_transfers paid → flags a session whose transfer is missing or not paid (catches a never-delivered webhook, D-03).
    - A session inside the ~10-min lookback window is NOT flagged (no in-flight false positive, Pitfall 1).
    - When an OPEN health_events row already exists for (kind='reconciliation_discrepancy', entity_id) → no new alert (dedup, Pitfall 2).
    - A new discrepancy → inserts a health_events row + insertNotification(admin) + sendEmail({tier:'critical'}) (D-09). Each alert is log-and-continue.
    - reconcile.ts writes ZERO status:'paid' (D-01) — source contains no `status:'paid'`.
  </behavior>
  <read_first>
    - platform/notifications/digest.ts — copy the `import "server-only"` (line 1) + createAdminClient header discipline + the CR-01 no-JWT read trap (lines 67-90): read base tables directly with an explicit projection, NEVER wp_pool()
    - app/api/stripe/webhook/route.ts — lines 113-129 (session.metadata.transfer_id link key + the `no_matching_transfer`/`write_failed` outcomes), lines 216-260 (the admin-recipient resolve + per-block log-and-continue alert fan-out shape to mirror)
    - platform/payments/stripe.ts — `getStripe()` server-only client (apiVersion pinned); the reconcile lists Checkout Sessions through it
    - platform/notifications/send-email.ts — `sendEmail({tier:'critical'})` signature + the once-per-day dedup pattern (lines 131-136) to mirror for the per-entity dedup
    - platform/notifications/notify.ts — `insertNotification` signature (recipientId/type/title/entityType/entityId)
    - platform/health/reconcile.test.ts — the RED contract this file turns GREEN (-t lookback, -t dedup, detection, never-writes-paid)
  </read_first>
  <action>
    Create `platform/health/reconcile.ts` with `import "server-only"` as line 1. Export `async function reconcile(): Promise<void>`.

    D-03 RESOLVED — **Stripe API is the source of truth** (NOT the DB-only anti-join, which cannot see a never-delivered webhook). Implementation:
    1. `const admin = createAdminClient();` and `const stripe = getStripe();` (both server-only; createAdminClient AFTER any guard).
    2. List recent paid Checkout Sessions: `stripe.checkout.sessions.list({ ... })` filtered to `payment_status === 'paid'` (or `status: 'complete'`), bounded to a recent window (e.g. `created` ≥ now−24h to keep the page small for the pilot). Read each session's `metadata.transfer_id` (the link key the webhook set at route.ts:117).
    3. LOOKBACK (Pitfall 1): skip any session whose `created` is within the last ~10 minutes (`now - 600s`) — an in-flight transfer mid-processing is not a discrepancy. This kills false positives while still alerting a real drop within ~15-25 min.
    4. For the remaining sessions, read the matching `wp_transfers` row directly on `admin` with an explicit projection (`id,status` — CR-01: NEVER wp_pool()). A discrepancy = transfer row missing OR `status` is distinct from `'paid'`.
    5. DEDUP (Pitfall 2): before alerting, query open `health_events` for `kind='reconciliation_discrepancy'` AND `entity_id = <stripe event/session id>` AND `resolved_at is null`; if one exists, skip (no re-alert). The 0008 partial index backs this.
    6. ALERT (D-09): insert a `health_events` row (`kind='reconciliation_discrepancy'`, `entity_type='webhook_event'` or `'checkout_session'`, `entity_id=<session/event id>`, `detail` = non-PII facts: transfer_id, amount, session id — NEVER guest contact PII). Resolve admins via `admin.from("app_users").select("id,email").eq("role","admin")`. For each: `insertNotification({recipientId, type:'reconciliation_discrepancy', entityType:'transfer', entityId:transferId, title:<EN copy>})` AND `sendEmail({to:email, tier:'critical', idempotencyKey:'recon:'+sessionId, subject/html with the discrepancy FACT only})`. Wrap each alert in its own try/catch (log-and-continue; log the error object only, NEVER recipient PII — mirror route.ts:223-225,240-242).

    CRITICAL: this file performs ZERO `wp_transfers` writes of `status:'paid'` (D-01, single-writer.test.ts stays green). It detects and alerts only; remediation is human-driven webhook replay (idempotent via webhook_events.event_id).

    Use `webhook_events` outcomes (`no_matching_transfer`/`write_failed`) as a SECONDARY cross-check if cheap, but the Stripe API list is primary (D-03).
  </action>
  <verify>
    <automated>npm run test -- platform/health/reconcile.test.ts && npm run test -- platform/payments/single-writer.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `platform/health/reconcile.ts` starts with `import "server-only";`.
    - reconcile.test.ts GREEN (detection, `-t lookback` no-false-positive, `-t dedup` single-alert).
    - single-writer.test.ts GREEN — no `status:'paid'` write in reconcile.ts (comment-stripped grep finds none).
    - Source contains `getStripe().checkout.sessions.list`, `insertNotification`, `sendEmail` with `tier: "critical"`, and a lookback comparison against ~10 min.
    - The discrepancy email/notification body carries transfer id / session id / amount only — no guest contact PII (assert no `guest_email`/`guest_phone`/`guest_name`/address read in reconcile.ts).
    - `npm run typecheck` green.
  </acceptance_criteria>
  <done>reconcile() detects via Stripe API, dedups, and fans out in-app + critical email; never writes paid; RED spec GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stuck-transfer predicate + in-app-only alert (platform/health/stuck.ts) + keep-alive heartbeat (platform/health/keepalive.ts)</name>
  <files>platform/health/stuck.ts, platform/health/keepalive.ts</files>
  <behavior>
    - detectStuck flags a wp_transfers row where status='paid' AND driver_id IS NULL AND arrival_at <= now()+12h (D-04); ignores claimed rows and paid rows with arrival >12h away.
    - A stuck transfer fires insertNotification(admin) ONLY — NO sendEmail anywhere in stuck.ts (D-05).
    - Stuck alerts dedup per transfer_id against open health_events (no re-alert every 15 min).
    - touchHeartbeat performs a service-role write each cycle (defeats the 7-day pause, HLTH-05); wrapped non-fatal.
  </behavior>
  <read_first>
    - platform/notifications/digest.ts — the CR-01 no-JWT read trap (read base tables directly with explicit columns, never wp_pool()); the server-only header
    - platform/notifications/notify.ts:38-60 — `insertNotification` (the ONLY sink stuck.ts uses; NO sendEmail import in this file) + the non-fatal try/catch wrap (lines 47-59) to mirror for the heartbeat
    - supabase/migrations/0004_transfer_entity.sql — line 55 `arrival_at timestamptz` + line 67-68 the arrival_at index (the predicate columns)
    - supabase/migrations/0008_platform_health.sql — the `health_events` shape for the dedup query + the heartbeat write target (decide: a `touch` of health_events, or a dedicated 1-row heartbeat — see action)
    - platform/health/stuck.test.ts — the RED contract (predicate + in-app-only)
  </read_first>
  <action>
    Create `platform/health/stuck.ts` (`import "server-only";` line 1). Export `async function detectStuck(): Promise<void>`:
    1. `const admin = createAdminClient();` read `wp_transfers` directly (CR-01, explicit columns `id, arrival_at, status, driver_id`) with the predicate `status='paid' AND driver_id IS NULL AND arrival_at IS NOT NULL AND arrival_at <= now()+interval '12 hours'` (D-04). Implement the time bound in the query (`.lte("arrival_at", <now+12h ISO>)`) plus `.eq("status","paid").is("driver_id",null)`.
    2. DEDUP per `transfer_id`: skip if an open `health_events` row of `kind='stuck_unclaimed'` AND `entity_id=<transfer id>` AND `resolved_at is null` already exists (Pitfall 2).
    3. ALERT (D-05 IN-APP ONLY): insert a `health_events` row (`kind='stuck_unclaimed'`, `entity_type='transfer'`, `entity_id=<transfer id>`, `detail` non-PII: arrival_at) + `insertNotification({recipientId:admin, type:'stuck_unclaimed', entityType:'transfer', entityId, title:<EN copy>})`. **NO `sendEmail` import or call anywhere in this file** (D-05; stuck.test.ts asserts absence). Log-and-continue per alert.

    Create `platform/health/keepalive.ts` (`import "server-only";` line 1). Export `async function touchHeartbeat(): Promise<void>`: a tiny service-role write that counts as DB activity to defeat the 7-day pause (Pitfall 3). Simplest belt-and-braces: `update health_events set ...` is not ideal (no guaranteed row) — instead insert-or-update a dedicated heartbeat. Decision: add a one-row `health_heartbeat` upsert. NOTE: if `health_heartbeat` is not in 0008, the heartbeat MAY instead write a benign row to `health_events` with `kind='keepalive'` and immediately `resolved_at=now()` (never alerts) OR perform a no-op `select 1` write-equivalent — pick the option that requires NO schema change beyond 0008; if a dedicated table is cleaner, add `create table if not exists public.health_heartbeat (id int primary key default 1, last_ping timestamptz)` to 0008 (coordinate: 0008 is authored in Plan 01, applied in Plan 05 — add the heartbeat table to 0008 here if chosen, before the file is applied). Wrap in try/catch (non-fatal — a heartbeat miss never fails the cron).

    CRITICAL: neither file writes `status:'paid'` (D-01).
  </action>
  <verify>
    <automated>npm run test -- platform/health/stuck.test.ts && npm run test -- platform/payments/single-writer.test.ts && ! grep -E "sendEmail" platform/health/stuck.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - stuck.test.ts GREEN (predicate matches paid+unclaimed≤12h; ignores claimed/early).
    - `grep sendEmail platform/health/stuck.ts` returns NOTHING (D-05 in-app only).
    - stuck.ts + keepalive.ts both start with `import "server-only";`.
    - single-writer.test.ts GREEN (no paid write in either file).
    - touchHeartbeat performs a service-role write wrapped non-fatal; if a `health_heartbeat` table is used it is added to 0008 (applied in Plan 05).
    - `npm run typecheck` green.
  </acceptance_criteria>
  <done>detectStuck flags paid-but-unclaimed-near-arrival (in-app only) + touchHeartbeat keeps the DB warm; RED spec GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: The authenticated 15-min health route (app/api/cron/health/route.ts)</name>
  <files>app/api/cron/health/route.ts</files>
  <read_first>
    - app/api/stripe/webhook/route.ts — lines 25-48 (the `runtime = "nodejs"` lock + the `new NextResponse(JSON.stringify(...))` response helper that deliberately avoids `.json(`); line 75 (createAdminClient AFTER the auth gate); mirror this exact shape
    - .planning/phases/08-platform-health/08-RESEARCH.md — §Pattern 1 (the route auth-by-secret reference impl: timingSafeEqual, 401-zero-work, runtime nodejs) + §"Vercel daily backstop" (the route should also accept Vercel's `Authorization: Bearer ${CRON_SECRET}` header so the daily backstop in Plan 04 reaches it)
    - platform/health/reconcile.ts, platform/health/stuck.ts, platform/health/keepalive.ts — the three workers this route invokes (built in Tasks 1-2)
    - app/api/cron/health/route.test.ts — the RED auth-gate contract (-t unauthorized → 401, zero work)
  </read_first>
  <action>
    Create `app/api/cron/health/route.ts` with `export const runtime = "nodejs";` (NEVER Edge — service-role + crypto fragile on Edge, mirrors the webhook). Export `async function POST(req: NextRequest)`.

    AUTH GATE FIRST (zero work before it passes):
    - Read `x-cron-secret` header (pg_cron path) AND tolerate Vercel's `Authorization: Bearer <CRON_SECRET>` header (the daily backstop, Plan 04). Compare against `process.env.CRON_SECRET` using `crypto.timingSafeEqual` over equal-length buffers (V6 — NEVER `===`, which is timing-leaky; return false on length mismatch or empty secret). On failure return `new NextResponse(JSON.stringify({error:"unauthorized"}),{status:401})` and do NOTHING else (no DB read, no worker call).
    - On success: invoke the three workers in sequence — `await reconcile(); await detectStuck(); await touchHeartbeat();` — each is independently log-and-continue internally; wrap the route body so a worker throw is caught and logged (the keep-alive especially must not be skipped by an earlier failure — order keep-alive defensively or wrap each in try/catch). Return `new NextResponse(JSON.stringify({ok:true}),{status:200})`.

    Use the same `JSON.stringify` response shape as the webhook (no `.json(` token). The route does real work (alerts, emails) so it is a public-internet endpoint that MUST authenticate every caller (Pitfall 5). It performs ZERO `wp_transfers` writes (D-01).
  </action>
  <verify>
    <automated>npm run test -- app/api/cron/health/route.test.ts && npm run test -- platform/payments/single-writer.test.ts && grep -q "timingSafeEqual" app/api/cron/health/route.ts && grep -q 'runtime = "nodejs"' app/api/cron/health/route.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - route.test.ts GREEN: `-t unauthorized` → 401 with the mocked workers NEVER invoked; authorized → 200 with workers invoked.
    - `export const runtime = "nodejs"` present.
    - `crypto.timingSafeEqual` used for the secret compare (no `===` on the secret).
    - Route accepts both `x-cron-secret` and `Authorization: Bearer` (Vercel backstop) header forms.
    - single-writer.test.ts GREEN (route writes no paid).
    - typecheck + lint green.
  </acceptance_criteria>
  <done>The 15-min health route authenticates timing-safe, runs reconcile+stuck+keep-alive, returns 200; RED auth spec GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/health | Untrusted POST crosses here; the route does privileged work (alerts, critical emails) — must authenticate every caller |
| route handler → Stripe API | Server-only Stripe secret key lists paid sessions; key never reaches client |
| route handler → Supabase (service-role) | Service-role bypasses RLS; used only after the auth gate passes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing / DoS | unauthenticated POST to /api/cron/health triggers mass alerts/critical emails | mitigate | `x-cron-secret` timing-safe gate (`crypto.timingSafeEqual`); 401 with ZERO work before any DB read or worker call (route.test.ts `-t unauthorized`) |
| T-08-05 | Tampering | sweep mutated into a `paid` writer (breaks money lock) | mitigate | DETECT-ONLY (D-01); single-writer.test.ts grep gate covers `app/api/cron/*` + `platform/health/*` — RED on any `status:'paid'` write |
| T-08-06 | DoS (self-inflicted) | re-alert storm exhausts the Resend cap (critical email every 15 min) | mitigate | per-entity dedup against open `health_events` before alert; reconciliation email is `critical` so dedup is mandatory (Pitfall 2) |
| T-08-07 | Spoofing | timing attack on the secret compare | mitigate | `crypto.timingSafeEqual` over equal-length buffers; never `===` |
| T-08-08 | Information Disclosure | reconciliation email/notification leaks guest PII | mitigate | alert body carries transfer id / session id / amount only — no guest contact PII (mirror digest non-PII discipline); reconcile.ts reads no guest_* columns |
| T-08-09 | Information Disclosure | service-role / Stripe key exposed to client | mitigate | all `platform/health/*` modules `import "server-only"` (build fails on client import); route is server-only by default |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 8 installs ZERO npm packages; no checkpoint needed |
</threat_model>

<verification>
- reconcile.test.ts, stuck.test.ts, app/api/cron/health/route.test.ts all GREEN.
- single-writer.test.ts GREEN (the sweep is not a paid writer — covers the new files).
- `grep sendEmail platform/health/stuck.ts` returns nothing (D-05).
- `grep timingSafeEqual app/api/cron/health/route.ts` present (V6).
- typecheck + lint green.
</verification>

<success_criteria>
- HLTH-02 detection (Stripe-API source of truth, lookback, dedup) + in-app + critical email fan-out works; never writes paid (D-01).
- HLTH-04 stuck predicate (paid+unclaimed≤12h) alerts in-app only (D-05).
- HLTH-05 keep-alive heartbeat write runs each cycle.
- The 15-min route authenticates timing-safe (401 zero-work on failure).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**Functions:** `reconcile()` (platform/health/reconcile.ts), `detectStuck()` (platform/health/stuck.ts), `touchHeartbeat()` (platform/health/keepalive.ts), `POST` + `runtime` (app/api/cron/health/route.ts)
**New files:** `platform/health/reconcile.ts`, `platform/health/stuck.ts`, `platform/health/keepalive.ts`, `app/api/cron/health/route.ts`
**New notification types (rows, not schema):** `reconciliation_discrepancy`, `stuck_unclaimed`
**New health_events kinds:** `reconciliation_discrepancy`, `stuck_unclaimed` (+ optional `keepalive`)
**Env var consumed (set in Plan 05):** `CRON_SECRET` (Vercel server env)
**Possible 0008 addition:** `health_heartbeat` table (if the heartbeat uses a dedicated row — added to the Plan-01-authored 0008 before Plan-05 apply)
</artifacts_produced>

<output>
Create `.planning/phases/08-platform-health/08-02-SUMMARY.md` when done
</output>
