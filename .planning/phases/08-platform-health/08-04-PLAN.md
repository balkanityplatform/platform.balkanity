---
phase: 08-platform-health
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/api/cron/digest/route.ts
  - vercel.json
autonomous: true
requirements: [HLTH-05, HLTH-02]
must_haves:
  truths:
    - "The hourly digest cron route rejects any caller without a valid x-cron-secret (401, zero work) and otherwise invokes the existing sendDueDigests() (D-10)"
    - "sendDueDigests is called unchanged — it already self-filters drivers by digest_send_hour == current UTC hour; Phase 8 only schedules it"
    - "Vercel Hobby cron is configured as a once-daily BACKSTOP hitting /api/cron/health (HLTH-05 belt-and-braces) — never the 15-min sweep"
    - "The digest route writes ZERO status:'paid' (D-01)"
  artifacts:
    - path: "app/api/cron/digest/route.ts"
      provides: "x-cron-secret-gated nodejs route wrapping sendDueDigests()"
      exports: ["POST", "runtime"]
    - path: "vercel.json"
      provides: "daily backstop cron config (crons[])"
      contains: "crons"
  key_links:
    - from: "app/api/cron/digest/route.ts"
      to: "sendDueDigests()"
      via: "thin authenticated wrapper (D-10)"
      pattern: "sendDueDigests"
    - from: "vercel.json crons[]"
      to: "/api/cron/health"
      via: "daily backstop ping (re-warms DB / catches paused pg_cron)"
      pattern: "/api/cron/health"
---

<objective>
Wire the two remaining schedule seams: the hourly digest cron route that fires Phase 7's existing `sendDueDigests()` (D-10 — the time trigger Phase 7 deferred here), and the Vercel Hobby daily-backstop cron config (HLTH-05 belt-and-braces). Both are thin wiring on top of seams that already exist.

Purpose: Closes the digest scheduling gap (Phase 7 built the invokable + PII gate; Phase 8 schedules it) and adds the external daily backstop so even a lapsed pg_cron is re-warmed. The digest route reuses the exact auth pattern from the health route's reference impl; sendDueDigests is invoked unchanged.
Output: app/api/cron/digest/route.ts + vercel.json crons[] backstop.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-platform-health/08-CONTEXT.md
@.planning/phases/08-platform-health/08-RESEARCH.md
@.planning/phases/08-platform-health/08-PATTERNS.md

@app/api/stripe/webhook/route.ts
@platform/notifications/digest.ts
@vercel.json
@supabase/migrations/0008_platform_health.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hourly digest cron route (app/api/cron/digest/route.ts)</name>
  <files>app/api/cron/digest/route.ts</files>
  <read_first>
    - app/api/stripe/webhook/route.ts — lines 25-48 (the `runtime = "nodejs"` lock + the `new NextResponse(JSON.stringify(...))` response helper, no `.json(` token) — copy the shape
    - platform/notifications/digest.ts — `sendDueDigests()` (lines 109-155): it ALREADY self-filters drivers by `digest_send_hour == getUTCHours()` (lines 117,126); the route adds NO scheduling logic, only authn + the call. Note the PHASE 8 SEAM header comment (lines 8-13) this route satisfies
    - .planning/phases/08-platform-health/08-RESEARCH.md — §Pattern 1 (the route auth-by-secret impl: timingSafeEqual, 401-zero-work, runtime nodejs); §Code Examples digest cron `'0 * * * *'`
    - app/api/cron/digest/route.test.ts — the RED contract (-t unauthorized → 401 zero work; authorized → sendDueDigests awaited)
    - app/api/cron/health/route.ts (Plan 02) — reuse the IDENTICAL auth gate shape (x-cron-secret + Vercel Authorization Bearer fallback, crypto.timingSafeEqual); factor a shared `authorized(req)` helper if it keeps both routes consistent (a tiny shared util is acceptable, or duplicate the small gate — keep it readable)
  </read_first>
  <action>
    Create `app/api/cron/digest/route.ts` with `export const runtime = "nodejs";`. Export `async function POST(req: NextRequest)`.

    AUTH GATE FIRST (identical to the health route, Plan 02): read `x-cron-secret` (pg_cron path) and tolerate Vercel's `Authorization: Bearer <CRON_SECRET>` (the daily-backstop convention); compare against `process.env.CRON_SECRET` via `crypto.timingSafeEqual` over equal-length buffers (V6 — never `===`; false on length mismatch / empty secret). On failure: `new NextResponse(JSON.stringify({error:"unauthorized"}),{status:401})` and DO NOTHING ELSE (sendDueDigests is never reached — route.test.ts `-t unauthorized` asserts this).

    On success: `await sendDueDigests();` (imported from `@/platform/notifications/digest` — invoked UNCHANGED; it self-filters by `digest_send_hour == current UTC hour`). Wrap in try/catch and log-and-continue (a digest fan-out error must not 500 the cron meaningfully — return 200 ok). Return `new NextResponse(JSON.stringify({ok:true}),{status:200})`.

    The route adds ZERO scheduling logic and ZERO `wp_transfers` writes (D-01). It is a public-internet endpoint doing real work (sends) → it MUST authenticate every caller (Pitfall 5).
  </action>
  <verify>
    <automated>npm run test -- app/api/cron/digest/route.test.ts && npm run test -- platform/payments/single-writer.test.ts && grep -q "sendDueDigests" app/api/cron/digest/route.ts && grep -q "timingSafeEqual" app/api/cron/digest/route.ts && grep -q 'runtime = "nodejs"' app/api/cron/digest/route.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - digest/route.test.ts GREEN: `-t unauthorized` → 401 with `sendDueDigests` (mocked) NEVER called; authorized → `sendDueDigests` awaited, 200.
    - `export const runtime = "nodejs"` present; `crypto.timingSafeEqual` used; accepts both `x-cron-secret` and `Authorization: Bearer`.
    - `sendDueDigests` imported from `@/platform/notifications/digest` and invoked unchanged (no new scheduling logic in the route).
    - single-writer.test.ts GREEN (no paid write).
    - typecheck + lint green.
  </acceptance_criteria>
  <done>The hourly digest route authenticates timing-safe and fires sendDueDigests() unchanged; RED spec GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Vercel daily-backstop cron config (vercel.json)</name>
  <files>vercel.json</files>
  <read_first>
    - vercel.json — the current config (framework/buildCommand keys); add a `crons` array without disturbing existing keys
    - .planning/phases/08-platform-health/08-RESEARCH.md — §"Vercel daily backstop" (`{ "crons": [ { "path": "/api/cron/health", "schedule": "0 3 * * *" } ] }`; Hobby = 1/day, hour-imprecise → BACKSTOP ONLY, never the 15-min sweep, D-02/D-08) + §Code Examples (Vercel auto-adds an `Authorization: Bearer ${CRON_SECRET}` header — the health route's gate already accepts it, Plan 02)
    - CLAUDE.md — Verified Provider Facts: Vercel Hobby cron = 1 run/day max, hour-imprecise; deployment FAILS if the expression is more frequent than daily
  </read_first>
  <action>
    Edit `vercel.json` to add a `crons` array (preserve `$schema`, `framework`, `buildCommand`):
    `"crons": [ { "path": "/api/cron/health", "schedule": "0 3 * * *" } ]`

    This is the DAILY BACKSTOP ONLY (D-02/D-08) — it re-warms the DB / catches a paused pg_cron once a day; it is NOT the 15-min sweep (that is pg_cron, Plan 01/05). Hobby caps cron at 1/day — a more frequent expression FAILS the deploy (CLAUDE.md), so keep it daily. The path hits the same health route (Plan 02), which already accepts Vercel's `Authorization: Bearer ${CRON_SECRET}` header (set as a Vercel env in Plan 05). Do NOT add a digest backstop (the hourly digest needs pg_cron's hourly cadence; a daily Vercel cron cannot honour `digest_send_hour`).

    Single cron entry only. Keep the file valid JSON.
  </action>
  <verify>
    <automated>node -e "const c=require('./vercel.json'); if(!Array.isArray(c.crons)||c.crons.length!==1) throw new Error('crons not single-entry'); if(c.crons[0].path!=='/api/cron/health') throw new Error('wrong path'); if(c.crons[0].schedule!=='0 3 * * *') throw new Error('not daily'); if(!c.framework||!c.buildCommand) throw new Error('existing keys lost'); console.log('PASS')" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json has a single `crons` entry: path `/api/cron/health`, schedule `0 3 * * *` (daily).
    - Existing `framework` + `buildCommand` keys preserved; file is valid JSON.
    - NO digest backstop entry (hourly digest is pg_cron-only).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Vercel daily backstop config added (health route only); existing config intact; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/digest | Untrusted POST crosses here; the route triggers email sends — must authenticate every caller |
| Vercel cron → /api/cron/health | The daily backstop calls a privileged route; Vercel injects an `Authorization: Bearer ${CRON_SECRET}` header the route validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Spoofing / DoS | unauthenticated POST to /api/cron/digest triggers mass driver-digest sends | mitigate | `x-cron-secret` timing-safe gate; 401 with ZERO work before `sendDueDigests` is reached (route.test.ts `-t unauthorized`) |
| T-08-14 | Tampering | digest route mutated into a `paid` writer | mitigate | thin wrapper, no transfer writes (D-01); single-writer.test.ts grep gate covers `app/api/cron/*` |
| T-08-15 | DoS (availability) | Vercel cron more frequent than daily fails the deploy / over-sends | mitigate | single daily entry `0 3 * * *` (Hobby caps at 1/day; backstop only — the 15-min sweep is pg_cron) |
| T-08-16 | Information Disclosure | digest leaks guest PII | mitigate | `sendDueDigests` invoked unchanged — its Phase-7 PII gate (no-JWT projection, no guest contact PII in the digest body) is preserved; route adds no new read |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 8 installs ZERO npm packages; no checkpoint needed |
</threat_model>

<verification>
- digest/route.test.ts GREEN (401 zero-work on bad secret; sendDueDigests awaited when authorized).
- single-writer.test.ts GREEN (digest route is not a paid writer).
- vercel.json has a single daily backstop entry; build green.
- typecheck + lint green.
</verification>

<success_criteria>
- D-10 digest trigger: the hourly route fires sendDueDigests() (unchanged, honours digest_send_hour) behind a timing-safe gate.
- HLTH-05 backstop: Vercel daily cron hits /api/cron/health (belt-and-braces; pg_cron remains primary).
- No paid writer introduced (D-01).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

**Functions:** `POST` + `runtime` (app/api/cron/digest/route.ts)
**New files:** `app/api/cron/digest/route.ts`
**Modified:** `vercel.json` (additive `crons[]` daily backstop)
**New route:** `/api/cron/digest` (x-cron-secret gated, nodejs)
**Env var consumed (set in Plan 05):** `CRON_SECRET` (Vercel server env — also used by the Vercel backstop's Authorization header)
</artifacts_produced>

<output>
Create `.planning/phases/08-platform-health/08-04-SUMMARY.md` when done
</output>
