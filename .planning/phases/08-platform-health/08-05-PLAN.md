---
phase: 08-platform-health
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - .planning/phases/08-platform-health/08-GATES-EVIDENCE.md
autonomous: false
requirements: [HLTH-02, HLTH-03, HLTH-04, HLTH-05]
user_setup:
  - service: supabase
    why: "Apply the FLAGGED 0008 migration LIVE (extensions + health_events + cron schedules) + create the Vault cron secret"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: ".env.local (Management API token; reaches Balkanity, NOT MCP)"
    dashboard_config:
      - task: "Sign-off required before any DDL — schema is FLAGGED/irreversible (CLAUDE.md review gate)"
        location: "Supabase Management API /database/query, ref qyhdogajtmnvxphrslwm ONLY"
  - service: vercel
    why: "Set CRON_SECRET (matches the Vault cron_secret) so the cron routes authenticate"
    env_vars:
      - name: CRON_SECRET
        source: "Generate a long random value; set in Vercel project env (server-only, NEVER NEXT_PUBLIC_); same value goes into Supabase Vault as cron_secret"
    dashboard_config:
      - task: "Set CRON_SECRET as a Vercel server env on balkanity_platform_project; redeploy so the cron routes read it"
        location: "Vercel project env (team balkanity-platform-s-projects)"
must_haves:
  truths:
    - "Migration 0008 is applied LIVE to Balkanity (qyhdogajtmnvxphrslwm) — pg_cron + pg_net installed, health_events table + RLS live, two cron schedules registered"
    - "The Vault cron_secret and the Vercel CRON_SECRET hold the SAME value so the cron routes authenticate the pg_cron and backstop callers"
    - "DoD GATE: a deliberately-dropped/never-delivered webhook is caught by the sweep within one window and surfaced as an admin in-app alert + critical email; the sweep NEVER set paid (D-01)"
    - "DoD GATE: cron actually fires on the live project (cron.job_run_details shows runs); the keep-alive (kind='keepalive' health_events row) keeps the project warm"
  artifacts:
    - path: ".planning/phases/08-platform-health/08-GATES-EVIDENCE.md"
      provides: "live-apply runbook + DoD gate evidence (mirrors 05/07 GATES-EVIDENCE)"
  key_links:
    - from: "Supabase Vault cron_secret"
      to: "Vercel CRON_SECRET"
      via: "same value — the cron route's timing-safe compare passes"
      pattern: "cron_secret|CRON_SECRET"
    - from: "pg_cron health-sweep"
      to: "/api/cron/health"
      via: "net.http_post with x-cron-secret from Vault → 200"
      pattern: "health-sweep"
---

<objective>
[BLOCKING / SIGN-OFF] Apply the FLAGGED migration `0008_platform_health.sql` LIVE to Balkanity via the Supabase Management API, provision the cron secret (Vault + matching Vercel env), and prove the three live DoD gates: a dropped webhook is caught + alerted (in-app + critical email) without ever setting `paid`; the cron fires on the live project; the keep-alive keeps it warm.

Purpose: Schema is FLAGGED/irreversible — this is the gated, human-signed-off live apply that makes the whole phase real. Build/type checks pass without it (types are not DB-derived), so the phase CANNOT truly pass verification until this runs. This plan is the DoD gate for the pilot's reconciliation requirement.
Output: live DB state (extensions, health_events, cron jobs, Vault secret) + 08-GATES-EVIDENCE.md.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-platform-health/08-CONTEXT.md
@.planning/phases/08-platform-health/08-RESEARCH.md

@supabase/migrations/0008_platform_health.sql
@.planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md
@.planning/phases/07-notifications/07-GATES-EVIDENCE.md
@app/api/cron/health/route.ts
@platform/health/reconcile.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: SIGN-OFF — review the FLAGGED 0008 DDL before any live apply</name>
  <files>supabase/migrations/0008_platform_health.sql</files>
  <action>
    Present the authored 0008 DDL for the FLAGGED-schema review gate (CLAUDE.md). NO code runs in this task — it pauses for explicit human sign-off before the live apply (Task 2). Surface the migration file + the review checklist below; do NOT apply anything until the human replies "approved".
  </action>
  <what-built>
    Migration `0008_platform_health.sql` is authored (Plan 01). It creates pg_cron + pg_net extensions, the `health_events` table (service-role write, admin-read RLS, NO write policy, polymorphic — NO transfer_id; `kind` is free-form text covering `reconciliation_discrepancy` / `stuck_unclaimed` / `keepalive`), and two idempotent cron schedules (15-min health sweep + hourly digest) calling the Vercel routes with an `x-cron-secret` read from Vault. The keep-alive uses a benign `kind='keepalive'` row in this SAME table (NO `health_heartbeat` table, NO extra schema). The cron routes (Plans 02/04) and the admin health console (Plan 03) are merged and green on automated checks.
  </what-built>
  <how-to-verify>
    1. Read `supabase/migrations/0008_platform_health.sql` end-to-end. Confirm: (a) FLAGGED header names Balkanity ref `qyhdogajtmnvxphrslwm` ONLY and forbids Kalvia + `db push` + MCP; (b) `create extension if not exists pg_cron;` + `pg_net;` precede any `cron.schedule`; (c) `health_events` has NO `transfer_id` column, `kind` is free-form text with NO CHECK constraint (so the Plan-02 `kind='keepalive'` heartbeat is allowed), and NO INSERT/UPDATE/DELETE policy (only `health_events_admin_read` via `is_admin()`); (d) the cron block is idempotent (`unschedule … where exists` before each `schedule`); (e) the receiver URL is the production `balkanityplatformproject.vercel.app`.
    2. Confirm you have generated a long random CRON_SECRET value and are ready to set it BOTH in Supabase Vault (`cron_secret`) AND in Vercel env (`CRON_SECRET`) — same value.
    3. Confirm the target ref is Balkanity (`qyhdogajtmnvxphrslwm`), NOT Kalvia (`utyatpadtibqqswsfvtr`).
  </how-to-verify>
  <verify>
    <human-check>Human has read 0008 end-to-end, confirmed the FLAGGED header + Balkanity-only ref + no-write-policy + free-form `kind` + idempotent cron block, and replied "approved" before any live apply.</human-check>
  </verify>
  <resume-signal>Type "approved" to authorize the live apply, or describe required changes to the DDL.</resume-signal>
  <done>Human sign-off recorded; the live apply (Task 2) is authorized for Balkanity only.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply 0008 LIVE via Management API + provision the cron secret (Vault + Vercel)</name>
  <files>.planning/phases/08-platform-health/08-GATES-EVIDENCE.md</files>
  <read_first>
    - supabase/migrations/0008_platform_health.sql — the exact DDL to apply
    - .planning/phases/07-notifications/07-GATES-EVIDENCE.md — the live-apply-via-Management-API runbook (the `/database/query` POST shape with SUPABASE_ACCESS_TOKEN; the Kalvia-absent guardrail assertion before any DDL)
    - .planning/phases/05-claim-correctness/05-GATES-EVIDENCE.md — the BEGIN..COMMIT live-apply discipline + zero-residue posture
    - CLAUDE.md — Verified Provider Facts + infra guardrail (Balkanity ref only); memory: Management/REST APIs reach Balkanity (token works), MCP is blocked
  </read_first>
  <action>
    Re-assert the guardrail FIRST: query project identity via the Management API and confirm the ref is `qyhdogajtmnvxphrslwm` (Balkanity). If anything reports Kalvia (`utyatpadtibqqswsfvtr`), STOP.

    1. Provision the secret: generate a long random value. In Supabase Vault: `select vault.create_secret('<RANDOM>','cron_secret');` (via Management API `/database/query`). Set the IDENTICAL value as `CRON_SECRET` in Vercel project env (server-only, NEVER `NEXT_PUBLIC_`) on `balkanity_platform_project` (team `balkanity-platform-s-projects`) and redeploy so the cron routes read it.
    2. Apply the DDL: POST the full `0008_platform_health.sql` body to the Management API `/database/query` (NOT MCP, NOT `supabase db push`). The cron-schedule block reads the just-created Vault secret. Apply in a way that mirrors 0007's runbook (idempotent statements; re-runnable).
    3. Verify live: `select extname, extversion from pg_extension where extname in ('pg_cron','pg_net');` (both present); `select tablename from pg_tables where tablename='health_events';` + `select polname from pg_policy …` (admin-read only, no write policy); `select jobname, schedule from cron.job where jobname in ('health-sweep','digest-hourly');` (both registered).

    Record every command + output in 08-GATES-EVIDENCE.md. Leave zero residue (no test rows beyond what the DoD gate Task 3 deliberately seeds + cleans).
  </action>
  <verify>
    <human-check>Management-API queries confirm pg_cron + pg_net installed, health_events table + admin-read-only RLS live, both cron jobs registered on Balkanity (qyhdogajtmnvxphrslwm); Vault cron_secret + Vercel CRON_SECRET set to the same value; outputs pasted into 08-GATES-EVIDENCE.md.</human-check>
  </verify>
  <acceptance_criteria>
    - `pg_extension` shows `pg_cron` (1.6.4) + `pg_net` (0.20.3) installed on Balkanity.
    - `health_events` exists with RLS enabled, a single admin-read SELECT policy, NO write policy.
    - `cron.job` lists `health-sweep` (*/15) and `digest-hourly` (0 *).
    - Vault `cron_secret` and Vercel `CRON_SECRET` hold the same value; Vercel redeployed.
    - Ref guardrail asserted (Balkanity only; Kalvia absent); evidence recorded.
  </acceptance_criteria>
  <done>0008 applied LIVE to Balkanity; extensions + table + RLS + cron jobs live; secret provisioned both sides; evidence captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DoD live gates — dropped webhook caught (no paid write) + cron fires + keep-alive holds</name>
  <files>.planning/phases/08-platform-health/08-GATES-EVIDENCE.md</files>
  <read_first>
    - platform/health/reconcile.ts — the detection + alert path being exercised live
    - platform/health/keepalive.ts — the keep-alive heartbeat (a benign health_events kind='keepalive' row) exercised live
    - app/api/cron/health/route.ts — the route the live cron hits
    - .planning/phases/08-platform-health/08-RESEARCH.md — §Validation Architecture "DoD" rows (dropped-webhook caught; cron fires; keep-alive holds) + Open Q1/A1 (the dropped-webhook test: with D-03 Stripe-API-as-source-of-truth, the sweep catches a never-delivered webhook via the Stripe session list anti-join)
    - .planning/phases/03-payments-trust-spine/ plans — the Stripe-CLI replay path used as the idempotent human remediation after the alert
  </read_first>
  <action>
    Run the three live DoD gates against Balkanity + the deployed app, recording each in 08-GATES-EVIDENCE.md:

    GATE A — DROPPED WEBHOOK CAUGHT (the pilot DoD): Create a Stripe-paid Checkout Session whose corresponding `wp_transfers` row is NOT flipped to `paid` (simulate the dropped/never-delivered webhook — e.g. a paid session whose transfer stays `requested`, or a deliberately-not-forwarded webhook in TEST mode). Wait one sweep window (or invoke `/api/cron/health` directly WITH the valid `x-cron-secret`). ASSERT: (1) a `health_events` row `kind='reconciliation_discrepancy'` appears for that session/transfer; (2) an admin in-app notification appears (the bell); (3) a critical-tier admin email is sent (D-09); (4) the transfer is STILL NOT `paid` — the sweep wrote NOTHING (D-01, money single-writer lock); confirm via `select status from wp_transfers where id=…` unchanged AND `single-writer.test.ts` still green. THEN verify remediation: replay the event through the signature-verified webhook (Stripe CLI) → it idempotently flips `paid` (existing webhook_events.event_id dedup), and a second sweep does NOT re-alert (dedup).

    GATE B — CRON FIRES LIVE: `select jobname, status, start_time from cron.job_run_details order by start_time desc limit 10;` (Management API) shows the `health-sweep` (and after the top of an hour, `digest-hourly`) jobs running. Confirm the route received the call (a fresh `health_events` keepalive/activity row or a job_run_details success). Note net.http_post is async fire-and-forget (Pitfall 4) — observability is health_events rows + job_run_details, not the cron return.

    GATE C — KEEP-ALIVE: confirm the heartbeat write occurs each cycle — a fresh `health_events` row `kind='keepalive'` (with `resolved_at` set, so it never appears as an open alert) per sweep — the recurring DB activity defeats the 7-day pause (HLTH-05). Confirm via `select created_at from health_events where kind='keepalive' order by created_at desc limit 5;` (rows ~15 min apart). Confirm the Vercel daily backstop entry resolves (a manual `curl` of `/api/cron/health` with the Vercel `Authorization: Bearer ${CRON_SECRET}` header returns 200; without it returns 401).

    Clean up any test rows seeded for Gate A (zero residue; keepalive rows may remain — they are benign recurring activity). Finalize 08-GATES-EVIDENCE.md with all three gates GREEN + the no-paid-write proof.
  </action>
  <verify>
    <human-check>Gate A: dropped-webhook flagged as a health_events reconciliation row + admin in-app + critical email, transfer NOT paid (single-writer intact), idempotent webhook replay remediates, no re-alert. Gate B: cron.job_run_details shows health-sweep firing. Gate C: keep-alive kind='keepalive' rows recur ~15 min apart; backstop curl 200 with secret / 401 without. All recorded in 08-GATES-EVIDENCE.md; Gate A test rows cleaned.</human-check>
  </verify>
  <acceptance_criteria>
    - A deliberately-dropped/never-delivered webhook is detected by the sweep within one window → health_events reconciliation row + admin in-app notification + critical email.
    - The sweep set NO `paid` (transfer status unchanged by the sweep; single-writer.test.ts green) — D-01 proven live.
    - Human remediation (idempotent webhook replay) flips paid; the next sweep does NOT re-alert (dedup).
    - `cron.job_run_details` shows the live cron firing; keep-alive `kind='keepalive'` rows recur (~15 min apart).
    - Backstop route returns 200 with the valid Authorization/x-cron-secret, 401 without.
    - 08-GATES-EVIDENCE.md captures all three gates + the no-paid-write proof; no Gate-A residue left on the live DB.
  </acceptance_criteria>
  <done>All three DoD gates GREEN live; reconciliation catches the dropped webhook without writing paid; phase HLTH-02..05 proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Management token → live Balkanity DB | The signed-off DDL crosses to production; ref guardrail asserted before any statement |
| Vault secret ↔ Vercel env | The shared cron secret must match exactly across both stores; mismatch silently 401s every cron call |
| pg_cron → /api/cron/* (public internet) | The live cron calls a public route doing privileged work; the x-cron-secret gate is the only authn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering | DDL applied to the WRONG project (Kalvia) | mitigate | re-assert ref `qyhdogajtmnvxphrslwm` via Management API before any statement; STOP on Kalvia; FLAGGED sign-off (Task 1) precedes apply |
| T-08-18 | Information Disclosure | cron secret leaked via `cron.job.command` | mitigate | secret lives in Vault `decrypted_secrets` (referenced in the cron body), never inlined; Vercel side is a server-only env (never NEXT_PUBLIC_) |
| T-08-19 | Tampering | the live sweep sets `paid` (money lock broken) | mitigate | Gate A explicitly proves the transfer stays unpaid after the sweep + single-writer.test.ts green; remediation is the idempotent verified webhook only (D-01) |
| T-08-20 | DoS (availability) | Vault/Vercel secret mismatch → every cron 401s silently | mitigate | Gate C curls the route with + without the secret (200/401) to confirm the secret matches before trusting the schedule |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 8 installs ZERO npm packages; extensions are managed Supabase ext (RESEARCH §Package Legitimacy APPROVED); no install checkpoint |
</threat_model>

<verification>
- Live: pg_cron + pg_net installed, health_events + RLS live, both cron jobs registered (Balkanity only).
- Live DoD Gate A: dropped webhook → reconciliation alert (in-app + critical email), transfer NOT paid, idempotent replay remediates, no re-alert.
- Live DoD Gate B: cron.job_run_details shows the sweep firing.
- Live DoD Gate C: keep-alive kind='keepalive' rows recur; backstop 200-with-secret / 401-without.
- single-writer.test.ts green (the sweep never became a paid writer).
</verification>

<success_criteria>
- 0008 applied LIVE to Balkanity with sign-off; extensions + health_events + RLS + 2 cron jobs live.
- Vault cron_secret + Vercel CRON_SECRET provisioned (same value).
- All three live DoD gates GREEN; reconciliation catches the dropped webhook WITHOUT setting paid (D-01).
- HLTH-02, HLTH-03, HLTH-04, HLTH-05 proven end-to-end on the live pilot stack.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

**Live DB state (applied from 0008, reproduced only by re-running the migration):**
- extensions `pg_cron`, `pg_net` (installed)
- table `public.health_events` + `health_events_open_idx` + `health_events_admin_read` policy (the keep-alive writes `kind='keepalive'` rows into THIS table — NO `health_heartbeat` table)
- cron jobs `health-sweep`, `digest-hourly` (in `cron.job`)
- Vault secret `cron_secret`
**Live external state:** Vercel env `CRON_SECRET` (server-only)
**New files:** `.planning/phases/08-platform-health/08-GATES-EVIDENCE.md`
</artifacts_produced>

<output>
Create `.planning/phases/08-platform-health/08-05-SUMMARY.md` when done
</output>
