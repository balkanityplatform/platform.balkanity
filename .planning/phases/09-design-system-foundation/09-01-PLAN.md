---
phase: 09-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - platform/transfers/lifecycle.ts
autonomous: true
requirements: [DS-01, DS-04]
must_haves:
  truths:
    - "Named semantic typography utilities (text-display/heading/body/label) resolve app-wide from @theme"
    - "Radii utilities (rounded-sm/md/lg/xl/full) and brand spacing aliases resolve from @theme"
    - "Brand primary stays #029b87; no #00685a and no JS tailwind.config exist anywhere"
    - "STEPPER_ORDER exposes the 6 happy-path steps paid through completed as the single source for the stepper"
  artifacts:
    - path: "app/globals.css"
      provides: "Extended @theme with --text-*, --radius-*, --spacing-* tokens (DS-01)"
      contains: "--radius-lg"
    - path: "platform/transfers/lifecycle.ts"
      provides: "STEPPER_ORDER 6-step const (D-06, DS-04 source of truth)"
      contains: "STEPPER_ORDER"
  key_links:
    - from: "platform/transfers/lifecycle.ts"
      to: "platform/ui/StatusDot.tsx"
      via: "imports TransferState (no local enum, Don't-Hand-Roll lock)"
      pattern: "import type TransferState"
---

<objective>
Lay the design-system foundation that every Phase 9 component and every downstream surface (10/11/12) consumes: the named "Balkanity Path" `@theme` tokens (DS-01) and the new `STEPPER_ORDER` lifecycle const that drives the DS-04 stepper (per D-06).

Purpose: Surfaces must author with semantic classes (`text-heading`, `rounded-lg`, `gap-*`) instead of arbitrary values, and the stepper must consume one centralised step order rather than hand-rolling its own array. These two are pure, disjoint foundation files with no component dependencies — they unblock all of Wave 2+.
Output: Extended `@theme` block in `app/globals.css`; new `STEPPER_ORDER` export in `platform/transfers/lifecycle.ts`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-PATTERNS.md
</context>
<artifacts_this_phase_produces>
This plan creates the foundation symbols the rest of Phase 9 builds on:
- **New `@theme` tokens in `app/globals.css`** (DS-01):
  - Typography roles (`--text-*`): `--text-display` (28px mobile baseline) + paired line-height, `--text-heading` (24px), `--text-body` (16px), `--text-label` (14px) — generating `text-display` / `text-heading` / `text-body` / `text-label` utilities.
  - Radii (`--radius-*`): `--radius-sm` 0.25rem, `--radius` 0.5rem (DEFAULT 8px), `--radius-md` 0.75rem, `--radius-lg` 1rem, `--radius-xl` 1.5rem, `--radius-full` 9999px → `rounded-sm/md/lg/xl/full`.
  - Spacing aliases (`--spacing-*`): `--spacing-touch-target` 44px, `--spacing-cta-height` 52px, plus 8px-grid aliases (gutter 16 / margin 32) → `p-*` / `gap-*` / `h-*` utilities.
  - The existing seven `--color-*` tokens + `--font-sans` are CONFIRMED unchanged (primary `#029b87`).
- **New `STEPPER_ORDER` const in `platform/transfers/lifecycle.ts`** (D-06): `readonly TransferState[]` = `["paid","claimed","en_route","arrived","picked_up","completed"]` — consumed by the DS-04 `LifecycleStepper` in Plan 04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the @theme with named typography, radii, and spacing tokens (DS-01)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (the full current file — the existing `@theme` block with the seven `--color-*` tokens + `--font-sans`; you EXTEND this same block, never redefine the colors or font)
    - .planning/phases/09-design-system-foundation/09-UI-SPEC.md (Typography role table: Body 16/400, Label 14/600, Heading 24/600, Display 28 to 32/600; the Spacing scale table; the Radii line under Color)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (the `app/globals.css` section — exact token names: `--radius-sm/--radius/--radius-md/--radius-lg/--radius-xl/--radius-full`, spacing aliases, and the `--text-*` guidance)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-01 token convention; D-02 — migrating existing primitives off arbitrary values is NOT in scope)
  </read_first>
  <action>
    Inside the SINGLE existing `@theme` block in app/globals.css (do not add a second block, do not introduce a tailwind.config.js), add three groups of tokens after the existing `--font-sans`, matching the file's lowercase-hex and comment style:
    1. Typography role tokens using Tailwind v4's `--text-<name>` convention so `text-<name>` utilities (with paired line-height) generate: `--text-display` = 1.75rem (28px mobile baseline) with paired line-height approx 2.125rem; `--text-heading` = 1.5rem (24px) / 2rem; `--text-body` = 1rem (16px) / 1.5rem; `--text-label` = 0.875rem (14px) / 1.25rem. Use Tailwind v4's `--text-<name>--line-height` paired-key syntax. Weight is applied at call-site via `font-normal`/`font-semibold` (the 400/600 working pair) — do NOT load extra font weights.
    2. Radii tokens: `--radius-sm: 0.25rem;` `--radius: 0.5rem;` (DEFAULT 8px) `--radius-md: 0.75rem;` `--radius-lg: 1rem;` `--radius-xl: 1.5rem;` `--radius-full: 9999px;`.
    3. Spacing aliases: `--spacing-touch-target: 44px;` `--spacing-cta-height: 52px;` and 8px-grid aliases for gutter/margin (e.g. `--spacing-gutter: 16px;` `--spacing-margin: 32px;` — names at your discretion). Tailwind v4 auto-generates `rounded-*` and `p-*`/`gap-*`/`h-*` utilities from these.
    Do NOT touch the `--color-*` values or `--font-sans` — they are CONFIRMED correct (`#029b87` primary). Do NOT introduce `#00685a` anywhere. Add a short comment per group citing DS-01 / D-01.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q -- "--radius-lg: 1rem" app/globals.css && grep -q -- "--text-heading" app/globals.css && grep -q -- "--spacing-touch-target: 44px" app/globals.css && grep -q -- "--spacing-cta-height: 52px" app/globals.css && grep -q "#029b87" app/globals.css && ! grep -qi "00685a" app/globals.css && [ ! -f tailwind.config.js ] && [ ! -f tailwind.config.ts ] && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds; `text-heading`/`text-body`/`text-label`/`text-display`, `rounded-lg`, `--spacing-touch-target` 44px and `--spacing-cta-height` 52px all present in globals.css; `#029b87` present; zero `#00685a`; no tailwind.config file exists.</done>
  <acceptance_criteria>
    - SOURCE: `grep -- "--radius-lg: 1rem" app/globals.css` matches; `--radius-sm/--radius/--radius-md/--radius-xl/--radius-full` all present.
    - SOURCE: `grep -- "--text-heading" app/globals.css` matches (and `--text-display`, `--text-body`, `--text-label`).
    - SOURCE: `grep -- "--spacing-touch-target: 44px"` and `grep -- "--spacing-cta-height: 52px"` both match.
    - SOURCE: `grep "#029b87" app/globals.css` matches; `grep -i "00685a"` returns nothing (exit 1).
    - SOURCE: no `tailwind.config.js` / `tailwind.config.ts` in repo root.
    - BUILD: `npm run build` exits 0 (tokens are valid CSS-first `@theme` syntax).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the STEPPER_ORDER 6-step lifecycle const (DS-04 source of truth, D-06)</name>
  <files>platform/transfers/lifecycle.ts</files>
  <read_first>
    - platform/transfers/lifecycle.ts (the full current file — its header "Don't-Hand-Roll" lock, the `TransferState` import from StatusDot, `ALLOWED_TRANSITIONS`, and the existing 7-step `LIFECYCLE_ORDER` you mirror in shape)
    - platform/ui/StatusDot.tsx (the `TransferState` union — the single source of the state enum; do NOT declare a local enum)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-06: STEPPER_ORDER is the 6 steps paid→completed, NOT the 7-step LIFECYCLE_ORDER; lives in lifecycle.ts so the stepper never hand-rolls its array)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (the `platform/transfers/lifecycle.ts` section — exact const shape, do NOT modify LIFECYCLE_ORDER or ALLOWED_TRANSITIONS)
  </read_first>
  <action>
    Add a NEW sibling export `STEPPER_ORDER: readonly TransferState[]` after `LIFECYCLE_ORDER`, value `["paid", "claimed", "en_route", "arrived", "picked_up", "completed"]` — the 6 happy-path steps starting at `paid` (drops `requested`). Add a short comment: this drives the DS-04 horizontal `LifecycleStepper` (per D-06), keeping lifecycle.ts the single source of truth so the stepper does not hand-roll its array; `cancelled` is excluded (rendered as a distinct terminal treatment by the consumer, mirroring the LIFECYCLE_ORDER rule). Do NOT modify `LIFECYCLE_ORDER`, `ALLOWED_TRANSITIONS`, or `canTransition`. Do NOT declare a local state enum — `TransferState` stays imported from StatusDot.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q "export const STEPPER_ORDER" platform/transfers/lifecycle.ts && grep -q "\"picked_up\", \"completed\"" platform/transfers/lifecycle.ts && ! grep -A3 "STEPPER_ORDER" platform/transfers/lifecycle.ts | grep -q "requested" && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>STEPPER_ORDER is exported with exactly the 6 steps paid→completed (no `requested`); LIFECYCLE_ORDER/ALLOWED_TRANSITIONS unchanged; `tsc --noEmit` passes.</done>
  <acceptance_criteria>
    - SOURCE: `grep "export const STEPPER_ORDER" platform/transfers/lifecycle.ts` matches; the array contains paid, claimed, en_route, arrived, picked_up, completed and NOT requested or cancelled.
    - SOURCE: `LIFECYCLE_ORDER` array still has its 7 entries (unchanged); no local `type`/`enum` for state declared (still `import type { TransferState }`).
    - TYPECHECK: `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
This plan is presentation/config-only (CSS tokens + a TS const). No auth, RLS, payment-path, data, or untrusted input is touched. Trust boundaries and STRIDE surface are minimal; recorded for completeness per ASVS L1.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS / TS source | No runtime trust boundary crossed; tokens and the const are static, server-and-build-time only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | `@theme` tokens / `STEPPER_ORDER` | accept | Static design tokens + a typed const; no user input, no PII, no secrets. The tsc parity/typecheck gate catches malformed values. |
| T-09-01-SC | Tampering | npm installs | accept | No new packages installed in this plan (tokens + const only); package-legitimacy gate N/A. |
</threat_model>

<verification>
- `npm run build` succeeds with the extended `@theme`.
- `npm run typecheck` passes (STEPPER_ORDER typed against the imported `TransferState`).
- `npm test` (existing suite) stays green — no component behaviour changed yet.
- Grep confirms `#029b87` present and `#00685a` absent; no `tailwind.config.*`.
</verification>

<success_criteria>
- Named typography (`text-display/heading/body/label`), radii (`rounded-sm/md/lg/xl/full`), and spacing aliases (`--spacing-touch-target` 44, `--spacing-cta-height` 52) resolve from the single `@theme` block (DS-01).
- `STEPPER_ORDER` exported from lifecycle.ts as the 6-step source for the DS-04 stepper (D-06).
- No `#00685a`, no JS tailwind.config anywhere; brand primary remains `#029b87`.
</success_criteria>

<output>
Create `.planning/phases/09-design-system-foundation/09-01-SUMMARY.md` when done.
</output>
