---
phase: 09-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - platform/ui/StatusDot.tsx
  - platform/ui/StatusDot.test.tsx
autonomous: true
requirements: [DS-02]
must_haves:
  truths:
    - "StatusDot renders any transfer status as a coloured dot/badge PLUS a worded label (never colour alone)"
    - "A variant='pill' renders a solid filled badge; variant='dot' (default) keeps the exact current inline dot+label so existing callers are unchanged"
    - "cancelled renders as a hollow coral ring (2px stroke, transparent fill) in both variants, with the 'Cancelled' label still present"
  artifacts:
    - path: "platform/ui/StatusDot.tsx"
      provides: "Extended StatusDot with variant prop + StatusVariant type; one colour/label source map (DS-02, D-03/D-04)"
      contains: "StatusVariant"
    - path: "platform/ui/StatusDot.test.tsx"
      provides: "Variant + cancelled-hollow-ring coverage extending the existing CASES table (D-12)"
      contains: "pill"
  key_links:
    - from: "platform/ui/StatusDot.tsx"
      to: "STATE_META colour/label map"
      via: "both variants read one map (single source of truth)"
      pattern: "STATE_META"
---

<objective>
Extend the existing `StatusDot` with a `variant` prop (DS-02, D-03) so every surface can render a status either as the current inline dot+label (`"dot"`, default) or a solid filled badge (`"pill"`, e.g. coral "Unclaimed" on a driver claim card) — driven by ONE colour/label source map. Introduce the one status-rendering change this phase ships: `cancelled` becomes a hollow coral ring (2px, transparent fill) in both variants (D-04), keeping the always-present worded label (WCAG 1.4.1).

Purpose: The pill is the concrete driver of DS-02 (driver claim cards in Phase 11), and the hollow-ring cancelled treatment is reused by the DS-04 stepper's terminal state. All existing callers (LifecycleTimeline + every Phase 2–8 surface) must stay on the default dot, untouched.
Output: Extended `platform/ui/StatusDot.tsx`; extended `platform/ui/StatusDot.test.tsx`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-PATTERNS.md
</context>
<artifacts_this_phase_produces>
- **Extended `platform/ui/StatusDot.tsx`** (DS-02):
  - New exported `StatusVariant` type = `"dot" | "pill"`.
  - New `variant?: StatusVariant` prop on `StatusDot` (default `"dot"`) — existing callers unchanged.
  - `"pill"` renderer: solid filled badge (status colour fill + worded label).
  - `cancelled` treatment changed in BOTH variants: hollow coral ring (`border-2 border-coral bg-transparent`) instead of a solid `bg-coral` dot, label still rendered (D-04).
  - The `STATE_META` colour/label map stays the single source consumed by both renderers.
- **Extended `platform/ui/StatusDot.test.tsx`** (D-12): cases asserting both variants render the label, the pill variant renders, and cancelled uses the hollow-ring class (`border-coral`) not `bg-coral`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the variant prop and cancelled hollow-ring to StatusDot (DS-02, D-03/D-04)</name>
  <files>platform/ui/StatusDot.tsx, platform/ui/StatusDot.test.tsx</files>
  <read_first>
    - platform/ui/StatusDot.tsx (the full current file — the `TransferState` union, the authoritative `STATE_META` colour/label map, and the current `"dot"` render at lines 38-51; `data-testid="status-dot"` + `aria-hidden="true"` on the shape, the 14px/600 slate label)
    - platform/ui/StatusDot.test.tsx (the full current file — the `CASES` table + `it.each` structure you extend; note `cancelled` currently expects `bg-coral`)
    - platform/ui/Button.tsx (the variant-prop pattern to copy: exported `ButtonVariant` union + `Record<Variant, string>` map + defaulted `variant = "primary"` prop)
    - .planning/phases/09-design-system-foundation/09-UI-SPEC.md (Status colour mapping table — Unclaimed=coral, Claimed=teal, En route=amber, Completed=grey, Cancelled=hollow coral ring; dot 10px; label Montserrat 600/14px slate)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-03 variant prop; D-04 cancelled hollow ring is the ONE status-rendering change)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (the StatusDot section — apply variant pattern; preserve `data-testid` + `aria-hidden`; both variants keep the worded label)
  </read_first>
  <behavior>
    - variant="dot" (default): renders the EXACT current markup — `inline-flex items-center gap-[4px]`, a 10px `rounded-full` dot with `data-testid="status-dot"` + `aria-hidden="true"` carrying the per-state colour class, plus the 14px/600 slate label. Existing callers see no change.
    - variant="pill": renders a solid filled badge — the per-state colour as the fill + the worded label, rounded (`rounded-full`), keeping a label element. Still WCAG 1.4.1 (label present).
    - cancelled (both variants): the shape is a hollow coral ring — `border-2 border-coral bg-transparent` (NOT `bg-coral`) — and the "Cancelled" label still renders.
    - All 8 states still render a non-empty worded label in both variants.
  </behavior>
  <action>
    Add an exported `StatusVariant = "dot" | "pill"` type and a `variant?: StatusVariant` prop defaulting to `"dot"` (mirror Button.tsx's pattern). Keep `STATE_META` as the single source of colour/label — do NOT duplicate it per variant. Refactor the render so both variants read the one map: `"dot"` reproduces the current inline dot+label verbatim; `"pill"` renders a solid filled badge using the same per-state colour as the fill plus the worded label. For `cancelled` in BOTH variants, render the shape as a hollow coral ring (`border-2 border-coral bg-transparent`) rather than the solid `bg-coral` dot — this is the one behavioural change (D-04); keep the always-present worded label. PRESERVE `data-testid="status-dot"` and `aria-hidden="true"` on the shape element (the test queries them). The label stays 14px/600 slate (consider the new `text-label` token per D-02, but keep slate colour + semibold). Update the file header comment to note the variant + cancelled-ring change with DS-02 / D-03 / D-04.
    Then EXTEND StatusDot.test.tsx (reuse the `CASES` + `it.each` structure): change the `cancelled` expectation from `bg-coral` to the hollow-ring class (`border-coral`); add a case/assertion that each state renders its label under `variant="pill"`; add an assertion that `variant="pill"` renders (e.g. a pill container present) and still shows the worded label. Imports stay `@testing-library/react` + `vitest`.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q "StatusVariant" platform/ui/StatusDot.tsx && grep -q "border-2 border-coral" platform/ui/StatusDot.tsx && grep -q "bg-transparent" platform/ui/StatusDot.tsx && grep -q "pill" platform/ui/StatusDot.test.tsx && npx vitest run platform/ui/StatusDot.test.tsx 2>&1 | tail -8 && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>StatusDot exports `StatusVariant` and accepts `variant` (default "dot"); cancelled is a hollow coral ring in both variants with the label intact; default-dot callers are byte-for-byte equivalent in behaviour; the extended StatusDot.test.tsx passes; typecheck is clean.</done>
  <acceptance_criteria>
    - SOURCE: `grep "StatusVariant" platform/ui/StatusDot.tsx` matches; `variant` prop defaults to `"dot"`.
    - SOURCE: `grep "border-2 border-coral" platform/ui/StatusDot.tsx` and `grep "bg-transparent"` both match (cancelled hollow ring).
    - SOURCE: `data-testid="status-dot"` and `aria-hidden="true"` still present on the shape.
    - TEST: `npx vitest run platform/ui/StatusDot.test.tsx` exits 0; the cancelled case asserts `border-coral` (not `bg-coral`); a `variant="pill"` case asserts the label renders.
    - TYPECHECK: `npm run typecheck` exits 0.
    - BEHAVIOUR: the 7 non-cancelled states keep their existing solid colour class in `variant="dot"`; every state renders a non-empty worded label in both variants.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Presentation-only component change (state→view; status is non-PII). No auth, RLS, payment, secret, or untrusted input is touched. STRIDE surface minimal.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props→render | `state` is a typed `TransferState` enum value supplied by trusted server data; no free-form/user-controlled string is interpolated into markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information disclosure | StatusDot label | accept | Status label is non-PII lifecycle text; no guest data flows through this component. |
| T-09-02-02 | Tampering | render path | mitigate | `state` is constrained to the `TransferState` union (tsc-enforced); no `dangerouslySetInnerHTML`, no untrusted HTML/SVG injected. |
| T-09-02-SC | Tampering | npm installs | accept | No new packages installed (component edit + test only). |
</threat_model>

<verification>
- `npx vitest run platform/ui/StatusDot.test.tsx` passes with the new variant + hollow-ring cases.
- `npm run typecheck` passes (exported `StatusVariant`, defaulted prop).
- Full `npm test` stays green — existing default-dot callers (LifecycleTimeline etc.) unaffected.
</verification>

<success_criteria>
- One `StatusDot` component, two variants (dot/pill), one colour/label source map (DS-02, D-03).
- cancelled = hollow coral ring + "Cancelled" label in both variants (D-04); never colour alone.
- Existing callers unchanged (default variant = "dot").
</success_criteria>

<output>
Create `.planning/phases/09-design-system-foundation/09-02-SUMMARY.md` when done.
</output>
