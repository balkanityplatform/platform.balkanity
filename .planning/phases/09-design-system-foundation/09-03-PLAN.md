---
phase: 09-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - platform/ui/RouteMotif.tsx
  - public/brand/transfer-badge.svg
autonomous: true
requirements: [DS-03]
must_haves:
  truths:
    - "RouteMotif renders a Departure→Arrival route with the real brand Transfer Badge as the auto-centered midpoint connective element"
    - "Endpoints are configurable via start/end {icon,label} props, defaulting to Plane→Building"
    - "The midpoint is the committed brand pictogram served verbatim (never re-drawn / no invented infinity loop)"
  artifacts:
    - path: "platform/ui/RouteMotif.tsx"
      provides: "Reusable route/infinity motif component with configurable endpoints (DS-03, D-09/D-10)"
      contains: "RouteMotif"
    - path: "public/brand/transfer-badge.svg"
      provides: "The committed brand Transfer Badge served under public/ for the midpoint mark"
      contains: "svg"
  key_links:
    - from: "platform/ui/RouteMotif.tsx"
      to: "public/brand/transfer-badge.svg"
      via: "renders the committed brand mark at the midpoint (next/image or img)"
      pattern: "transfer-badge"
---

<objective>
Ship the reusable `RouteMotif` component (DS-03): a Departure→Arrival route visualization whose midpoint connective element is the REAL committed brand Transfer Badge — never a re-drawn logo or invented infinity loop (STATE.md ASSET guardrail, D-10). Endpoints are configurable `start`/`end` `{ icon, label }` props (D-09), defaulting to Plane→Building, so Guest (airport→property), Driver, and Admin surfaces all reuse it.

Purpose: This is the connective brand element every surface's route header needs. The one un-precedented mechanic — serving a committed pictogram SVG inside a React component — is resolved here: copy the committed `platform/ui/pictograms/transfers.svg` to `public/brand/transfer-badge.svg` and reference it via `next/image` (the `app/admin/page.tsx` analog). The SVG is moved/copied VERBATIM — its paths are not edited.
Output: New `platform/ui/RouteMotif.tsx`; the committed Transfer Badge copied to `public/brand/transfer-badge.svg`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-PATTERNS.md
</context>
<artifacts_this_phase_produces>
- **New `platform/ui/RouteMotif.tsx`** (DS-03):
  - Exported `RouteEndpoint` type = `{ icon: ReactNode; label: string }` and `RouteMotifProps` = `{ start?: RouteEndpoint; end?: RouteEndpoint; className?: string }`.
  - `RouteMotif` component: `start` icon+label … connector line … fixed midpoint Transfer Badge … `end` icon+label, midpoint auto-centered. Defaults `start` = Plane endpoint, `end` = Building endpoint (simple inline 1.5px-stroke line pictograms where no brand asset exists, per D-10).
- **New `public/brand/transfer-badge.svg`** — the committed `platform/ui/pictograms/transfers.svg` copied VERBATIM so `next/image` can serve it as the midpoint mark.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Serve the committed Transfer Badge under public/ (D-10 asset delivery)</name>
  <files>public/brand/transfer-badge.svg</files>
  <read_first>
    - platform/ui/pictograms/transfers.svg (the committed brand mark — teal circle + white two-arrow exchange, viewBox `0 0 85.1 85`; this is copied VERBATIM, never edited)
    - platform/ui/pictograms/README.md (the ASSET rule: never re-drawn or invented; only the committed real marks)
    - .planning/STATE.md (v1.1 ASSET guardrail — never re-draw the logo / invent icons)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (RouteMotif section — the SVGs live under `platform/ui/pictograms/` not `public/`; resolve the serving path by copying to `public/brand/` for `next/image`)
  </read_first>
  <action>
    Copy `platform/ui/pictograms/transfers.svg` to `public/brand/transfer-badge.svg` VERBATIM (byte-for-byte; do not edit its paths, viewBox, or the embedded `#009B87`/`#FFFFFF` style fills — it is the committed brand mark). Use a shell copy so the bytes are identical. Do NOT re-draw, re-colour, optimise, or otherwise modify the mark. `public/brand/` already exists (holds `balkanity-logo.png`); add the SVG alongside it.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && [ -f public/brand/transfer-badge.svg ] && diff -q platform/ui/pictograms/transfers.svg public/brand/transfer-badge.svg && echo "VERBATIM OK"</automated>
  </verify>
  <done>`public/brand/transfer-badge.svg` exists and is byte-identical to the committed `platform/ui/pictograms/transfers.svg`.</done>
  <acceptance_criteria>
    - SOURCE: `[ -f public/brand/transfer-badge.svg ]` true.
    - SOURCE: `diff -q platform/ui/pictograms/transfers.svg public/brand/transfer-badge.svg` reports no differences (verbatim copy — mark not re-drawn).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build the RouteMotif component with configurable endpoints + brand midpoint (DS-03, D-09/D-10)</name>
  <files>platform/ui/RouteMotif.tsx</files>
  <read_first>
    - platform/ui/Card.tsx (prop + export conventions: `export type ...Props`, named `export function`, `className = ""` passthrough merged last, top-of-file brand-rule comment)
    - platform/ui/Button.tsx (the same export/comment conventions + the named-token preference D-02)
    - app/admin/page.tsx (the `import Image from "next/image"` usage pattern for serving a brand asset from public/)
    - platform/ui/pictograms/transfers.svg (the midpoint mark being rendered — for understanding its square viewBox so it centers cleanly)
    - .planning/phases/09-design-system-foundation/09-UI-SPEC.md (the Infinity/route motif row in Component Inventory — connective element at the EXACT midpoint of a Departure→Arrival line; Departure Plane → Arrival; never re-draw)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-09 configurable start/end {icon,label} default Plane→Building; D-10 fixed midpoint = real Transfer Badge, simple 1.5px-stroke line pictogram only where no brand asset exists)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (RouteMotif section — props shape, layout `start … line … midpoint badge … end`, midpoint via next/image from public/, inline Plane pictogram permitted)
  </read_first>
  <action>
    Create `platform/ui/RouteMotif.tsx` following the Card/Button file conventions (top-of-file comment citing DS-03 + D-09/D-10 + the ASSET rule; named `export function`; `className = ""` passthrough; prefer the new named tokens from Plan 01 — `rounded-full`, `text-label`, `gap-*` — per D-02). Export a `RouteEndpoint` type `{ icon: ReactNode; label: string }` and `RouteMotifProps` `{ start?: RouteEndpoint; end?: RouteEndpoint; className?: string }`. Render a horizontal route: `start` icon-over/beside-label … a connector line … the FIXED midpoint Transfer Badge (auto-centered) … `end` icon+label. The midpoint mark is the committed brand asset served via `next/image` from `/brand/transfer-badge.svg` (the public path produced in Task 1) — set explicit width/height and an `alt` (e.g. "Balkanity transfer badge"); NEVER inline a re-drawn logo or an invented infinity loop. Default `start` = a Plane endpoint and `end` = a Building endpoint; for these defaults, author a simple inline 1.5px-stroke SVG line pictogram (Plane / Building) — these are line icons where no brand asset exists (permitted by D-10), NOT brand marks. Endpoint `label`s are passed in by the surface (already translated); the component itself takes them as props (do NOT wire i18n inside the component). Do NOT use `dangerouslySetInnerHTML`; render the badge via `next/image` and the line pictograms as inline JSX `<svg>` with literal path data.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q "export function RouteMotif" platform/ui/RouteMotif.tsx && grep -q "RouteEndpoint" platform/ui/RouteMotif.tsx && grep -q "/brand/transfer-badge.svg" platform/ui/RouteMotif.tsx && ! grep -q "dangerouslySetInnerHTML" platform/ui/RouteMotif.tsx && npm run typecheck 2>&1 | tail -3 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>`RouteMotif` renders configurable start/end endpoints (default Plane→Building) with the committed brand Transfer Badge auto-centered at the midpoint via `next/image` from `/brand/transfer-badge.svg`; no re-drawn logo / invented loop; no `dangerouslySetInnerHTML`; typecheck + lint clean.</done>
  <acceptance_criteria>
    - SOURCE: `grep "export function RouteMotif" platform/ui/RouteMotif.tsx` matches; `RouteEndpoint` + `RouteMotifProps` (or equivalent props type with `start`/`end`) present.
    - SOURCE: `grep "/brand/transfer-badge.svg" platform/ui/RouteMotif.tsx` matches (midpoint uses the committed asset path).
    - SOURCE: `grep "dangerouslySetInnerHTML"` returns nothing (no untrusted HTML/SVG injection).
    - SOURCE: defaults present — a Plane endpoint and a Building endpoint as `start`/`end` defaults.
    - TYPECHECK + LINT: `npm run typecheck` and `npm run lint` both exit 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Presentation-only component + a verbatim asset copy. No auth/RLS/payment/data. The two genuinely relevant items: (1) the brand SVG is served from `public/` as a static asset rendered via `next/image` (no inline untrusted SVG); (2) no `dangerouslySetInnerHTML` anywhere.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → render | `transfer-badge.svg` is a committed, reviewed brand asset served from `public/`; not user-supplied. Endpoint icons/labels are trusted props from the surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | midpoint SVG render | mitigate | Render the committed badge via `next/image` from `public/brand/`; NO `dangerouslySetInnerHTML`, NO inline injection of untrusted SVG. Line pictograms use literal in-source path data only. |
| T-09-03-02 | Spoofing | brand mark integrity | mitigate | The asset is copied verbatim from the committed `transfers.svg` (diff-verified); the mark is never re-drawn or invented (ASSET guardrail). |
| T-09-03-SC | Tampering | npm installs | accept | No new packages (`next/image` is already a dependency). |
</threat_model>

<verification>
- `diff -q` proves the served badge is verbatim.
- `npm run typecheck` + `npm run lint` pass.
- `npm run build` succeeds (Plan-04 showcase + surfaces will render it; build proves the import + `next/image` path resolve).
</verification>

<success_criteria>
- `RouteMotif` is reusable with configurable `start`/`end` endpoints (default Plane→Building) (D-09).
- Midpoint is the real committed brand Transfer Badge, served verbatim — never re-drawn / no invented infinity loop (D-10, ASSET guardrail).
- No `dangerouslySetInnerHTML` / untrusted SVG injection.
</success_criteria>

<output>
Create `.planning/phases/09-design-system-foundation/09-03-SUMMARY.md` when done.
</output>
