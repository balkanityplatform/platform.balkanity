---
phase: 09-design-system-foundation
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - platform/ui/LifecycleStepper.tsx
  - platform/ui/LifecycleStepper.test.tsx
autonomous: true
requirements: [DS-04]
must_haves:
  truths:
    - "LifecycleStepper renders the 6 steps Paid→Claimed→En route→Arrived→Picked up→Completed horizontally, driven by a passed-in current state"
    - "Each step encodes its state beyond colour: Completed=teal circle+white check, Active=amber solid circle, Pending=grey outline ring, each with a worded label"
    - "When current is cancelled, the stepper renders a distinct terminal treatment (hollow coral ring + 'Cancelled') instead of the 6-step track"
  artifacts:
    - path: "platform/ui/LifecycleStepper.tsx"
      provides: "New horizontal step-styled lifecycle stepper consuming STEPPER_ORDER + StatusDot (DS-04, D-05/D-06/D-07/D-08)"
      contains: "STEPPER_ORDER"
    - path: "platform/ui/LifecycleStepper.test.tsx"
      provides: "Step-state + cancelled-terminal coverage (D-12)"
      contains: "cancelled"
  key_links:
    - from: "platform/ui/LifecycleStepper.tsx"
      to: "platform/transfers/lifecycle.ts STEPPER_ORDER"
      via: "imports + iterates the 6-step const (no hand-rolled array, D-06)"
      pattern: "STEPPER_ORDER"
---

<objective>
Ship the new horizontal, step-styled `LifecycleStepper` (DS-04, D-05): renders Paid → Claimed → En route → Arrived → Picked up → Completed driven by a passed-in `current` state, with each step encoding its state beyond colour (Completed = teal circle + white check, Active = amber solid circle, Pending = grey `#66676F` outline ring, D-07) plus a worded label per step. When `current` is `cancelled`, it renders a distinct terminal treatment (the StatusDot hollow coral ring + "Cancelled") instead of the 6-step track (D-08). The existing vertical `LifecycleTimeline` is left UNTOUCHED.

Purpose: This is the lifecycle visual every surface uses to show transfer progress (Guest status pass, Driver trip detail, Admin row/detail). It consumes the centralised `STEPPER_ORDER` (Plan 01) and the `StatusDot` colour/label map + cancelled hollow-ring (Plan 02) — never hand-rolling its own order or label arrays (Don't-Hand-Roll lock, D-06).
Output: New `platform/ui/LifecycleStepper.tsx`; new `platform/ui/LifecycleStepper.test.tsx`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-PATTERNS.md
</context>
<artifacts_this_phase_produces>
- **New `platform/ui/LifecycleStepper.tsx`** (DS-04): exported `LifecycleStepper({ current }: { current: TransferState })` — a horizontal step row iterating `STEPPER_ORDER`, each step a shape encoding completed/active/pending state + a worded label, with a distinct `cancelled` terminal treatment. Consumes `STEPPER_ORDER` from lifecycle.ts and `StatusDot`/`STATE_META` labels (no local arrays).
- **New `platform/ui/LifecycleStepper.test.tsx`** (D-12): asserts step-state classification (completed/active/pending) for a sample `current`, that all 6 worded labels render, and that `current="cancelled"` renders the terminal hollow-ring "Cancelled" treatment and NOT the 6-step track.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the horizontal LifecycleStepper consuming STEPPER_ORDER + StatusDot (DS-04, D-05/06/07/08)</name>
  <files>platform/ui/LifecycleStepper.tsx, platform/ui/LifecycleStepper.test.tsx</files>
  <read_first>
    - platform/ui/LifecycleTimeline.tsx (the full current file — the consumption pattern to replicate: `orderIndex` classification into past/current/future, the `aria-current="step"` on the current step, and especially the DISTINCT terminal cancelled row at lines 56-65 you mirror; this file is left UNTOUCHED — it is the starting-point analog only)
    - platform/transfers/lifecycle.ts (the new `STEPPER_ORDER` 6-step const from Plan 01 — import and iterate THIS, not LIFECYCLE_ORDER)
    - platform/ui/StatusDot.tsx (the `TransferState` union, the `STATE_META` labels to derive worded step labels from, and the cancelled hollow-ring treatment from Plan 02 to reuse for the terminal state)
    - platform/ui/Card.tsx (file/export conventions + named-token preference D-02)
    - .planning/phases/09-design-system-foundation/09-UI-SPEC.md (Lifecycle stepper row in Component Inventory + Stepper labels line — exact order/casing Paid → Claimed → En route → Arrived → Picked up → Completed; Completed=teal circle+white check, Active=amber solid circle, Pending=grey #66676F outline ring)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-05 new separate component, LifecycleTimeline untouched; D-06 consume STEPPER_ORDER; D-07 shape-encodes-state; D-08 cancelled = distinct terminal treatment, not wedged into the track)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (the LifecycleStepper section — import STEPPER_ORDER, horizontal layout, step shape per D-07, cancelled per D-08, derive labels from STATE_META not a hand-rolled array)
  </read_first>
  <behavior>
    - Given `current="claimed"` (index 1 in STEPPER_ORDER): step "paid" is Completed (teal circle + white check), step "claimed" is Active (amber solid circle), steps "en_route"/"arrived"/"picked_up"/"completed" are Pending (grey outline ring). Every step renders its worded label.
    - Given `current="completed"`: all 6 steps are Completed (teal + check), last is the active/terminal step.
    - Given `current="cancelled"`: the 6-step track is NOT rendered; instead a single distinct terminal treatment renders — the StatusDot cancelled hollow coral ring + the "Cancelled" worded label.
    - The current/active step carries `aria-current="step"`.
    - Worded labels are derived from the single STATE_META source (Paid, Claimed, En route, Arrived, Picked up, Completed), not a local array.
  </behavior>
  <action>
    Create `platform/ui/LifecycleStepper.tsx` (a NEW separate component; do NOT modify LifecycleTimeline — no orientation-prop refactor, D-05). Follow the LifecycleTimeline consumption pattern but render HORIZONTALLY (e.g. a `flex items-center` row with connector segments between steps) iterating `STEPPER_ORDER` (imported from lifecycle.ts — never a hand-rolled array, D-06), prop `{ current: TransferState }`. Classify each step relative to `current`'s index in STEPPER_ORDER: Completed (index < current) = teal circle + a white check mark inside; Active (index === current) = amber solid circle; Pending (index > current) = grey `#66676F` outline ring (`border border-grey`). Each step ALSO renders a worded label beneath/beside it (WCAG 1.4.1 — shape is never the sole signal); derive the label text from `StatusDot`'s `STATE_META` (do NOT hand-roll a label array). Put `aria-current="step"` on the active step. For `current === "cancelled"` (D-08): short-circuit and render a DISTINCT terminal treatment mirroring LifecycleTimeline's cancelled row — render `<StatusDot state="cancelled" />` (the hollow coral ring + "Cancelled" label) as a terminal banner/badge INSTEAD of the 6-step track; do NOT wedge cancelled into the horizontal row. Prefer the Plan-01 named tokens (`rounded-full`, `text-label`, `gap-*`) per D-02. Add a top-of-file comment citing DS-04 + D-05/06/07/08 + the Don't-Hand-Roll lock.
    Then create `platform/ui/LifecycleStepper.test.tsx` (vitest + @testing-library/react, mirroring the StatusDot.test.tsx structure): assert that for a representative `current` (e.g. "claimed") the completed step shows the completed treatment, the active step the active treatment, and a pending step the outline-ring treatment (query by class or test ids you add); assert all 6 worded labels render; assert `current="cancelled"` renders the "Cancelled" terminal treatment (hollow-ring class `border-coral`) and that the 6 happy-path step labels are absent.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q "STEPPER_ORDER" platform/ui/LifecycleStepper.tsx && ! grep -q "\"paid\".*\"claimed\".*\"en_route\"" platform/ui/LifecycleStepper.tsx && grep -q "aria-current" platform/ui/LifecycleStepper.tsx && grep -q "cancelled" platform/ui/LifecycleStepper.test.tsx && npx vitest run platform/ui/LifecycleStepper.test.tsx 2>&1 | tail -8 && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>`LifecycleStepper` renders the 6 STEPPER_ORDER steps horizontally with distinct completed/active/pending shapes + worded labels, a distinct cancelled terminal treatment, consuming STEPPER_ORDER + STATE_META (no hand-rolled arrays); LifecycleTimeline is unchanged; the new test passes; typecheck clean.</done>
  <acceptance_criteria>
    - SOURCE: `grep "STEPPER_ORDER" platform/ui/LifecycleStepper.tsx` matches (imports + iterates the const); no inline 6-element happy-path array literal declared in the component.
    - SOURCE: step shapes present — teal completed circle with a check, amber active circle, grey outline-ring pending (`border-grey`); `aria-current="step"` on the active step.
    - SOURCE: cancelled path renders `<StatusDot state="cancelled" />` terminal treatment, NOT a 7th track step.
    - SOURCE: `platform/ui/LifecycleTimeline.tsx` is byte-unchanged (not in this plan's diff).
    - TEST: `npx vitest run platform/ui/LifecycleStepper.test.tsx` exits 0 — covers completed/active/pending classification, all 6 labels, and the cancelled terminal case.
    - TYPECHECK: `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Presentation-only state→view component (lifecycle status is non-PII). No auth/RLS/payment/data/input. STRIDE surface minimal.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props→render | `current` is a typed `TransferState` from trusted server data; no free-form user string interpolated into markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information disclosure | step labels | accept | Step/status labels are non-PII lifecycle text; no guest data flows through the stepper. |
| T-09-04-02 | Tampering | render path | mitigate | `current` constrained to the `TransferState` union (tsc-enforced); no `dangerouslySetInnerHTML`; the check mark and shapes are inline JSX/tokens. |
| T-09-04-SC | Tampering | npm installs | accept | No new packages (component + test only). |
</threat_model>

<verification>
- `npx vitest run platform/ui/LifecycleStepper.test.tsx` passes.
- `npm run typecheck` passes.
- Full `npm test` stays green; `LifecycleTimeline` untouched and its tests (if any) unaffected.
</verification>

<success_criteria>
- A new separate horizontal `LifecycleStepper` renders STEPPER_ORDER with shape-encoded completed/active/pending styling + worded labels (DS-04, D-05/06/07).
- cancelled = distinct terminal treatment, not wedged into the track (D-08).
- Consumes STEPPER_ORDER + StatusDot/STATE_META; no hand-rolled order/label arrays (D-06).
</success_criteria>

<output>
Create `.planning/phases/09-design-system-foundation/09-04-SUMMARY.md` when done.
</output>
