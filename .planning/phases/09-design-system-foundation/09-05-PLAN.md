---
phase: 09-design-system-foundation
plan: 05
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - app/dev/design-system/page.tsx
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04]
must_haves:
  truths:
    - "A dev-only showcase route renders every Phase 9 deliverable across its states/variants and is eyeballable in-browser"
    - "StatusDot is shown across all 8 states in both dot and pill variants; LifecycleStepper at each state + cancelled; a RouteMotif sample; token swatches; the type scale"
    - "The route is NOT linked from any user-facing nav and is gated off production"
  artifacts:
    - path: "app/dev/design-system/page.tsx"
      provides: "Throwaway dev showcase route for the Phase 9 design-system foundation (D-11)"
      contains: "LifecycleStepper"
    - path: "platform/i18n/en.ts"
      provides: "Showcase chrome labels (EN) behind the parity gate"
      contains: "devShowcase"
    - path: "platform/i18n/bg.ts"
      provides: "Showcase chrome labels (BG) — parity with en.ts (tsc-enforced)"
      contains: "devShowcase"
  key_links:
    - from: "app/dev/design-system/page.tsx"
      to: "platform/ui (StatusDot, LifecycleStepper, RouteMotif)"
      via: "imports + renders every Phase 9 component across states/variants"
      pattern: "LifecycleStepper"
---

<objective>
Build the throwaway, dev-only showcase route (D-11) that renders every Phase 9 deliverable across all states/variants so the foundation can be eyeballed in-browser before any surface consumes it: `StatusDot` × 8 states × {dot, pill}, `LifecycleStepper` at each `STEPPER_ORDER` state + cancelled, a `RouteMotif` sample, token swatches (colours/radii/spacing), and the type scale (display/heading/body/label). Add the showcase's chrome labels to the EN/BG dictionary behind the tsc parity gate. The route is gated off production and never linked from user-facing nav.

Purpose: This is the phase's "demonstrable slice" — the renderable proof that DS-01 tokens + DS-02/03/04 components are correct and consistent. It is the in-browser verification surface (D-11) and the reference the surface phases (10/11/12) read before consuming the components.
Output: New `app/dev/design-system/page.tsx`; new showcase keys in `platform/i18n/en.ts` + `platform/i18n/bg.ts`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-foundation/09-UI-SPEC.md
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-PATTERNS.md
</context>
<artifacts_this_phase_produces>
- **New `app/dev/design-system/page.tsx`** (D-11): a Server Component dev-only showcase rendering all Phase 9 deliverables across states/variants. Production-gated (404 in prod via `NODE_ENV`/`notFound()`), not linked from any nav.
- **New i18n keys in `platform/i18n/en.ts` + `platform/i18n/bg.ts`**: showcase chrome strings (section headings such as a `devShowcase*` prefix) added to BOTH dictionaries so the tsc parity gate stays green. (Component-internal StatusDot/stepper labels stay as-is — existing pattern; only NEW showcase chrome strings need keys.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add showcase chrome labels to the EN/BG dictionary (parity gate)</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts</files>
  <read_first>
    - platform/i18n/en.ts (the full shape — keys end before `} as const;` and the `Dict` type derivation; you insert new keys before that closing marker)
    - platform/i18n/bg.ts (the parallel file — ends with `};`; every en.ts key must have a bg.ts counterpart or tsc fails)
    - platform/i18n/dictionary.ts (the `getDict()` / `getLang()` server-side accessors the page uses)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (the i18n parity-gate section — add the key to BOTH en.ts and bg.ts; only NEW showcase chrome strings strictly need keys)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-11 — showcase renders token swatches, type scale, all component states; Claude's Discretion covers exact key names)
  </read_first>
  <action>
    Add a small set of showcase chrome label keys (use a clear `devShowcase`-prefixed naming, e.g. a page heading + one heading per section: tokens/colours, radii, spacing, type scale, StatusDot, LifecycleStepper, RouteMotif) to `platform/i18n/en.ts` (English canonical copy, inserted before the `} as const;` closing marker) AND to `platform/i18n/bg.ts` (Bulgarian translations, inserted before its `};` closing marker) — the SAME key set in both so the `Dict` parity gate passes. Keep copy short and presentational ("Status badge", "Lifecycle stepper", "Route motif", "Type scale", "Tokens", etc.). Do NOT route the component-internal StatusDot/stepper status labels through the dictionary (they stay English-only per the existing pattern) — only the showcase's own section chrome needs keys.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q "devShowcase" platform/i18n/en.ts && grep -q "devShowcase" platform/i18n/bg.ts && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>Matching `devShowcase*` keys exist in both en.ts and bg.ts; `tsc --noEmit` passes (parity gate green).</done>
  <acceptance_criteria>
    - SOURCE: `grep "devShowcase" platform/i18n/en.ts` and `grep "devShowcase" platform/i18n/bg.ts` both match the SAME key names.
    - TYPECHECK: `npm run typecheck` exits 0 (a missing/extra key would fail the parity gate).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build the dev-only design-system showcase route (D-11)</name>
  <files>app/dev/design-system/page.tsx</files>
  <read_first>
    - app/admin/page.tsx (the Server Component page shape: async default export, `const [t, lang] = await Promise.all([getDict(), getLang()])`, JSX layout)
    - app/page.tsx (route-file conventions for an App Router page)
    - platform/ui/StatusDot.tsx (the `TransferState` union + the `variant` prop from Plan 02 — render all 8 states × {dot, pill})
    - platform/transfers/lifecycle.ts (STEPPER_ORDER from Plan 01 — iterate it to show the stepper at each state; plus render the cancelled terminal case)
    - platform/ui/LifecycleStepper.tsx (the Plan 04 component — render at each state + cancelled)
    - platform/ui/RouteMotif.tsx (the Plan 03 component — render a sample, e.g. default Plane→Building and one with custom labels like "SOF Airport"→a property name)
    - app/globals.css (the Plan 01 tokens — render swatches for `--color-*`, `rounded-sm/md/lg/xl/full`, spacing aliases, and the type scale `text-display/heading/body/label`)
    - .planning/phases/09-design-system-foundation/09-CONTEXT.md (D-11 full inventory of what the showcase must render; not linked from user-facing nav; dev-only path)
    - .planning/phases/09-design-system-foundation/09-PATTERNS.md (the showcase route section — `app/dev/design-system/` creates `app/dev/`; no auth gate required but keep off all nav; visible label strings go through the dictionary)
  </read_first>
  <action>
    Create `app/dev/design-system/page.tsx` as a Server Component (mirror app/admin/page.tsx: `export default async function`, resolve `getDict()`/`getLang()` for the chrome labels added in Task 1). FIRST, gate it off production: if `process.env.NODE_ENV === "production"`, call `notFound()` (from `next/navigation`) so the route 404s in prod — it is a throwaway dev showcase, never reachable in production, never linked from user nav. Render sections for every Phase 9 deliverable using the Task-1 `devShowcase*` labels as section headings:
    1. Tokens: colour swatches for all seven `--color-*` brand tokens; radii samples (`rounded-sm/md/lg/xl/full`); spacing samples (touch-target 44 / cta-height 52 / gutter / margin).
    2. Type scale: one line each at `text-display`, `text-heading`, `text-body`, `text-label`.
    3. StatusDot: every one of the 8 `TransferState` values rendered in BOTH `variant="dot"` and `variant="pill"` (a small matrix) — confirming the cancelled hollow-ring in both.
    4. LifecycleStepper: rendered once per `STEPPER_ORDER` state (passing each as `current`) PLUS one with `current="cancelled"` (the terminal treatment).
    5. RouteMotif: a default Plane→Building sample and one with custom endpoint labels.
    Do NOT add a link to this route from any header/sidebar/nav. Do NOT add an auth gate (it is a non-prod showcase). Keep imports from `@/platform/ui/*` and `@/platform/transfers/lifecycle`. Add a top-of-file comment: dev-only D-11 showcase, production-gated, not linked from nav, safe to delete later.
  </action>
  <verify>
    <automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q "notFound" app/dev/design-system/page.tsx && grep -q "NODE_ENV" app/dev/design-system/page.tsx && grep -q "LifecycleStepper" app/dev/design-system/page.tsx && grep -q "RouteMotif" app/dev/design-system/page.tsx && grep -q "StatusDot" app/dev/design-system/page.tsx && grep -q "STEPPER_ORDER" app/dev/design-system/page.tsx && ! grep -rq "design-system" app/admin app/page.tsx platform/ui/LanguageToggle.tsx 2>/dev/null && npm run build 2>&1 | tail -6</automated>
  </verify>
  <done>`app/dev/design-system/page.tsx` renders all Phase 9 deliverables across states/variants, 404s in production (NODE_ENV + notFound), is not linked from any nav, and the build succeeds.</done>
  <acceptance_criteria>
    - SOURCE: page imports + renders `StatusDot` (8 states × dot+pill), `LifecycleStepper` (iterated over STEPPER_ORDER + a cancelled case), `RouteMotif` (a sample), token swatches, and the type scale.
    - SOURCE: `grep "notFound" && grep "NODE_ENV"` both match — production-gated.
    - SOURCE: no nav/header/sidebar file references `design-system` (route is not linked from user-facing nav).
    - BUILD: `npm run build` exits 0 (proves all imports + the RouteMotif `/brand/transfer-badge.svg` path resolve and the tokens render).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
Presentation-only dev showcase. The two genuinely relevant items (per the phase security note): (1) the dev showcase route MUST NOT be reachable in production / linked from user nav; (2) no secrets/PII enter the showcase. No backend/data/auth/payment touched.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request→dev route | `app/dev/design-system` is a non-prod showcase; gated off production and unlinked from any user-facing nav so it is not a discoverable surface in the deployed app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information disclosure | dev showcase route exposure in prod | mitigate | Gate the route with `process.env.NODE_ENV === "production"` → `notFound()` (404 in prod); never link it from header/sidebar/nav. Verify gate via grep + the no-nav-reference check. |
| T-09-05-02 | Information disclosure | PII/secrets in showcase | mitigate | Showcase renders only static design samples + hardcoded label props ("SOF Airport", a sample property name); no DB read, no env secrets, no guest PII flows in. |
| T-09-05-SC | Tampering | npm installs | accept | No new packages (page + dictionary edits only). |
</threat_model>

<verification>
- `npm run build` succeeds with the new route.
- `npm run typecheck` passes (i18n parity gate green with the new `devShowcase*` keys).
- Grep confirms the production gate (`NODE_ENV` + `notFound`) and that no nav file references the route.
- Manual eyeball (dev server): visit `/dev/design-system`, confirm every deliverable renders across states/variants (the D-11 demonstrable slice).
</verification>

<success_criteria>
- A dev-only, production-gated, unlinked showcase route renders every Phase 9 deliverable across states/variants (D-11) — the in-browser proof of the foundation.
- Showcase chrome strings live behind the EN/BG parity gate; component-internal status labels unchanged.
- DS-01/02/03/04 are all demonstrably renderable together.
</success_criteria>

<output>
Create `.planning/phases/09-design-system-foundation/09-05-SUMMARY.md` when done.
</output>
