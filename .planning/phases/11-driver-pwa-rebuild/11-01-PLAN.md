---
phase: 11-driver-pwa-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
  - app/driver/_ui/icons.tsx
  - app/driver/_nav/DriverBottomNav.tsx
  - app/driver/layout.tsx
autonomous: true
requirements: [DUI-02]
must_haves:
  truths:
    - "Every driver route renders one persistent bottom nav with Available / My Trips / Profile, the current tab highlighted in teal"
    - "Every driver route renders one slim top header (logo + Alerts bell + Language toggle) from the shared layout"
    - "Every new user-facing string exists in BOTH en.ts and bg.ts (tsc passes)"
  artifacts:
    - path: "app/driver/layout.tsx"
      provides: "Shared driver shell: slim top header (logo + NotificationBell seeded from readOwnNotifications + LanguageToggle) + DriverBottomNav, with bottom padding for the fixed nav (D-01)"
      contains: "readOwnNotifications"
    - path: "app/driver/_nav/DriverBottomNav.tsx"
      provides: "Client island bottom nav, active tab via usePathname() (DUI-02)"
      contains: "usePathname"
    - path: "app/driver/_ui/icons.tsx"
      provides: "Luggage + 3 nav-tab 1.5px-stroke line icons in the guest _pass/icons.tsx style"
    - path: "platform/i18n/en.ts"
      provides: "New nav + badge + CTA + sign-out + detail-caption keys (EN)"
      contains: "navAvailable"
    - path: "platform/i18n/bg.ts"
      provides: "Identical key set with BG values (parity gate)"
      contains: "navAvailable"
  key_links:
    - from: "app/driver/layout.tsx"
      to: "app/driver/_nav/DriverBottomNav.tsx"
      via: "import + render with copy prop bag"
      pattern: "DriverBottomNav"
    - from: "app/driver/_nav/DriverBottomNav.tsx"
      to: "next/navigation usePathname"
      via: "active-tab derivation"
      pattern: "usePathname"
    - from: "app/driver/layout.tsx"
      to: "platform/notifications/feed readOwnNotifications"
      via: "bell seed in the layout RSC"
      pattern: "readOwnNotifications"
---

<objective>
Build the shared driver shell that every driver surface slice consumes: a single `app/driver/layout.tsx` that mounts the slim top header (logo + Alerts bell + Language toggle) and the new persistent `DriverBottomNav` once (D-01), the surface-local line-icon set (`_ui/icons.tsx`), and ALL new EN/BG dictionary keys this phase introduces. This is the MVP foundation slice — after it lands, every driver route gains the bottom nav (DUI-02) and the four surface restyle slices (Plans 02–05) run in parallel against a fixed chrome + dictionary.

Purpose: Consolidate the four duplicated `<header>` blocks into one source and give every later slice its nav, icons, and copy contract up front (interface-first ordering — contracts before consumers).
Output: `app/driver/layout.tsx`, `app/driver/_nav/DriverBottomNav.tsx`, `app/driver/_ui/icons.tsx`, and new keys in `platform/i18n/en.ts` + `bg.ts`.
</objective>

## Phase Goal

**As a** Balkanity driver, **I want to** move between Available, My Trips, and Profile from a persistent bottom navigation bar with the current tab highlighted, **so that** the driver PWA reads as a real native-style app and I always know where I am.

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md
@.planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md
@.planning/phases/11-driver-pwa-rebuild/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add all Phase 11 EN/BG dictionary keys</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts</files>
  <read_first>
    - platform/i18n/en.ts — existing driver key block (lines 117-119 airport/zone/address; 237-252 claim/run/advance keys); `Dict = typeof en` parity contract
    - platform/i18n/bg.ts — the file that must mirror every en.ts key (`bg: Dict`); tsc fails on any missing/extra key
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "platform/i18n/en.ts + bg.ts (MODIFY — parity gate)" section listing the exact new keys + reuse-verbatim keys
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — "Copywriting Contract" table for the exact EN copy of each new key
  </read_first>
  <action>
    Add these NEW keys to BOTH platform/i18n/en.ts and platform/i18n/bg.ts (EN value from the UI-SPEC Copywriting Contract; provide a real BG translation, never the EN string copied):
    - `navAvailable` ("Available"), `navMyTrips` ("My Trips"), `navProfile` ("Profile") — bottom-nav labels (DUI-02).
    - `driverUnclaimedBadge` ("Unclaimed") — the coral DS-02 pool badge (DUI-01, consumed by Plan 02).
    - `driverConfirmArrivalCta` ("Confirm arrival") — the en_route→arrived advance label (DUI-04, consumed by Plan 04).
    - `driverSignOutCta` ("Sign out") — Profile sign-out button (D-05, consumed by Plan 05).
    - Detail-grid caption keys replacing the hardcoded English in app/driver/run/[id]/page.tsx: `driverArrivalLabel` ("Arrival"), `driverFlightLabel` ("Flight"), `driverFareLabel` ("Fare"), `driverPassengersLabel` ("Passengers"), `driverLuggageLabel` ("Luggage"), `driverGuestNameLabel` ("Guest name"), `driverGuestPhoneLabel` ("Guest phone"), `driverNotesLabel` ("Notes") — consumed by Plan 04.
    Do NOT add a `"unclaimed"` TransferState and do NOT edit STATE_META (Pitfall 5) — `driverUnclaimedBadge` is presentation copy only. Do NOT duplicate keys that already exist verbatim (`claimTransferCta`, `claimLostToast`, `claimFailedToast`, `poolEmptyHeading`, `poolEmptyBody`, `myRunTitle`, `runEmptyHeading`, `runEmptyBody`, `completedTodayTitle`, `advanceToEnRouteCta`/`advanceToArrivedCta`/`advanceToPickedUpCta`/`advanceToCompletedCta`, `advanceFailedToast`, `airportLabel`, `zoneLabel`, `addressLabel`, `langToggle`) — reuse them. Keep both dictionaries in the same key order so the parity diff is clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (proves en.ts/bg.ts key parity — no missing BG key).
    - Source assertion: every new key (navAvailable, navMyTrips, navProfile, driverUnclaimedBadge, driverConfirmArrivalCta, driverSignOutCta, driverArrivalLabel, driverFlightLabel, driverFareLabel, driverPassengersLabel, driverLuggageLabel, driverGuestNameLabel, driverGuestPhoneLabel, driverNotesLabel) is present in BOTH en.ts and bg.ts.
    - Source assertion: no `"unclaimed"` TransferState added and STATE_META unchanged.
    - Source assertion: BG values are translated (not byte-identical to the EN value for translatable strings).
  </acceptance_criteria>
  <done>tsc green; all 14 new keys present in both dictionaries with translated BG values; no STATE_META / TransferState change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface-local line icons (Luggage + 3 nav-tab icons)</name>
  <files>app/driver/_ui/icons.tsx</files>
  <read_first>
    - app/(guest)/_pass/icons.tsx — EXACT analog: the `baseProps` factory (width/height 20-24, viewBox "0 0 24 24", fill none, stroke currentColor, strokeWidth 1.5, round caps, aria-hidden) and each `export function XIcon(props: SVGProps<SVGSVGElement>)` shape; PlaneIcon/BuildingIcon/CalendarIcon/ClockIcon/PeopleIcon already there
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "app/driver/_ui/icons.tsx (NEW — assets)" section with the baseProps excerpt to mirror
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — "Driver line pictograms" inventory row + Icon library row (1.5px-stroke, NEVER Material Symbols / re-drawn logo / invented loop)
  </read_first>
  <action>
    Create app/driver/_ui/icons.tsx mirroring app/(guest)/_pass/icons.tsx exactly: the same `baseProps(props)` factory and the same `export function NameIcon(props: SVGProps<SVGSVGElement>)` pattern with literal SVG paths at strokeWidth 1.5, strokeLinecap/Linejoin round, aria-hidden. Export a `LuggageIcon` (for the claim/trip card luggage meta) and three nav-tab icons — `AvailableTabIcon`, `MyTripsTabIcon`, `ProfileTabIcon` — each a simple recognizable line glyph (e.g. a list/grid for Available, a route/car for My Trips, a person for Profile) in the same 1.5px-stroke style. Do NOT import Material Symbols, do NOT re-draw the Balkanity logo, do NOT invent an infinity loop. Reuse the existing guest Plane/Building/Calendar/Clock/People icons by importing from `@/app/(guest)/_pass/icons` where the surface slices need them (do NOT re-declare those five here).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/driver/_ui/icons.tsx exists and exports LuggageIcon, AvailableTabIcon, MyTripsTabIcon, ProfileTabIcon.
    - Source assertion: every icon uses `stroke="currentColor"` and `strokeWidth={1.5}` (or strokeWidth="1.5") — no Material Symbols import, no `<img>` of a logo.
    - Source assertion: the five guest icons (Plane/Building/Calendar/Clock/People) are NOT re-declared in this file.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Four new line icons exist in the 1.5px-stroke guest style; tsc green; no Material Symbols / re-drawn brand mark.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DriverBottomNav island + shared app/driver/layout.tsx</name>
  <files>app/driver/_nav/DriverBottomNav.tsx, app/driver/layout.tsx</files>
  <read_first>
    - app/driver/page.tsx — the bell seed (`const bellInitial = await readOwnNotifications();` line 43) and the `bellCopy` prop bag (lines 72-80) the layout must now own; the warm-light header markup pattern
    - app/driver/settings/page.tsx — the existing `<header>` block (lines 59-70: logo Image + LanguageToggle) to lift into the layout
    - platform/ui/NotificationBell.tsx — `NotificationBell` props: `initial: NotificationRow[]`, `lang: "en"|"bg"`, `copy: NotificationBellCopy` (the bell copy keys alertsTrigger/alertsTriggerAria/alertsPanelTitle/markAllReadCta/alertsEmptyHeading/alertsEmptyBody/alertsLoadFailed)
    - platform/ui/LanguageToggle.tsx — small client island taking `current` + `label`; the structural analog for DriverBottomNav (current + copy prop bag)
    - platform/notifications/feed.ts — `readOwnNotifications(): Promise<NotificationRow[]>` (callable from a layout RSC)
    - platform/i18n/dictionary.ts — `getDict` / `getLang` for server-side copy resolution
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "app/driver/layout.tsx (NEW)" header markup excerpt + Pattern 2 shell shape; "DriverBottomNav.tsx (NEW)" contract (fixed bottom, 1px top border, pb-[env(safe-area-inset-bottom)], 3 tabs, ≥44px hit, 12px/600 active-teal labels)
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — "Bottom navigation bar" inventory row + Color "active bottom-nav tab" reserved-teal note + Typography 12px/600 nav-label exception
  </read_first>
  <action>
    Create app/driver/_nav/DriverBottomNav.tsx as a `"use client"` island modeled on LanguageToggle's prop-bag shape. Props: `{ copy: { navAvailable: string; navMyTrips: string; navProfile: string } }` (lang not needed — labels arrive resolved). Use `usePathname()` from `next/navigation` to derive the active tab: Available active when pathname === "/driver" (exact match, NOT a prefix, so it does not light under /driver/run), My Trips active when pathname starts with "/driver/run" (covers /driver/run AND /driver/run/[id] per D-02), Profile active when pathname starts with "/driver/settings". Render three `next/link` tabs (icon from app/driver/_ui/icons.tsx + a 12px/font-semibold label — the one deliberate sub-14px exception) targeting /driver, /driver/run, /driver/settings; active tab = teal icon+label (+ small teal indicator), inactive = slate/grey; each tab ≥44px tall. Container: fixed bottom-0 inset-x-0, bg-white, 1px top border (border-grey/20), `pb-[env(safe-area-inset-bottom)]`.
    Create app/driver/layout.tsx as an RSC (Pattern 2): resolve `[t, lang]` via Promise.all([getDict(), getLang()]); seed the bell with `const bellInitial = await readOwnNotifications();`. Render a min-h-dvh white wrapper with bottom padding ≈ nav height (`pb-[calc(64px+env(safe-area-inset-bottom))]`), a slim top `<header>` (logo Image chip + `<NotificationBell initial={bellInitial} lang={lang} copy={{...bell keys}} />` + `<LanguageToggle current={lang} label={t.langToggle} />`), a `<main>{children}</main>`, and `<DriverBottomNav copy={{ navAvailable: t.navAvailable, navMyTrips: t.navMyTrips, navProfile: t.navProfile }} />`. Pass the same bell copy keys the page currently passes (alertsTrigger…alertsLoadFailed). Do NOT delete the per-page `<header>` blocks here — each surface slice (Plans 02–05) removes its own page header so file ownership stays disjoint (transient double-header until each slice lands is acceptable mid-wave).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver</automated>
  </verify>
  <acceptance_criteria>
    - app/driver/layout.tsx exists and contains `readOwnNotifications` (bell seeded from the layout RSC).
    - app/driver/layout.tsx renders `<DriverBottomNav` and `<NotificationBell` and `<LanguageToggle`.
    - app/driver/_nav/DriverBottomNav.tsx exists, is a `"use client"` island, and contains `usePathname`.
    - Source assertion: DriverBottomNav links target exactly "/driver", "/driver/run", "/driver/settings".
    - Source assertion: Available active state uses an exact "/driver" equality (not startsWith) so it is not active under /driver/run.
    - Source assertion: nav container uses `pb-[env(safe-area-inset-bottom)]` (or the calc form) and a fixed-bottom class.
    - `npx tsc --noEmit` exits 0; `npx vitest run app/driver` passes (existing 4 driver tests green — no behaviour change).
  </acceptance_criteria>
  <done>Shared layout mounts header+bell+nav once; bottom nav highlights the active tab via usePathname; tsc green; existing driver tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols/files created here (exclude from drift/source-grounding verification — they are NEW, not pre-existing):
- File `app/driver/layout.tsx` — shared driver shell RSC (D-01).
- File `app/driver/_nav/DriverBottomNav.tsx` — `DriverBottomNav` client island (DUI-02).
- File `app/driver/_ui/icons.tsx` — `LuggageIcon`, `AvailableTabIcon`, `MyTripsTabIcon`, `ProfileTabIcon`.
- New i18n keys (en.ts + bg.ts): `navAvailable`, `navMyTrips`, `navProfile`, `driverUnclaimedBadge`, `driverConfirmArrivalCta`, `driverSignOutCta`, `driverArrivalLabel`, `driverFlightLabel`, `driverFareLabel`, `driverPassengersLabel`, `driverLuggageLabel`, `driverGuestNameLabel`, `driverGuestPhoneLabel`, `driverNotesLabel`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver browser → driver RSC (layout/pages) | Authenticated driver session; chrome only, no data write |
| driver browser → readOwnNotifications | Own-rows-only feed read (caller-auth RLS), seeded server-side |

No NEW trust boundary is introduced. The layout only relocates the already-existing bell seed (`readOwnNotifications`, own-rows RLS) from the page to the layout RSC; the bottom nav is pure client routing (no data).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | Bell seed moved to layout RSC | accept | `readOwnNotifications()` is unchanged own-rows-only caller-auth RLS; layout is server-side and may call it; no PII surface added. Verify no double-fetch when pages later drop their headers (Pitfall 1). |
| T-11-02 | Spoofing | DriverBottomNav active state | accept | Pure client `usePathname()` highlight; no authz decision rides on it — every route still re-gates via `getCurrentRole()`. |
| T-11-03 | Information Disclosure | New i18n keys | accept | Static UI strings only; `driverUnclaimedBadge` is presentation copy over `status='paid'` data, introduces no PII key and no STATE_META change. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs this phase (RESEARCH Package Legitimacy Audit: zero packages). No slopcheck needed. |

PII guard: this plan renders NO transfer rows. The pool/claim PII boundary is preserved by Plan 02 (it reads `wp_pool()` only). No guest-PII key is referenced anywhere in this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` green (EN/BG parity gate).
- `npx vitest run app/driver` green (existing 4 driver behavioural tests unchanged).
- Source review: layout consolidates header+bell+nav; DriverBottomNav active-state logic correct for the four routes (D-02 keeps My Trips active on /driver/run/[id]).
</verification>

<success_criteria>
- DUI-02 foundation in place: a persistent bottom nav (Available / My Trips / Profile) is mounted once via the shared layout and highlights the active tab.
- All 14 new EN/BG keys exist with translated BG values (tsc parity green).
- Surface-local Luggage + 3 nav icons exist in the guest 1.5px-stroke style (no Material Symbols / re-drawn logo).
- Existing driver tests stay green (presentation-only guarantee).
</success_criteria>

<output>
Create `.planning/phases/11-driver-pwa-rebuild/11-01-SUMMARY.md` when done.
</output>
