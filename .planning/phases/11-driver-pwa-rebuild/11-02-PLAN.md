---
phase: 11-driver-pwa-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/driver/page.tsx
  - app/driver/PoolView.tsx
autonomous: true
requirements: [DUI-01, DUI-05]
must_haves:
  truths:
    - "Available transfers render as claim cards: arrival date · pickup time, coral Unclaimed badge, route with the infinity/RouteMotif, flight/fare/pax/luggage meta, and a 52px teal Claim CTA"
    - "Claim cards render ZERO guest-PII keys pre-claim (only the 9 masked wp_pool() columns)"
    - "Tapping Claim invokes the existing claimAction → atomic claim_transfer RPC: win pushes to the trip detail, already-claimed shows a neutral toast and silently removes the card, other failures show a coral error toast"
    - "The pool refreshes live (focus + ~25s poll) exactly as before"
  artifacts:
    - path: "app/driver/PoolView.tsx"
      provides: "Restyled claim-card pool island consuming RouteMotif + coral Unclaimed badge + Button; claim/poll/PII logic verbatim (DUI-01/DUI-05)"
      contains: "claimAction"
    - path: "app/driver/page.tsx"
      provides: "Pool RSC: masked wp_pool() read unchanged; per-page header removed (now in layout)"
      contains: "wp_pool"
  key_links:
    - from: "app/driver/PoolView.tsx"
      to: "app/driver/actions.ts claimAction"
      via: "onClaim → claimAction(id) (atomic claim_transfer RPC)"
      pattern: "claimAction"
    - from: "app/driver/page.tsx"
      to: "wp_pool() RPC"
      via: "supabase.rpc('wp_pool') masked read"
      pattern: "rpc\\(\"wp_pool\"\\)"
    - from: "app/driver/PoolView.tsx"
      to: "platform/ui/RouteMotif"
      via: "claim-card route visualization"
      pattern: "RouteMotif"
---

<objective>
Re-skin the Available pool into mockup claim cards (DUI-01) while keeping the masked-pool read, the atomic claim path, and the live-refresh poll byte-for-byte intact (DUI-05). Each masked-pool row becomes a card: arrival date · pickup time, a coral "Unclaimed" DS-02 badge, a `RouteMotif` (airport → zone), a flight/fare/pax/luggage meta row, and a 52px teal "Claim transfer" CTA. The card renders ONLY the 9 columns `wp_pool()` returns — zero guest PII pre-claim, the core invariant this plan must not regress.

Purpose: Deliver the first user-visible driver capability of the rebuild — a driver opens Available and claims a paid transfer from styled cards — on the existing correct data layer.
Output: restyled `app/driver/PoolView.tsx` + `app/driver/page.tsx` (header removed, read unchanged).
</objective>

## Phase Goal

**As a** Balkanity driver, **I want to** see paid, unclaimed transfers as clear claim cards (with no passenger personal details) and claim one with a single tap, **so that** I can pick up work safely and reliably with first-to-claim-wins.

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md
@.planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md
@.planning/phases/11-driver-pwa-rebuild/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle PoolView into claim cards (preserve claim + poll + PII boundary)</name>
  <files>app/driver/PoolView.tsx</files>
  <read_first>
    - app/driver/PoolView.tsx — self; KEEP VERBATIM the focus+~25s poll, the `PoolRow` shape (9 cols, zero PII keys), and the claim branch (claimAction → win router.push(`/driver/run/${id}`); already_claimed → neutral claimLostToast + setRows filter-out; else → coral claimFailedToast); identify the current `<header>` block (lines ~151-170) + the bell props to delete; current card markup (lines ~184-220) renders `<StatusDot state="paid" />`
    - app/driver/actions.ts — `claimAction(id)` and `refetchPool` (DO NOT modify; reuse verbatim) and the `PoolRow`/result types
    - platform/ui/RouteMotif.tsx — props `start={{ icon, label }}` / `end={{ icon, label }}`; serves the committed brand Transfer Badge midpoint (DS-03)
    - platform/ui/StatusDot.tsx — `variant="pill"` solid-badge mechanism + `stateLabel()`; STATE_META map (DO NOT add an "unclaimed" state or edit STATE_META — Pitfall 5)
    - platform/ui/Button.tsx — 52px / ≥44px CTA geometry (h-cta-height)
    - app/(guest)/_pass/icons.tsx — PlaneIcon/BuildingIcon for the RouteMotif endpoints
    - app/driver/_ui/icons.tsx — LuggageIcon for the meta row (created in Plan 01)
    - platform/money/commission.ts — `fmtEur` for the fare
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "app/driver/page.tsx + PoolView.tsx (RESTYLE)" section: the verbatim claim branch + the card restyle bullets (Unclaimed coral badge, RouteMotif, fmtEur, Button, delete header)
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — "Available claim card" inventory row (exact card layout) + Decision 2 (zero PII) + Color note on the Unclaimed=coral presentation choice
  </read_first>
  <action>
    Restyle each pool row into a claim card per the UI-SPEC "Available claim card" contract: top row = arrival date · pickup time (Body 600, formatted from `arrival_at` via toLocaleString in the row's locale) + the coral "Unclaimed" badge using `t.driverUnclaimedBadge` (the new Plan-01 key). Render the badge as the DS-02 coral pill via `StatusDot variant="pill"` styled coral OR a coral `rounded-full` pill carrying the worded `driverUnclaimedBadge` label — DO NOT add a `"unclaimed"` TransferState and DO NOT edit STATE_META (Pitfall 5). Middle = `<RouteMotif start={{ icon: <PlaneIcon/>, label: r.airport ?? copy.airportLabel }} end={{ icon: <BuildingIcon/>, label: r.zone ?? copy.zoneLabel }} />`. Meta row = flight_no · `fmtEur(r.amount_cents)` · pax · luggage (LuggageIcon + value), each as a line-icon + value (per-item from the 9 masked columns only). Bottom = `<Button className="w-full" disabled={claimingId===r.id} onClick={() => onClaim(r.id)}>{copy.claimTransferCta}</Button>`.
    KEEP VERBATIM: the `claimAction(id)` claim branch (win → router.push(`/driver/run/${id}`); `already_claimed` → `setToast({message: copy.claimLostToast, tone: "neutral"})` + `setRows(prev => prev.filter(r => r.id !== id))`; else → `setToast({message: copy.claimFailedToast, tone: "error"})`); the focus + ~25s `refetchPool` poll; the `PoolRow` type (do NOT add any guest-PII field). DELETE the per-page `<header>` block (logo + NotificationBell + LanguageToggle) — the chrome now lives in app/driver/layout.tsx (Plan 01) — and remove the now-unused `bellInitial`/`bellCopy` props from the PoolView signature. Apply warm-light card tokens (rounded-md border-grey/30 bg-white p-[16px] shadow-sm). NO map, ratings, earnings, ETA, vehicle, avatar (truthfulness rule).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (PII): PoolView.tsx references NO guest-PII keys — grep finds no `guest_name`, `guest_phone`, `notes`, `address` (full/exact), or `guest_email` token in the file. Only the 9 masked columns (id, status, arrival_at, airport, zone, flight_no, amount_cents, pax, luggage_count) are read.
    - Source assertion: the claim branch is preserved — `claimAction`, `router.push(\`/driver/run/${...}\`)`, `already_claimed`, `claimLostToast` (neutral), `claimFailedToast` (error), and a focus/poll `refetchPool` are all present.
    - Source assertion: no `"unclaimed"` TransferState and no STATE_META edit; the Unclaimed badge uses `driverUnclaimedBadge`.
    - Source assertion: PoolView renders `<RouteMotif` and `<Button` and `fmtEur`; the `<header>`/`NotificationBell` block is removed from this island.
    - `npx tsc --noEmit` exits 0; `npx vitest run app/driver` passes (existing 4 driver tests green).
  </acceptance_criteria>
  <done>Claim cards render the 9 masked columns with a coral Unclaimed badge, RouteMotif, and 52px Claim CTA; claim + poll logic verbatim; zero PII keys; header removed; tsc + driver tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pool RSC header removal + bell-prop cleanup</name>
  <files>app/driver/page.tsx</files>
  <read_first>
    - app/driver/page.tsx — self; the masked read `await supabase.rpc("wp_pool")` (line 40) + the `PoolRow` map (lines 45-55) to KEEP; the `readOwnNotifications()` bell seed (line 43) + `bellInitial`/`bellCopy` props (lines 71-80) now owned by the layout
    - app/driver/layout.tsx — the new shared chrome (Plan 01) that now seeds the bell and renders the header — so this page must stop passing bell props
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — Pitfall 1 (avoid double bell render) + Pattern 1 (RSC server-guard → resolve copy → prop bag)
  </read_first>
  <action>
    Keep the RSC unchanged in its data path: the `getCurrentRole()` driver gate, `Promise.all([getDict(), getLang()])`, `await supabase.rpc("wp_pool")`, and the `PoolRow` map (9 columns, zero PII) all stay VERBATIM. Remove the bell seed (`readOwnNotifications()` import + call) and the `bellInitial`/`bellCopy` props passed to `<PoolView>` (the layout now owns the bell — Pitfall 1). Keep `export const dynamic = "force-dynamic"` so the live pool is never SW-cached (Pitfall 3). Pass only the copy keys PoolView still needs (claimTransferCta, poolEmptyHeading, poolEmptyBody, claimLostToast, claimFailedToast, airportLabel, zoneLabel, and the new driverUnclaimedBadge + the luggage/flight/fare/pax captions PoolView renders). Do NOT change the wp_pool read or add any base-table select.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: app/driver/page.tsx still contains `rpc("wp_pool")` and `export const dynamic = "force-dynamic"`.
    - Source assertion: the bell seed (`readOwnNotifications`) and `bellInitial`/`bellCopy` props are removed from this page (no double-seed with the layout).
    - Source assertion: no base-table `from("wp_transfers")` select added to the pool page (masking stays structural at the RPC).
    - `npx tsc --noEmit` exits 0; `npx vitest run app/driver` passes.
  </acceptance_criteria>
  <done>Pool RSC reads wp_pool() unchanged, drops the bell seed/props (layout owns it), stays force-dynamic; tsc + driver tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

No new exported symbols or files — this plan RESTYLES existing files (`app/driver/page.tsx`, `app/driver/PoolView.tsx`). It consumes Plan-01 artifacts (`driverUnclaimedBadge` key, `LuggageIcon`, the shared layout/bell). The `PoolRow` type and `claimAction` reuse are pre-existing (do not flag as drift).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unclaimed driver → wp_pool() | Pre-claim masked read; MUST expose zero guest PII (the critical invariant) |
| driver → claimAction → claim_transfer RPC | Atomic conditional UPDATE resolves the race; caller-auth, never service-role |
| driver browser → /driver document/data | NetworkFirst SW (Pitfall 3); force-dynamic page |

No NEW trust boundary. The masked read and the claim path are reused verbatim; this plan only restyles their rendering.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | Claim card render | mitigate | Card reads ONLY the 9 `wp_pool()` columns; source-grep acceptance criterion asserts no guest-PII key (`guest_name`/`guest_phone`/`notes`/exact `address`/`guest_email`) appears in PoolView.tsx. Masking is structural at the RPC — never re-implemented in UI. |
| T-11-05 | Tampering | Claim concurrency | mitigate | `claimAction → claim_transfer` atomic UPDATE preserved verbatim; no client-side claim resolution; already-claimed handled gracefully (neutral toast + card removal). |
| T-11-06 | Information Disclosure | Stale SW-cached pool | mitigate | `export const dynamic = "force-dynamic"` kept; app/sw.ts NetworkFirst for /driver unchanged (do not regress). |
| T-11-07 | Spoofing | Forged claim id from client | accept | `claimAction` derives identity from `auth.uid()` server-side; id is the row to claim, the atomic UPDATE + RLS bound the effect — unchanged from Phase 6. |

PII guard (critical): the Available surface is the primary PII-leak risk in this phase. Acceptance criteria enforce a source-grep that the claim card references zero guest-PII keys and reads only the masked `wp_pool()` path.
</threat_model>

<verification>
- `npx tsc --noEmit` green; `npx vitest run app/driver` green (claim path untouched; existing 4 tests cover it).
- Source-grep PII gate: PoolView.tsx contains no guest-PII key; only the 9 masked columns.
- Visual UAT (end-of-phase): Available cards match the mockup (Unclaimed coral badge, RouteMotif, 52px Claim CTA), claim win navigates, already-claimed shows neutral toast + card removed.
</verification>

<success_criteria>
- DUI-01: Available transfers render as claim cards (pickup time, pax, Unclaimed badge, route w/ motif, date, price, Claim CTA) with zero guest-PII keys pre-claim.
- DUI-05: Claim invokes the existing atomic claim RPC; first-to-claim-wins / already-claimed reflected gracefully (neutral toast + silent card removal).
- Existing driver tests green; force-dynamic + NetworkFirst preserved.
</success_criteria>

<output>
Create `.planning/phases/11-driver-pwa-rebuild/11-02-SUMMARY.md` when done.
</output>
