---
phase: 11-driver-pwa-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/driver/run/page.tsx
  - app/driver/run/RunView.tsx
autonomous: true
requirements: [DUI-03]
must_haves:
  truths:
    - "My Trips renders the driver's claimed/active transfers as trip cards: date, status badge (real per-row state), route, pax, duration/arrival, and a teal details link to /driver/run/[id]"
    - "The active run stays ordered by arrival_at ASC (soonest first)"
    - "Completed transfers drop into the collapsed Completed today partition and never reappear in the active list"
    - "No earnings and no ratings are shown anywhere on My Trips"
    - "The existing inline advance CTAs stay functional"
    - "D-06: no new component tests are added; the restyle keeps the existing RunView source-grep test (app/driver/run/RunView.test.tsx) green as the presentation-only guarantee, and new visual pieces are eyeballed against the mockups"
  artifacts:
    - path: "app/driver/run/RunView.tsx"
      provides: "Restyled My Trips island: trip cards with StatusDot per-row + route + details link; arrival_at ASC + Completed-today partition preserved (DUI-03)"
      contains: "arrival_at"
    - path: "app/driver/run/page.tsx"
      provides: "My Trips RSC: claiming-driver RLS read unchanged; per-page header removed (now in layout)"
      contains: "getCurrentRole"
  key_links:
    - from: "app/driver/run/RunView.tsx"
      to: "platform/ui/StatusDot stateLabel"
      via: "per-row real status badge (dot + worded label)"
      pattern: "StatusDot"
    - from: "app/driver/run/RunView.tsx"
      to: "/driver/run/[id]"
      via: "teal details link per trip card"
      pattern: "/driver/run/"
    - from: "app/driver/run/RunView.tsx"
      to: "app/driver/actions.ts advanceStatus"
      via: "preserved inline advance CTA"
      pattern: "advanceStatus"
---

<objective>
Re-skin the driver "My run" list into mockup trip cards (DUI-03) without changing any behaviour the existing source-grep test asserts. Each active claimed transfer becomes a trip card: date, a per-row `StatusDot` badge (real state — Claimed/En route/Completed/Cancelled), a route (RouteMotif or compact teal route line), pax, duration/arrival, and a teal details link to `/driver/run/[id]`. The arrival_at ASC ordering, the `completedTodayTitle` "Completed today" partition, and the `'completed'` status literal MUST survive the restyle (RunView.test.tsx asserts them at the source level). No earnings, no ratings.

Purpose: Deliver the My Trips driver capability — a driver sees their claimed work as a sorted card list and opens any trip — on the unchanged claiming-driver RLS read.
Output: restyled `app/driver/run/RunView.tsx` + `app/driver/run/page.tsx` (header removed, read unchanged).
</objective>

## Phase Goal

**As a** Balkanity driver, **I want to** see my claimed and recently-completed trips as clear cards ordered by pickup time, **so that** I can work my run in order and open any trip's details — without distracting earnings or rating chrome.

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md
@.planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md
@.planning/phases/11-driver-pwa-rebuild/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle RunView into trip cards (preserve ordering + Completed-today + advance)</name>
  <files>app/driver/run/RunView.tsx</files>
  <read_first>
    - app/driver/run/RunView.tsx — self; KEEP: `arrival_at` ASC sort (lines ~123-126), the `completedToday` partition on `status === "completed"` (line ~127), the `completedTodayTitle` / "Completed today" `<details>` block (lines ~229-253), and `nextEdgeCta`/`onAdvance` (lines ~67-83, 129-146) for the inline advance CTAs; the current `<header>` block (lines ~151-170) to delete
    - app/driver/run/RunView.test.tsx — the source-grep contract (lines 66-75): the file MUST contain `arrival_at`, `completedTodayTitle` OR "Completed today", and a `'completed'`/`"completed"` literal — RED-by-absence if removed (Pitfall 4)
    - app/driver/actions.ts — `advanceStatus(id)` (reuse verbatim; do NOT modify)
    - platform/ui/StatusDot.tsx — `StatusDot state={r.status}` per-row real state + `stateLabel()`; Claimed=teal / En route=amber / Completed=grey / Cancelled=hollow coral ring (DS-02, dot+label) — do NOT override these to coral here (that's only the pool's Unclaimed presentation)
    - platform/ui/RouteMotif.tsx — full motif (may be tall in a dense list — Open Question 2; a compact teal route line is acceptable, executor's visual call)
    - app/(guest)/_pass/icons.tsx + app/driver/_ui/icons.tsx — Plane/Building/People/Luggage line icons for card meta
    - platform/transfers/lifecycle.ts — `ALLOWED_TRANSITIONS` for the inline advance next-edge (do NOT hand-roll the order)
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "app/driver/run/page.tsx + RunView.tsx (RESTYLE)" section: MUST-KEEP source-grep items + Pitfall 4
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — "My Trips list" inventory row (date, StatusDot, route, pax, duration, details link; NO earnings/ratings, Decision 1)
  </read_first>
  <action>
    Restyle each active-run row into a trip card per the UI-SPEC "My Trips list" contract: date + a per-row `<StatusDot state={r.status} />` (real state with its worded label — NOT a coral override; coral Unclaimed is the pool's presentation only), a route via `RouteMotif` OR a compact teal route line (executor's visual call given list density, Open Question 2), pax, duration/arrival, and a teal details link to `/driver/run/${r.id}` (reuse the existing link pattern). Keep the existing inline advance CTAs (`nextEdgeCta`/`onAdvance` → `advanceStatus(id)`; coral `advanceFailedToast` on failure) functional and unchanged. NO earnings, NO ratings (Decision 1).
    MUST PRESERVE (RunView.test.tsx source-grep gate, Pitfall 4): the `arrival_at` ASC sort of the active list, the partition routing `status === "completed"` rows into the collapsed `completedTodayTitle` / "Completed today" `<details>` section (never inline in the active run), and keep the `"completed"` status literal in the partition logic. DELETE the per-page `<header>` block (logo + NotificationBell + LanguageToggle) — chrome now lives in app/driver/layout.tsx (Plan 01) — and drop the now-unused bell props from the RunView signature.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver/run/RunView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/driver/run/RunView.test.tsx` passes (the three source-grep assertions: contains `arrival_at`, contains `completedTodayTitle` or "Completed today", contains a `'completed'`/`"completed"` literal).
    - Source assertion: RunView.tsx renders `<StatusDot` per row with the row's real state (no hardcoded coral override of every row's status).
    - Source assertion: a teal details link targets `/driver/run/${...}`; the inline advance CTA still calls `advanceStatus`.
    - Source assertion: no earnings/ratings tokens (no `earnings`, `rating`, `★`, `+12%`) in the file.
    - Source assertion: the `<header>`/`NotificationBell` block is removed from this island.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>My Trips renders sorted trip cards with per-row StatusDot + route + details link; ordering + Completed-today + advance preserved; no earnings/ratings; header removed; RunView.test.tsx green; tsc green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: My Trips RSC header removal</name>
  <files>app/driver/run/page.tsx</files>
  <read_first>
    - app/driver/run/page.tsx — self; the `getCurrentRole()` driver gate + the claiming-driver RLS own-rows read to KEEP; any header/bell props now owned by the layout
    - app/driver/layout.tsx — the shared chrome (Plan 01)
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — Pattern 1 (RSC server-guard → prop bag) + Pitfall 1 (no double bell)
  </read_first>
  <action>
    Keep the RSC data path VERBATIM: the `getCurrentRole()` driver gate, copy resolution, and the claiming-driver own-rows read of the active run. If this RSC currently renders or passes bell/header props, remove them (the layout owns the chrome now — Pitfall 1). Pass only the copy keys RunView still needs (myRunTitle, runEmptyHeading, runEmptyBody, completedTodayTitle, advanceTo*Cta, advanceFailedToast, plus any trip-card caption keys RunView renders). Do NOT change the data read or ordering source.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: app/driver/run/page.tsx still contains the `getCurrentRole()` driver gate and the own-rows read (no service-role base-table read added).
    - Source assertion: bell/header props removed from this RSC (no double-seed with the layout).
    - `npx tsc --noEmit` exits 0; `npx vitest run app/driver` passes (existing 4 driver tests green).
  </acceptance_criteria>
  <done>My Trips RSC keeps its role gate + claiming-driver read, drops chrome props (layout owns them); tsc + driver tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

No new exported symbols or files — this plan RESTYLES existing files (`app/driver/run/page.tsx`, `app/driver/run/RunView.tsx`). It consumes Plan-01 artifacts (shared layout, line icons). `advanceStatus`, `StatusDot`, `ALLOWED_TRANSITIONS`, and the `completedTodayTitle` key are pre-existing (do not flag as drift).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claiming driver → wp_transfers own-rows read | Claiming-driver RLS scopes rows to the owner; full PII legitimate post-claim |
| driver → advanceStatus → claim_transfer/advance | Gated service-role write inside a "use server" action; ownership-checked |

No NEW trust boundary. My Trips reads the claiming-driver's own rows (RLS-scoped) and reuses the gated `advanceStatus`; this plan only restyles.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | Trip-card render (post-claim PII) | accept | My Trips shows only the owning driver's own claimed rows via claiming-driver RLS; PII here is legitimate post-claim. No base-table read of others' rows added. |
| T-11-09 | Elevation/Tampering | Inline advance CTA | mitigate | `advanceStatus` reused verbatim (ownership `.eq("driver_id", auth.uid())` + lifecycle-edge + `.eq("status", current)` guard inside the gated action); no client write. |
| T-11-10 | Tampering | RunView source-grep contract | mitigate | Acceptance test `RunView.test.tsx` enforces arrival_at ASC + Completed-today + `'completed'` literal survive the restyle (presentation-only guarantee). |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs this phase. |

PII guard: My Trips renders the driver's OWN claimed rows (RLS-scoped, post-claim) — no pre-claim PII surface. No `wp_pool()` masking concern here (that's the Available surface, Plan 02).
</threat_model>

<verification>
- `npx tsc --noEmit` green; `npx vitest run app/driver/run/RunView.test.tsx` green (source-grep contract intact); `npx vitest run app/driver` green.
- Source review: trip cards use per-row StatusDot real state, details link to /driver/run/[id], no earnings/ratings, advance CTA preserved.
- Visual UAT (end-of-phase): My Trips matches the mockup; Completed-today collapses; ordering correct.
</verification>

<success_criteria>
- DUI-03: My Trips renders claimed/past transfers as trip cards (date, status, route, pax, duration, details link) with no earnings or ratings.
- arrival_at ASC ordering + Completed-today partition + inline advance preserved (RunView.test.tsx green).
- Existing driver tests green.
</success_criteria>

<output>
Create `.planning/phases/11-driver-pwa-rebuild/11-03-SUMMARY.md` when done.
</output>
