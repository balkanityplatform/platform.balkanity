---
phase: 11-driver-pwa-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/driver/run/[id]/page.tsx
  - app/driver/run/[id]/DetailView.tsx
autonomous: true
requirements: [DUI-04]
must_haves:
  truths:
    - "The en-route trip detail renders the horizontal LifecycleStepper driven by the current status (replacing the vertical LifecycleTimeline)"
    - "The detail page shows the claimed passenger info + passenger note + a route card, all post-claim via claiming-driver RLS"
    - "A single next-forward-edge advance CTA is present and wired to the existing advanceStatus(id) — labeled Confirm arrival on the en_route→arrived edge"
    - "All detail-grid captions resolve from the EN/BG dictionary (no hardcoded English)"
    - "No live map, no call/chat, no vehicle details are shown"
  artifacts:
    - path: "app/driver/run/[id]/DetailView.tsx"
      provides: "NEW client island: next-forward-edge advance CTA wired to advanceStatus, en_route→arrived labeled driverConfirmArrivalCta (DUI-04)"
      contains: "advanceStatus"
    - path: "app/driver/run/[id]/page.tsx"
      provides: "Detail RSC: LifecycleTimeline → LifecycleStepper swap; dictionary captions; passes {id,status,copy} to DetailView; per-page header removed"
      contains: "LifecycleStepper"
  key_links:
    - from: "app/driver/run/[id]/DetailView.tsx"
      to: "app/driver/actions.ts advanceStatus"
      via: "Confirm-Arrival CTA → advanceStatus(id) (gated service-role write)"
      pattern: "advanceStatus"
    - from: "app/driver/run/[id]/page.tsx"
      to: "platform/ui/LifecycleStepper"
      via: "horizontal stepper swap (current={status})"
      pattern: "LifecycleStepper"
    - from: "app/driver/run/[id]/DetailView.tsx"
      to: "platform/transfers/lifecycle ALLOWED_TRANSITIONS"
      via: "next-forward-edge resolution (no hand-rolled order)"
      pattern: "ALLOWED_TRANSITIONS"
---

<objective>
Re-skin the driver trip-detail page to the design system and add the Confirm-Arrival capability it lacks today (DUI-04). Swap the vertical `LifecycleTimeline` for the horizontal Phase 9 `LifecycleStepper current={status}`, replace the hardcoded English detail captions with EN/BG dictionary keys, and CREATE a new `DetailView` client island holding a single next-forward-edge advance CTA wired to the existing `advanceStatus(id)` — labeled "Confirm arrival" on the en_route→arrived edge. The page keeps its claiming-driver RLS read (full PII legitimate post-claim) and shows no map.

Purpose: Deliver the en-route trip-detail capability — a driver opens a claimed trip, sees passenger info + route + a progress stepper, and confirms arrival — completing the claim→fulfil loop in the UI.
Output: restyled `app/driver/run/[id]/page.tsx` (stepper swap + dictionary captions + island wiring) and the NEW `app/driver/run/[id]/DetailView.tsx`.
</objective>

## Phase Goal

**As a** Balkanity driver, **I want to** open a claimed trip and see the passenger details, route, and a progress stepper, then confirm arrival with one tap, **so that** I can fulfil the transfer and advance its status from the detail screen.

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md
@.planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md
@.planning/phases/11-driver-pwa-rebuild/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create DetailView client island (Confirm-Arrival CTA → advanceStatus)</name>
  <files>app/driver/run/[id]/DetailView.tsx</files>
  <read_first>
    - app/driver/run/RunView.tsx — the EXACT advance logic to copy: `nextEdgeCta` next-forward-edge resolution (lines ~67-83) and `onAdvance` handler with `useTransition` + `advanceStatus(id)` + coral `advanceFailedToast` (lines ~129-146). Copy this VERBATIM, changing ONLY the en_route→arrived label
    - app/driver/actions.ts — `advanceStatus(id)` (reuse verbatim; DO NOT modify) and its result shape; it lives in app/driver/actions.ts (import via the relative path from the [id] dir)
    - platform/transfers/lifecycle.ts — `ALLOWED_TRANSITIONS` (the next-forward-edge source — `ALLOWED_TRANSITIONS[status].find(s => s !== "cancelled" && s !== "paid")`); do NOT hand-roll the order
    - platform/ui/StatusDot.tsx — `TransferState` type import
    - platform/ui/Button.tsx + platform/ui/Toast.tsx — 52px CTA + neutral/error toast tones (coral error on advance failure)
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "app/driver/run/[id]/DetailView.tsx (NEW)" section: the `labelByNext` excerpt (keying `arrived: copy.driverConfirmArrivalCta`) + Pitfall 2 (this CTA must be CREATED, not restyled)
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — Decision 5 (Confirm-Arrival is the detail advance CTA) + Copywriting Contract (driverConfirmArrivalCta = "Confirm arrival" for en_route→arrived; other edges reuse advanceTo*Cta)
  </read_first>
  <action>
    Create app/driver/run/[id]/DetailView.tsx as a `"use client"` island with props `{ id: string; status: TransferState; copy: {...advance CTA + driverConfirmArrivalCta + advanceFailedToast} }`. Copy the RunView next-forward-edge logic verbatim: `const next = ALLOWED_TRANSITIONS[status].find((s) => s !== "cancelled" && s !== "paid");` then a `labelByNext: Partial<Record<TransferState, string>>` mapping `en_route: copy.advanceToEnRouteCta`, `arrived: copy.driverConfirmArrivalCta` (NEW — the en_route→arrived edge), `picked_up: copy.advanceToPickedUpCta`, `completed: copy.advanceToCompletedCta`. If `next` is null, render nothing (no CTA at the terminal/cancelled state). Use `useTransition` + an `onAdvance` that calls `await advanceStatus(id)` and on `!result.ok` sets a coral `advanceFailedToast` Toast. Render the CTA as a 52px `<Button className="w-full">{labelByNext[next]}</Button>`. Import `advanceStatus` from the driver actions module (`../../actions`), `ALLOWED_TRANSITIONS` from `@/platform/transfers/lifecycle`, `TransferState` from `@/platform/ui/StatusDot`. Do NOT add an un-claim / give-back control (CLAIM-04). Do NOT create any new server action — reuse `advanceStatus` verbatim.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver</automated>
  </verify>
  <acceptance_criteria>
    - app/driver/run/[id]/DetailView.tsx exists, is a `"use client"` island, and contains `advanceStatus` and `ALLOWED_TRANSITIONS`.
    - Source assertion: the en_route→arrived edge label is keyed `arrived: copy.driverConfirmArrivalCta`; other edges reuse `advanceTo*Cta`.
    - Source assertion: next-forward-edge uses `ALLOWED_TRANSITIONS[status].find(...)` (no hand-rolled if/else order chain).
    - Source assertion: no new server action is defined in this file (advanceStatus is imported, not redefined); no un-claim/release control.
    - `npx tsc --noEmit` exits 0; `npx vitest run app/driver` passes (existing 4 driver tests green — advanceStatus untouched).
  </acceptance_criteria>
  <done>DetailView island renders the next-forward-edge CTA (Confirm arrival on en_route→arrived) wired to the unchanged advanceStatus; tsc + driver tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle detail RSC — stepper swap, dictionary captions, island wiring, header removal</name>
  <files>app/driver/run/[id]/page.tsx</files>
  <read_first>
    - app/driver/run/[id]/page.tsx — self; KEEP the row read (lines 64-77, claiming-driver RLS, full PII post-claim) VERBATIM; the `Fact` block (lines 40-50); the hardcoded English captions to replace (lines 104-121: "Arrival"/"Flight"/"Fare"/"Passengers"/"Luggage"/"Guest name"/"Guest phone"/"Notes"); the current `LifecycleTimeline` import (line 17) + render (line 100) to swap; the back-link `<header>` (lines 89-97) to delete
    - platform/ui/LifecycleStepper.tsx — props `{ current: TransferState }` (the horizontal DS-04 stepper that replaces LifecycleTimeline)
    - platform/ui/RouteMotif.tsx — the hero route card (airport → zone/address) via RouteMotif
    - app/(guest)/_pass/icons.tsx + app/driver/_ui/icons.tsx — Plane/Building/Luggage line icons
    - app/driver/run/[id]/DetailView.tsx — the island (Task 1) this RSC mounts with `{id, status, copy}`
    - platform/i18n keys added in Plan 01 — driverArrivalLabel/driverFlightLabel/driverFareLabel/driverPassengersLabel/driverLuggageLabel/driverGuestNameLabel/driverGuestPhoneLabel/driverNotesLabel + driverConfirmArrivalCta; reuse airportLabel/zoneLabel/addressLabel
    - .planning/phases/11-driver-pwa-rebuild/11-PATTERNS.md — "app/driver/run/[id]/page.tsx (RESTYLE)" section: the one mandated stepper swap excerpt + caption-key replacement + Pitfall 6 (BG parity)
    - .planning/phases/11-driver-pwa-rebuild/11-UI-SPEC.md — "En-route trip detail" inventory row + Decision 4 (horizontal stepper) + Decision 1 (NO map/call/chat/vehicle)
  </read_first>
  <action>
    Restyle the detail RSC per the UI-SPEC "En-route trip detail" contract: replace `import { LifecycleTimeline }` + `<LifecycleTimeline current={row.status as TransferState} />` with `import { LifecycleStepper } from "@/platform/ui/LifecycleStepper"` + `<LifecycleStepper current={row.status as TransferState} />` (the single mandated component swap, DS-04, Decision 4). Add a hero route card via `RouteMotif` (airport → zone/address). Replace EVERY hardcoded English caption in the `Fact` blocks with its Plan-01 dictionary key: "Arrival"→`t.driverArrivalLabel`, "Flight"→`t.driverFlightLabel`, "Fare"→`t.driverFareLabel`, "Passengers"→`t.driverPassengersLabel`, "Luggage"→`t.driverLuggageLabel`, "Guest name"→`t.driverGuestNameLabel`, "Guest phone"→`t.driverGuestPhoneLabel`, "Notes"→`t.driverNotesLabel` (airport/zone/address already use t.airportLabel/zoneLabel/addressLabel) (Pitfall 6 — no hardcoded English). KEEP the row read (claiming-driver RLS, full PII post-claim) VERBATIM. Mount `<DetailView id={row.id} status={row.status as TransferState} copy={{ advanceToEnRouteCta, advanceToArrivedCta, advanceToPickedUpCta, advanceToCompletedCta, driverConfirmArrivalCta, advanceFailedToast }} />` for the Confirm-Arrival CTA (the server action can't be invoked from the RSC — Pitfall 2). DELETE the back-link `<header>` (lines 89-97) — the nav now lives in the layout, and D-02 keeps the My Trips tab active here. NO map, NO call/chat, NO vehicle details (Decision 1). Keep `force-dynamic` if present.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run app/driver</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: app/driver/run/[id]/page.tsx imports and renders `<LifecycleStepper current={...} />`; the `LifecycleTimeline` import and render are removed.
    - Source assertion: no hardcoded English captions remain — grep finds no `label="Arrival"`/`"Flight"`/`"Fare"`/`"Passengers"`/`"Luggage"`/`"Guest name"`/`"Guest phone"`/`"Notes"` string literals; captions use `t.driver*Label` keys.
    - Source assertion: the page renders `<DetailView` with `{id, status, copy}` and the claiming-driver RLS row read (`from("wp_transfers")...single()`) is preserved verbatim.
    - Source assertion: the back-link `<header>` is removed; no map/call/chat/vehicle markup added.
    - `npx tsc --noEmit` exits 0 (proves BG parity for the new caption keys); `npx vitest run app/driver` passes.
  </acceptance_criteria>
  <done>Detail page uses the horizontal LifecycleStepper, dictionary captions, a RouteMotif hero, and mounts DetailView's Confirm-Arrival CTA; row read verbatim; header removed; tsc + driver tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols/files created here (exclude from drift/source-grounding verification):
- File `app/driver/run/[id]/DetailView.tsx` — `DetailView` client island (Confirm-Arrival CTA, DUI-04).

Consumes Plan-01 artifacts (driverConfirmArrivalCta + the driver*Label caption keys). `advanceStatus`, `LifecycleStepper`, `RouteMotif`, `ALLOWED_TRANSITIONS` are pre-existing (do not flag as drift). `LifecycleTimeline` is removed from THIS page only (it remains in use elsewhere — not deprecated).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claiming driver → wp_transfers single-row read | Claiming-driver RLS returns the row ONLY if driver_id = auth.uid(); full PII legitimate post-claim |
| driver browser → DetailView CTA → advanceStatus | Gated service-role write inside a "use server" action; ownership + lifecycle-edge guarded |

No NEW trust boundary. The Confirm-Arrival CTA invokes the EXISTING `advanceStatus` (unchanged authz); the RSC keeps the existing claiming-driver RLS read.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Elevation/Tampering | DetailView advance (cross-driver/forged id) | mitigate | `advanceStatus(id)` reused verbatim: identity from `auth.uid()` server-side, `.eq("driver_id", user.id)` ownership + `.eq("status", current)` optimistic guard + legal-edge-only via `ALLOWED_TRANSITIONS`. The island only triggers it; no client write, no new action. |
| T-11-12 | Information Disclosure | Detail-page PII render | accept | Full PII (name/phone/notes) is shown ONLY because the claiming-driver RLS returned the row to the owning driver — legitimate post-claim. The masked-pool boundary does not apply here. No base-table read of others' rows. |
| T-11-13 | Information Disclosure | Hardcoded→dictionary captions | accept | Caption keys are static UI labels; replacing literals with `t.*` keys changes presentation only, no data exposure. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs this phase. |

PII guard: the detail page is post-claim, RLS-scoped to the owning driver — PII here is by design. The pre-claim PII boundary (Plan 02's concern) is untouched.
</threat_model>

<verification>
- `npx tsc --noEmit` green (caption-key BG parity); `npx vitest run app/driver` green (advanceStatus untouched — covered by advance.* tests).
- Source review: LifecycleStepper swap done; no hardcoded captions; DetailView wired; claiming-driver read verbatim; no map/call/chat/vehicle.
- Visual + functional UAT (end-of-phase): detail matches the mockup; tapping Confirm arrival advances en_route→arrived via advanceStatus; bottom nav stays with My Trips active (D-02).
</verification>

<success_criteria>
- DUI-04: the en-route trip detail renders the claimed passenger info, a route card, the DS-04 stepper, the passenger note, and a Confirm-Arrival CTA wired to the existing advance-status action — no live map.
- Stepper swap (LifecycleTimeline → LifecycleStepper) done; all captions dictionary-keyed (EN/BG parity green).
- advanceStatus unchanged; existing driver tests green.
</success_criteria>

<output>
Create `.planning/phases/11-driver-pwa-rebuild/11-04-SUMMARY.md` when done.
</output>
