---
phase: 12-admin-console-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/admin/page.tsx
  - app/admin/DashboardView.tsx
autonomous: true
requirements: [AUI-02]
must_haves:
  truths:
    - "The /admin Dashboard shows four KPI cards — Unclaimed (coral), Claimed (teal), En route (amber), Total today (slate) — each a count from real transfer data"
    - "Each KPI card pairs a big figure with a worded label (colour is never the sole cue)"
    - "Below the KPI cards a 'Recent transfers' section lists the top ~5 rows with a 'View all' link to /admin/transfers"
    - "No daily-goal %, no trend arrows, no earnings appear anywhere on the dashboard"
    - "The page renders inside the new shell with no per-page slate header of its own"
  artifacts:
    - path: "app/admin/page.tsx"
      provides: "Dashboard RSC: admin role gate + existing transfer read + KPI compute + props to DashboardView"
      contains: "getCurrentRole"
    - path: "app/admin/DashboardView.tsx"
      provides: "Presentational KPI grid (4 cards) + Recent transfers list"
      exports: ["DashboardView"]
  key_links:
    - from: "app/admin/page.tsx"
      to: "wp_transfers (wp_transfers_admin_read RLS)"
      via: "anon cookie-bound createClient().from('wp_transfers').select(...)"
      pattern: "from\\([\"']wp_transfers[\"']\\)"
    - from: "app/admin/page.tsx"
      to: "app/admin/DashboardView.tsx"
      via: "KPI counts + recent rows prop bag"
      pattern: "DashboardView"
---

<objective>
Rebuild the `/admin` landing page as the "Transfer Pool" dashboard: four KPI cards (Unclaimed / Claimed / En route / Total today) computed from the EXISTING admin transfer read, plus a "Recent transfers" top-~5 list with a "View all" link to `/admin/transfers`. Replaces the current placeholder `emptyHeading`/`emptyBody` copy + the section-nav list (which moved to the sidebar in Plan 01).

Purpose: Delivers AUI-02 and D-05 — a useful at-a-glance landing surface instead of a bare placeholder. Counts derive from the same `wp_transfers_admin_read` anon-RLS read the transfers list uses — NO new query/endpoint, presentation-only.

Output: A reworked `app/admin/page.tsx` RSC (role gate + read + KPI compute) and a new presentational `app/admin/DashboardView.tsx`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-console-rebuild/12-CONTEXT.md
@.planning/phases/12-admin-console-rebuild/12-UI-SPEC.md
@.planning/phases/12-admin-console-rebuild/12-PATTERNS.md
@app/admin/transfers/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework app/admin/page.tsx — admin gate + existing transfer read + KPI compute</name>
  <files>app/admin/page.tsx</files>
  <read_first>
    - app/admin/page.tsx (the current placeholder — keep the `getCurrentRole()==='admin'` gate; remove the emptyHeading/emptyBody + the section-nav list, which moved to the sidebar; remove this page's own slate `<header>` — the shell owns chrome now)
    - app/admin/transfers/page.tsx lines 80-162 (the EXACT read to reuse: `getCurrentRole` gate, anon cookie-bound `createClient()`, `wp_transfers` select with `destinations(zone,airport,...)`, the RawRow shape; reuse the read shape, do NOT add a new endpoint — D-03/D-05)
    - app/driver/settings/page.tsx (the RSC + getCurrentRole gate + prop-bag-to-view pattern)
    - .planning/phases/12-admin-console-rebuild/12-UI-SPEC.md (Decision 3 — exact KPI definitions; Color — KPI accents)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (DashboardView assignment section — KPI counts derive from the existing read; the "Total today" date-field discretion)
  </read_first>
  <action>
    Rework app/admin/page.tsx: keep `if ((await getCurrentRole()) !== "admin") redirect("/sign-in")` BEFORE any read (preservation — the role gate stays on the page, never moved to the layout). Resolve copy/lang with `Promise.all([getDict(), getLang()])`. Reuse the anon cookie-bound `createClient()` `wp_transfers` read shape from app/admin/transfers/page.tsx (select id,status,arrival_at,guest_name,flight_no,driver_id,amount_cents,destinations(zone,airport)) ordered by arrival_at — NO new query/endpoint, the wp_transfers_admin_read RLS policy is the data gate (never service-role).
    Compute the four KPI counts in the RSC from the loaded rows: Unclaimed = `status === 'paid' && driver_id === null`; Claimed = `status === 'claimed'`; EnRoute = `status === 'en_route'`; TotalToday = rows whose date field falls on the current local day (pick `arrival_at` — the field already on the row — per Claude's Discretion; document the choice in a comment). Take the top ~5 rows (by the existing arrival ordering) as the Recent-transfers list.
    Remove this page's own slate `<header>` block and the section-nav list (now in the sidebar/settings hub) and the emptyHeading/emptyBody landing copy. Pass to DashboardView: the four counts, the recent rows (id/status/arrival_at/guest_name/zone/airport/amount_cents/needsAttention as available), lang, and a copy bag (adminDashboardTitle, kpiUnclaimed/kpiClaimed/kpiEnRoute/kpiTotalToday, adminRecentTransfersHeading, adminViewAllCta, transfersEmptyHeading/transfersEmptyBody, needsAttentionBadge).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/admin/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint app/admin/page.tsx` clean.
    - `grep -c "getCurrentRole" app/admin/page.tsx` is > 0 AND the gate precedes the read (role gate preserved on the page).
    - `grep -c "createAdminClient\|service.role\|SERVICE_ROLE" app/admin/page.tsx` returns 0 (anon cookie-bound read only — RLS is the gate).
    - `grep -cE "from\\(['\"]wp_transfers['\"]\\)" app/admin/page.tsx` is > 0 (reuses the existing transfer read; no new table/endpoint).
    - `grep -c "emptyHeading\|emptyBody" app/admin/page.tsx` returns 0 (placeholder landing copy replaced).
    - `grep -c "bg-slate px-\[24px\]" app/admin/page.tsx` returns 0 (own slate header removed — shell owns chrome).
  </acceptance_criteria>
  <done>app/admin/page.tsx gates admin, reuses the existing anon-RLS wp_transfers read, computes 4 KPI counts + top-5 recent rows, has no own header and no placeholder copy; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create DashboardView — 4 KPI cards + Recent transfers list (View all)</name>
  <files>app/admin/DashboardView.tsx</files>
  <read_first>
    - platform/ui/Card.tsx (the KPI/content card chrome — `rounded-md border border-grey/30 bg-white p-[24px]`, className passthrough)
    - app/driver/settings/page.tsx lines 82-105 (Card-composition layout pattern)
    - app/admin/transfers/TransfersView.tsx lines 205-245 (the row styling + StatusDot + coral needs-attention badge + fmtArrival/fmtEur to reuse for the Recent-transfers rows)
    - platform/ui/StatusDot.tsx (status pill + stateLabel — worded label always present, WCAG 1.4.1)
    - .planning/phases/12-admin-console-rebuild/12-UI-SPEC.md (Decision 3 KPI cards; Typography — Display figure + Label caption; Color — Unclaimed=coral/Claimed=teal/EnRoute=amber/TotalToday=slate accents)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (DashboardView assignment — responsive grid 4→2→1, Recent transfers reuse, View all link)
  </read_first>
  <action>
    Create app/admin/DashboardView.tsx exporting `DashboardView`. Render the page title `adminDashboardTitle` ("Transfer Pool") as Display (28px/600), then a responsive KPI grid (4 columns → 2 → 1) of four `Card`s — Unclaimed (coral accent), Claimed (teal), En route (amber), Total today (slate/neutral). Each card = a big Display figure (the count) + the worded Label caption (kpiUnclaimed/kpiClaimed/kpiEnRoute/kpiTotalToday) + a status-coloured accent bar/border — the worded label is ALWAYS present so colour is never the sole cue (WCAG 1.4.1). NO daily-goal %, NO trend arrows, NO earnings (Decision 1).
    Below the grid, render a "Recent transfers" section: heading `adminRecentTransfersHeading`, a compact list of the passed top-~5 rows reusing TransfersView's row grammar (guest_name or arrival, StatusDot, airport→zone, fmtArrival, fmtEur, coral needs-attention left-border + needsAttentionBadge text marker where flagged), each linking to `/admin/transfers/[id]`, then a "View all" link (`adminViewAllCta`) to `/admin/transfers`. If there are no rows, show the existing transfersEmptyHeading/transfersEmptyBody copy. This component is presentational — it receives counts + rows + copy as props (no read, no auth call). It may be a client island or a server component (no interactivity required beyond links) — implementer's call; keep it prop-driven.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/admin/DashboardView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint app/admin/DashboardView.tsx` clean.
    - `grep -c "kpiUnclaimed" app/admin/DashboardView.tsx` AND `grep -c "kpiClaimed\|kpiEnRoute\|kpiTotalToday" app/admin/DashboardView.tsx` are > 0 (all 4 KPI labels render — worded label present, WCAG 1.4.1).
    - `grep -cE "daily.goal|goal.%|trend|earnings|\\+[0-9]+%" app/admin/DashboardView.tsx` returns 0 (no invented metrics — Decision 1).
    - `grep -c "adminViewAllCta\|/admin/transfers" app/admin/DashboardView.tsx` is > 0 (Recent transfers links to the full list — D-05).
    - `grep -c "StatusDot" app/admin/DashboardView.tsx` is > 0 (status shown as dot + worded label in the recent list).
  </acceptance_criteria>
  <done>DashboardView renders 4 labelled KPI cards in a responsive grid, a Recent-transfers top-5 list with a View-all link, no invented metrics; tsc + eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin RSC | `getCurrentRole()==='admin'` gate runs BEFORE the read — UNCHANGED. |
| RSC → wp_transfers | Anon cookie-bound read; `wp_transfers_admin_read` RLS is the data gate — same read as the transfers list, no new endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Elevation of Privilege | app/admin/page.tsx | mitigate | The `getCurrentRole()==='admin'` gate is preserved and precedes the read; grep gate confirms its presence. |
| T-12-06 | Information Disclosure | KPI/recent read | mitigate | Reuses the anon cookie-bound `wp_transfers` read (RLS-gated); NO service-role client. Grep gate confirms zero service-role refs — KPI/recent never expose data the RLS read did not already return. |
| T-12-07 | Tampering | `paid` invariant | mitigate | Dashboard is read-only — it computes counts from loaded rows and writes nothing. No `status='paid'` writer introduced (no DB write at all). |
| T-12-SC | Tampering | npm installs | mitigate | No package installs in this plan — no slopcheck checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; `npx eslint app/admin/page.tsx app/admin/DashboardView.tsx` clean.
- `npm test -- --run app/admin/transfers/TransfersView.test.tsx app/admin/transfers/actions.test.ts` stays green (no behaviour regression).
- Visual review: four labelled KPI cards (coral/teal/amber/slate) + a Recent-transfers list with View-all; no goal %, no trends, no earnings.
</verification>

<success_criteria>
- AUI-02: the Transfer Pool dashboard shows KPI cards (Unclaimed, Claimed, En route, Total today) computed from real transfer data, with no invented daily-goal metric.
- D-05: the dashboard adds a Recent-transfers top-5 section with a View-all link, reusing the loaded rows + row styling (no new query).
- Presentation-only: read-only, anon-RLS read reused, no service-role client, no write, role gate preserved.
</success_criteria>

<output>
Create `.planning/phases/12-admin-console-rebuild/12-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02 contributions)

New files:
- `app/admin/DashboardView.tsx` — exports `DashboardView` (4 KPI cards + Recent transfers list).

Modified files:
- `app/admin/page.tsx` — Dashboard RSC: admin gate + reused anon-RLS `wp_transfers` read + KPI compute + top-5 recent rows; own header + placeholder copy + section-nav list removed.

New prop shapes: `DashboardView` props (four KPI counts, recent rows array, lang, copy bag); a KPI-card item shape (figure, label, accent role).

(i18n keys consumed here — `adminDashboardTitle`, `kpiUnclaimed/kpiClaimed/kpiEnRoute/kpiTotalToday`, `adminRecentTransfersHeading`, `adminViewAllCta` — are created in Plan 01.)
