---
phase: 12-admin-console-rebuild
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/admin/transfers/TransfersView.tsx
  - app/admin/transfers/page.tsx
autonomous: true
requirements: [AUI-03, AUI-05]
must_haves:
  truths:
    - "The transfers list renders as a pending-transmissions table (Time/ID · Passenger · Route · Lifecycle · Status · Driver · Actions) on desktop and as stacked cards on mobile"
    - "The top-bar search filters the already-loaded rows client-side (guest name / flight no. / destination / id) with no server round-trip"
    - "A client sort control with Needs attention (default) / Soonest arrival / Status is the sole ordering authority over loaded rows"
    - "Status filter chips + the needs-attention quick filter remain a server URL-param re-query; the coral needs-attention border + badge are preserved"
    - "Each row's View action navigates to /admin/transfers/[id]; the Unassigned driver cell shows worded text, never an empty cell"
  artifacts:
    - path: "app/admin/transfers/TransfersView.tsx"
      provides: "Pending-transmissions table + mobile cards + client search + client sort"
      exports: ["TransfersView", "TransferRow"]
    - path: "app/admin/transfers/page.tsx"
      provides: "Admin gate + anon-RLS read + status/attention server filter (q machinery retired)"
      contains: "getCurrentRole"
  key_links:
    - from: "app/admin/transfers/TransfersView.tsx"
      to: "/admin/transfers/[id]"
      via: "Link href per row (View action)"
      pattern: "/admin/transfers/"
    - from: "app/admin/transfers/page.tsx"
      to: "wp_transfers (wp_transfers_admin_read RLS)"
      via: "anon cookie-bound .in('status', ...) server filter"
      pattern: "from\\([\"']wp_transfers[\"']\\)"
---

<objective>
Restyle the admin transfers list from the current stacked `<ul>` into the pending-transmissions `<table>` (desktop) with the existing `<ul>` rows reused as the mobile-card fallback (D-04). Add a client-side sort control (Needs attention default / Soonest arrival / Status — sole ordering authority, D-02) and wire the shell top-bar search to filter the loaded rows client-side (D-01). Retire the in-page server `q` search machinery in favour of the single top-bar client search.

Purpose: Delivers AUI-03 (the table with filter + sort + row actions) and the AUI-05 client-side search over loaded transfers (D-01/D-02). Presentation-only: the RSC keeps the same anon-RLS read + status/attention server filter; search and sort reorder/filter the LOADED set client-side. No new endpoint.

Output: Reworked `app/admin/transfers/TransfersView.tsx` (table + mobile cards + client search/sort) and a lightly-edited `app/admin/transfers/page.tsx` (retire the `q` path, keep the read + status/attention filter).
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-console-rebuild/12-CONTEXT.md
@.planning/phases/12-admin-console-rebuild/12-UI-SPEC.md
@.planning/phases/12-admin-console-rebuild/12-PATTERNS.md
@app/admin/transfers/TransfersView.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework TransfersView — table + mobile cards + client search/sort (keep test greens)</name>
  <files>app/admin/transfers/TransfersView.tsx</files>
  <read_first>
    - app/admin/transfers/TransfersView.tsx (the file being modified — the current `<ul>` rows at 205-245 become the mobile-card fallback; the applyParams/toggleStatus chip filters at 78-103 STAY a server URL-param re-query; the search-form at 136-146 is REMOVED per D-01)
    - app/admin/transfers/TransfersView.test.tsx (THE REGRESSION GATE — its source-grep assertions require the file to still contain `guest_name`, `flight_no`, `search`, `transfersNoMatchBody`, `needsAttention`, `needsAttentionBadge`, and `status`+`filter`; the new client-search-over-loaded-rows MUST keep these tokens present so the test stays green)
    - platform/ui/StatusDot.tsx (status pill + stateLabel for the Status column)
    - platform/ui/RouteMotif.tsx (compact route cell option for the Route column — or airport→zone text)
    - platform/ui/LifecycleStepper.tsx (the Lifecycle mini-bar option for the Lifecycle column; `current={status}` prop)
    - .planning/phases/12-admin-console-rebuild/12-UI-SPEC.md (Decision 4 columns + Decision 5 search; Copywriting — colTimeId/colPassenger/colRoute/colLifecycle/colStatus/colDriver/colActions, rowActionView, driverUnassigned, adminSortLabel/sortAttention/sortArrival/sortStatus)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (TransfersView assignment — the D-01/D-02/D-04 deltas in full)
  </read_first>
  <action>
    Rework app/admin/transfers/TransfersView.tsx. Desktop: render a semantic `<table>` with columns Time/ID (fmtArrival + truncated real id) · Passenger (guest_name) · Route (RouteMotif compact OR airport→zone text) · Lifecycle (mini LifecycleStepper `current={r.status}`) · Status (StatusDot + worded label) · Driver (name or `driverUnassigned` "Unassigned" — never an empty cell) · Actions (`rowActionView` "View" → /admin/transfers/[id]; the existing Assign/Cancel route to the detail action). Column headers use the new col* keys (UPPERCASE permitted). Preserve the coral needs-attention left treatment + the needsAttentionBadge text marker on flagged rows (WCAG 1.4.1). Row height floor `min-h-[56px]`.
    Mobile (below the desktop breakpoint): reuse the CURRENT stacked `<ul>` row markup as the card fallback — do NOT invent a new mobile layout (D-04). Table visible on desktop, `<ul>` cards on mobile.
    REMOVE the in-page server `q` search `<form>` (D-01). The top-bar search (Plan 01 / shell) is the single search affordance — implement the CLIENT-SIDE filter of the loaded `rows` here: a `useState` search term that filters the loaded set by guest_name / flight_no / destination (zone/airport) / truncated id substring, with the no-match result still rendering `transfersNoMatchBody`. Source the search term from the shell top bar (accept it via prop/context the layout threads through, or expose a controlled input the shell drives) — keep it client-side, no URL `q` param, no server round-trip. CRITICAL: the client-search code must still reference `guest_name`, `flight_no`, `search`, and `transfersNoMatchBody` so app/admin/transfers/TransfersView.test.tsx stays green.
    ADD a client sort control (`adminSortLabel` + options `sortAttention` default / `sortArrival` / `sortStatus`) as the SOLE ordering authority over the loaded rows (D-02): "Needs attention" reproduces today's coral-pin-then-arrival order client-side; "Soonest arrival" sorts by arrival_at ASC; "Status" sorts by lifecycle order. No hidden server pin layered underneath.
    KEEP the existing status-filter chips + needs-attention quick filter as a server URL-param re-query (applyParams/toggleStatus unchanged) — filter narrows the loaded set server-side; sort/search reorder/filter that set client-side. Remove this view's own slate `<header>` (shell owns chrome). Widen the content column from max-w-2xl to a wider console column (e.g. max-w-6xl) for the table.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/admin/transfers/TransfersView.tsx && npm test -- --run app/admin/transfers/TransfersView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint app/admin/transfers/TransfersView.tsx` clean.
    - `npm test -- --run app/admin/transfers/TransfersView.test.tsx` PASSES — all five assertions green (the file still references `guest_name`, `flight_no`, `search`, `transfersNoMatchBody`, `needsAttention`, `needsAttentionBadge`, and a `status`+`filter` implementation).
    - `grep -c "<table" app/admin/transfers/TransfersView.tsx` is > 0 (semantic table) AND `grep -c "<ul" app/admin/transfers/TransfersView.tsx` is > 0 (mobile-card fallback retained — D-04).
    - `grep -cE "adminSortLabel|sortAttention|sortArrival|sortStatus" app/admin/transfers/TransfersView.tsx` is > 0 (client sort control — D-02).
    - `grep -c "driverUnassigned" app/admin/transfers/TransfersView.tsx` is > 0 (worded Unassigned cell, never empty).
    - `grep -c 'name="q"\|onSearchSubmit\|params.set("q"' app/admin/transfers/TransfersView.tsx` returns 0 (the in-page server `q` search form is removed — D-01).
    - `grep -c "bg-slate px-\[24px\]" app/admin/transfers/TransfersView.tsx` returns 0 (own header removed).
  </acceptance_criteria>
  <done>TransfersView renders a desktop table + mobile cards, a client search over loaded rows (test tokens preserved), a client sort control (Needs attention default), keeps the server status/attention chip filters + coral badge, drops the q form + own header; TransfersView.test.tsx stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the server `q` search machinery in app/admin/transfers/page.tsx (keep read + filter)</name>
  <files>app/admin/transfers/page.tsx</files>
  <read_first>
    - app/admin/transfers/page.tsx (the file being modified — KEEP the getCurrentRole gate at 85-87, the anon-RLS `wp_transfers` read at 99-119, the `.in("status", ...)` status filter at 109-111, the computeNeedsAttention compute + the attentionOnly filter; REMOVE the `q` searchParam plumbing at 96/114-136 and the `.or(ilike)` + in-RSC destination search since search becomes client-side per D-01)
    - app/admin/transfers/actions.test.ts (the ops action regression gate — confirm the read change does not touch the actions)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (transfers/page.tsx assignment — retire q, keep everything else; do NOT touch the RLS read shape or the wp_transfers select)
  </read_first>
  <action>
    Edit app/admin/transfers/page.tsx to retire the server `q` path (D-01): remove the `q` from the searchParams type, the `search` derivation, the `.or(guest_name.ilike…,flight_no.ilike…)` query branch, and the in-RSC destination-name `searched` filter — search is now client-side over the loaded rows. KEEP unchanged: the `getCurrentRole()==='admin'` gate before any read, the anon cookie-bound `createClient()` `wp_transfers` select (do NOT alter the select columns or the RLS read shape), the `.in("status", statusFilter)` parameterized status filter, the `computeNeedsAttention` compute, the `attentionOnly` server filter, and the needs-attention compute/sort. The RSC still owns the read + status/attention filter; the loaded rows flow to TransfersView which now handles search + sort client-side. Stop passing `query`/`q` props to TransfersView (the view no longer takes them); keep passing rows, lang, statusOptions, activeStatus, attentionOnly, and the copy bag (which no longer needs transferSearchPlaceholder if the shell owns search — confirm against the Plan-01 top-bar wiring).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/admin/transfers/page.tsx && npm test -- --run app/admin/transfers/actions.test.ts app/admin/transfers/TransfersView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint app/admin/transfers/page.tsx` clean.
    - `grep -c "getCurrentRole" app/admin/transfers/page.tsx` is > 0 AND it precedes the read (gate preserved).
    - `grep -cE "from\\(['\"]wp_transfers['\"]\\)" app/admin/transfers/page.tsx` is > 0 AND `grep -c "createAdminClient\|service.role\|SERVICE_ROLE" app/admin/transfers/page.tsx` returns 0 (anon-RLS read shape untouched, no service-role).
    - `grep -c '\.or(' app/admin/transfers/page.tsx` returns 0 AND `grep -c "params.q\|searchParams.*q\b\|q?:" app/admin/transfers/page.tsx` returns 0 (server `q` machinery retired — D-01).
    - `grep -c '\.in("status"' app/admin/transfers/page.tsx` is > 0 (status server filter preserved).
    - `npm test -- --run app/admin/transfers/actions.test.ts app/admin/transfers/TransfersView.test.tsx` PASSES (ops + list behaviour unchanged).
  </acceptance_criteria>
  <done>The RSC keeps the role gate + anon-RLS read + status/attention server filter and drops the q/ilike/destination-search machinery; ops + list tests stay green; no RLS read-shape change, no service-role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/transfers RSC | `getCurrentRole()==='admin'` gate before the read — UNCHANGED. |
| client search/sort → loaded rows | Search + sort operate ONLY over rows the RLS read already returned; no new fetch, no new data. |
| status/attention chip filter → RSC | URL-param re-query through the same anon-RLS read — UNCHANGED. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Information Disclosure | client search/sort | mitigate | Search + sort run over the already-loaded `rows` (the RLS read's output); they NEVER fetch or expose data the `wp_transfers_admin_read` read did not already return. No new endpoint introduced (grep confirms no service-role, the select shape is untouched). |
| T-12-09 | Elevation of Privilege | app/admin/transfers/page.tsx | mitigate | The `getCurrentRole()==='admin'` gate is preserved and precedes the read; grep gate confirms presence. |
| T-12-10 | Tampering | input injection (search) | mitigate | Retiring the server `.or(ilike)` removes a string-built filter surface; the new search is a pure client-side substring match over in-memory rows (no query construction). The remaining `.in("status", ...)` stays parameterized. |
| T-12-11 | Tampering | `paid` invariant | mitigate | The list/page are read-only — no write, no `status='paid'` writer introduced. Ops actions are untouched (actions.test.ts stays green). |
| T-12-SC | Tampering | npm installs | mitigate | No package installs in this plan — no slopcheck checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; `npx eslint app/admin/transfers` clean.
- `npm test -- --run app/admin/transfers/TransfersView.test.tsx app/admin/transfers/actions.test.ts` green (the behavioural regression gates — list filter/search/pin contract + ops semantics unchanged).
- Visual review: desktop table with 7 columns + row actions; mobile stacked cards; sort control with Needs attention default; coral needs-attention rows pinned with the text badge; top-bar search filters the loaded set live.
</verification>

<success_criteria>
- AUI-03: the transfers list renders as the pending-transmissions table (Time/ID, passenger, route, lifecycle, status, driver, actions) with working filter and sort.
- AUI-05: the top-bar search is a client-side filter of the loaded transfers (D-01); sort is the sole client-side ordering authority (D-02).
- D-04: table on desktop, stacked cards on mobile (reusing the existing `<ul>` rows).
- Presentation-only: anon-RLS read shape untouched, no service-role, no write, role gate preserved, TransfersView.test.tsx + actions.test.ts green.
</success_criteria>

<output>
Create `.planning/phases/12-admin-console-rebuild/12-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03 contributions)

Modified files:
- `app/admin/transfers/TransfersView.tsx` — pending-transmissions `<table>` + mobile `<ul>` cards + client search (over loaded rows) + client sort control; in-page `q` form + own header removed. Exports `TransfersView`, `TransferRow` (unchanged shape, possibly extended for the sort term).
- `app/admin/transfers/page.tsx` — server `q`/ilike/destination-search machinery retired; role gate + anon-RLS read + status/attention server filter preserved.

New prop shapes: a sort-option union (`needs-attention` | `arrival` | `status`); the TransfersView search-term prop/context wiring from the shell top bar.

(i18n keys consumed here — `colTimeId/colPassenger/colRoute/colLifecycle/colStatus/colDriver/colActions`, `rowActionView`, `driverUnassigned`, `adminSortLabel/sortAttention/sortArrival/sortStatus` — are created in Plan 01.)
