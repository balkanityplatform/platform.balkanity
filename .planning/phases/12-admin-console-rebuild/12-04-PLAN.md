---
phase: 12-admin-console-rebuild
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/admin/transfers/[id]/TransferDetailView.tsx
  - app/admin/transfers/[id]/RefundForm.tsx
autonomous: true
requirements: [AUI-04]
must_haves:
  truths:
    - "The transfer detail renders the lifecycle via the horizontal LifecycleStepper (replacing the vertical LifecycleTimeline)"
    - "All five ops controls — assign, reassign, release, cancel (offers refund shortcut, never auto-refunds), refund — behave identically to today"
    - "The refund still records last_action_* only and NEVER sets paid; the always-shown fee disclosure remains"
    - "Destructive controls keep the coral token; positive CTAs teal; 52px CTAs; the page renders inside the shell with no own header"
  artifacts:
    - path: "app/admin/transfers/[id]/TransferDetailView.tsx"
      provides: "Restyled detail with LifecycleStepper swap + the 5 ops controls verbatim"
      exports: ["TransferDetailView", "TransferDetail"]
    - path: "app/admin/transfers/[id]/RefundForm.tsx"
      provides: "Refund form, DS chrome only, behaviour verbatim"
      exports: ["RefundForm"]
  key_links:
    - from: "app/admin/transfers/[id]/TransferDetailView.tsx"
      to: "platform/ui/LifecycleStepper"
      via: "LifecycleStepper current={row.status}"
      pattern: "LifecycleStepper"
    - from: "app/admin/transfers/[id]/TransferDetailView.tsx"
      to: "../actions (assign/reassign/release/cancel/refund)"
      via: "useActionState on the unchanged Server Actions"
      pattern: "assign|reassign|release|cancel"
---

<objective>
Restyle the admin transfer detail to the slate-console design system and swap the vertical `LifecycleTimeline` for the horizontal `LifecycleStepper current={status}` (DS-04), keeping ALL FIVE ops controls (assign / reassign / release / cancel / refund) wired verbatim. Apply DS chrome to `RefundForm` without touching its behaviour.

Purpose: Delivers AUI-04 — the restyled detail with the existing operations intact and behaving identically (refund never sets `paid`, D-12). Presentation-only: no new action, no changed action behaviour, no changed authz. The `getCurrentRole()` re-gate on the page and every Server Action is untouched.

Output: A restyled `app/admin/transfers/[id]/TransferDetailView.tsx` (stepper swap + DS chrome, ops verbatim) and a lightly-restyled `RefundForm.tsx`.
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-console-rebuild/12-CONTEXT.md
@.planning/phases/12-admin-console-rebuild/12-UI-SPEC.md
@.planning/phases/12-admin-console-rebuild/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap LifecycleTimeline → LifecycleStepper + DS-restyle the detail (ops verbatim)</name>
  <files>app/admin/transfers/[id]/TransferDetailView.tsx</files>
  <read_first>
    - app/admin/transfers/[id]/TransferDetailView.tsx (the file being modified — the import + usage at 24/298-301 swap to LifecycleStepper; the Fact block at 99-106, both dl grids at 304-332, all five ops buttons + dialogs at 336-459, the useActionState + coral/teal token split MUST be preserved verbatim)
    - platform/ui/LifecycleStepper.tsx (the Phase 9 horizontal stepper — `current={TransferState}` prop; handles the cancelled short-circuit + worded labels itself; do NOT hand-roll labels)
    - platform/ui/LifecycleTimeline.tsx (the vertical component being replaced — confirm the prop contract matches `current`)
    - .planning/phases/12-admin-console-rebuild/12-UI-SPEC.md (Decision 6 — all five ops preserved verbatim, restyled only; stepper swap; destructive=coral / positive=teal / 52px)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (TransferDetailView assignment — the exact swap + "preserve untouched" list)
  </read_first>
  <action>
    Edit app/admin/transfers/[id]/TransferDetailView.tsx. Replace the `import { LifecycleTimeline } from "@/platform/ui/LifecycleTimeline"` with `import { LifecycleStepper } from "@/platform/ui/LifecycleStepper"`, and replace `<LifecycleTimeline current={row.status} />` with `<LifecycleStepper current={row.status} />` (the horizontal Phase 9 stepper — it derives worded labels + the cancelled terminal itself; do NOT hand-roll labels). This is the ONLY behavioural-adjacent change.
    Restyle to the slate-console DS: drop this page's own slate `<header>` (the shell owns chrome now — remove the `<header className="...bg-slate...">` block + its Image/LanguageToggle; keep a back affordance to /admin/transfers if useful as a content-area link, not a header). Restyle the two `dl` grids as DS `Fact` blocks (the existing `Fact` helper is fine; tidy spacing to the console scale) and widen the content column from max-w-2xl to a wider console column.
    PRESERVE VERBATIM (no behaviour change — Decision 6 / D-12): the `Fact` component, both `dl` fact grids' data, ALL FIVE ops controls — AssignForm (one-tap inline driver-id, no reason), the ReasonDialog-driven reassign/release/cancel (required reason note, *Confirm dialog prompts), cancel's `cancelOfferRefundCta` shortcut that OPENS RefundForm and never auto-refunds (D-11), and the RefundForm panel — plus the `useActionState` + pending-disable double-submit guard and the destructive=coral / positive=teal / 52px token split. Do NOT change the imported Server Actions (`assign`/`reassign`/`release`/`cancel`/`refund` from ../actions), their wiring, or their authz. Keep the copy bag keys unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/admin/transfers/[id]/TransferDetailView.tsx" && npm test -- --run app/admin/transfers/actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; eslint clean on the file.
    - `grep -c "LifecycleStepper" "app/admin/transfers/[id]/TransferDetailView.tsx"` is > 0 AND `grep -c "LifecycleTimeline" "app/admin/transfers/[id]/TransferDetailView.tsx"` returns 0 (the DS-04 swap is complete).
    - `grep -cE "assign|reassign|release|cancel|refund" "app/admin/transfers/[id]/TransferDetailView.tsx"` shows all five ops still imported/wired (verify each of the five action names is present).
    - `grep -c "cancelOfferRefundCta" "app/admin/transfers/[id]/TransferDetailView.tsx"` is > 0 (cancel still OFFERS the refund shortcut — never auto-refunds, D-11).
    - `grep -c "status='paid'\|status: 'paid'\|status: \"paid\"\|\.update.*paid" "app/admin/transfers/[id]/TransferDetailView.tsx"` returns 0 (no paid writer introduced — single-writer preservation).
    - `grep -c "bg-slate px-\[24px\]" "app/admin/transfers/[id]/TransferDetailView.tsx"` returns 0 (own header removed).
    - `npm test -- --run app/admin/transfers/actions.test.ts` PASSES (ops authz/behaviour unchanged).
  </acceptance_criteria>
  <done>The detail renders the horizontal LifecycleStepper, all five ops controls + their dialogs + the coral/teal/52px token split are preserved verbatim, no own header, no paid writer; actions.test.ts stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Light DS-chrome restyle of RefundForm (behaviour verbatim, never sets paid)</name>
  <files>app/admin/transfers/[id]/RefundForm.tsx</files>
  <read_first>
    - app/admin/transfers/[id]/RefundForm.tsx (the file being modified — built on Button + TextField; the always-shown `refundFeeDisclosure` at 47/66, the full-amount editable-down `defaultValue={fullAmountEur}`, the pending-disable submit MUST be preserved verbatim; restyle is visual-only)
    - app/admin/transfers/actions.ts (confirm the `refund` action this form posts to records last_action_* only and never sets paid — do NOT touch it)
    - platform/ui/Button.tsx + platform/ui/TextField.tsx (the DS primitives already consumed)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (RefundForm assignment — DS chrome only, behaviour verbatim, do NOT change the refund wiring or the disclosure)
  </read_first>
  <action>
    Apply a light DS-chrome restyle to app/admin/transfers/[id]/RefundForm.tsx to sit in the slate-console detail (spacing/token tidy to the console scale). PRESERVE VERBATIM (D-12): the `refund` action wiring from ../actions, the full-amount pre-fill `defaultValue={fullAmountEur}` editable down, the required reason field, the ALWAYS-shown `refundFeeDisclosure` (the ~€{fee}-not-recovered note rendered unconditionally), the pending-disable double-submit guard, and the role="alert"/role="status" inline messages. Do NOT change the action, the disclosure logic, or the field set. This is presentation-only chrome.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/admin/transfers/[id]/RefundForm.tsx" && npm test -- --run app/admin/transfers/actions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; eslint clean on the file.
    - `grep -c "refundFeeDisclosure" "app/admin/transfers/[id]/RefundForm.tsx"` is > 0 (the always-shown fee disclosure preserved — D-12).
    - `grep -c "defaultValue={fullAmountEur}" "app/admin/transfers/[id]/RefundForm.tsx"` is > 0 (full-amount pre-fill editable down preserved).
    - `grep -cE "from ['\"]\\.\\./actions['\"]" "app/admin/transfers/[id]/RefundForm.tsx"` is > 0 AND `grep -c "refund" "app/admin/transfers/[id]/RefundForm.tsx"` is > 0 (posts to the unchanged refund action).
    - `grep -c "status='paid'\|\.update.*paid\|paid:" "app/admin/transfers/[id]/RefundForm.tsx"` returns 0 (the form never sets paid — single-writer preservation).
    - `npm test -- --run app/admin/transfers/actions.test.ts` PASSES.
  </acceptance_criteria>
  <done>RefundForm is restyled to the console DS with the refund action, full-amount pre-fill, required reason, and always-shown fee disclosure all preserved verbatim; never sets paid; actions.test.ts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → /admin/transfers/[id] RSC | `getCurrentRole()==='admin'` gate on the page + re-gate inside every Server Action — UNCHANGED. |
| detail view → ops Server Actions | The five actions (assign/reassign/release/cancel/refund) and their authz are untouched; this plan only restyles the controls + swaps the stepper. |
| refund → `paid` invariant | Refund records `last_action_*` only and NEVER sets `paid`; the verified webhook stays the sole `paid` author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-12 | Tampering | `paid` single-writer | mitigate | No restyled component writes `status='paid'`; grep gate on both files confirms zero paid-write patterns. Refund records `last_action_*` only (D-12). The webhook remains the sole writer. |
| T-12-13 | Elevation of Privilege | ops Server Actions | mitigate | The five actions + their `getCurrentRole()==='admin'` re-gate are NOT touched (presentation-only). actions.test.ts is the regression gate and must stay green. |
| T-12-14 | Repudiation | cancel/refund audit | mitigate | The required reason note (recorded as `last_action_reason`) and the never-auto-refund cancel behaviour (D-11) are preserved verbatim — the audit trail is unchanged. |
| T-12-15 | Spoofing | stepper swap | accept | The LifecycleStepper renders the row's real `status` (same data); it is display-only and introduces no new data path. |
| T-12-SC | Tampering | npm installs | mitigate | No package installs in this plan — no slopcheck checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; `npx eslint "app/admin/transfers/[id]"` clean.
- `npm test -- --run app/admin/transfers/actions.test.ts` green (ops authz/behaviour unchanged).
- Visual review: horizontal LifecycleStepper on the detail; all five ops controls present + working; refund shows the fee disclosure and full-amount pre-fill; destructive controls coral, positive teal, 52px; no own header (sits in the shell).
</verification>

<success_criteria>
- AUI-04: the transfer detail is restyled to the design system with the existing assign / reassign / cancel / refund actions intact and behaving identically (refund never sets `paid`).
- DS-04: the vertical LifecycleTimeline is swapped for the horizontal LifecycleStepper (Guest/Driver precedent).
- Presentation-only: no new action, no changed action behaviour, no changed authz, no `paid` writer; actions.test.ts green.
</success_criteria>

<output>
Create `.planning/phases/12-admin-console-rebuild/12-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 04 contributions)

Modified files:
- `app/admin/transfers/[id]/TransferDetailView.tsx` — LifecycleTimeline→LifecycleStepper swap + slate-console DS restyle; own header removed; all five ops controls + dialogs + coral/teal/52px token split preserved verbatim. Exports `TransferDetailView`, `TransferDetail` (unchanged shapes).
- `app/admin/transfers/[id]/RefundForm.tsx` — light DS chrome; refund action, full-amount pre-fill, required reason, always-shown fee disclosure preserved verbatim. Exports `RefundForm` (unchanged shape).

No new i18n keys (reuses existing ops/refund keys). No new prop shapes — restyle only.
