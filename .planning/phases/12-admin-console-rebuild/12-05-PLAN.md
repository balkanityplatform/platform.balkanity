---
phase: 12-admin-console-rebuild
plan: 05
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/admin/settings/page.tsx
  - app/admin/drivers/DriversView.tsx
  - app/admin/drivers/page.tsx
  - app/admin/companies/CompaniesView.tsx
  - app/admin/properties/PropertiesView.tsx
  - app/admin/destinations/DestinationsView.tsx
  - app/admin/health/page.tsx
autonomous: true
requirements: [AUI-01]
must_haves:
  truths:
    - "The sidebar Settings item targets a /admin/settings hub page that links Companies / Properties / Destinations / Platform health"
    - "Every supply CRUD page, the drivers page, and the health page render inside the shell with no per-page slate header of their own"
    - "The drivers page no longer mounts its own NotificationBell (the shell's single bell supersedes it)"
    - "All CRUD / invite / health behaviour is unchanged"
  artifacts:
    - path: "app/admin/settings/page.tsx"
      provides: "Presentational settings hub RSC linking the four existing routes (admin-gated)"
      contains: "getCurrentRole"
  key_links:
    - from: "app/admin/settings/page.tsx"
      to: "/admin/companies, /admin/properties, /admin/destinations, /admin/health"
      via: "links to the four existing routes"
      pattern: "/admin/(companies|properties|destinations|health)"
    - from: "app/admin/drivers/DriversView.tsx"
      to: "shell NotificationBell"
      via: "removal of the per-page bell (shell owns the single bell)"
      pattern: "DriversView"
---

<objective>
Create the `/admin/settings` hub page (D-03) — a presentational grouping that links Companies / Properties / Destinations / Platform health — and apply the shell-consolidation cleanup to every remaining admin surface: drop each per-page slate `<header>` (the shell now owns chrome) and remove the drivers page's own `NotificationBell` (superseded by the shell's single bell). CRUD / invite / health behaviour is unchanged.

Purpose: Completes AUI-01 (the Settings sidebar item now has a real landing target and the parent-lit prefix highlight works) and finishes the Phase 11-style header consolidation across the supply CRUD + drivers + health pages so the whole console sits in one shell. Presentation-only: no new CRUD, no new schema, no behaviour change.

Output: New `app/admin/settings/page.tsx`; lightly-edited `DriversView.tsx` + `drivers/page.tsx`, `CompaniesView.tsx`, `PropertiesView.tsx`, `DestinationsView.tsx`, `health/page.tsx` (all header-removal + light restyle).
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-admin-console-rebuild/12-CONTEXT.md
@.planning/phases/12-admin-console-rebuild/12-UI-SPEC.md
@.planning/phases/12-admin-console-rebuild/12-PATTERNS.md
@app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the /admin/settings hub page (admin-gated, links 4 existing routes)</name>
  <files>app/admin/settings/page.tsx</files>
  <read_first>
    - app/admin/page.tsx lines 76-94 (the existing console section-nav list — the exact cards/links pattern + styling to reuse: `inline-flex min-h-[44px] ... rounded-md border border-grey/30 ... focus-visible:outline-teal`)
    - app/driver/settings/page.tsx (RSC + getCurrentRole gate + Promise.all([getDict(), getLang()]) + prop-bag pattern; the page renders NO own header since the layout owns chrome)
    - app/admin/companies/page.tsx (the admin-gated RSC shape to mirror)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (settings/page.tsx assignment — the exact link list + new keys adminSettingsTitle/navSettings, reuse companiesTitle/propertiesTitle/destinationsTitle/healthTitle)
    - .planning/phases/12-admin-console-rebuild/12-UI-SPEC.md (Decision 2 Settings grouping; Settings hub group titles row)
  </read_first>
  <action>
    Create app/admin/settings/page.tsx as an `async` admin-gated RSC: `if ((await getCurrentRole()) !== "admin") redirect("/sign-in")` BEFORE any render (preservation), resolve copy/lang with `Promise.all([getDict(), getLang()])`, then render the hub heading `adminSettingsTitle` ("Settings") and a list of cards/links to the FOUR existing routes — Companies `/admin/companies` (t.companiesTitle), Properties `/admin/properties` (t.propertiesTitle), Destinations `/admin/destinations` (t.destinationsTitle), Platform health `/admin/health` (t.healthTitle) — reusing the section-nav link styling from app/admin/page.tsx verbatim. NO own slate `<header>` (the shell owns chrome). NO new CRUD, NO new schema — purely a navigation grouping of routes that already exist (D-03). No data read beyond copy/lang. Use the new `adminSettingsTitle` + reuse the existing four *Title keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/admin/settings/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; eslint clean on the file.
    - `grep -c "getCurrentRole" app/admin/settings/page.tsx` is > 0 (admin gate present — preservation).
    - `grep -cE "/admin/(companies|properties|destinations|health)" app/admin/settings/page.tsx` is >= 4 (links all four existing routes — D-03).
    - `grep -c "adminSettingsTitle" app/admin/settings/page.tsx` is > 0 (hub heading key used).
    - `grep -cE "from\\(['\"]|\\.insert\\(|\\.update\\(|\\.delete\\(" app/admin/settings/page.tsx` returns 0 (no DB read/write — pure navigation grouping, no new CRUD/schema).
    - `grep -c "bg-slate px-\[24px\]" app/admin/settings/page.tsx` returns 0 (no own header — shell owns chrome).
  </acceptance_criteria>
  <done>app/admin/settings/page.tsx is an admin-gated hub linking the four existing routes, no own header, no DB access; tsc + eslint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Drop per-page headers + the drivers bell; light DS restyle to sit in the shell</name>
  <files>app/admin/drivers/DriversView.tsx, app/admin/drivers/page.tsx, app/admin/companies/CompaniesView.tsx, app/admin/properties/PropertiesView.tsx, app/admin/destinations/DestinationsView.tsx, app/admin/health/page.tsx</files>
  <read_first>
    - app/admin/drivers/DriversView.tsx (the per-page slate `<header>` at lines 62-85 + the NotificationBell mount at 73-78 are REMOVED — the shell owns the single bell; the InviteDriverForm + roster behaviour are UNCHANGED)
    - app/admin/drivers/page.tsx (confirm what it passes to DriversView — the bellInitial/bellCopy props become unused once the bell is removed from the view; drop those props from both sides to keep tsc clean)
    - app/admin/companies/CompaniesView.tsx line 123, app/admin/properties/PropertiesView.tsx line 127, app/admin/destinations/DestinationsView.tsx line 152 (each per-page slate `<header>` block to remove — keep the section + CRUD behaviour verbatim)
    - app/admin/health/page.tsx line 122 (the slate `<header>` in the PAGE, not a View — remove it; keep the EmailCapGauge + reconciliation/stuck lists + admin gate + reads unchanged)
    - app/admin/health/EmailCapGauge.test.tsx + app/admin/destinations/you-keep.test.tsx (regression gates — these behavioural tests must stay green; the restyle must not change behaviour)
    - .planning/phases/12-admin-console-rebuild/12-PATTERNS.md (DriversView + companies/properties/destinations/health assignment — remove per-page <header>, drop the superseded bell, light restyle, behaviour unchanged)
  </read_first>
  <action>
    Across all six files, perform the shell-consolidation cleanup (mirrors the Phase 11 driver-layout carry-forward): remove each page's own slate `<header className="...bg-slate px-[24px]...">` block (logo chip + LanguageToggle, and in DriversView the NotificationBell) — the shell (Plan 01) now mounts the single bell + LanguageToggle + logo once. In DriversView.tsx specifically, remove the `NotificationBell` import + mount and the now-unused `bellInitial`/`bellCopy` props; in drivers/page.tsx stop passing those props (and stop seeding `readOwnNotifications()` there if it was only for the page bell — the shell seeds it now). Apply a light DS restyle so each surface sits cleanly inside the shell content area (drop the now-redundant `min-h-dvh bg-white` outer `<main>` wrappers where the shell already provides the content frame; keep the inner `<section>` content). PRESERVE VERBATIM: the InviteDriverForm + roster (DriversView, ONBD-05/AUTH-03), all companies/properties/destinations CRUD + the "you keep" logic, and the health EmailCapGauge + reconciliation/stuck lists + admin gate + reads. No CRUD, invite, or health behaviour changes — header/chrome removal + light restyle only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/admin/drivers app/admin/companies app/admin/properties app/admin/destinations app/admin/health && npm test -- --run app/admin/health/EmailCapGauge.test.tsx app/admin/destinations/you-keep.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; eslint clean across all six paths.
    - `grep -rc "bg-slate px-\[24px\]" app/admin/drivers/DriversView.tsx app/admin/companies/CompaniesView.tsx app/admin/properties/PropertiesView.tsx app/admin/destinations/DestinationsView.tsx app/admin/health/page.tsx` returns 0 for each file (every per-page slate header removed).
    - `grep -c "NotificationBell" app/admin/drivers/DriversView.tsx` returns 0 (the drivers page no longer mounts its own bell — shell owns the single bell).
    - `grep -c "InviteDriverForm" app/admin/drivers/DriversView.tsx` is > 0 (invite/roster behaviour preserved).
    - `grep -c "EmailCapGauge" app/admin/health/page.tsx` is > 0 AND `grep -c "getCurrentRole" app/admin/health/page.tsx` is > 0 (health gauge + admin gate preserved).
    - `npm test -- --run app/admin/health/EmailCapGauge.test.tsx app/admin/destinations/you-keep.test.tsx` PASSES (no behaviour regression).
  </acceptance_criteria>
  <done>All six surfaces drop their own header (and the drivers bell), sit in the shell, and keep CRUD/invite/health behaviour verbatim; EmailCapGauge + you-keep tests stay green; tsc + eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → each admin RSC route | Every route (settings hub, drivers, companies, properties, destinations, health) keeps its `getCurrentRole()==='admin'` gate before any read — UNCHANGED. |
| settings hub → existing routes | The hub only renders links to routes that already exist; it performs no DB read/write and creates no new CRUD or schema. |
| health page → admin-read RLS | The EmailCapGauge + reconciliation/stuck reads (email_log/health_events admin-read RLS) are untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Elevation of Privilege | settings hub + restyled pages | mitigate | Every route keeps its `getCurrentRole()==='admin'` gate (grep gate confirms it on the hub + health). The hub does no DB access at all. |
| T-12-17 | Information Disclosure | drivers bell removal | mitigate | Removing the per-page bell does not widen any read; the shell's single bell uses the SAME `readOwnNotifications()` own-rows RLS seed. No new data surface. |
| T-12-18 | Tampering | CRUD / invite / health behaviour | mitigate | Header/chrome removal + light restyle only; the InviteDriverForm, CRUD actions, "you keep" logic, and health reads are preserved verbatim. EmailCapGauge + you-keep tests are the regression gates and stay green. |
| T-12-19 | Spoofing/Tampering | new routes / schema | mitigate | The settings hub invents NO new route or table — it links existing routes. Grep gate confirms zero DB access in the hub. |
| T-12-SC | Tampering | npm installs | mitigate | No package installs in this plan — no slopcheck checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes; `npx eslint app/admin/{settings,drivers,companies,properties,destinations,health}` clean.
- `npm test -- --run app/admin/health/EmailCapGauge.test.tsx app/admin/destinations/you-keep.test.tsx` green (health + you-keep behaviour unchanged).
- Visual review: the Settings sidebar item lights its parent prefix and opens a hub linking the four routes; every supply CRUD + drivers + health page sits in the shell with no duplicate header and one bell.
</verification>

<success_criteria>
- AUI-01 (completion): the Settings sidebar item targets a real `/admin/settings` hub linking the four existing routes; the prefix-match highlight keeps Settings lit on its children.
- D-03: Settings is a presentational navigation grouping — no new CRUD, no new schema.
- Shell consolidation: every remaining admin surface drops its per-page header (and the drivers bell) and sits in the single shell.
- Presentation-only: CRUD / invite / health behaviour verbatim; EmailCapGauge + you-keep tests green; every route's admin gate preserved.
</success_criteria>

<output>
Create `.planning/phases/12-admin-console-rebuild/12-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 05 contributions)

New files:
- `app/admin/settings/page.tsx` — presentational admin-gated Settings hub linking Companies / Properties / Destinations / Platform health.

Modified files:
- `app/admin/drivers/DriversView.tsx` + `app/admin/drivers/page.tsx` — per-page header + NotificationBell removed (shell owns the single bell); unused bell props dropped; invite/roster verbatim.
- `app/admin/companies/CompaniesView.tsx`, `app/admin/properties/PropertiesView.tsx`, `app/admin/destinations/DestinationsView.tsx` — per-page slate header removed; CRUD + "you keep" verbatim.
- `app/admin/health/page.tsx` — per-page slate header removed; EmailCapGauge + reconciliation/stuck lists + admin gate verbatim.

New i18n keys consumed: `adminSettingsTitle`, `navSettings` (created in Plan 01); reuses `companiesTitle`/`propertiesTitle`/`destinationsTitle`/`healthTitle`. No new prop shapes (drops the DriversView bell props).
