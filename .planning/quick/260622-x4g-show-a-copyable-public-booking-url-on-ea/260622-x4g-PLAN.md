---
phase: quick-260622-x4g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/i18n/en.ts
  - platform/i18n/bg.ts
  - app/admin/destinations/CopyBookingLink.tsx
  - app/admin/destinations/DestinationsView.tsx
  - app/admin/destinations/page.tsx
autonomous: true
requirements: [QUICK-260622-x4g]
must_haves:
  truths:
    - "Each ACTIVE destination row shows the full public booking URL (origin + /pickup/<slug>)"
    - "A Copy button copies that exact URL with a brief 'Copied' confirmation"
    - "Inactive destinations show NO live/copyable link (D-11: inactive stops resolving /pickup)"
    - "All new strings render in EN and BG via the dictionary (no hardcoded English)"
  artifacts:
    - path: "app/admin/destinations/CopyBookingLink.tsx"
      provides: "Client component: origin + /pickup/<slug> URL + accessible Copy button"
      contains: "navigator.clipboard.writeText"
    - path: "platform/i18n/en.ts"
      provides: "EN keys copyBookingLinkCta + bookingLinkCopiedLabel"
      contains: "copyBookingLinkCta"
    - path: "platform/i18n/bg.ts"
      provides: "BG translations for the two keys (tsc Dict-parity gate)"
      contains: "copyBookingLinkCta"
  key_links:
    - from: "page.tsx"
      to: "DestinationsView copy prop"
      via: "threads the two new keys into the copy bag"
      pattern: "copyBookingLinkCta"
    - from: "DestinationsView.tsx"
      to: "CopyBookingLink"
      via: "rendered in the actions slot for active rows only"
      pattern: "CopyBookingLink"
    - from: "CopyBookingLink.tsx"
      to: "/pickup/<slug>"
      via: "window.location.origin + /pickup/ + slug"
      pattern: "/pickup/"
---

<objective>
Show the full public booking URL (origin + `/pickup/<slug>`) with a Copy button on each ACTIVE destination row in `/admin/destinations`. Today the row shows only the bare relative slug `/<slug>` — incomplete (real path is `/pickup/<slug>`) and not copyable. Operators need the exact link to send to property companies.

Purpose: remove the manual, error-prone step of reconstructing the link by hand. The slug already lives on the row; this is presentation-only surfacing of an existing link.

Output: surface-local `CopyBookingLink` client component, two EN/BG dictionary keys, wiring in `DestinationsView` + `page.tsx`. No backend / actions.ts / schema / RLS / auth changes (v1.1 presentation-only guardrail).
</objective>

<execution_context>
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/workflows/execute-plan.md
@/Users/balkanitytours/GitHub/platform.balkanity/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/admin/destinations/DestinationsView.tsx
@app/admin/destinations/page.tsx
@platform/ui/Button.tsx
@platform/ui/DataList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: EN/BG keys + CopyBookingLink component</name>
  <files>platform/i18n/en.ts, platform/i18n/bg.ts, app/admin/destinations/CopyBookingLink.tsx</files>
  <action>Add to `en.ts` near `slugLabel`: `copyBookingLinkCta` ("Copy booking link") and `bookingLinkCopiedLabel` ("Copied"). Add the SAME keys to `bg.ts` with real Bulgarian ("Копирай линк за резервация" / "Копирано") so the tsc Dict-parity gate (`Dict = typeof en`) stays green; match each file's comment style. Create `app/admin/destinations/CopyBookingLink.tsx` as a `"use client"` surface-local component (NOT platform/ui). Props `{ slug, copyCta, copiedLabel }`. Build URL = `window.location.origin` + `"/pickup/" + slug`; derive origin post-mount (useEffect/lazy state) so first server render never touches `window`, falling back to relative `/pickup/<slug>` until resolved (row never blank). Never hardcode the Vercel domain. Render the URL as wrapping text (`<span>` `break-all`) beside a `Button variant="ghost" type="button"`. On click `navigator.clipboard.writeText(url)`; on resolve set `copied=true`, reset after ~1500ms via a `setTimeout` cleared on unmount and repeat-click. Label = copyCta idle / copiedLabel copied; button `aria-label` always carries action wording (worded in BOTH states, never colour/icon alone); wrap changing label in `aria-live="polite"`. No hardcoded English, no server action / DB.</action>
  <verify><automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q copyBookingLinkCta platform/i18n/en.ts && grep -q copyBookingLinkCta platform/i18n/bg.ts && grep -q "navigator.clipboard.writeText" app/admin/destinations/CopyBookingLink.tsx && grep -q '"use client"' app/admin/destinations/CopyBookingLink.tsx && echo OK</automated></verify>
  <done>Both locales have the two keys; CopyBookingLink builds the absolute /pickup URL client-side and copies it with an accessible copied state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire keys + render CopyBookingLink for active rows</name>
  <files>app/admin/destinations/DestinationsView.tsx, app/admin/destinations/page.tsx</files>
  <action>In `DestinationsView.tsx`: extend the `DestinationsViewCopy` type with `copyBookingLinkCta: string` and `bookingLinkCopiedLabel: string`. Import `CopyBookingLink`. In the row builder, for ACTIVE rows only (`d.active`), render `<CopyBookingLink slug={d.slug} copyCta={copy.copyBookingLinkCta} copiedLabel={copy.bookingLinkCopiedLabel} />` inside the existing per-row `actions` `<div className="flex items-center gap-[8px]">`, alongside Edit/Deactivate (do not render it while that row is being edited). Inactive rows get NO link (D-11). Optionally drop the now-misleading `/${d.slug}` token from the meta string for active rows since the real link is shown; keep parent + price. In `page.tsx`: add `copyBookingLinkCta: t.copyBookingLinkCta` and `bookingLinkCopiedLabel: t.bookingLinkCopiedLabel` to the copy bag passed to `DestinationsView`. No DB/query/action changes.</action>
  <verify><automated>cd /Users/balkanitytours/GitHub/platform.balkanity && grep -q CopyBookingLink app/admin/destinations/DestinationsView.tsx && grep -q copyBookingLinkCta app/admin/destinations/page.tsx && npx tsc --noEmit 2>&1 | grep -E "admin/destinations|i18n" | grep -v '^#' || echo TS-OK</automated></verify>
  <done>page.tsx threads both keys; active rows render CopyBookingLink; inactive rows show no link; tsc has no new errors in these files.</done>
</task>

</tasks>

<verification>
- `grep` confirms both keys in en.ts + bg.ts and the CopyBookingLink import/use in DestinationsView.
- `npx tsc --noEmit` surfaces no new type errors in app/admin/destinations or platform/i18n (Dict-parity holds).
- Manual: in `/admin/destinations`, an active row shows the absolute `https://…/pickup/<slug>` URL + a Copy button that copies it and flips to "Copied"; an inactive row shows no link.
</verification>

<success_criteria>
- Active destination rows display the full public booking URL and a working, accessible Copy button (EN + BG).
- Inactive rows show no live/copyable link (D-11).
- Zero backend/schema/RLS/auth/payment changes; only the five listed files touched.
</success_criteria>

<output>
Create `.planning/quick/260622-x4g-show-a-copyable-public-booking-url-on-ea/260622-x4g-SUMMARY.md` when done.
</output>
