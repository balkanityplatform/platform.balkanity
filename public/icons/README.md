# PWA icons — TEMPORARY PLACEHOLDERS (replace in Task 2 / 01-05)

The files `placeholder-192.png` and `placeholder-512.png` (and `app/icon.png`) are
**clearly-marked TEMPORARY monochrome teal (#029B87) placeholders**. They exist ONLY
so the Serwist service worker and `next build` compile while the real D-09 brand
assets are not yet committed to the repo.

They are **NOT** the real Balkanity mark and **MUST NOT ship** — Success Criterion 5
("real logo/pictogram assets") is explicitly not met by placeholders.

Task 2 of plan 01-05 replaces these with the PWA icon set derived from the real
committed `mark-teal.png` / `mark-white.png` and removes every placeholder above.
